# Story 20.1: 接口契约最终化（GET /chest/current + POST /chest/open）

Status: review

<!-- Validation 可选。建议运行 validate-create-story 在 dev-story 前做一次质检。 -->

## Story

As a 跨端契约负责人,
I want 在写第一行 server Epic 20 业务代码（Story 20.2 ~ 20.9）之前，把节点 7 涉及的 `GET /api/v1/chest/current` 与 `POST /api/v1/chest/open` 两个 REST 接口的字段层 schema 在 `docs/宠物互动App_V1接口设计.md` §7.1 / §7.2 里逐字段锚定（含必填项、类型、长度 / 范围约束、`status` 枚举值域 `{1,2}` 与数据库设计 §6.7 `user_chests.status` 同义、`unlockAt` ISO 8601 / `remainingSeconds` 派生语义、`reward` payload 字段集合（含**节点 7 阶段 `userCosmeticItemId` 占位值规则**）、`stepAccount` / `nextChest` 嵌套字段（与 §5.1 / §6.2 / §7.1 跨节点同义对齐）、错误码 1001 / 1002 / 1005 / 1009 / 3002 / 4001 / 4002 触发条件（**r11 review 锁定**：1008 在节点 7 阶段本接口已退役，全局错误码定义保留）、idempotencyKey 字符集 / 长度约束 / **MySQL `chest_open_idempotency_records` 同事务持久化幂等语义**（r5 ~ r11 11 轮 fix-review 锁定，详见 docs/lessons/2026-05-14-*-20-1-r*.md）、节点 7 阶段冻结声明 + 节点 8 阶段 `userCosmeticItemId` 真实化升级路径声明），
so that iOS Epic 21（首页宝箱倒计时 + 奖励弹窗）和 server Epic 20 后续 Story（20.2 ~ 20.9）可以基于同一份冻结契约**并行**开工，不再出现"REST 接口字段名 / 大小写不一致 / `status` 枚举 1/2 与 §5.1 `chest.status` 三处语义对不上 / `reward` payload 字段名与 client Codable struct 对不上 / 节点 7 阶段 `userCosmeticItemId` 是 NULL / 0 / 字符串 `\"0\"` 还是别的占位值不清楚 / `idempotencyKey` 是否可空 / 长度上限不清楚 / 4002 触发条件不精确（chest 不存在 vs 未解锁分别走 4001 / 4002 模糊）/ **幂等存储介质**（Redis cache vs MySQL 同事务持久化）/ pending 阶段同 key 重试行为 / cached success replay 是否计入 rate_limit 配额 / 1008 是否在本接口实际触发 不清楚 / iOS Story 21.4 奖励弹窗 5 字段是否可空 / `nextChest` 永远非 null 还是可能 null 不清楚"的返工。

## 故事定位（Epic 20 第一条 = 节点 7 第一个 Server Epic 的契约起手；对标 Story 4.1 / 7.1 / 10.1 / 11.1 / 14.1 / 17.1 在各自 Epic 的角色，本 story 是 Epic 20 **唯一的纯文档 story**，开局而非收官）

- **Epic 20 进度**：本 story → 20.2 (cosmetic_items migration) → 20.3 (cosmetic_items seed) → 20.4 (chest_open_logs migration) → 20.5 (GET /chest/current 接口) → 20.6 (POST /chest/open 事务 + idempotencyKey + 加权抽取) → 20.7 (dev 端点 POST /dev/force-unlock-chest) → 20.8 (dev 端点 POST /dev/grant-cosmetic-batch，节点 8 完成后真实写库) → 20.9 (Layer 2 集成测试 - 开箱事务全流程)。**20.1 是 20.2 ~ 20.9 的强前置**：handler 层 request / response DTO 必须严格按 20.1 锚定后的 schema；service 层错误码映射必须严格按 20.1 锚定的 1008 / 3002 / 4001 / 4002 触发条件；同时是 iOS Epic 21 的强前置（Story 21.1 首页宝箱组件 + 21.2 GET /chest/current 调用 + 21.3 POST /chest/open 调用 + 21.4 奖励弹窗 + 21.5 开箱前主动同步步数 必须严格按 20.1 冻结契约）

- **epics.md AC 钦定**：`_bmad-output/planning-artifacts/epics.md` §Story 20.1（行 2788-2805）已**精确**列出 §7.1 必须锚定的字段（response data: `{id, status, unlockAt, openCostSteps, remainingSeconds}` + status 枚举 `1=counting / 2=unlockable`）+ §7.2 必须锚定的字段（request: `{idempotencyKey: string}`；response data: `{reward: {userCosmeticItemId, cosmeticItemId, name, slot, rarity, assetUrl, iconUrl}, stepAccount: {totalSteps, availableSteps, consumedSteps}, nextChest: {id, status, unlockAt, openCostSteps, remainingSeconds}}`）+ 错误码 4001 / 4002 / 3002 / 1008 + "契约自此 Epic 起冻结"声明

- **Story 4.1 / 7.1 / 10.1 / 11.1 / 14.1 / 17.1 历史遗留对接**：
  - 本 story 必须把 V1 文档 §7.1（行 798-826）从"节点 2 阶段草稿（仅有 JSON 示例 + status 枚举段）"升级为"已由 Story 20.1 锚定"，并**补齐**该节缺失的"接口元信息 / 请求 / 服务端逻辑 / 响应体字段表 / 可能的错误码 / 关键约束"内容（草稿 JSON 示例 + status 枚举段已存在）
  - 本 story 必须把 V1 文档 §7.2（行 828-882）从"节点 2 阶段草稿（仅有 request JSON + 服务端逻辑 7 步 bullet list + response JSON 示例）"升级为"已由 Story 20.1 锚定"，并**补齐 / 重构**该节缺失的"接口元信息 / 请求体字段表 / 服务端逻辑细化（含 idempotencyKey 流程 + 事务边界）/ 响应体字段表（三段：reward / stepAccount / nextChest）/ 可能的错误码 / 关键约束 + 节点 7 阶段 `userCosmeticItemId` 占位语义 + 节点 8 真实化升级路径"内容
  - 本 story 必须在 §1 末尾追加"节点 7 宝箱业务契约冻结声明"（与节点 2 / 节点 3 / 节点 4 协议骨架冻结 / 节点 4 房间业务冻结 / 节点 5 宠物状态业务冻结 / 节点 6 表情业务冻结声明并列，**不**覆盖 / 删除）

- **下游立即依赖**：
  - **Story 20.2（cosmetic_items migration）**：migration DDL 严格按 数据库设计 §5.8（全字段 + `UNIQUE KEY uk_code` + `KEY idx_slot_rarity` + `KEY idx_enabled_weight`，**不含** `render_config` —— 节点 10 才加）；本 story **不**改 数据库设计 §5.8 schema（DB 已锚定，DB → API 方向）
  - **Story 20.3（cosmetic_items seed）**：seed 按 epics.md AR18 数量约束（common ≥ 8 / rare ≥ 4 / epic ≥ 2 / legendary ≥ 1，common 至少覆盖 4 个不同槽位）；本 story **不**钦定具体 cosmetic code 字面量（由 20.3 实装层决定）；本 story 锚定 `assetUrl` / `iconUrl` 字段都允许 placeholder URL（如 `https://placehold.co/128x128?text=Hat-Yellow`），不允许空字符串作为奖励 reward.* 字段（与 §7.2 reward 字段表"length ≥ 1"约束一致；§8.1 cosmetics/catalog 那条 catalog 字段是否允许空由 Epic 23 锚定，本 story 不预先决定）
  - **Story 20.4（chest_open_logs migration）**：migration DDL 严格按 数据库设计 §5.7（全字段 + `KEY idx_user_id_created_at` + `KEY idx_reward_cosmetic_item_id`）；本 story **不**改 数据库设计 §5.7 schema
  - **Story 20.5（GET /chest/current 接口）**：handler 响应严格按 20.1 §7.1 锚定的 `{id, status, unlockAt, openCostSteps, remainingSeconds}` 5 字段；service 层"按 unlock_at 动态判定 status"严格按 20.1 §7.1 服务端逻辑段（数据库 status=1 AND `unlock_at <= now` → 返回 status=2, remainingSeconds=0；数据库 status=1 AND `unlock_at > now` → 返回 status=1, remainingSeconds=`unlock_at - now` 秒数；数据库 status=2 → 返回 status=2, remainingSeconds=0）；**不**更新 DB（动态判定，节省写）—— 真正状态变更在开箱时；错误码 1001 / 1009 / 4001 严格按 20.1 §7.1 错误码表（**不**触发 1002 —— GET 无 body / query 参数）
  - **Story 20.6（POST /chest/open 事务）**：handler request 严格按 20.1 §7.2 锚定的 `{idempotencyKey: string (1-128 chars, [A-Za-z0-9_:-])}`；service 层流程严格按 20.1 §7.2 r11 锁定后的服务端逻辑段（**步骤 3 committed success 幂等命中预检**：autocommit `SELECT status, response_json FROM chest_open_idempotency_records WHERE user_id=? AND idempotency_key=?` → 命中 `status='success'` 直接返回缓存（同源同时刻补算 `nextChest.status` + `remainingSeconds` + 顶层 `requestId`），**且免 rate_limit**；未命中（含真新请求 + MVCC 下 pending 不可见的同 key 并发）→ 走步骤 4 rate_limit + 步骤 5 业务事务；**步骤 5 业务事务**：步骤 5a `INSERT INTO chest_open_idempotency_records ... ON DUPLICATE KEY UPDATE id=LAST_INSERT_ID(id)` 预声明 `pending` 行（事务内首条语句，借 InnoDB unique-key X-lock 阻塞同 key 并发）→ `affected_rows=1` 走步骤 5c~5i / `affected_rows=0` 走步骤 5b cached success 短路读；步骤 5c FOR UPDATE 锁 chest + 判定 unlockable → 否则 4002；步骤 5d FOR UPDATE 锁 user_step_accounts + available_steps ≥ 1000 → 否则 3002；扣 1000 步数；加权抽取 cosmetic_items（按 `drop_weight`，仅 `is_enabled=1`）；**节点 7 阶段不创建 user_cosmetic_items 实例**（按节点规划），由 Story 23.5 节点 8 才补"入仓"+ 升级本节点契约；写 chest_open_logs（reward_user_cosmetic_item_id = 0 占位）；删旧 chest 创建下一轮 chest（status=1, unlock_at=now+10min）；**步骤 5k**：`UPDATE chest_open_idempotency_records SET status='success', response_json=?` 与业务表写入**同一事务原子提交**（rollback 时 pending 行随之回滚 → 无 pending 残留卡死；**无任何 best-effort post-rollback 补偿写**，schema 为二态机 `('pending', 'success')`）；错误码 1001 / 1002 / 1005 / 1009 / 3002 / 4001 / 4002 严格按 20.1 §7.2 错误码表；**1008 在节点 7 阶段本接口已退役**（r11 review 锁定，X-lock serialize 排队下永远观察到 status='success'，无可达触发路径；保留全局错误码定义供未来扩展）；**移除 Redis 在 chest_open idempotency 路径上的所有角色**（r5 锁定 MySQL 同事务方案；r6 把预声明 INSERT 纳入同一事务消除 pending 卡死悖论；r7 简化为二态机消除 failed upsert race）
  - **Story 20.7（dev /dev/force-unlock-chest）**：dev 端点不在 V1 文档锚定（dev 接口不进客户端契约文档，按 Story 1.6 Dev Tools 框架约束 + Story 7.5 / Story 17.x 等 dev 端点的既有惯例）；本 story 只**提及**：节点 7 阶段引入 dev 接口 `/dev/force-unlock-chest` 用于 demo 跳过倒计时，**不**在 §7.x 锚定其 schema
  - **Story 20.8（dev /dev/grant-cosmetic-batch）**：同上，dev 接口不在 V1 文档锚定；节点 7 阶段实装路由 + handler 框架（真实写库等节点 8 Story 23.2 user_cosmetic_items migration 完成后开放，由 Epic 23 sprint 内打开开关）
  - **Story 20.9（Layer 2 集成测试）**：dockertest 严格按 20.1 §7.2 锚定的 happy path + 错误码触发条件 + idempotencyKey 流程做覆盖（含回滚 / 并发 / 边界 / 抽奖分布）
  - **iOS Story 21.1（首页宝箱组件 SwiftUI）**：iOS 端 `ChestStateDTO` Codable struct 严格按 20.1 §7.1 字段表（`id` 是 `String` BIGINT 字符串化；`status` 是 `Int` 枚举 1/2；`unlockAt` 是 `String` ISO 8601；`openCostSteps` 是 `Int`；`remainingSeconds` 是 `Int`）
  - **iOS Story 21.2（GET /chest/current 调用 + 状态展示 + 主动定时纠正）**：client 应基于 `remainingSeconds` 做本地倒计时显示（自首次拿到 response 起本地 `Timer` 每秒递减），到 0 时**主动**再调 GET /chest/current 一次校正（因 server 端 `unlock_at` 计算可能受 server 时钟漂移 / client 时钟漂移影响，本地倒计时仅供 UI 展示，最终状态以下次 GET 响应为准）；client **应**在房间页 / 仓库页等其他场景 navigated away 后再回到首页时**主动**重新调 GET /chest/current（不依赖本地 cache —— 步数 / 解锁状态可能已变）
  - **iOS Story 21.3（开箱按钮 → POST /chest/open）**：iOS 端 `OpenChestRequestDTO` Codable struct 严格按 20.1 §7.2 锚定的 `{idempotencyKey: String}` 单字段；client **应**在每次点击"开箱"按钮时生成新的 idempotencyKey（如 `"chest_open_{userId}_{nanoTimestamp}"`），并在 request 失败重试时**复用同一 key**（避免短时间内 server 端因 client 网络抖动重试导致多次开箱）；具体 idempotencyKey 生成规则由 iOS Story 21.3 实装层决定，但 20.1 锚定字符集 `[A-Za-z0-9_:-]` + length 1-128
  - **iOS Story 21.4（奖励弹窗 popup）**：iOS 端 `ChestRewardDTO` Codable struct 严格按 20.1 §7.2 字段表（`reward.userCosmeticItemId` 是 `String` —— 节点 7 阶段固定值 `"0"`，节点 8 起为真实主键；`reward.cosmeticItemId` 是 `String` 真实主键；`reward.name` 是 `String`；`reward.slot` / `reward.rarity` 是 `Int`；`reward.assetUrl` / `reward.iconUrl` 是 `String`）；client UI 层弹窗显示 reward.name + reward.iconUrl 图标 + reward.rarity 标识颜色；**不**展示 `userCosmeticItemId` —— 节点 7 阶段是占位值；节点 8 起亦不展示（仅用于后端审计 + 跨端关联，UI 无展示需求）；`stepAccount.availableSteps` 用于更新主界面右上角余额；`nextChest` 用于立即刷新宝箱组件 UI（避免 UI 出现 1 秒"无 chest"空白）
  - **iOS Story 21.5（开箱前主动同步步数）**：client 在点击"开箱"按钮**之前**应主动调一次 `POST /api/v1/steps/sync`（§6.1）确保 available_steps 是最新值，避免开箱时 server 端返回 3002（步数不足）但 client 看到的本地缓存余额是足的；该调用语义由 Story 21.5 实装决定，本 story 不锚定（属调用顺序而非字段层契约）

- **节点 7 vs 节点 8 阶段差异说明（本 story 锚定的最大新颖契约点）**：
  - **节点 7 阶段（Epic 20 / Epic 21 / 节点 7 demo 验收）**：开箱奖励**不入仓**（不创建 `user_cosmetic_items` 实例，仅展示弹窗）；故 `reward.userCosmeticItemId` 字段**固定为字符串 `"0"`**（占位值；与 `pets.id` / `users.id` 等真实 BIGINT 主键字符串化对齐 —— 不下发 `null` 防 Swift Codable 解析失败 / 不下发 `0` 数字防 client 误把它当合法主键算术运算）
  - **节点 8 阶段（Epic 23 完成后）**：开箱事务由 Story 23.5 修改 → 创建 `user_cosmetic_items` 实例 → `chest_open_logs.reward_user_cosmetic_item_id` 填真实 id → API 层 `reward.userCosmeticItemId` 返回真实 BIGINT 字符串化主键
  - **client 处理规则**：iOS 端 `ChestRewardDTO` Codable struct 严格按 `String` 解析（不是 `Optional<String>`）；client UI 层**禁止**展示 `userCosmeticItemId` 字段值（与"用户感知奖励"无关）；client **可**在内部 log 该字段供 debug，但**不**作为业务路径分支判断（如 `if userCosmeticItemId == "0" then ...`）—— 业务路径以节点（实际部署版本）为准，而非动态字符串比对（避免节点 8 升级后 client 代码遗留 dead branch）
  - **契约层升级路径**：节点 7 → 节点 8 升级**不**视为契约变更（`userCosmeticItemId` 类型 / 字段名 / 必填性都不变，只是 server 端语义从"占位 `\"0\"`"变成"真实主键"）；Story 23.5 落地时**应**在 §7.2 关键约束段中标注"自 YYYY-MM-DD（Story 23.5 完成日，对应 git commit hash 见 commit message）起，`reward.userCosmeticItemId` 字段不再返回占位 `\"0\"`，统一返回真实主键"

- **范围红线**：本 story **只**改 `docs/宠物互动App_V1接口设计.md` 一份文件 + sprint-status.yaml + 本 story 文件；**不**改任何 Go / Swift 代码、**不**改 `_bmad-output/` 下其他 yaml / md（除自己的 story 文件 + sprint-status.yaml 标记 ready-for-dev → in-progress → review → done）、**不**改其他 6 份 `docs/宠物互动App_*.md`（包括数据库设计 / Go 项目结构 / iOS 项目结构 / 时序图 / 总体架构 / MVP 节点规划 等 —— 它们对应的 schema 已锚定，本 story 只**对齐 / 引用**，不**修改**）

**本 story 不做**（明确范围红线）：

- 不实装任何 Go handler / service / repo / 加权抽奖 / MySQL 同事务持久化幂等记录代码（20.5 / 20.6 才做；本 story 是 contract first 的契约定稿）
- 不实装任何 Swift Codable DTO / ChestViewModel / RewardPopupView / OpenChestUseCase（iOS Epic 21 各 story 才做；本 story 提供给 iOS 的是文档 + Git commit hash，不是代码）
- 不修改 `docs/宠物互动App_数据库设计.md` §5.6 user_chests / §5.7 chest_open_logs / §5.8 cosmetic_items / §6.7 user_chests.status / §6.8 cosmetic_items.slot / §6.9 cosmetic_items.rarity schema（已锚定；本 story 的 API 字段语义来源是数据库设计，方向是 DB → API，**不反向**修改 DB）
- 不修改 §5.1 GET /home schema（§5.1 已在 Story 4.1 锚定 + `chest` 字段已 Future Fields 标注；本 story 仅在 §7.1 引用 `chest.*` 字段与 §5.1 同义对齐，**不**反向修改 §5.1）
- 不修改 §6.1 POST /steps/sync / §6.2 GET /steps/account schema（已在 Story 7.1 锚定；本 story 在 §7.2 reward / stepAccount 字段引用 §6.2 `stepAccount.{totalSteps, availableSteps, consumedSteps}` 三字段同义对齐，**不**反向修改 §6.x）
- 不修改 §8.1 GET /cosmetics/catalog / §8.2 GET /cosmetics/inventory schema（节点 8 Story 23.1 锚定；本 story 在 §7.2 reward 字段引用 cosmetic_items 字段 + `reward.assetUrl` / `reward.iconUrl` 与节点 8 §8.1 `iconUrl` / `assetUrl` 字段名同义对齐，**不**反向修改 §8.x —— §8.x 由 Epic 23 / Story 23.1 锚定）
- 不修改 §10 / §11 / §12 房间 / 表情 / WS 章节（已在 Story 10.1 / 11.1 / 14.1 / 17.1 锚定）
- 不修改 §3 错误码表（4001 / 4002 / 3002 / 1008 / 1001 / 1002 / 1005 / 1009 已锚定；本 story 仅**引用**这些错误码到 §7.1 / §7.2 错误码段，**不**新增 / **不**修改错误码定义）
- 不锚定 `/dev/force-unlock-chest` / `/dev/grant-cosmetic-batch` dev 端点 schema（dev 接口不进 V1 客户端契约文档，按既有 dev 端点惯例 —— Story 1.6 Dev Tools 框架 + Story 7.5 等不在 V1 锚定）
- 不修改 `docs/宠物互动App_Go项目结构与模块职责设计.md`（Go 工程目录已锚定 §4 + §6 + §9 + §12；本 story 不引入新目录 / 模块；`internal/domain/chest/` / `internal/service/chest_service.go` 由 Story 20.5 / 20.6 实装时按既有目录规则落地）
- 不修改 `docs/宠物互动App_iOS客户端工程结构与模块职责设计.md`（iOS 工程目录已 ADR-0002 + Story 2.2 + Story 37.x 落地；本 story 不影响 iOS 目录）
- 不修改 `docs/宠物互动App_时序图与核心业务流程设计.md`（本 story 字段层契约对齐**不**触发流程级修订；如时序图未来需补开箱事务时序，由对应 epic retro / 文档同步 story 决定）
- 不修改 `docs/宠物互动App_总体架构设计.md` / `docs/宠物互动App_MVP节点规划与里程碑.md`（这两份是更上层的产品设计；本 story 只是字段层契约定稿）
- 不为 schema 字段写 OpenAPI / AsyncAPI / JSON Schema 形式化定义（旧架构残留；新架构 V1 文档以 markdown 表格 + JSON 示例为唯一真相，参见 V1接口设计 §1）
- 不追加 §7.1 / §7.2 之外的接口或消息锚定（§8.x 走 Story 23.1 / §9.x 走 Story 32.1）
- 不在 V1 文档里写 server 实装细节（如"用 GORM 还是 sqlx"、"事务用 WithTx 包"、"加权抽奖用 alias method 还是线性扫描"具体调用方式）—— 这些属 ADR / Go 项目结构文档 / Story 20.5 / 20.6 实装层决定，V1 文档**只**写客户端可见的 HTTP request/response 语义；但"MySQL 同事务幂等记录命中行为 / 事务边界 / 加权抽取语义 / cached success replay 免 rate_limit 配额 / pending 阶段同 key 重试走 X-lock 排队 / 1008 在本接口退役"的客户端可感知契约**必须**在 20.1 文档侧写明
- 不写英文版接口文档（项目 communication_language=Chinese；用户 huing7373@gmail.com 单一开发者，无国际化诉求）

## Acceptance Criteria

**AC1 — 文档定位与编辑范围**

唯一受编辑的文档：`docs/宠物互动App_V1接口设计.md`（绝对路径 `C:/fork/cat/docs/宠物互动App_V1接口设计.md`）。

- 编辑模式：**就地修改**已有 §7.1（行 798-826）章节内容（保留 H2/H3 标题 + 现有 `#### 返回示例` JSON 示例 + 现有 `#### status 枚举` 段；补全接口元信息 / 请求 / 服务端逻辑 / 响应体字段表 / 可能的错误码 / 关键约束）+ **就地修改**已有 §7.2（行 828-882）章节内容（保留 H2/H3 标题 + 现有 `#### 请求体` JSON 示例 + 现有 `#### 返回示例` JSON 示例；扩充 / 重构服务端逻辑段 + 补齐接口元信息 / 请求体字段表 / 响应体字段表（三段 reward / stepAccount / nextChest）/ 可能的错误码 / 关键约束 + 节点 7 / 节点 8 阶段差异说明）+ §1 末尾**追加**节点 7 宝箱业务契约冻结声明（与节点 2 / 节点 3 / 节点 4 / 节点 5 / 节点 6 冻结声明并列，**不**覆盖 / 删除）
- 编辑形式：**保留**现有 markdown 结构（H2 / H3 / H4 标题层级 + ` ```json ` fenced code block + 段落顺序）；只**补全**字段说明表格 + 必填 / 类型 / 长度约束 + 错误码列表 + 服务端逻辑细化 + JSON 示例（§7.1 已有 response JSON 示例 + status 枚举 / §7.2 已有 request JSON 示例 + response JSON 示例 + 服务端逻辑 7 步 bullet 全部保留）
- 文件编码 UTF-8 + LF 行尾（与项目其他 .md 一致）
- 修改后的文档**仍可直接被复制粘贴**作为 client / server 双方实装参照（所有 JSON 示例可直接 `curl` 出来比对；所有字段说明可直接当作"DTO / Codable struct 字段表"用）
- 不删除任何**已有**字段示例或说明（仅追加 / 补全）；§2.5 全局约定 / §3 错误码表 / §4 / §5 / §6 / §8 / §9 / §10 / §11 / §12 等已锚定章节**不动**

**AC2 — §7.1 GET /api/v1/chest/current schema 锚定**

V1 文档 §7.1 编辑后必须包含以下完整内容：

**§7.1.1 接口元信息**（**新增** H4 块，紧接 `### GET /api/v1/chest/current` 之后）：

```markdown
#### 接口元信息

| 字段 | 值 |
|---|---|
| HTTP Method | GET |
| Path | /api/v1/chest/current |
| 认证 | **需要** Bearer token（auth 中间件） |
| 限频 | 默认（按 Story 4.5 rate_limit 默认值 60 次/分按 user_id 计） |
| 幂等 | 天然幂等（GET 查询，无副作用） |
| 节点 | 节点 7（Epic 20 落地 / Epic 21 客户端集成） |
| 分页 | 无（单条 chest 查询） |
| Query 参数 | **无**（不接受任何 query string） |
```

**§7.1.2 请求**（**新增** H4 块）：

```markdown
#### 请求

无请求体（GET 接口）；仅需 `Authorization: Bearer <token>` Header。
```

**§7.1.3 服务端逻辑**（**新增** H4 块，紧接 `#### 请求` 之后）：

```markdown
#### 服务端逻辑

1. **认证 & 限频**：auth 中间件校验 Bearer token；rate_limit 中间件按默认配置限频
2. **查询当前 chest**：`SELECT id, status, unlock_at, open_cost_steps FROM user_chests WHERE user_id = ? LIMIT 1`
   - 用户没有 chest 行（理论不该发生，因 Story 4.6 登录初始化已创建首个 chest）→ 4001
3. **动态判定 status**（**不**更新 DB —— 仅在 response 计算，节省写入；真正状态变更在开箱时 Story 20.6 触发）：
   - 数据库 `status = 1 (counting)` AND `unlock_at <= now` → 返回 `status = 2 (unlockable)`, `remainingSeconds = 0`
   - 数据库 `status = 1 (counting)` AND `unlock_at > now` → 返回 `status = 1`, `remainingSeconds = max(0, ceil((unlock_at - now) / 1s))`
   - 数据库 `status = 2 (unlockable)` → 返回 `status = 2`, `remainingSeconds = 0`（已解锁待开）
4. **响应**：返回 `{id, status, unlockAt, openCostSteps, remainingSeconds}` 5 字段

**事务边界规则**：本接口**不**需要 MySQL 事务（仅 1 个 SELECT 查询 + 内存判定）。
```

**§7.1.4 响应体**（**就地扩充**现有 `#### 返回示例` 小节：保留现有 JSON 示例 + 前置字段表，将标题改为 `#### 响应体`；保留现有 `#### status 枚举` 子段在表格之后）：

```markdown
#### 响应体

成功（code = 0）：

| 字段 | 类型 | 必填 | 范围/约束 | 说明 |
|---|---|---|---|---|
| `data.id` | string | 必填 | BIGINT 字符串化（与 §2.5 全局约定 + 数据库 `user_chests.id BIGINT UNSIGNED` 一致） | 当前宝箱主键 |
| `data.status` | number (int) | 必填 | 枚举 `1` / `2` | 当前宝箱状态：`1 = counting`（未解锁，倒计时中），`2 = unlockable`（已解锁，可点击开箱）；与数据库 §6.7 `user_chests.status` 同义；与 §5.1 `data.chest.status` 同义；**节点 7 起 server 端按服务端逻辑步骤 3 动态判定，不再以 DB 原始值返回** |
| `data.unlockAt` | string (ISO 8601 UTC) | 必填 | length = 20（如 `"2026-04-23T10:20:00Z"`） | 宝箱解锁时间；与数据库 `user_chests.unlock_at` 同义；client 用作本地倒计时显示基线 |
| `data.openCostSteps` | number (int) | 必填 | 节点 7 阶段固定 `1000` | 开启所需步数；与数据库 `user_chests.open_cost_steps` 同义；与 §5.1 `data.chest.openCostSteps` 同义；MVP 阶段固定为 1000，未来如调整需视为契约变更 |
| `data.remainingSeconds` | number (int) | 必填 | `0 ≤ value ≤ 2^31 - 1` | 距离 unlockAt 的剩余秒数；> 0 表示 counting，= 0 表示已可开启（与 `status = 2` 等价 —— client 可二选一判定）；server 端按 `ceil((unlock_at - now) / 1s)` 计算，到 0 时**不**会出现负数（见服务端逻辑步骤 3 的 `max(0, ...)` 兜底）；client 应基于此本地 `Timer` 每秒递减做 UI 倒计时显示，到 0 时**主动**重新 GET 一次以确认 server 端 status 已切换为 2（避免 server / client 时钟漂移导致 UI 与 server 端状态错位） |

JSON 示例（保留现有，无需修改 —— `remainingSeconds = 253` 表示距解锁还有 253 秒）：

\`\`\`json
{
  "code": 0,
  "message": "ok",
  "data": {
    "id": "5001",
    "status": 1,
    "unlockAt": "2026-04-23T10:20:00Z",
    "openCostSteps": 1000,
    "remainingSeconds": 253
  },
  "requestId": "req_xxx"
}
\`\`\`

JSON 示例（已解锁待开 edge case）：

\`\`\`json
{
  "code": 0,
  "message": "ok",
  "data": {
    "id": "5001",
    "status": 2,
    "unlockAt": "2026-04-23T10:20:00Z",
    "openCostSteps": 1000,
    "remainingSeconds": 0
  },
  "requestId": "req_xxx"
}
\`\`\`

#### status 枚举

- `1 = counting`（未解锁，倒计时中）
- `2 = unlockable`（已解锁，可点击开箱）

> **注**：本接口**不**下发 `created_at` / `updated_at` / `version` 字段 —— client 无 UI 用途；`version` 是 server 端 user_chests 表乐观锁版本号，仅在开箱事务内用（Story 20.6 实装层决定），client 不需要感知。
```

**§7.1.5 可能的错误码**（**新增** H4 块）：

```markdown
#### 可能的错误码

| code | message | 触发条件 |
|---|---|---|
| 1001 | 未登录 / token 无效 | auth 中间件拦截（无 Authorization 头 / token 非法 / token 过期 / token 解析失败） |
| 1005 | 操作过于频繁 | rate_limit 中间件拦截（**已认证路由**按 `user_id` 限频，每用户每分钟 > 60 次；按 Story 4.5 默认值，配置可调） |
| 1009 | 服务繁忙 | DB 异常（SELECT 执行返回 `err != nil`，含 driver / 网络 / 慢查询超时等）/ 内部 panic |
| 4001 | 当前宝箱不存在 | 用户在 user_chests 表中无任何行（理论上 Story 4.6 登录初始化必然创建首个 chest，此错误表征数据完整性异常 —— 如 DB 数据被 admin 误删 / migration 顺序错乱等）；client 收到 4001 时应展示"系统异常，请重新登录"+ log error 上报（不应静默） |

> **注**：本接口**不**触发 1002 参数错误（无 query / body 字段可校验）/ **不**触发 4002 / 4003（4002 / 4003 是开箱时机相关错误，仅在 §7.2 POST /chest/open 触发）/ **不**触发 3xxx / 5xxx / 6xxx / 7xxx。
```

**§7.1.6 关键约束**（**新增** H4 块）：

```markdown
#### 关键约束

- **status 动态判定 vs DB 原始值**：server 端 status 字段值由"DB 原始值 + 当前时间"动态计算，**不**等同于 DB `user_chests.status` 列值；client 收到 `status = 2` 不可推断 DB 行也是 `status = 2`（DB 可能仍为 `status = 1` 但 `unlock_at <= now`）；该设计是为节省一次 UPDATE 写入（每次 GET 都更新 DB 状态会严重放大写入压力，而 status = unlockable 是无副作用的查询时态判定）。真正的 DB 状态变更仅在开箱事务（Story 20.6）成功后发生（删旧 chest + 新建下一轮 status = 1 chest）
- **remainingSeconds 不会为负**：服务端按 `max(0, ceil((unlock_at - now) / 1s))` 计算；即使 server 时钟跳变 / `unlock_at` 略早于 now，也只会返回 0；client 解析层**应**按 `Int` 处理（不是 `UInt` —— Swift 端 `UInt` 在解析时若收到负数会 crash，虽 server 保证不发但 client 应防御性按 `Int`）
- **本地倒计时与 server 状态校对**：client **应**在 `remainingSeconds` 倒到 0 时**主动**调一次 GET /chest/current 校正 status（避免本地时钟漂移导致 UI 误判）；如果 GET 返回 `status = 1` 且 `remainingSeconds > 0`（如本地早跳到 0，但 server 仍认为未解锁）→ client 以 server 响应为准重置本地 Timer；如果 GET 返回 `status = 2` 且 `remainingSeconds = 0`（与本地一致）→ UI 切换"点击开箱"按钮可点状态
- **首次登录后 1 秒内调本接口**：刚 Story 4.6 登录初始化的 chest `unlock_at = now + 10 min`；首次 GET 返回 `status = 1`, `remainingSeconds ≈ 600`，与 `GET /home` 节点 2 阶段示例一致（§5.1 行 423-425）
- **BIGINT 主键字符串化**：`data.id` 遵循 §2.5 全局约定字符串化；其他字段（`status` / `openCostSteps` / `remainingSeconds`）是 int，**不**字符串化
- **client 缓存策略**：本接口数据有时效性（每秒变化），client **不应**做长时间本地缓存；建议每次进入首页 / 从其他页面返回首页时主动重新调一次（不依赖任何 cache 中间件 / ETag）
```

**AC3 — §7.2 POST /api/v1/chest/open schema 锚定**

> **AC3 内容声明（r14 review 锁定，2026-05-14）**：原 AC3 内嵌的 r1 Redis 幂等设计快照（"Redis 幂等键 + TTL 24h + 事务后写 Redis + 1008 主动返回"六大块共 200+ 行）已在 r14 review 中**完整删除**。该 r1 设计在 r5 ~ r11 共 7 轮 fix-review 中被彻底推翻，留在 AC3 内嵌会让下游 reader（Story 20.5 / 20.6 / 20.9 / iOS Epic 21.x 实装者）有概率绕过 super-note 警告、照内嵌文本踩坑。本 r14 修订采取"删除即删除"原则 —— 不保留任何 r1 文案，AC3 内容以 V1 文档当前版本 §7.2 + 数据库设计 §5.16 为唯一权威，下方仅保留 r11 钦定的 MySQL 同事务持久化方案的**简洁摘要 + reference 锚点**，避免内嵌长文随 V1 文档持续演进而再次 stale。

V1 文档 §7.2 编辑后必须包含以下结构与契约要点（**完整字段表 / JSON 示例 / 错误码表 / 关键约束全文以 V1 文档 §7.2 当前版本为唯一权威，本 AC 仅做要点摘要 + 边界约束声明**）：

**§7.2.x 六大块结构骨架**（H4 块标题与顺序）：

1. `#### 接口元信息` — HTTP Method=POST / Path=`/api/v1/chest/open` / 认证=Bearer / 限频=handler 内层（r10/r11 锁定）/ 幂等=MySQL `chest_open_idempotency_records` 同事务持久化 / 节点=节点 7 / 事务边界=MySQL 单一事务
2. `#### 请求体` — 字段表 + JSON 示例：仅 `idempotencyKey` 一个字段（string / 必填 / length 1-128 / charset `[A-Za-z0-9_:-]`）
3. `#### 服务端逻辑` — 步骤 3 / 4 / 5 详见下方"r11 钦定步骤简述"
4. `#### 响应体` — 三段 payload：`reward` / `stepAccount` / `nextChest`；字段表 + JSON 示例
5. `#### 可能的错误码` — 表格 + 1008 退役注解
6. `#### 关键约束` — 节点 7 vs 节点 8 占位 / idempotencyKey charset / client 重试策略 / 事务边界 / 加权抽奖 / r5~r11 七轮决策段（含「r7 移除 best-effort failed upsert 决策」/「rate_limit 位置 r10 调整 + r11 修订」/「MVCC 下 pending 不可见 r11 决策」/「1008 错误码 r11 退役决策」等）

**r11 钦定服务端逻辑步骤简述**（步骤 3 / 4 / 5 关键路径，完整 11 子步骤 5a~5k 见 V1 §7.2 服务端逻辑当前版本）：

- **步骤 3 幂等命中预检（事务外 autocommit SELECT）**：`SELECT status, response_json FROM chest_open_idempotency_records WHERE user_id = ? AND idempotency_key = ?` —— 命中 `status = 'success'` → 直接返回 cached `response_json`（**跳过步骤 4 rate_limit**，r10 锁定的"committed success replay 免配额"承诺）；未命中（含真新请求 + pending 阶段同 key 重试因 MVCC 不可见而表现为未命中）→ 进步骤 4
- **步骤 4 rate_limit 检查（handler 内层）**：按 `user_id` 60/min 限频；超限 → 1005；通过 → 进步骤 5
- **步骤 5 业务事务**（单一 MySQL 事务，所有 repo 调用用 `txCtx`）：
  - **5a 预声明 INSERT**（事务首条语句）：`INSERT INTO chest_open_idempotency_records (user_id, idempotency_key, status, response_json, created_at, updated_at) VALUES (?, ?, 'pending', NULL, NOW(3), NOW(3)) ON DUPLICATE KEY UPDATE id = LAST_INSERT_ID(id)` —— 借 `UNIQUE(user_id, idempotency_key)` 在事务内**单语句原子声明**；同 key 并发请求通过 InnoDB unique-key X-lock 阻塞排队
  - **5b 分支判定**：`affected_rows = 1` → 行不存在（首次或首次 rollback 后到达）→ 进 5c 走全流程；`affected_rows = 0` → 同事务内再 SELECT 判 `status` —— `status = 'success'` → 短路读 cached `response_json` 返回；`status = 'pending'` → 不可达（首事务持锁阻塞排队后只会观察到 success；万一观察到 pending 返回 1009 兜底）
  - **5c~5i 业务表写入**：FOR UPDATE 锁 `user_chests` + `user_step_accounts` → 判 unlockable → 扣步数（乐观锁 version）→ 加权抽奖 `cosmetic_items` → 写 `chest_open_logs`（`reward_user_cosmetic_item_id = 0` 节点 7 占位）→ DELETE 旧 chest + INSERT 新 chest（`status = 1`, `unlock_at = NOW() + INTERVAL 10 MINUTE`）
  - **5k 最终化 UPDATE**（commit 前）：`UPDATE chest_open_idempotency_records SET status = 'success', response_json = ?, updated_at = NOW(3) WHERE user_id = ? AND idempotency_key = ?` —— 写入步骤 5j 序列化的**可缓存 payload**
  - **事务原子 commit**：业务表写入 + 幂等记录 success 状态同事务原子提交；rollback 时 pending 行随之消失（无 pending 卡死、无 failed 占位 race、无 best-effort 异步补偿）

**`response_json` 缓存内容钦定（r7 / r9 / r11 锁定，时间派生字段穷尽排除）**：

- 仅持久化 `{code, message, data: {reward.*, stepAccount.*, nextChest.{id, unlockAt, openCostSteps}}}`
- **`nextChest.status` 与 `nextChest.remainingSeconds` 均不写入 `response_json`** —— 两者都是 **time-derived**：`status = (unlock_at > now) ? 1 : 2`、`remainingSeconds = max(0, ceil((unlock_at - now) / 1s))`；由 server 在响应序列化时按"当前时刻"**同源同时刻**实时计算填入
- **顶层 `requestId` 也不写入 `response_json`** —— 每次请求独立的 trace ID，重试请求必须返回当前请求的 `requestId` 以维持 log / trace 关联语义
- 首次成功路径与同 key 重试 cached 路径走**同一序列化逻辑**，与 §7.1 GET /chest/current 时间派生语义对齐，无 stale 漂移

**关键约束集合（r5~r11 七轮决策摘要）**：

- **节点 7 vs 节点 8 占位**：`reward.userCosmeticItemId` 节点 7 阶段固定字符串 `"0"`；节点 8 Story 23.5 起为真实 BIGINT 字符串化主键；client UI 层禁止展示此字段；不视为契约变更
- **idempotencyKey charset**：`[A-Za-z0-9_:-]` + length 1-128；兼容 UUID / nanoid / ULID + 业务前缀分隔符；禁止 URL / SQL / shell 保留字符
- **client 重试策略**：网络层 timeout / 5xx → 同 key 重试（cached success replay 免配额）；首次重试间隔 **≥ 200ms**（覆盖业务事务时长，避开 pending 窗口同 key 重试消耗 quota）；撞 1005 → 等 60s 限频窗口后再试同 key；3002 / 4002 业务错误 → 不用同 key 重试，引导用户走"补步数 → 等解锁 → 新点击生成新 key"流程
- **加权抽奖**：按 `cumulative_weight / total_weight` 比例抽取，仅 `is_enabled = 1` 行参与；切换为非加权 / 加权但不按 drop_weight 视为契约变更
- **跨字段同义对齐**：`data.stepAccount.*` 与 §6.2 GET /steps/account 同义；`data.nextChest.*` 与 §7.1 GET /chest/current 同义；`data.reward.*` 与 §8.1 GET /cosmetics/catalog 同义（§8.1 由 Epic 23 / Story 23.1 锚定）
- **BIGINT 主键字符串化**：`reward.userCosmeticItemId` / `reward.cosmeticItemId` / `nextChest.id` 均遵循 §2.5 字符串化
- **1008 退役**：1008 错误码在节点 7 阶段本接口**无可达触发路径**（X-lock serialize 排队下步骤 5b 永远观察到 `status = 'success'`，pending 兜底分支映射 1009 而非 1008）；§3 全局错误码定义保留供其他业务复用
- **rate_limit 位置**：handler 内层（步骤 4），**不**在 middleware 链上；仅 committed success replay（步骤 3 命中 `status = 'success'`）跳过限频
- **错误码集合**：1001（未登录）/ 1002（参数错误）/ 1005（限频）/ 1009（DB 异常 / 乐观锁冲突 / seed 异常 / pending 兜底）/ 3002（步数不足）/ 4001（chest 不存在）/ 4002（chest 未解锁）；**不**触发 1008 / 4003 / 5xxx / 6xxx / 7xxx

**JSON 示例对齐要点**：

- 现有 §7.2 JSON 示例中 `reward.userCosmeticItemId = "91001"`（Story 4.1 节点 2 阶段占位）→ Story 20.1 落地时**更新**为 `"0"` 匹配节点 7 阶段行为；节点 8 Story 23.5 落地后再次更新为真实主键
- 保留示例中 `reward.iconUrl`（节点 2 阶段已含，与 §8.1 / §8.2 同义，不删除）

**Reference（AC3 内容的权威来源 —— 任何契约细节以下方文档当前版本为准，本 AC 摘要仅作概览）**：

- `docs/宠物互动App_V1接口设计.md` §7.2 当前版本（含 §7.2.1 ~ §7.2.6 六大块 + 关键约束 r5~r11 七轮决策段）
- `docs/宠物互动App_数据库设计.md` §5.16 `chest_open_idempotency_records` 表 schema（二态机 `('pending', 'success')` + `UNIQUE(user_id, idempotency_key)` + 同事务持久化语义）
- `docs/宠物互动App_数据库设计.md` §8.3 开箱事务边界（与 V1 §7.2 服务端逻辑步骤 5a~5k 一一对齐）
- `docs/lessons/2026-05-14-idempotency-set-fail-retry-and-data-integrity-error-mapping-20-1-r3.md`（r3 决策 lesson）
- `docs/lessons/2026-05-14-idempotency-sentinel-short-ttl-eliminates-redis-writeback-lockout-20-1-r4.md`（r4 sentinel + 短 TTL lesson，后被 r5 取代）
- `docs/lessons/2026-05-14-db-same-tx-idempotency-replaces-redis-writeback-fragility-20-1-r5.md`（r5 DB 同事务幂等取代 Redis 写回 lesson）
- `docs/lessons/2026-05-14-idempotency-pre-claim-must-be-inside-business-tx-20-1-r6.md`（r6 预声明纳入业务事务消除 pending 卡死悖论 lesson）
- `docs/lessons/2026-05-14-idempotency-no-async-failed-compensation-and-no-cached-requestId-20-1-r7.md`（r7 二态机简化 + 移除 failed upsert race + requestId 不入缓存 lesson）
- `docs/lessons/2026-05-14-design-doc-cross-section-summary-must-track-canonical-20-1-r8.md`（r8 跨章节 summary 跟踪权威源 lesson）
- `docs/lessons/2026-05-14-time-derived-fields-exhaustive-exclusion-from-idempotency-cache-20-1-r9.md`（r9 时间派生字段穷尽排除 lesson）
- `docs/lessons/2026-05-14-rate-limit-must-yield-to-idempotency-hit-precheck-20-1-r10.md`（r10 rate_limit 位置调整 lesson）
- `docs/lessons/2026-05-14-mvcc-invisibility-of-uncommitted-pending-rows-20-1-r11.md`（r11 MVCC 不可见 + 1008 退役 lesson）
- `docs/lessons/2026-05-14-story-file-must-track-canonical-contract-drift-20-1-r12.md`（r12 story 文件跟踪契约漂移 lesson）

> **下游实装绝对禁令**：Story 20.5 / 20.6 / 20.9 / iOS Epic 21.x 实装者**必须**按上述 Reference 文档当前版本落地，**禁止**按任何旧版本 / 历史快照 / 已 deprecated 的设计变体（如 Redis 幂等键、TTL 缓存、事务后写 Redis、1008 主动返回、三态机 `'failed'`、middleware 层 rate_limit、`response_json` 含 `nextChest.status` / `remainingSeconds` / `requestId` 等）实装。如发现 Reference 文档与本 AC 摘要冲突，以 Reference 文档为准（本 AC 摘要可能未及时同步契约演进 —— 这本身是 r12 lesson 钦定的"内嵌长文必然 stale，权威以 V1 文档当前版本为准"原则）。

**JSON 响应示例**（节点 7 阶段实际行为）：

\`\`\`json
{
  "code": 0,
  "message": "ok",
  "data": {
    "reward": {
      "userCosmeticItemId": "0",
      "cosmeticItemId": "24",
      "name": "星星围巾",
      "slot": 4,
      "rarity": 2,
      "assetUrl": "https://...",
      "iconUrl": "https://..."
    },
    "stepAccount": {
      "totalSteps": 12560,
      "availableSteps": 740,
      "consumedSteps": 400
    },
    "nextChest": {
      "id": "5002",
      "status": 1,
      "unlockAt": "2026-04-23T10:35:00Z",
      "openCostSteps": 1000,
      "remainingSeconds": 600
    }
  },
  "requestId": "req_xxx"
}
\`\`\`

（**注**：`nextChest.status` 在首次成功路径返回 `1`；同 key cached replay 时 server 端按 `(unlock_at > now) ? 1 : 2` **time-derived** 实时计算，可能为 `2`；`nextChest.remainingSeconds` 始终按 `max(0, ceil((unlock_at - now) / 1s))` 实时计算）


**AC4 — §1 末尾追加节点 7 宝箱业务契约冻结声明**

V1 文档 §1 末尾（紧接节点 6 表情业务冻结声明之后）**追加**以下声明（与节点 2 / 节点 3 / 节点 4 协议骨架 / 节点 4 房间业务 / 节点 5 宠物状态业务 / 节点 6 表情业务冻结声明并列，**不**覆盖 / 删除现有声明）：

```markdown
- 自 YYYY-MM-DD（Story 20.1 完成日，对应 git commit hash 见 commit message）起，§7.1（GET /api/v1/chest/current）/ §7.2（POST /api/v1/chest/open）两个节点 7 宝箱 REST 接口的 schema 进入**冻结**状态。
- 任何字段名 / 字段类型 / `status` 枚举值（1 / 2）/ 错误码（1001 / 1002 / 1005 / 1008 / 1009 / 3002 / 4001 / 4002）触发条件 / `idempotencyKey` 字符集（`[A-Za-z0-9_:-]` + length 1-128）/ **MySQL `chest_open_idempotency_records` 同事务持久化幂等语义**（r5~r11 锁定，**移除 Redis** 在 chest_open idempotency 路径上的所有角色）/ MySQL 事务边界 / 加权抽取语义 / **节点 7 阶段 `reward.userCosmeticItemId` 占位值 `\"0\"` 契约** / `nextChest` 永远非 null 且 server 端 `unlock_at = now + 10min`、`nextChest.status` **首次返回 1 但同 key 重试时 server 端按 `(unlock_at > now) ? 1 : 2` 实时计算**（**time-derived**，与 §7.1 GET /chest/current 同语义；`nextChest.remainingSeconds` 始终按 `max(0, ceil((unlock_at - now) / 1s))` 实时计算；两者均不写入 `response_json` 缓存，由 server 在响应序列化时同源同时刻填入；详见 V1 §7.2 关键约束 r9 / r11 锁定段） 的修改都必须（**冻结边界说明**：`reward.userCosmeticItemId` 字段值**从节点 8 Story 23.5 起切换为真实主键**这一升级**不**视为契约变更 —— 字段类型 / 名 / 必填性都不变，仅服务端语义升级；Story 23.5 落地时应在 §7.2.6 标注升级日期 + commit hash；1008 / 1005 / 4003 等"触发条件"冻结在**抽象层** —— "走 §3 错误码表对应映射"这一不变量；**幂等存储介质**冻结在**抽象层** —— "幂等记录 + 业务数据原子写"这一不变量（r5 锁定 MySQL 同事务方案；r6 进一步把预声明纳入同一事务消除 pending 卡死悖论；r7 简化为二态机消除 failed upsert race；移除 Redis）；删除 idempotencyKey + DB 幂等机制 / 把原子声明退化为非原子两步 / 把预声明退回到业务事务外 / 把 `nextChest.status` / `remainingSeconds` / `requestId` 等动态字段写回 `response_json` 缓存 / 重新引入异步 `failed` 补偿写 / 把幂等记录退回 Redis / 切换为非加权抽奖 / 修改 nextChest 创建时机才视为契约变更）：
  1. 触发 iOS Epic 21 重新评审（影响 Story 21.1 ~ 21.5 所有首页宝箱组件 + 倒计时 + 开箱 + 奖励弹窗 + 主动同步步数 story）
  2. 触发 server Epic 20 已完成 story 的回归（影响 Story 20.2 ~ 20.9 已落地的 migration / seed / handler / service / 集成测试）
  3. 触发下游 Epic 23 的契约 story（23.1）回归（节点 8 入仓事务基于本骨架扩展）
  4. 在本 story 文件 + epics.md 同步标注变更原因 + 影响范围
- `user_chests.open_cost_steps` 默认值 **1000** 属契约一部分：**prod 部署必须使用默认值**，不允许通过配置 / migration 覆盖；**dev / test 环境**可通过 fixture 覆盖（仅用于单测 / 调试 / fixture），**不**视为契约变更；**修改默认值本身**视为契约变更走完整冻结流程。
- `user_chests.status` 枚举（`1 = counting` / `2 = unlockable`）属契约一部分；新增枚举值（如 `3 = opened`）视为契约变更（**注**：当前设计中 opened 状态不存在 —— 开箱后立即创建下一轮 chest，opened 是 transient state，仅在事务内瞬间存在）。
- `cosmetic_items` 表 seed 数量约束（epics.md AR18 钦定 common ≥ 8 / rare ≥ 4 / epic ≥ 2 / legendary ≥ 1）属契约一部分；admin 后台增删 cosmetic_items 行**不**视为契约变更，但删到不足 AR18 约束视为线上事故。
- `drop_weight` 加权抽奖算法（按 `cumulative_weight / total_weight` 比例抽取，仅 `is_enabled = 1` 行参与）属契约一部分；切换为非加权 / 加权但不按 drop_weight 排序 视为契约变更。
```

**AC5 — 节点 7 阶段 `userCosmeticItemId` 占位规则双向引用一致性**

- AC3 §7.2.4 响应体 `reward.userCosmeticItemId` 字段说明 + AC3 §7.2.6 关键约束首条 + AC4 §1 末尾冻结声明三处**对节点 7 阶段固定 `"0"` 占位**的描述**必须**文字一致（值 / 类型 / 升级路径 / client 处理规则）
- 现有 §7.2 JSON 示例（行 853-880）中 `reward.userCosmeticItemId` 值**必须**更新为 `"0"`（如现有为 `"91001"`，落地时需改）
- §5.1 GET /home 现有内容**不**涉及 `userCosmeticItemId` 字段（§5.1 不返回开箱奖励），故无需修改

**AC6 — 跨章节引用 / 错误码 / 字段同义一致性**

- §7.1 引用的所有错误码（1001 / 1005 / 1009 / 4001）必须与 §3 全局错误码表（行 100-152）字面量一致
- §7.2 引用的所有错误码（1001 / 1002 / 1005 / 1008 / 1009 / 3002 / 4001 / 4002）必须与 §3 全局错误码表字面量一致
- `data.stepAccount.*` 三字段（totalSteps / availableSteps / consumedSteps）必须与 §6.2 GET /steps/account 字段表完全同义（字段名 / 类型 / 语义一致）
- `data.nextChest.*` 五字段（id / status / unlockAt / openCostSteps / remainingSeconds）必须与 §7.1 GET /chest/current 字段表完全同义
- `data.reward.*` 七字段（userCosmeticItemId / cosmeticItemId / name / slot / rarity / assetUrl / iconUrl）：cosmeticItemId / name / slot / rarity / assetUrl / iconUrl 必须与节点 8 §8.1 GET /cosmetics/catalog 字段表保持同义对齐 —— 但 §8.1 由 Epic 23 / Story 23.1 锚定，本 story 落地时可能 §8.1 仍是节点 2 草稿；故本 AC 要求：本 story **不**修改 §8.1，但锚定 §7.2 reward 字段时**应**与现有 §8.1 草稿示例（行 893-911）字段名一致（已对齐：cosmeticItemId / code / name / slot / rarity / iconUrl / assetUrl —— 注：§8.1 含 `code` 字段但 §7.2 reward **不**含 `code`，这是设计选择 —— reward 不需要 `code`，玩家不感知 code 是业务编码 / 配置主键的别名，§8.1 / §8.2 inventory 才会用 code 做去重 / 查找）

**AC7 — Story Done 状态验证（文档一致性 5 项）**

> **AC7 scope 修订（r12 review 锁定，2026-05-14）**：本 story 实装期间历经 11 轮 fix-review，迭代过程中**允许**新增 `docs/lessons/*.md` 和**修订** `docs/宠物互动App_数据库设计.md`（含新增 §5.16 `chest_open_idempotency_records` 表 + 修订 §8.3 开箱事务边界）。原 AC7.5 "git diff 范围仅触及 V1 文档 + sprint-status + 本 story 文件三处"承诺**不再成立**（详见 AC7.5 r12 修订段 + Change Log r1~r11）。该 scope 扩张是 11 轮 fix-review 推动 V1 契约从 r1 Redis 设计 → r11 MySQL 同事务持久化设计的必然结果，**不**视为 scope creep —— 契约层修订必须同步反映到 DB schema 设计文档（DB→API 单向 + 跨文档同步原则）+ 沉淀为 lesson 文档（供后续 Claude 蒸馏复用）。

本 story 标记 done 之前必须完成以下 5 项文档一致性验证：

1. **§7.1 / §7.2 字段表与 JSON 示例完全对齐**：每个字段表行的 `data.X.Y` 字段必须在 JSON 示例中出现，且类型 / 取值范围一致
2. **§3 错误码表与 §7.1 / §7.2 错误码段完全一致**：每个引用的 code 字面量 + message 字面量必须与 §3 表格一致
3. **跨章节引用真实存在**：所有 `[Source: docs/...]` 引用 + AC 内 `§X.Y` 引用必须指向真实存在的章节（不存在 dead link）
4. **§1 末尾追加的冻结声明文案与节点 6 (Story 17.1) 模板形式一致**：`"自 YYYY-MM-DD（Story 20.1 完成日）起 + §7.x 进入冻结状态 + 任何修改必须触发 X 步流程"` 结构对齐
5. **git diff 范围检查**：`git diff --stat` 输出应仅显示 1 file changed（`docs/宠物互动App_V1接口设计.md`），diff hunks 仅落在 §1 末尾 + §7.1 + §7.2 三处预期修改区域；如发现其他章节意外修改，立即 revert（用 `git checkout` 单 hunk）

## Tasks / Subtasks

- [x] Task 1 在 §1 末尾追加节点 7 宝箱业务契约冻结声明 (AC: 4)
  - [x] 用 Edit 工具按"节点 6 表情业务契约冻结声明"段之后插入新声明（保留现有所有冻结声明）
  - [x] 替换 `YYYY-MM-DD` 占位为实际完成日期（2026-05-14）
  - [x] 验证：`git diff docs/宠物互动App_V1接口设计.md` 显示 §1 区域有新增内容，其他章节无意外修改
- [x] Task 2 锚定 §7.1 GET /api/v1/chest/current (AC: 2, 6)
  - [x] 保留现有 H2 `## 7. 宝箱接口` / H2 `## 7.1 获取当前宝箱` / H3 `### GET /api/v1/chest/current` / 现有 `#### status 枚举` 段
  - [x] 紧接 H3 后插入 `#### 接口元信息` H4 块（按 AC2.§7.1.1）
  - [x] 接口元信息之后插入 `#### 请求` H4 块（按 AC2.§7.1.2）
  - [x] `#### 请求` 之后插入 `#### 服务端逻辑` H4 块（按 AC2.§7.1.3）
  - [x] 将现有 `#### 返回示例` H4 标题改为 `#### 响应体`，在 JSON 示例之前插入字段表（按 AC2.§7.1.4）；保留 `#### status 枚举` 段在字段表之后；并补充已解锁待开 edge case JSON 示例
  - [x] 响应体 + status 枚举之后插入 `#### 可能的错误码` H4 块（按 AC2.§7.1.5）
  - [x] 错误码之后插入 `#### 关键约束` H4 块（按 AC2.§7.1.6）
  - [x] 验证：JSON 示例字段与字段表一一对应；错误码与 §3 表字面量一致
- [x] Task 3 锚定 §7.2 POST /api/v1/chest/open (AC: 3, 5, 6)
  - [x] 保留现有 H2 `## 7.2 开启宝箱` / H3 `### POST /api/v1/chest/open` / 现有 `#### 请求体` JSON 示例 / 现有 `#### 返回示例` JSON 示例
  - [x] 紧接 H3 后插入 `#### 接口元信息` H4 块（按 AC3.§7.2.1）
  - [x] 在 `#### 请求体` 现有 JSON 示例之前插入字段表（按 AC3.§7.2.2）
  - [x] **重构** `#### 服务端逻辑` 段：把原 7 步 bullet list 扩展为完整步骤序号 + 幂等键流程 + 事务边界（按 AC3.§7.2.3 + r5~r11 锁定的 MySQL 同事务持久化方案；r1 内联文本仅为历史快照，**实际生效内容以 V1 §7.2 当前版本为准**）
  - [x] 将现有 `#### 返回示例` H4 标题改为 `#### 响应体`，在 JSON 示例之前插入三段字段表（reward / stepAccount / nextChest）（按 AC3.§7.2.4）
  - [x] **更新现有 JSON 示例中 `reward.userCosmeticItemId` 值从 `"91001"` 改为 `"0"`**（按 AC5 节点 7 阶段占位）；同时补充 `iconUrl` 字段（与 reward 字段表对齐）
  - [x] 响应体之后插入 `#### 可能的错误码` H4 块（按 AC3.§7.2.5）
  - [x] 错误码之后插入 `#### 关键约束` H4 块（按 AC3.§7.2.6）
  - [x] 验证：JSON 示例字段与字段表一一对应；错误码与 §3 表字面量一致；`userCosmeticItemId = "0"` 在 JSON 示例 + 字段表 + 关键约束三处一致
- [x] Task 4 跨章节同义一致性验证 (AC: 5, 6)
  - [x] `data.stepAccount.*` 与 §6.2 GET /steps/account 字段表对照（同 totalSteps / availableSteps / consumedSteps 三字段名一致）
  - [x] `data.nextChest.*` 与 §7.1 字段表对照（同 id / status / unlockAt / openCostSteps / remainingSeconds 五字段一致）
  - [x] `data.reward.*` 与 §8.1 / 数据库 §5.8 cosmetic_items 字段对照（cosmeticItemId / name / slot / rarity / assetUrl / iconUrl 字段名与现有 §8.1 草稿一致；§8.1 含 `code` 字段但 §7.2 reward 不含 —— 设计选择）
  - [x] 所有 BIGINT 主键字段（id / userCosmeticItemId / cosmeticItemId / nextChest.id）在 JSON 示例中是字符串
- [x] Task 5 AC7 文档一致性 5 项验证 (AC: 7)
  - [x] 字段表 ↔ JSON 示例对齐（§7.1 + §7.2 都做）
  - [x] §3 错误码字面量对齐（1001 / 1002 / 1005 / 1008 / 1009 / 3002 / 4001 / 4002 message 全部与 §3 表 lines 135-153 一致）
  - [x] 跨章节引用真实存在（§2.5 / §3 / §5.1 / §6.2 / §7.1 / §8.1 / §8.2 均有对应章节）
  - [x] §1 冻结声明文案与 Story 17.1 节点 6 模板对齐（"自 YYYY-MM-DD 起 + §X.Y 进入冻结状态 + 任何修改必须 4 步流程 + 默认值条款 + 枚举条款"结构对齐）
  - [x] `git diff --stat` 范围检查（**r12 review 修订**：scope 已扩张以容纳 11 轮 fix-review）：本 story 实装 + 11 轮 fix-review 累计修改 `docs/宠物互动App_V1接口设计.md`（§1 末尾 + §7.1 + §7.2 + §13.3 多处）+ `docs/宠物互动App_数据库设计.md`（新增 §5.16 `chest_open_idempotency_records` 表 + 修订 §8.3 开箱事务边界 + 相关锚点）+ `_bmad-output/implementation-artifacts/sprint-status.yaml` + 本 story 文件 + `docs/lessons/2026-05-14-*-20-1-r*.md` 系列 lesson 文档（含 r3~r11 每轮一份独立 lesson + r1~r11 index.md 追加）。**r12 修订前的"diff 仅触及 3 个文件"承诺不再成立**，详见 Change Log r1~r11 各轮 commit 链 + Follow-up r12 段
- [x] Task 6 完成 story 并更新 sprint status (AC: 1-7)
  - [x] 把本 story 文件 Status 从 `ready-for-dev` 改为 `in-progress` → `review`（dev-story 流程负责）→ `done`（code-review 流程负责）
  - [x] 把 sprint-status.yaml 中 `20-1-接口契约最终化` 状态对应更新（ready-for-dev → in-progress → review）
  - [x] 在 Dev Agent Record 填写 Completion Notes List + File List + Change Log

### Follow-up（r5 review 锁定）

- **chest_open_idempotency_records migration**：r5 review 锁定本 story 在契约层钦定 `chest_open_idempotency_records` 表 schema（数据库设计 §5.16），但**migration SQL 不在本 story 范围**。需在 Story 20.6（开箱事务实装）开工前由 dev 决定：
  - **方案 A**：把该表 migration 收进 Story 20.6（与 chest_open_logs 同 migration 文件）
  - **方案 B**：起新 Story 20.10「chest_open_idempotency_records migration」独立 PR
  - **不**在本 story 修改 `docs/sprint-status.yaml`（避免本 review-only commit 影响 sprint 状态）；决策由 Story 20.6 dev kickoff 时确认

## Dev Notes

### 文档编辑模式（与已 done 的 17.1 / 14.1 / 11.1 一致）

- **本 story 是纯文档 story**：**不**写 Go / Swift 代码；**唯一**修改 `docs/宠物互动App_V1接口设计.md` 一份文件 + 自己的 story 文件 + sprint-status.yaml
- **编辑工具**：用 `Edit` 工具直接修改 markdown（避免大段重写整个 §7.1 / §7.2 —— 这些章节的现有内容必须保留，仅**追加** / **就地扩充**内容）
- **避免大段重写**：现有 JSON 示例 + H2 / H3 / H4 标题 + 已有段落**全部保留**，仅在已有结构间隙**插入**新段落（参考已 done 的 Story 17.1 / 14.1 编辑模式：Edit 工具按"old_string + 上下文 + 新插入段落 + new_string"形式做精准 anchor 插入）
- **唯一例外**：§7.2 现有 JSON 示例中 `reward.userCosmeticItemId` 值需从 `"91001"` 改为 `"0"`（节点 7 占位）—— 这是**就地修改**已有值，不算"删除已有内容"
- **markdown 结构**：保留 V1 文档现有格式 —— H2 用 `## X.Y 标题` / H3 用 `### 子标题` / H4 用 `#### 进一步细分` / fenced code block 用 ` ```json ` / ` ```sql ` 等；引用块用 `> **段标题**：...`

### 跨文档语义同步检查（DB → API 单向）

- 本 story 锚定的 §7.1 / §7.2 字段**只**反映 `docs/宠物互动App_数据库设计.md` §5.6 user_chests / §5.7 chest_open_logs / §5.8 cosmetic_items / §6.7 user_chests.status / §6.8 cosmetic_items.slot / §6.9 cosmetic_items.rarity 的语义，**禁止反向**修改 DB schema
- 如发现 DB schema 与本 story 锚定 API 字段有不一致（如 `name VARCHAR(64)` 但 API 锚定 `length ≤ 128`），**应**以 DB schema 为准并向下兼容（API length ≤ 64），**禁止**改 DB
- `idempotencyKey` 字符集 `[A-Za-z0-9_:-]` 是本 story 新增的契约层约束 —— **r5 review 锁定后**：DB 层面**有对应字段**（`chest_open_idempotency_records.idempotency_key VARCHAR(128) NOT NULL`，与 `user_id` 组成 `UNIQUE KEY uk_user_id_idempotency_key`，参见数据库设计 §5.16），idempotencyKey 不再仅是 Redis 缓存键，而是 MySQL 持久化幂等记录的 unique key；本 story 在契约层补充字符集约束（避免 client 发奇怪字符 trigger URL 编码异常 / SQL 注入兜底 / 日志可读性等问题），由 server 端 service 层校验（Story 20.6 实装时按正则校验 → 失败回 1002）
- `reward.userCosmeticItemId` 节点 7 阶段固定 `"0"` 占位的契约是本 story 新增 —— DB 层面无对应硬约束（`chest_open_logs.reward_user_cosmetic_item_id BIGINT UNSIGNED NOT NULL` 节点 7 阶段写 0；API 层下发字符串 `"0"`）；该占位规则属本 story 独有，节点 8 Story 23.5 落地时升级

### 错误码引用

- 本 story 引用的 1001 / 1002 / 1005 / 1008 / 1009 / 3002 / 4001 / 4002 都已在 §3 全局错误码表（行 121-163）锚定 —— 本 story **不**新增 / **不**修改任何错误码定义
- 4001 "当前宝箱不存在" + 4002 "宝箱尚未解锁" 在 §3 已定义；本 story 在 §7.1 / §7.2 错误码段钦定其触发条件（4001 = user_chests 表无行；4002 = chest counting 状态时尝试开箱）
- 1008 "幂等冲突" 在 §3 已定义；**r11 review 锁定**：1008 在节点 7 阶段本接口 `POST /chest/open` **已退役**（X-lock serialize 排队下步骤 5b 永远观察到 `status='success'`，无可达触发路径；步骤 5b 兜底分支万一读到 `status='pending'` 返回 1009 而非 1008）；1008 全局错误码定义在 §3 保留供其他业务（如合成 §9）或未来其他幂等接口（不同实现路径）按需使用

### 跨 epic 依赖追溯

- **上游冻结**：
  - §3 错误码表 / §2.5 全局约定 ← Story 4.1 锚定
  - §6.2 GET /steps/account 字段表 ← Story 7.1 锚定
  - §5.1 GET /home `chest.*` 字段 ← Story 4.1 锚定（节点 2 阶段含 `chest.status` / `chest.unlockAt` / `chest.openCostSteps` / `chest.remainingSeconds`，与本 story §7.1 字段同义对齐）
  - 数据库 §5.6 user_chests / §5.7 chest_open_logs / §5.8 cosmetic_items / §6.7 / §6.8 / §6.9 / §8.3 开箱事务 ← 数据库设计文档已锚定
- **下游强依赖**（本 story 不动后才能开工）：
  - Story 20.2 ~ 20.9（Epic 20 后续实装 story）
  - iOS Epic 21.1 ~ 21.5（节点 7 客户端实装 story）
  - 间接影响 Epic 23（节点 8 Story 23.5 修改本接口实装为入仓事务）

### 测试 / 验证

- **本 story 不写代码 → 无单测 / 集成测**：本 story 的"测试"= AC7 列出的 5 项文档一致性验证（字段表对齐 / 错误码一致 / 跨章节引用 / 冻结声明结构 / git diff 范围）
- **如需 schema 形式化 lint**：节点 7 阶段**不**引入（V1 文档以 markdown 表 + JSON 示例为唯一真相，无 OpenAPI / AsyncAPI / JSON Schema 形式化定义工具链）
- **下游验证**：本 story done 后由 Story 20.5 实装时的 dockertest 集成测试（curl GET /chest/current → 验证 response 与 §7.1 字段表对齐）+ Story 20.6 实装时的 dockertest 集成测试（curl POST /chest/open → 验证 response 与 §7.2 字段表对齐 + idempotencyKey 重复调用走 cache 路径）+ Story 20.9 Layer 2 完整事务集成测试做真实验证

### 范围红线 + 风险

- **红线**：本 story **不**修改任何 Go / Swift 代码、**不**修改其他 6 份 `docs/宠物互动App_*.md`、**不**修改 `_bmad-output/` 下其他文档（除自己 story 文件 + sprint-status.yaml）
- **红线**：本 story **不**锚定 dev 端点（`/dev/force-unlock-chest` / `/dev/grant-cosmetic-batch`）的 schema（dev 接口不进客户端契约文档）
- **风险**：编辑 V1 文档时可能误改 §5.1 / §6.2 / §8.1 等已锚定章节（如不小心改了 `stepAccount` 字段表）→ AC7.5 git diff 范围检查兜底
- **风险**：现有 §7.2 JSON 示例中 `reward.userCosmeticItemId = "91001"` 是节点 2 阶段示例值（Story 4.1 锚定时是占位字面量），本 story 改为 `"0"` 是节点 7 阶段实际行为；如未来节点 8 Story 23.5 落地时应再次更新（届时改为真实主键如 `"91001"`）—— 这是预期的契约层"渐进真实化"，不是 schema 变更
- **风险**：字段同义对齐（`stepAccount` / `nextChest` / `reward.cosmeticItemId` 等）与跨章节引用一致性 —— 需用 grep + diff 工具反复校对，避免局部一致但全局漂移

### Project Structure Notes

- 本 story 唯一编辑文件：`docs/宠物互动App_V1接口设计.md`（绝对路径 `C:/fork/cat/docs/宠物互动App_V1接口设计.md`）
- 不涉及 Go / Swift 源码目录变更 —— 与 `docs/宠物互动App_Go项目结构与模块职责设计.md` / `docs/宠物互动App_iOS客户端工程结构与模块职责设计.md` 当前结构完全兼容

### References

- [Source: docs/宠物互动App_V1接口设计.md#1] 文档说明 + 现有契约冻结声明（节点 2 / 节点 3 / 节点 4 协议骨架 / 节点 4 房间业务 / 节点 5 宠物状态业务 / 节点 6 表情业务），本 story 在此末尾追加节点 7 宝箱业务冻结声明
- [Source: docs/宠物互动App_V1接口设计.md#2.5] 字段类型与编码约定（BIGINT 字符串化、int 不字符串化、ISO 8601 时间），本 story 引用
- [Source: docs/宠物互动App_V1接口设计.md#3] 错误码全局表（1001 / 1002 / 1005 / 1008 / 1009 / 3002 / 4001 / 4002），本 story 引用
- [Source: docs/宠物互动App_V1接口设计.md#5.1] GET /home 节点 2 阶段 `data.chest.*` 字段表（与本 story §7.1 同义对齐），本 story **不**修改 §5.1
- [Source: docs/宠物互动App_V1接口设计.md#6.2] GET /steps/account 字段表（`stepAccount.{totalSteps, availableSteps, consumedSteps}` 三字段，与本 story §7.2 `data.stepAccount.*` 同义对齐），本 story **不**修改 §6.2
- [Source: docs/宠物互动App_V1接口设计.md#7.1] GET /chest/current 现有内容（草稿 JSON 示例 + status 枚举段），本 story 就地扩充
- [Source: docs/宠物互动App_V1接口设计.md#7.2] POST /chest/open 现有内容（草稿 request JSON + 服务端逻辑 7 步 bullet + response JSON 示例），本 story 就地扩充
- [Source: docs/宠物互动App_V1接口设计.md#8.1] GET /cosmetics/catalog 现有草稿（节点 8 Story 23.1 锚定时定稿；本 story §7.2 `data.reward.*` 字段名与现有 §8.1 草稿示例对齐），本 story **不**修改 §8.1
- [Source: docs/宠物互动App_数据库设计.md#5.6] user_chests 表 schema（`id` / `user_id` / `status` / `unlock_at` / `open_cost_steps` / `version` / `created_at` / `updated_at` + `UNIQUE KEY uk_user_id` + `KEY idx_status_unlock_at`），本 story 引用（**不**修改）
- [Source: docs/宠物互动App_数据库设计.md#5.7] chest_open_logs 表 schema（`id` / `user_id` / `chest_id` / `cost_steps` / `reward_user_cosmetic_item_id` / `reward_cosmetic_item_id` / `reward_rarity` / `created_at` + 索引），本 story 引用
- [Source: docs/宠物互动App_数据库设计.md#5.8] cosmetic_items 表 schema（全字段 + 索引；**不含** `render_config` —— 节点 10 才加），本 story 引用
- [Source: docs/宠物互动App_数据库设计.md#6.7] user_chests.status 枚举（1 = counting / 2 = unlockable），本 story 引用
- [Source: docs/宠物互动App_数据库设计.md#6.8] cosmetic_items.slot 枚举（1 ~ 7 + 99），本 story 引用
- [Source: docs/宠物互动App_数据库设计.md#6.9] cosmetic_items.rarity 枚举（1 = common / 2 = rare / 3 = epic / 4 = legendary），本 story 引用
- [Source: docs/宠物互动App_数据库设计.md#8.3] 开箱事务边界（FOR UPDATE 锁 chest + step_account + 扣步 + 抽奖 + 写日志 + 刷下一轮），本 story 引用
- [Source: _bmad-output/planning-artifacts/epics.md#Story 20.1] AC 钦定 §7.1 / §7.2 必须锚定的字段 + 错误码 + 冻结声明
- [Source: _bmad-output/implementation-artifacts/17-1-接口契约最终化.md] Story 17.1 已 done 姊妹 story（节点 6 表情契约锚定），参考编辑模式 + 冻结声明文案模板
- [Source: _bmad-output/implementation-artifacts/14-1-接口契约最终化.md] Story 14.1 已 done 姊妹 story（节点 5 宠物状态契约锚定），参考编辑模式
- [Source: _bmad-output/implementation-artifacts/7-1-接口契约最终化.md] Story 7.1 已 done 姊妹 story（节点 3 步数契约锚定），参考 stepAccount 字段表
- [Source: _bmad-output/implementation-artifacts/4-1-接口契约最终化.md] Story 4.1 已 done 姊妹 story（节点 2 §5.1 GET /home 含 chest 占位字段锚定），参考 chest 字段表 + Future Fields 模式
- [Source: _bmad-output/implementation-artifacts/sprint-status.yaml] Story 20.1 状态行 + Epic 20 状态行（由本 story create + dev-story + code-review 流程逐步推进）

## Dev Agent Record

### Agent Model Used

claude-opus-4-7 (1M context)

### Debug Log References

（无 —— 纯文档 story，无运行时 debug）

### Completion Notes List

> **r12 修订（2026-05-14）**：本 Completion Notes 已**重写**以反映 11 轮 fix-review 后的 r11 最终状态。原 r1 dev-story 完成笔记（Redis 幂等键 + TTL 24h + 事务后写 Redis + 1008 主动返回 / git diff 范围仅 3 文件）已不成立；详见下方迭代记录 + Change Log r1~r11 各轮 commit 链。

**最终状态（r11 锁定，2026-05-14）**：

- 纯文档 story，无 Go / Swift 代码改动。
- §1 末尾追加节点 7 宝箱业务契约冻结声明（与节点 2 / 3 / 4 / 5 / 6 冻结声明并列，文案模板对齐 Story 17.1）。冻结声明包含 4 步影响流程、`open_cost_steps` 默认值 1000 / `status` 枚举（1/2）/ `cosmetic_items` seed 数量约束 / `drop_weight` 加权抽奖算法 / **MySQL `chest_open_idempotency_records` 同事务持久化幂等存储介质冻结**五条契约附属条款。
- §7.1 GET /chest/current 章节重构：
  - 新增 `#### 接口元信息` / `#### 请求` / `#### 服务端逻辑` / `#### 可能的错误码` / `#### 关键约束` 五块 H4 内容
  - 把原 `#### 返回示例` 升级为 `#### 响应体`，前置 5 字段表（id / status / unlockAt / openCostSteps / remainingSeconds）
  - 补充已解锁待开 edge case 的 JSON 示例
  - 保留原 `#### status 枚举` 段（仅在枚举值后补充括号注释）
  - 错误码列表：1001 / 1005 / 1009 / 4001（明确不触发 1002 / 4002 / 4003）
  - 关键约束：status 动态判定 vs DB 原始值的设计意图、remainingSeconds `max(0,...)` 兜底、本地倒计时与 server 状态校对、首次登录后 chest 解锁倒计时基线、BIGINT 主键字符串化、client 缓存策略
- §7.2 POST /chest/open 章节重构（**r5~r11 锁定的 MySQL 同事务持久化幂等设计**）：
  - 新增 `#### 接口元信息` 块（含事务边界 = MySQL 事务 + 幂等机制 = MySQL `chest_open_idempotency_records` 表）
  - `#### 请求体` 在 JSON 示例之前插入 idempotencyKey 字段表（character set `[A-Za-z0-9_:-]` + length 1-128）
  - `#### 服务端逻辑` 重构为 r11 锁定的完整步骤：步骤 3 committed success 幂等命中预检（autocommit SELECT，免 rate_limit）+ 步骤 4 rate_limit（仅对未命中 idempotency 的请求计入配额）+ 步骤 5 业务事务（步骤 5a `INSERT ... ON DUPLICATE KEY UPDATE id=LAST_INSERT_ID(id)` 预声明 pending 行借 X-lock 阻塞同 key 并发 + 步骤 5b cached success 短路读 + 步骤 5c~5i 业务表写入 + 步骤 5k UPDATE 设 `status='success'` + `response_json` 同事务原子提交；schema 为二态机 `('pending', 'success')`；`response_json` 缓存**不**含 `nextChest.status` / `remainingSeconds` / 顶层 `requestId`，cached success 返回路径同源同时刻补算这三字段）
  - 把原 `#### 返回示例` 升级为 `#### 响应体`，前置三段字段表（reward 7 字段 / stepAccount 3 字段 / nextChest 5 字段）
  - 更新 JSON 示例：`reward.userCosmeticItemId` 从 `"91001"` → `"0"`（节点 7 占位），补 `iconUrl` 字段
  - 错误码列表：1001 / 1002 / 1005 / 1009 / 3002 / 4001 / 4002；**1008 在节点 7 阶段本接口已退役**（r11 review 锁定，无可达触发路径，全局错误码定义保留）
  - 关键约束：节点 7 vs 节点 8 `userCosmeticItemId` 占位规则、idempotencyKey 字符集与长度、client 重试策略（含 r11 修订：首次重试间隔 ≥ 200ms 避开 pending 窗口 + 撞 1005 退避到下个限频窗口 60s）、事务边界（**所有幂等记录写入都在业务事务内同事务原子提交**；移除 Redis 在 chest_open idempotency 路径上的所有角色）、加权抽奖语义、跨字段同义对齐、BIGINT 主键字符串化、r7 移除 best-effort failed upsert 决策、rate_limit 位置 r10 调整 + r11 修订、MVCC 下 pending 不可见 r11 决策、1008 错误码 r11 退役决策
- 跨章节同义对齐验证通过：
  - `data.stepAccount.{totalSteps, availableSteps, consumedSteps}` 与 §6.2 字段名一致
  - `data.nextChest.{id, status, unlockAt, openCostSteps, remainingSeconds}` 与 §7.1 字段名一致
  - `data.reward.*` 与现有 §8.1 草稿字段名一致（cosmeticItemId / name / slot / rarity / iconUrl / assetUrl）
  - 错误码 1001 / 1002 / 1005 / 1009 / 3002 / 4001 / 4002 message 全部与 §3 全局表字面量一致；1008 定义保留但本接口不触发
- **实际 git diff 范围**（r12 review 修订前后对比）：r1 dev-story 完成笔记声称"仅 V1 文档 + sprint-status + 本 story 文件三处修改"；**11 轮 fix-review 后实际 diff 包括**：
  1. `docs/宠物互动App_V1接口设计.md`（§1 末尾 + §7.1 + §7.2 多处 + §13.3 + 节点 7 冻结声明）
  2. `docs/宠物互动App_数据库设计.md`（**新增 §5.16 `chest_open_idempotency_records` 表 schema** + 修订 §8.3 开箱事务边界）
  3. `_bmad-output/implementation-artifacts/sprint-status.yaml`
  4. `_bmad-output/implementation-artifacts/20-1-接口契约最终化.md`（本文件）
  5. `docs/lessons/2026-05-14-*-20-1-r*.md` 系列 lesson（r3~r11 每轮一份 + r12 一份）
  6. `docs/lessons/index.md`（多次追加）
- AC7 文档一致性 5 项验证（r11 锁定后）全部通过

### File List

**r12 修订（2026-05-14）**：以下是 11 轮 fix-review 后的真实 diff 范围（取代 r1 完成笔记中的"3 文件"声明）：

- `docs/宠物互动App_V1接口设计.md`（修改：§1 末尾追加节点 7 宝箱业务冻结声明 + §7.1 / §7.2 章节完整重构 + r5~r11 各轮关键约束段累计扩充 + §13.3 client 重试伪代码）
- `docs/宠物互动App_数据库设计.md`（**新增** §5.16 `chest_open_idempotency_records` 表 schema + 修订 §8.3 开箱事务边界 + 相关锚点交叉引用）
- `_bmad-output/implementation-artifacts/sprint-status.yaml`（修改：`20-1-接口契约最终化` 状态 ready-for-dev → in-progress → review；last_updated 注释多次更新）
- `_bmad-output/implementation-artifacts/20-1-接口契约最终化.md`（本文件；Status / Tasks 勾选 / Dev Agent Record 填充 + r12 修订）
- `docs/lessons/2026-05-14-idempotency-set-fail-retry-and-data-integrity-error-mapping-20-1-r3.md`（r3 round）
- `docs/lessons/2026-05-14-idempotency-sentinel-short-ttl-eliminates-redis-writeback-lockout-20-1-r4.md`（r4 round）
- `docs/lessons/2026-05-14-db-same-tx-idempotency-replaces-redis-writeback-fragility-20-1-r5.md`（r5 round）
- `docs/lessons/2026-05-14-idempotency-pre-claim-must-be-inside-business-tx-20-1-r6.md`（r6 round）
- `docs/lessons/2026-05-14-idempotency-no-async-failed-compensation-and-no-cached-requestId-20-1-r7.md`（r7 round）
- `docs/lessons/2026-05-14-design-doc-cross-section-summary-must-track-canonical-20-1-r8.md`（r8 round）
- `docs/lessons/2026-05-14-time-derived-fields-exhaustive-exclusion-from-idempotency-cache-20-1-r9.md`（r9 round）
- `docs/lessons/2026-05-14-rate-limit-must-yield-to-idempotency-hit-precheck-20-1-r10.md`（r10 round）
- `docs/lessons/2026-05-14-mvcc-invisibility-of-uncommitted-pending-rows-20-1-r11.md`（r11 round）
- `docs/lessons/2026-05-14-story-file-must-track-canonical-contract-drift-20-1-r12.md`（r12 round，本轮新增）
- `docs/lessons/index.md`（多次追加）

### Change Log

| 日期 | 内容 | 作者 |
|---|---|---|
| 2026-05-14 | Story 20.1 dev-story 落地（r1）：V1 接口设计 §7.1 / §7.2 节点 7 宝箱契约锚定（**初版 Redis 幂等键 + TTL 24h 设计**）+ §1 末尾追加节点 7 冻结声明；状态 ready-for-dev → review | Dev (claude-opus-4-7) |
| 2026-05-14 | r3 fix-review：1008 / SET fail retry / 数据完整性错误码映射（详见 lesson `2026-05-14-idempotency-set-fail-retry-and-data-integrity-error-mapping-20-1-r3.md`） | fix-review (claude-opus-4-7) |
| 2026-05-14 | r4 fix-review：Redis sentinel + 短 TTL 消除 writeback lockout（详见 lesson `2026-05-14-idempotency-sentinel-short-ttl-eliminates-redis-writeback-lockout-20-1-r4.md`） | fix-review (claude-opus-4-7) |
| 2026-05-14 | r5 fix-review：**架构级翻转 —— Redis writeback 设计退役，改为 MySQL `chest_open_idempotency_records` 表 + 业务事务同事务原子写**（详见 lesson `2026-05-14-db-same-tx-idempotency-replaces-redis-writeback-fragility-20-1-r5.md`） | fix-review (claude-opus-4-7) |
| 2026-05-14 | r6 fix-review：预声明 INSERT 也纳入业务事务（消除 r5 残留的 pending 卡死悖论）（详见 lesson `2026-05-14-idempotency-pre-claim-must-be-inside-business-tx-20-1-r6.md`） | fix-review (claude-opus-4-7) |
| 2026-05-14 | r7 fix-review：移除 best-effort post-rollback failed 异步补偿 + response_json 缓存禁止包含 requestId；schema 简化为二态机 `('pending', 'success')`（详见 lesson `2026-05-14-idempotency-no-async-failed-compensation-and-no-cached-requestId-20-1-r7.md`） | fix-review (claude-opus-4-7) |
| 2026-05-14 | r8 fix-review：设计文档跨章节 summary 必须随 canonical 章节同步迭代（详见 lesson `2026-05-14-design-doc-cross-section-summary-must-track-canonical-20-1-r8.md`） | fix-review (claude-opus-4-7) |
| 2026-05-14 | r9 fix-review：time-derived 字段（`remainingSeconds`）在幂等缓存里必须穷举剔除，cached replay 同源同时刻补算（详见 lesson `2026-05-14-time-derived-fields-exhaustive-exclusion-from-idempotency-cache-20-1-r9.md`） | fix-review (claude-opus-4-7) |
| 2026-05-14 | r10 fix-review：rate_limit middleware 让位于 idempotency 命中预检，cached replay 不消耗 quota（详见 lesson `2026-05-14-rate-limit-must-yield-to-idempotency-hit-precheck-20-1-r10.md`） | fix-review (claude-opus-4-7) |
| 2026-05-14 | r11 fix-review：MVCC 决定 autocommit SELECT 看不到首事务的 pending 行 → r10 "pending 命中 → 1008 + 免配额" 路径不可实现；**1008 在节点 7 阶段本接口退役**（X-lock serialize 排队下无可达触发路径，全局定义保留）；cached success 返回路径必须同源同时刻补算 `nextChest.status` + `remainingSeconds` 两字段；client 重试间隔 ≥ 200ms（详见 lesson `2026-05-14-mvcc-invisibility-of-uncommitted-pending-rows-20-1-r11.md`） | fix-review (claude-opus-4-7) |
| 2026-05-14 | r12 fix-review：**story 文件自身同步追踪契约迭代** —— Tasks / AC / Completion Notes / File List / Change Log 全部从 r1 Redis 设计描述更新为 r11 MySQL 同事务持久化最终状态；AC7.5 scope 验证段同步反映实际 diff 范围（含 DB 设计文档 + lessons 系列）（详见 lesson `2026-05-14-story-file-must-track-canonical-contract-drift-20-1-r12.md`） | fix-review (claude-opus-4-7) |
