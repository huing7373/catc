# Story 26.1: 接口契约最终化（POST /cosmetics/equip + POST /cosmetics/unequip）

Status: review

<!-- Validation 可选。建议运行 validate-create-story 在 dev-story 前做一次质检。 -->

## Story

As a 跨端契约负责人,
I want 在写第一行 server Epic 26 业务代码（Story 26.2 ~ 26.6）之前，把节点 9 涉及的 `POST /api/v1/cosmetics/equip` 与 `POST /api/v1/cosmetics/unequip` 两个 REST 写接口的字段层 schema 在 `docs/宠物互动App_V1接口设计.md` §8.3 / §8.4 里逐字段锚定（含请求体必填项 / 类型 / 长度约束、`petId` / `userCosmeticItemId` BIGINT 字符串化语义（与 §2.5 全局约定一致）、`slot` 枚举值域 `{1,2,3,4,5,6,7,99}` 与数据库设计 §6.8 `cosmetic_items.slot` 同义、equip response data `{petId, equipped: {slot, userCosmeticItemId, cosmeticItemId, name}}` schema、unequip response data `{petId, slot, unequipped: true}` schema、equip 同槽换装事务语义（自动卸下旧装备）、错误码 5001 / 5002 / 5003 / 5008（equip）+ 5004（unequip）+ 通用 1001 / 1002 / 1005 / 1009 触发条件、auth 要求、MySQL 事务边界声明、与数据库设计 §5.10 `user_pet_equips` UNIQUE 约束（`UNIQUE(pet_id, slot)` + `UNIQUE(user_cosmetic_item_id)`）兜底语义对齐、与 §8.1 / §8.2 装扮查询接口的 `cosmeticItemId` / `userCosmeticItemId` 跨接口同义对齐声明、节点 9 阶段冻结声明），
so that iOS Epic 27（激活仓库穿戴按钮 + EquipUseCase / UnequipUseCase + 文字降级渲染）和 server Epic 26 后续 Story（26.2 ~ 26.6）可以基于同一份冻结契约**并行**开工，不再出现"REST 写接口字段名 / 大小写不一致 / equip 请求是传 `userCosmeticItemId`（实例 id）还是 `cosmeticItemId`（配置 id）不清楚 / unequip 请求按 `slot` 指定还是按 `userCosmeticItemId` 指定不清楚 / `slot` 枚举与 DB §6.8 对不上 / equip response 是否回显新装备元信息（slot/name）不清楚 / 同槽换装是 server 自动卸下旧装备还是 client 需先调 unequip 不清楚 / 错误码 5001-5008 触发条件不清楚 / 5008（装扮已装备）与 5003（道具状态不可用）边界不清楚 / unequip 空槽位返回 5004 还是幂等成功不清楚 / equip / unequip 是否需要 idempotencyKey 不清楚 / 事务回滚后实例 status 与 user_pet_equips 是否保持一致不清楚 / iOS Story 27.2 EquipUseCase Codable struct 字段是否可空不清楚"的返工。

## 故事定位（Epic 26 第一条 = 节点 9 第一个 Server Epic 的契约起手；对标 Story 4.1 / 7.1 / 10.1 / 11.1 / 14.1 / 17.1 / 20.1 / 23.1 在各自 Epic 的角色，本 story 是 Epic 26 **唯一的纯文档 story**，开局而非收官）

- **Epic 26 进度**：本 story → 26.2 (user_pet_equips migration) → 26.3 (POST /cosmetics/equip 事务，含同槽换装) → 26.4 (POST /cosmetics/unequip 事务) → 26.5 (Layer 2 集成测试 - 穿戴事务全流程) → 26.6 (GET /home 扩展 - pet.equips 真实数据)。**26.1 是 26.2 ~ 26.6 的强前置**：handler 层 request / response DTO 必须严格按 26.1 锚定后的 schema；service 层错误码映射必须严格按 26.1 锚定的 5001 / 5002 / 5003 / 5004 / 5008 触发条件；同时是 iOS Epic 27 的强前置（Story 27.1 激活 Story 24.5 预留按钮 + 27.2 EquipUseCase / UnequipUseCase + 27.3 装扮文字降级 UI + 27.4 同槽位换装动效 + 27.5 主界面猫显示区实时刷新 必须严格按 26.1 冻结契约）。

- **epics.md AC 钦定**：`_bmad-output/planning-artifacts/epics.md` §Story 26.1（行 3496-3514）已**精确**列出 §8.3 必须锚定的字段（request: `{petId: string, userCosmeticItemId: string}`；response data: `{petId, equipped: {slot, userCosmeticItemId, cosmeticItemId, name}}`；错误码 5001 道具不存在 / 5002 道具不属于当前用户 / 5003 道具状态不可用 / 5008 装扮已装备）+ §8.4 必须锚定的字段（request: `{petId: string, slot: int}`；response data: `{petId, slot, unequipped: true}`；错误码 5004 装备槽位不匹配）+ "标注契约自此 Epic 起冻结"。

- **Story 4.1 / 7.1 / 10.1 / 11.1 / 14.1 / 17.1 / 20.1 / 23.1 历史遗留对接**：
  - 本 story 必须把 V1 文档 §8.3（`## 8.3 穿戴装扮` → `### POST /api/v1/cosmetics/equip`，行 1448-1489）从"节点 2 阶段草稿（仅有 `#### 请求体` + `#### 服务端逻辑` 概述 bullet + `#### 返回示例` JSON 示例）"升级为"已由 Story 26.1 锚定"，并**补齐**该节缺失的"接口元信息 / 请求体字段表 / 服务端逻辑细化（含同槽换装事务步骤）/ 响应体字段表 / 可能的错误码 / 关键约束"内容（草稿请求体 / 服务端逻辑 bullet / JSON 示例已存在，保留）。
  - 本 story 必须把 V1 文档 §8.4（`## 8.4 卸下装扮` → `### POST /api/v1/cosmetics/unequip`，行 1491-1517）从"节点 2 阶段草稿（仅有 `#### 请求体` + `#### 返回示例` JSON 示例，**无** `#### 服务端逻辑`）"升级为"已由 Story 26.1 锚定"，并**补齐**该节缺失的"接口元信息 / 请求体字段表 / 服务端逻辑（含事务步骤）/ 响应体字段表 / 可能的错误码 / 关键约束"内容（草稿请求体 / JSON 示例已存在，保留）。
  - 本 story 必须在 §1 "契约冻结策略"区追加节点 9 穿戴 / 卸下业务契约冻结声明（与节点 2 / 节点 3 / 节点 4 / 节点 5 / 节点 6 / 节点 7 / 节点 8 冻结声明并列，**不**覆盖 / 删除任何已有声明）。

- **下游立即依赖**：
  - **Story 26.2（user_pet_equips migration）**：migration DDL 严格按 数据库设计 §5.10（全字段 + `UNIQUE KEY uk_pet_slot (pet_id, slot)` + `UNIQUE KEY uk_user_cosmetic_item_id (user_cosmetic_item_id)` + `KEY idx_user_pet (user_id, pet_id)`）；本 story **不**改 数据库设计 §5.10 schema（DB 已锚定，DB → API 方向，**不反向**修改 DB），但 §8.3 关键约束段必须显式引用这两个 UNIQUE 约束作为并发兜底（DB UNIQUE(pet_id, slot) 兜并发同槽 equip / DB UNIQUE(user_cosmetic_item_id) 兜同实例并发 equip）。
  - **Story 26.3（POST /cosmetics/equip 事务，含同槽换装）**：handler 响应严格按 26.1 §8.3 锚定的 `{petId, equipped: {slot, userCosmeticItemId, cosmeticItemId, name}}` 字段；service 层流程严格按 26.1 §8.3 服务端逻辑段（查 user_cosmetic_items WHERE id=? AND user_id=current → 不存在 5001 / 不属于当前用户 5002；校验实例 status=1 (in_bag) → 否则 5003；查 cosmetic_items 拿 slot；查该 pet 该 slot 是否已有装备 → 已有则删旧 user_pet_equips 行 + UPDATE 旧实例 status=1 [自动卸下]；INSERT user_pet_equips；UPDATE 当前实例 status=2 (equipped)；返回 equipped 对象）；错误码 5001 / 5002 / 5003 / 5008 + 1001 / 1002 / 1005 / 1009 严格按 26.1 §8.3 错误码表；MySQL 事务边界严格按 26.1 §8.3 事务声明（数据库设计 §8.4 穿戴事务）。
  - **Story 26.4（POST /cosmetics/unequip 事务）**：handler 响应严格按 26.1 §8.4 锚定的 `{petId, slot, unequipped: true}` 字段；service 层流程严格按 26.1 §8.4 服务端逻辑段（查 user_pet_equips WHERE pet_id=? AND slot=? → 不存在 5004；校验 pet 属于当前用户 → 否则 5002；拿到 user_cosmetic_item_id；DELETE user_pet_equips 行；UPDATE 实例 status=1 (in_bag)；返回 unequipped 对象）；错误码 5004 / 5002 + 1001 / 1002 / 1005 / 1009 严格按 26.1 §8.4 错误码表；MySQL 事务边界严格按 26.1 §8.4 事务声明。
  - **Story 26.5（Layer 2 集成测试 - 穿戴事务全流程）**：本 story 锚定的"事务回滚后 user_pet_equips 与 user_cosmetic_items.status 必须保持一致（NFR2）"+ "DB UNIQUE 约束兜并发"是 26.5 集成测试的契约依据；本 story **不**实装 26.5 测试（属 Story 26.5 实装层）。
  - **Story 26.6（GET /home 扩展 - pet.equips 真实数据）**：本 story 锚定 §8.3 equip response `equipped.{slot, userCosmeticItemId, cosmeticItemId, name}` 与 §5.1 GET /home `pet.equips[]`（节点 9 由 Story 26.6 真实化为 `[{slot, userCosmeticItemId, cosmeticItemId, name, rarity, assetUrl}]`）的字段同义对齐；本 story **不**改 §5.1 GET /home schema（已在 Story 4.1 锚定，`pet.equips` 是 Future Field 由 Story 26.6 自然激活，**不**视为契约变更 —— 见 §1 行 22），仅在 §8.3 关键约束段标注"`equipped.userCosmeticItemId` / `equipped.cosmeticItemId` / `equipped.slot` / `equipped.name` 与 §5.1 `pet.equips[]` 同名字段节点 9 起同义对齐（§5.1 额外含 `rarity` / `assetUrl`，由 Story 26.6 从 cosmetic_items JOIN 补充）"。
  - **iOS Story 27.2（EquipUseCase / UnequipUseCase）**：iOS 端 equip request 严格按 `{petId: String, userCosmeticItemId: String}`（两字段都是 BIGINT 字符串化，非可选）；equip response `EquippedDTO` 严格按 `{slot: Int, userCosmeticItemId: String, cosmeticItemId: String, name: String}`；unequip request 严格按 `{petId: String, slot: Int}`；unequip response 严格按 `{petId: String, slot: Int, unequipped: Bool}`（`unequipped` 恒为 `true`）。
  - **iOS Story 27.1（激活 Story 24.5 预留按钮）**：节点 8 阶段按钮 disabled（Story 24.5 已预留）；节点 9 起对 `instances[].status == 1 (in_bag)` 的实例按钮 enabled 触发 EquipUseCase，对 `status == 2 (equipped)` 的实例显示"已装备"标签（替代穿戴按钮，与 §8.2 inventory `instances[].status` 枚举对齐）。

- **节点 9 vs 节点 8 阶段差异说明（本 story 锚定的跨节点契约同义点）**：
  - **节点 8 阶段（Epic 23 / Epic 24 完成后）**：`user_cosmetic_items` 表已建（Story 23.2）+ 开箱奖励入仓（Story 23.5）；但**无** `user_pet_equips` 表（Story 26.2 才建）+ **无** equip / unequip 接口；实践中所有实例 `status = 1 (in_bag)`，§8.2 inventory `instances[].status` 永远返回 1，§5.1 GET /home `pet.equips` 强制返回 `[]`（节点 2 占位，Story 26.6 才真实化）。
  - **节点 9 阶段（Epic 26 完成后）**：Story 26.2 建 `user_pet_equips` 表 + Story 26.3/26.4 实装 equip/unequip 事务 → 实例可被穿戴（status 1↔2 切换）+ §8.2 inventory 开始返回 `status = 2` 的实例 + §5.1 GET /home `pet.equips` 由 Story 26.6 填充真实数据。
  - **client 处理规则**：iOS 端穿戴后**以 server response 为准**（NFR3 状态以 server 为最终权威）：equip 成功 → `equipped` 对象回写本地状态 + 主界面猫立即刷新（Story 27.5）；unequip 成功 → 对应 slot 标签移除；client 本地可做预展示动效（Story 27.4 同槽换装动效），但实例归属 / status 以 server 响应为最终态（NFR4）。节点 9 阶段所有 cosmetic 都走**文字降级渲染**（FR45 —— `render_config` 字段节点 10 Story 29.2 才加 + `asset_url` 节点 9 阶段是 placeholder URL）；本 story **不**锚定 `render_config` / 文字降级字段（属节点 10 Story 29.1 范围）。
  - **契约层升级路径**：§5.1 GET /home `pet.equips` 节点 2 占位 `[]` → 节点 9 Story 26.6 真实化**不**视为契约变更（已在 §1 行 22 Future Fields 声明锚定 + Story 4.1 冻结边界说明锚定）；本 story 仅**引用 / 对齐**该升级路径声明，**不**反向修改 §5.1。

- **范围红线**：本 story **只**改 `docs/宠物互动App_V1接口设计.md` 一份文件 + sprint-status.yaml + 本 story 文件；**不**改任何 Go / Swift 代码、**不**改 `_bmad-output/` 下其他 yaml / md（除自己的 story 文件 + sprint-status.yaml 标记 ready-for-dev → in-progress → review → done）、**不**改其他 6 份 `docs/宠物互动App_*.md`（包括数据库设计 / Go 项目结构 / iOS 项目结构 / 时序图 / 总体架构 / MVP 节点规划 等 —— 它们对应的 schema 已锚定，本 story 只**对齐 / 引用**，不**修改**）。

**本 story 不做**（明确范围红线）：

- 不实装任何 Go handler / service / repo / 事务 / migration 代码（26.2 / 26.3 / 26.4 / 26.5 / 26.6 才做；本 story 是 contract first 的契约定稿）。
- 不实装任何 Swift Codable DTO / EquipUseCase / UnequipUseCase / 文字降级 UI 组件（iOS Epic 27 各 story 才做；本 story 提供给 iOS 的是文档 + Git commit hash，不是代码）。
- 不修改 `docs/宠物互动App_数据库设计.md` §5.10 user_pet_equips / §5.9 user_cosmetic_items / §5.8 cosmetic_items / §6.8 slot / §6.10 status / §8.4 穿戴事务 schema（已锚定；本 story 的 API 字段语义来源是数据库设计，方向是 DB → API，**不反向**修改 DB）。
- 不修改 §8.1 GET /cosmetics/catalog / §8.2 GET /cosmetics/inventory schema（已在 Story 23.1 锚定 + 节点 8 冻结声明已锚定；本 story 仅在 §8.3 / §8.4 引用 `cosmeticItemId` / `userCosmeticItemId` / `slot` 与 §8.1 / §8.2 同名字段跨接口同义对齐，**不**反向修改 §8.1 / §8.2）。
- 不修改 §5.1 GET /home schema（已在 Story 4.1 锚定 + `pet.equips` 是 Future Field 由 Story 26.6 自然激活，**不**视为契约变更；本 story **不**触及 §5.1，仅在 §8.3 关键约束段引用 `pet.equips[]` 与 `equipped` 字段同义对齐声明）。
- 不修改 §7.x 宝箱 / §6.x 步数 / §9.x 合成 / §10.x 房间 / §11.x 表情 / §12.x WS 章节（已在 Story 20.1 / 7.1 / 32.1（未来）/ 11.1 / 17.1 / 10.1 锚定；其中 §9.x 合成由 Story 32.1 锚定，**本 story 不预先决定** `userCosmeticItemId` 在合成 `POST /compose/upgrade` 中的语义）。
- 不修改 §3 错误码表（5001 / 5002 / 5003 / 5004 / 5008 + 1001 / 1002 / 1005 / 1009 已在 §3 全局定义；本 story 仅**引用**这些错误码到 §8.3 / §8.4 错误码段，**不**新增 / **不**修改 §3 错误码定义；注意 §3 已含 5005/5006/5007 合成错误码——本 story **不**触及，属 Story 32.1 范围）。
- 不为 schema 字段写 OpenAPI / AsyncAPI / JSON Schema 形式化定义（旧架构残留；新架构 V1 文档以 markdown 表格 + JSON 示例为唯一真相，参见 V1接口设计 §1）。
- 不锚定 `idempotencyKey`：穿戴 / 卸下接口**无** `idempotencyKey`（NFR5 仅"开箱、合成"两个资产消耗类接口要求 idempotencyKey；穿戴 / 卸下是状态切换非资产消耗，重复 equip 同一实例由 5008 拦截、重复 unequip 空槽位由 5004 拦截，天然防重，**不**引入 idempotencyKey 字段）；本 story 必须在 §8.3 / §8.4 关键约束段显式声明"本接口无 idempotencyKey + 重复语义由 5008 / 5004 + DB UNIQUE 约束兜底"。
- 不锚定文字降级 / `render_config` 字段（FR45 文字降级 + FR42 render_config 属节点 10 Epic 29 / Story 29.1 范围；本 story 锚定的 equip response `equipped` 对象**不**含 `assetUrl` / `renderConfig`，仅 `{slot, userCosmeticItemId, cosmeticItemId, name}`——与 epics.md §Story 26.1 AC 一致；`assetUrl` 在 §5.1 `pet.equips[]` 由 Story 26.6 补充，`renderConfig` 由 Story 29.6 补充，均**不**在本 story §8.3 范围）。
- 不写英文版接口文档（项目 communication_language=Chinese；用户 huing7373@gmail.com 单一开发者，无国际化诉求）。

## Acceptance Criteria

**AC1 — 文档定位与编辑范围**

唯一受编辑的文档：`docs/宠物互动App_V1接口设计.md`（绝对路径 `C:/fork/cat/docs/宠物互动App_V1接口设计.md`）。

- 编辑模式：**就地修改**已有 §8.3（`## 8.3 穿戴装扮` → `### POST /api/v1/cosmetics/equip`，行 1448-1489）章节内容（保留 H2/H3 标题 + 现有 `#### 请求体` JSON 示例 + 现有 `#### 服务端逻辑` bullet 概述 + 现有 `#### 返回示例` JSON 示例 + 末尾 `---`；补全接口元信息 / 请求体字段表 / 服务端逻辑细化 / 响应体字段表 / 可能的错误码 / 关键约束）+ **就地修改**已有 §8.4（`## 8.4 卸下装扮` → `### POST /api/v1/cosmetics/unequip`，行 1491-1517）章节内容（保留 H2/H3 标题 + 现有 `#### 请求体` JSON 示例 + 现有 `#### 返回示例` JSON 示例 + 末尾 `---`；补全接口元信息 / 请求体字段表 / 服务端逻辑 / 响应体字段表 / 可能的错误码 / 关键约束）+ §1 "契约冻结策略"区**追加**节点 9 穿戴 / 卸下业务契约冻结声明（与节点 2 ~ 节点 8 既有声明并列，**不**覆盖 / 删除）。
- 编辑形式：**保留**现有 markdown 结构（H2 / H3 / H4 标题层级 + ` ```json ` fenced code block + 段落顺序 + 章节末 `---` 分隔线）；只**补全**字段说明表格 + 必填 / 类型 / 长度约束 + 错误码列表 + 服务端逻辑细化 + 接口元信息（§8.3 已有请求体 / 服务端逻辑 bullet / 返回示例 JSON、§8.4 已有请求体 / 返回示例 JSON 全部保留）。
- 文件编码 UTF-8 + LF 行尾（与项目其他 .md 一致）。
- 修改后的文档**仍可直接被复制粘贴**作为 client / server 双方实装参照（所有 JSON 示例可直接 `curl` 出来比对；所有字段说明可直接当作"DTO / Codable struct 字段表"用）。
- 不删除任何**已有**字段示例或说明（仅追加 / 补全）；§1 已有节点 2-8 冻结声明 / §2 全局约定 / §3 错误码表 / §4 / §5 / §6 / §7 / §8.1 / §8.2 / §9 / §10 / §11 / §12 等已锚定章节**不动**。
- 现有 §8.3 `#### 返回示例` / §8.4 `#### 返回示例` 升级为 `#### 响应体` 下的子块时，JSON 内容**逐字未改**（字段表向现有 JSON 示例对齐，不改示例）。

**AC2 — §8.3 POST /api/v1/cosmetics/equip schema 锚定**

V1 文档 §8.3 编辑后必须包含以下完整内容（**就地修改**，新增 H4 块紧接 `### POST /api/v1/cosmetics/equip` 之后；现有 `#### 请求体` / `#### 服务端逻辑` / `#### 返回示例` 升级 / 扩充而非删除）：

**§8.3 接口元信息**（新增 H4 块）：

| 字段 | 值 |
|---|---|
| HTTP Method | POST |
| Path | /api/v1/cosmetics/equip |
| 认证 | **需要** Bearer token（auth 中间件） |
| 限频 | 默认（按 Story 4.5 rate_limit 默认值 60 次/分按 user_id 计） |
| 幂等 | **无** idempotencyKey（重复 equip 同一实例由 5008 拦截 + DB `UNIQUE(user_cosmetic_item_id)` 兜底；穿戴是状态切换非资产消耗，与 NFR5 仅约束"开箱 / 合成"一致） |
| 节点 | 节点 9（Epic 26 落地 / Epic 27 客户端集成） |
| 事务 | **需要** MySQL 事务（数据库设计 §8.4 穿戴事务：校验 + 同槽换装卸旧 + INSERT user_pet_equips + UPDATE 实例 status 必须原子，NFR1） |
| Query 参数 | **无** |

**§8.3 请求体字段表**（**就地扩充**现有 `#### 请求体`：保留现有 JSON 示例 + 前置字段表）：

| 字段 | 类型 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `petId` | string | 必填 | BIGINT 字符串化（与 §2.5 全局约定 + 数据库 `pets.id BIGINT UNSIGNED` 一致）；length ≥ 1 | 目标宠物 id；与 §5.1 `pet.petId` 同义；service 层校验该 pet 属于当前用户（否则 5002，详见服务端逻辑） |
| `userCosmeticItemId` | string | 必填 | BIGINT 字符串化（与数据库 `user_cosmetic_items.id BIGINT UNSIGNED` 一致）；length ≥ 1 | 要穿戴的玩家**实例** id（**不是**配置 id）；与 §8.2 `instances[].userCosmeticItemId` / §7.2 `reward.userCosmeticItemId`（节点 8 真实化后）同义；与数据库 §5.9 `user_cosmetic_items.id` 同义 |

**§8.3 服务端逻辑**（**就地扩充**现有 `#### 服务端逻辑` bullet：把概述 bullet 升级为带事务步骤编号的细化版，原 6 条 bullet 语义保留）：

1. **认证 & 限频**：auth 中间件校验 Bearer token；rate_limit 中间件按默认配置（user_id 60/min）限频。
2. **参数校验**：`petId` / `userCosmeticItemId` 非空且为合法 BIGINT 字符串 → 否则 1002。
3. **开启 MySQL 事务**（数据库设计 §8.4；以下步骤 4-9 全部在同一事务内，任一步失败整体回滚 —— NFR1）：
4. **查实例归属**：`SELECT id, cosmetic_item_id, status FROM user_cosmetic_items WHERE id = ? AND user_id = ?`（当前用户）→ 行不存在 → **5001 道具不存在**（含"实例 id 不存在"与"实例存在但不属于当前用户"两种 —— 实装层可合并为"WHERE id=? AND user_id=?"查不到即 5001；若需区分"存在但不属于当前用户"返 **5002 道具不属于当前用户**，由实装层决定，契约层接受两种映射，**但 5002 必须可达**：epics.md §Story 26.3 AC 显式要求"实例不属于当前用户 → 5002"+"pet 不属于当前用户 → 5002"两个 case 都触发 5002）。
5. **校验实例状态**：实例 `status != 1 (in_bag)` → 若 `status = 2 (equipped)` → **5008 装扮已装备**（该实例当前已被穿戴，重复 equip 拒绝）；若 `status = 3 (consumed)` / `4 (invalid)` → **5003 道具状态不可用**。
6. **校验 pet 归属**：`petId` 对应的 pet 必须属于当前用户 → 否则 **5002 道具不属于当前用户**（epics.md §Story 26.3 AC "pet 不属于当前用户 → 5002"）。
7. **查配置槽位**：`SELECT slot FROM cosmetic_items WHERE id = <实例.cosmetic_item_id>` → 拿到 `slot`（枚举 §6.8）。
8. **同槽换装（自动卸下旧装备）**：查 `user_pet_equips WHERE pet_id = ? AND slot = ?`：
   - **已有旧装备** → `DELETE` 该旧 `user_pet_equips` 行 + `UPDATE user_cosmetic_items SET status = 1 (in_bag) WHERE id = <旧实例 id>`（自动卸下旧装备，client **无需**先调 unequip）。
   - **该 slot 无装备** → 跳过本步。
9. **绑定 + 状态推进**：`INSERT INTO user_pet_equips (user_id, pet_id, slot, user_cosmetic_item_id) VALUES (...)` + `UPDATE user_cosmetic_items SET status = 2 (equipped) WHERE id = <当前实例 id>`。
10. **提交事务**（任一步 err → 整体回滚：user_pet_equips 与 user_cosmetic_items.status 必须保持 NFR2 一致性；回滚后旧装备仍 equipped + 新装备仍 in_bag + user_pet_equips 不变）。
11. **响应**：返回 `{petId, equipped: {slot, userCosmeticItemId, cosmeticItemId, name}}`（`cosmeticItemId` = 当前实例的 cosmetic_item_id；`name` = cosmetic_items.name；`slot` = 步骤 7 查到的 slot）。

**§8.3 响应体字段表**（**就地扩充**现有 `#### 返回示例`：保留现有 JSON 示例 + 前置字段表，标题改为 `#### 响应体`，原 JSON 降为 `##### 返回示例` H5 子块）：

成功（code = 0）`data`：

| 字段 | 类型 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `petId` | string | 必填 | BIGINT 字符串化；length ≥ 1 | 目标宠物 id（回显请求 `petId`）；与 §5.1 `pet.petId` 同义 |
| `equipped` | object | 必填 | 非 null | 本次穿戴结果对象 |
| `equipped.slot` | int | 必填 | 枚举 `{1,2,3,4,5,6,7,99}`，与数据库 §6.8 `cosmetic_items.slot` 同义（`1=hat / 2=gloves / 3=glasses / 4=neck / 5=back / 6=body / 7=tail / 99=other`） | 装扮部位（由配置决定，非请求传入） |
| `equipped.userCosmeticItemId` | string | 必填 | BIGINT 字符串化；length ≥ 1 | 玩家**实例** id（回显请求 `userCosmeticItemId`）；与 §8.2 `instances[].userCosmeticItemId` / §5.1 `pet.equips[].userCosmeticItemId`（节点 9 Story 26.6 真实化后）同义 |
| `equipped.cosmeticItemId` | string | 必填 | BIGINT 字符串化；length ≥ 1 | 装扮**配置** id；与 §8.1 `items[].cosmeticItemId` / §8.2 `groups[].cosmeticItemId` / §7.2 `reward.cosmeticItemId` / §5.1 `pet.equips[].cosmeticItemId` 同义 |
| `equipped.name` | string | 必填 | 1 ≤ length ≤ 64 | 装扮名称；与 §8.1 `items[].name` / 数据库 §5.8 `cosmetic_items.name` 同义 |

**§8.3 可能的错误码**（新增 H4 块）：

| code | 含义 | 触发条件 |
|---|---|---|
| 1001 | 未登录 / token 无效 | auth 中间件拦截（无 Authorization 头 / token 非法 / token 过期 / token 解析失败） |
| 1002 | 参数错误 | `petId` / `userCosmeticItemId` 缺失 / 非合法 BIGINT 字符串（请求侧错，client 应停止重试并修正参数） |
| 1005 | 操作过于频繁 | rate_limit 中间件拦截（已认证路由按 `user_id` 限频，每用户每分钟 > 60 次；按 Story 4.5 默认值，配置可调） |
| 5001 | 道具不存在 | 实例 id 在 `user_cosmetic_items` 查不到（含"实例不存在"；实装层可与 5002 合并 `WHERE id=? AND user_id=?` 查不到即 5001） |
| 5002 | 道具不属于当前用户 | 实例存在但 `user_id != current_user`，或目标 `petId` 对应的 pet 不属于当前用户（epics.md §Story 26.3 AC 显式要求两 case 均触发 5002） |
| 5003 | 道具状态不可用 | 实例 `status = 3 (consumed)` 或 `status = 4 (invalid)`（已合成消耗 / 无效，不可穿戴） |
| 5008 | 装扮已装备 | 实例 `status = 2 (equipped)`（该实例当前已被穿戴，重复 equip 拒绝；与 5003 区别：5008 是"已装备"可恢复状态、5003 是"不可用"终态） |
| 1009 | 服务繁忙 | DB 异常（事务中任一 SQL 返回 `err != nil`，含 driver / 网络 / 慢查询超时等）/ DB UNIQUE 约束并发冲突兜底（见关键约束）/ 内部 panic → 事务整体回滚 |

> **注**：`equipped.cosmeticItemId` 由 server 从实例反查（请求只传 `userCosmeticItemId` 实例 id，**不**传配置 id —— client 不需要也不应传配置 id）；同槽换装"自动卸下旧装备"是 server 事务内行为，client **无需**先调 unequip（与 epics.md §Story 26.3 "如果该槽位已有装备会自动先卸下"一致）。

**§8.3 关键约束**（新增 H4 块）：

- **无 idempotencyKey**契约：本接口**不**接受 `idempotencyKey`（NFR5 仅约束"开箱 / 合成"两个资产消耗类接口；穿戴是状态切换非资产消耗）。重复 equip 同一实例：第二次因实例已 `status = 2` 触发 **5008**；如未来引入 idempotencyKey，视为契约变更。
- **DB UNIQUE 并发兜底**契约：service 事务步骤 9 `INSERT user_pet_equips` 受数据库设计 §5.10 `UNIQUE KEY uk_pet_slot (pet_id, slot)` + `UNIQUE KEY uk_user_cosmetic_item_id (user_cosmetic_item_id)` 兜底——同 pet 同 slot 并发 equip 不同实例 / 同一实例并发 equip 时，DB UNIQUE 拒绝第二条 INSERT，service 捕获后回滚 + 返回 **1009**（NFR11：一件实例同一时间只能装备一次）。该并发兜底属契约一部分；如未来移除任一 UNIQUE 约束，视为契约变更。
- **事务一致性（NFR2）**契约：事务回滚后必须保持"所有 `status = 2 (equipped)` 实例必有对应 `user_pet_equips` 行，反之亦然"——即 equip 失败回滚后旧装备仍 equipped + 新装备仍 in_bag + user_pet_equips 行不变（无半成品状态）。如未来允许"部分提交"，视为契约变更。
- **同槽自动换装**契约：equip 时若目标 slot 已有装备，server 在**同一事务内**自动卸下旧装备（删旧 user_pet_equips 行 + 旧实例 status 回 in_bag）；client **不**需要先调 unequip。如未来改为"client 必须先 unequip 否则报错"，视为契约变更。
- **5008 vs 5003 边界**契约：5008 = 实例 `status = 2 (equipped)`（可恢复——unequip 后可再 equip）；5003 = 实例 `status = 3 (consumed)` / `4 (invalid)`（终态——不可恢复）。两者语义不同，client 处理策略不同（5008 提示"该道具已穿戴"、5003 提示"该道具不可用"）。如未来合并为单一错误码，视为契约变更。
- **跨接口字段同义对齐**契约：`equipped.userCosmeticItemId` / `equipped.cosmeticItemId` / `equipped.slot` / `equipped.name` 与 §8.2 `instances[].userCosmeticItemId` / §8.1 `items[].cosmeticItemId` / §6.8 `slot` 枚举 / §8.1 `items[].name` **同义**；节点 9 起与 §5.1 `pet.equips[]` 同名字段同义对齐（§5.1 `pet.equips[]` 额外含 `rarity` / `assetUrl`，由 Story 26.6 从 `cosmetic_items` JOIN 补充——本 story **不**在 §8.3 equip response 加这两字段，与 epics.md §Story 26.1 AC 一致）。§5.1 `pet.equips` 节点 2 占位 `[]` → 节点 9 Story 26.6 真实化**不**视为契约变更（已在 §1 行 22 Future Fields + Story 4.1 冻结边界锚定）；本 story **不**反向修改 §5.1。

**AC3 — §8.4 POST /api/v1/cosmetics/unequip schema 锚定**

V1 文档 §8.4 编辑后必须包含以下完整内容（**就地修改**，新增 H4 块；现有 `#### 请求体` / `#### 返回示例` JSON 示例保留）：

**§8.4 接口元信息**（新增 H4 块）：

| 字段 | 值 |
|---|---|
| HTTP Method | POST |
| Path | /api/v1/cosmetics/unequip |
| 认证 | **需要** Bearer token（auth 中间件） |
| 限频 | 默认（按 Story 4.5 rate_limit 默认值 60 次/分按 user_id 计） |
| 幂等 | **无** idempotencyKey（重复 unequip 同一空槽位由 5004 拦截；卸下是状态切换非资产消耗，与 NFR5 一致） |
| 节点 | 节点 9（Epic 26 落地 / Epic 27 客户端集成） |
| 事务 | **需要** MySQL 事务（DELETE user_pet_equips 行 + UPDATE 实例 status 必须原子，NFR1 / NFR2） |
| Query 参数 | **无** |

**§8.4 请求体字段表**（**就地扩充**现有 `#### 请求体`：保留现有 JSON 示例 + 前置字段表）：

| 字段 | 类型 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `petId` | string | 必填 | BIGINT 字符串化；length ≥ 1 | 目标宠物 id；service 层校验该 pet 属于当前用户（否则 5002） |
| `slot` | int | 必填 | 枚举 `{1,2,3,4,5,6,7,99}`，与数据库 §6.8 `cosmetic_items.slot` 同义 | 要卸下的装备**槽位**（按槽位指定，**不是**按实例 id 指定——一个 slot 至多 1 件装备，§5.10 `UNIQUE(pet_id, slot)`） |

**§8.4 服务端逻辑**（新增 H4 块——现有 §8.4 草稿**无** `#### 服务端逻辑`，本 story 补全）：

1. **认证 & 限频**：auth 中间件校验 Bearer token；rate_limit 中间件按默认配置（user_id 60/min）限频。
2. **参数校验**：`petId` 非空合法 BIGINT 字符串 + `slot` 为 int 且在枚举 `{1,2,3,4,5,6,7,99}` 内 → 否则 1002。
3. **开启 MySQL 事务**（以下步骤 4-7 全部在同一事务内，任一步失败整体回滚 —— NFR1）：
4. **校验 pet 归属**：`petId` 对应 pet 必须属于当前用户 → 否则 **5002 道具不属于当前用户**（epics.md §Story 26.4 AC）。
5. **查装备关系**：`SELECT user_cosmetic_item_id FROM user_pet_equips WHERE pet_id = ? AND slot = ?` → 行不存在 → **5004 装备槽位不匹配**（该槽位当前无装备，无可卸下对象）。
6. **解绑 + 状态回退**：`DELETE FROM user_pet_equips WHERE pet_id = ? AND slot = ?` + `UPDATE user_cosmetic_items SET status = 1 (in_bag) WHERE id = <步骤 5 拿到的 user_cosmetic_item_id>`。
7. **提交事务**（任一步 err → 整体回滚：user_pet_equips 行仍存在 + 实例仍 equipped，保持 NFR2 一致性）。
8. **响应**：返回 `{petId, slot, unequipped: true}`（`unequipped` 恒为 `true` —— 失败走错误码，不返回 `unequipped: false`）。

**§8.4 响应体字段表**（**就地扩充**现有 `#### 返回示例`：保留现有 JSON 示例 + 前置字段表，标题改为 `#### 响应体`，原 JSON 降为 `##### 返回示例` H5 子块）：

成功（code = 0）`data`：

| 字段 | 类型 | 必填 | 约束 | 说明 |
|---|---|---|---|---|
| `petId` | string | 必填 | BIGINT 字符串化；length ≥ 1 | 目标宠物 id（回显请求 `petId`） |
| `slot` | int | 必填 | 枚举 `{1,2,3,4,5,6,7,99}`，与数据库 §6.8 同义 | 被卸下的槽位（回显请求 `slot`） |
| `unequipped` | bool | 必填 | 恒为 `true`（失败走错误码，不返回 `false`） | 卸下成功标志 |

**§8.4 可能的错误码**（新增 H4 块）：

| code | 含义 | 触发条件 |
|---|---|---|
| 1001 | 未登录 / token 无效 | auth 中间件拦截（无 Authorization 头 / token 非法 / token 过期 / token 解析失败） |
| 1002 | 参数错误 | `petId` 缺失 / 非合法 BIGINT 字符串，或 `slot` 缺失 / 非 int / 不在枚举 `{1,2,3,4,5,6,7,99}` 内（请求侧错，client 应停止重试并修正参数） |
| 1005 | 操作过于频繁 | rate_limit 中间件拦截（已认证路由按 `user_id` 限频，每用户每分钟 > 60 次；按 Story 4.5 默认值，配置可调） |
| 5002 | 道具不属于当前用户 | `petId` 对应的 pet 不属于当前用户（epics.md §Story 26.4 AC） |
| 5004 | 装备槽位不匹配 | `user_pet_equips` 中该 `pet_id + slot` 无对应行（该槽位当前无装备，无可卸下对象） |
| 1009 | 服务繁忙 | DB 异常（事务中任一 SQL 返回 `err != nil`）/ 内部 panic → 事务整体回滚 |

> **注**：unequip 空槽位（该 slot 无装备）返回 **5004 装备槽位不匹配**（**不是**幂等成功 `unequipped: true`）——契约层选择"显式报错"而非"幂等 noop"，让 client 感知"卸下的槽位本来就空"（可能是 UI 状态不同步信号）；如未来改为"空槽 unequip 幂等返回成功"，视为契约变更。

**§8.4 关键约束**（新增 H4 块）：

- **按 slot 指定**契约：unequip 请求按 `slot` 指定卸下哪个槽位（**不是**按 `userCosmeticItemId` 实例 id），因 §5.10 `UNIQUE(pet_id, slot)` 保证一个 slot 至多 1 件装备，slot 已能唯一定位。如未来改为按实例 id 卸下，视为契约变更。
- **空槽显式报错**契约：unequip 一个无装备的 slot 返回 **5004**（非幂等成功）；如未来改为幂等 noop，视为契约变更。
- **无 idempotencyKey**契约：本接口**不**接受 `idempotencyKey`（同 §8.3 理由——卸下是状态切换非资产消耗）。重复 unequip 同一槽位：第二次因该槽已空触发 **5004**；如未来引入 idempotencyKey，视为契约变更。
- **事务一致性（NFR2）**契约：事务回滚后 user_pet_equips 行仍存在 + 实例仍 `status = 2 (equipped)`（无半成品状态——保持"equipped 实例必有 user_pet_equips 行"一致性不变量）。如未来允许部分提交，视为契约变更。
- **`unequipped` 恒 true**契约：成功响应 `unequipped` 永远为 `true`（失败走错误码，不返回 `unequipped: false`）；防 client 解析 `unequipped` 为可选/可 false。如未来引入 `unequipped: false` 软失败路径，视为契约变更。

**AC4 — §1 节点 9 穿戴 / 卸下业务契约冻结声明追加**

在 `docs/宠物互动App_V1接口设计.md` §1 "契约冻结策略" 区（现有声明结束于行 83 节点 8 声明的第 4 条编号 bullet `4. 在本 story 文件 + epics.md 同步标注变更原因 + 影响范围` 之后、`---`（行 85）之前），**追加**一组节点 9 冻结声明 bullet（与节点 2 ~ 节点 8 既有声明并列）：

- 措辞参照既有节点 8（行 78-83，Story 23.1）声明风格：含"自 YYYY-MM-DD（Story 26.1 完成日，对应 git commit hash 见 commit message）起，§8.3（POST /api/v1/cosmetics/equip）/ §8.4（POST /api/v1/cosmetics/unequip）两个节点 9 穿戴 / 卸下 REST 接口的 schema 进入**冻结**状态"。
- 明确冻结边界：字段名 / 字段类型 / equip 请求 `{petId, userCosmeticItemId}` / equip response `{petId, equipped:{slot, userCosmeticItemId, cosmeticItemId, name}}` / unequip 请求 `{petId, slot}` / unequip response `{petId, slot, unequipped:true}` / `slot` 枚举（`{1,2,3,4,5,6,7,99}`）/ 同槽自动换装语义（server 事务内自动卸旧，client 无需先 unequip）/ unequip 空槽显式报 5004（非幂等 noop）/ 无 idempotencyKey 契约 / `unequipped` 恒 true / 事务一致性（NFR2 回滚不留半成品）/ DB UNIQUE(pet_id,slot)+UNIQUE(user_cosmetic_item_id) 并发兜底（NFR11）/ 错误码（equip: 5001 / 5002 / 5003 / 5008 + 1001 / 1002 / 1005 / 1009；unequip: 5002 / 5004 + 1001 / 1002 / 1005 / 1009）触发条件 / 5008 vs 5003 边界 的修改都视为契约变更。
- 明确**非**契约变更：`user_pet_equips` 行的正常业务增减（equip / unequip 产生）/ `user_cosmetic_items` 行 status 在 1↔2 间的正常业务切换 / §5.1 `pet.equips` 节点 2 占位 `[]` → 节点 9 Story 26.6 真实化（已在 §1 行 22 Future Fields + Story 4.1 冻结边界锚定）/ 限频阈值默认值调整（抽象层不变量"走 rate_limit 中间件按 user_id 限频"不变即不视为契约变更）**不**视为契约变更（参照节点 8 声明的"冻结边界说明"风格，1001 / 1005 / 1009 等触发条件冻结在抽象层"走 §3 错误码表对应映射"）。
- 明确变更触发的回归范围（参照节点 8 声明的 4 条编号列表）：
  1. 触发 iOS Epic 27 重新评审（影响 Story 27.1 ~ 27.5 所有激活穿戴按钮 / EquipUseCase / UnequipUseCase / 文字降级 / 同槽换装动效 / 主界面实时刷新 story）。
  2. 触发 server Epic 26 已完成 story 的回归（影响 Story 26.2 ~ 26.6 已落地的 migration / equip handler / unequip handler / 集成测试 / GET /home pet.equips 扩展）。
  3. 触发下游 Epic 29（render_config）/ Epic 32（合成）契约 story（29.1 / 32.1）回归（节点 10 渲染基于穿戴关系扩展；合成基于实例模型扩展）。
  4. 在本 story 文件 + epics.md 同步标注变更原因 + 影响范围。

**AC5 — 跨文档一致性自检（contract finalize 必须项）**

完成 §8.3 / §8.4 / §1 编辑后，必须逐项核对并在本 story "Completion Notes" 记录核对结论：

1. §8.3 / §8.4 所有 `slot` 枚举值域与数据库设计 §6.8 `cosmetic_items.slot` **逐值一致**（1-7/99）。
2. §8.3 `equipped.userCosmeticItemId` 与 §8.2 `instances[].userCosmeticItemId` / §7.2 `reward.userCosmeticItemId`（节点 8 真实化后）字段名 / 类型（string BIGINT 字符串化）**一致**；§8.3 `equipped.cosmeticItemId` 与 §8.1 `items[].cosmeticItemId` / §8.2 `groups[].cosmeticItemId` **一致**；§8.3 `equipped.{slot,userCosmeticItemId,cosmeticItemId,name}` 与 §5.1 `pet.equips[]` 同名字段节点 9 起同义对齐声明**无矛盾**（§5.1 `pet.equips` 是 Future Field，Story 26.6 真实化已在 §1 行 22 + Story 4.1 锚定）。
3. §8.3 / §8.4 错误码（5001 / 5002 / 5003 / 5004 / 5008 + 1001 / 1002 / 1005 / 1009）与 §3 全局错误码表（行 141-170）**逐码一致**（5001 道具不存在 / 5002 道具不属于当前用户 / 5003 道具状态不可用 / 5004 装备槽位不匹配 / 5008 装扮已装备 ——逐字对齐 §3 定义），且**未**触及 §3 合成错误码 5005/5006/5007（属 Story 32.1 范围）。
4. §8.3 / §8.4 字段名与现有 JSON 示例（§8.3 行 1454-1487 请求体 + 返回示例 / §8.4 行 1497-1517 请求体 + 返回示例）**逐字段一致**（不改示例，字段表必须匹配示例）。
5. §1 新增节点 9 冻结声明与既有节点 2-8 声明**风格一致**（日期 + commit hash 占位 + 冻结边界 + 非契约变更 + 4 条回归范围编号列表）。
6. §8.3 / §8.4 事务边界声明与数据库设计 §8.4 穿戴事务（行 1009-1019）+ 时序图 §9 装扮穿戴流程（行 300-340）**语义一致**（校验归属/状态 → 查配置槽位 → 同槽换装卸旧 → 更新 user_pet_equips → 实例 status 切换，全部一个事务）。
7. 未触碰 §8.1 / §8.2（Story 23.1 已锚定）/ §5.1（Story 4.1 已锚定，pet.equips Future Field 由 26.6 激活）/ §9.x（节点 11 Story 32.1 范围）/ §7.x（Story 20.1 已锚定）/ §3 错误码表本体 / 任何 `_bmad-output/` 之外其他 6 份 docs 文档。

## Tasks / Subtasks

- [x] Task 1：读取并定位编辑点（AC1）
  - [x] 读 `docs/宠物互动App_V1接口设计.md` §1 行 78-85（节点 8 声明结构 + 追加位置 = 行 83 第 4 条编号 bullet 后、行 85 `---` 前）
  - [x] 读 §8.3 行 1448-1489（equip 草稿：请求体 + 服务端逻辑 bullet + 返回示例）+ §8.4 行 1491-1517（unequip 草稿：请求体 + 返回示例，**无**服务端逻辑段）
  - [x] 读 `docs/宠物互动App_数据库设计.md` §5.10 user_pet_equips（行 533-566）/ §5.9 user_cosmetic_items / §6.8 slot / §6.10 status / §8.4 穿戴事务（行 1009-1019）（字段语义来源，DB → API 方向）
  - [x] 读 §8.1 / §8.2 `items[].cosmeticItemId` / `instances[].userCosmeticItemId` 字段表 + §5.1 `pet.equips[]` Future Field 引用块 + §3 错误码表（行 137-180）（确认跨接口同义对齐 + 错误码逐码一致措辞）
  - [x] 读 `docs/宠物互动App_时序图与核心业务流程设计.md` §9 装扮穿戴流程（行 300-344）（确认事务步骤语义一致）
- [x] Task 2：就地编辑 §8.3 POST /cosmetics/equip（AC2）
  - [x] 紧接 `### POST /api/v1/cosmetics/equip` 后新增 `#### 接口元信息` H4 块
  - [x] 现有 `#### 请求体` 扩充：前置 2 字段表（petId / userCosmeticItemId）+ 保留原 JSON 示例
  - [x] 现有 `#### 服务端逻辑` 升级：把概述 6 bullet 扩充为带事务步骤编号的细化版（1-11 步，含同槽换装 + 错误码触发点；原 bullet 语义保留）
  - [x] 现有 `#### 返回示例` 升级为 `#### 响应体`：前置 5 字段表（petId / equipped.{slot,userCosmeticItemId,cosmeticItemId,name}）+ 原 JSON 降为 `##### 返回示例` H5 子块（JSON 逐字未改）
  - [x] 新增 `#### 可能的错误码`（1001/1002/1005/5001/5002/5003/5008/1009）+ `#### 关键约束` H4 块
  - [x] 保留章节末 `---`
- [x] Task 3：就地编辑 §8.4 POST /cosmetics/unequip（AC3）
  - [x] 紧接 `### POST /api/v1/cosmetics/unequip` 后新增 `#### 接口元信息` H4 块
  - [x] 现有 `#### 请求体` 扩充：前置 2 字段表（petId / slot）+ 保留原 JSON 示例
  - [x] **新增** `#### 服务端逻辑` H4 块（现有草稿无此段，本 story 补全：1-8 步事务流程）
  - [x] 现有 `#### 返回示例` 升级为 `#### 响应体`：前置 3 字段表（petId / slot / unequipped）+ 原 JSON 降为 `##### 返回示例` H5 子块（JSON 逐字未改）
  - [x] 新增 `#### 可能的错误码`（1001/1002/1005/5002/5004/1009）+ `#### 关键约束` H4 块
  - [x] 保留章节末 `---`
- [x] Task 4：§1 追加节点 9 冻结声明（AC4）
  - [x] 在节点 8 声明第 4 条编号 bullet（行 83）后、`---`（行 85）前，追加节点 9 穿戴/卸下业务契约冻结声明（参照节点 8 风格：日期 + commit hash 占位 + 冻结边界 + 非契约变更 + 4 条回归范围编号列表）
  - [x] **不**覆盖 / 删除任何已有节点 2-8 声明
- [x] Task 5：跨文档一致性自检（AC5）
  - [x] 逐项核对 AC5 的 7 条，结论写入本 story "Completion Notes List"
  - [x] 确认字段表与现有 JSON 示例逐字段一致（不改示例）
  - [x] 确认未触碰 §8.1 / §8.2 / §5.1 / §9.x / §7.x / §3 错误码表本体 / 其他 docs 文档
- [x] Task 6：构建 / 测试影响确认（纯文档 story）
  - [x] 本 story 不改 Go / Swift 代码，**无需** `bash scripts/build.sh --test`（无代码变更）
  - [x] 确认 `docs/宠物互动App_V1接口设计.md` 仍是合法 markdown（fenced code 标记 116 个偶数闭合 + H2/H3/H4/H5 层级清晰 + 表格语法正确）
  - [x] 标记 sprint-status.yaml `26-1-接口契约最终化` → review（dev-story 状态流转）

## Dev Notes

### 这是什么类型的 story

纯文档契约定稿 story。**零代码改动**。对标 Story 4.1 / 7.1 / 10.1 / 11.1 / 14.1 / 17.1 / 20.1 / 23.1 在各自 Epic 的"开局契约"角色。产出物 = `docs/宠物互动App_V1接口设计.md` 的 §8.3 / §8.4 / §1 三处编辑 + 一个稳定可冻结的 commit hash（供 iOS Epic 27 / server Epic 26 后续 story 引用）。**与 Story 23.1 的关键差异**：23.1 锚定的是两个**只读查询**接口（GET catalog / inventory），本 story 锚定的是两个**写事务**接口（POST equip / unequip），因此关键约束侧重点不同——本 story 重在"事务一致性（NFR2）/ DB UNIQUE 并发兜底（NFR11）/ 同槽自动换装 / 无 idempotencyKey 重复语义"，而非 23.1 的"聚合维度 / status 过滤 / 排序契约"。

### 字段语义唯一来源 = 数据库设计文档（DB → API 单向）

- `user_pet_equips`：`docs/宠物互动App_数据库设计.md` §5.10（行 533-566；`id / user_id / pet_id / slot / user_cosmetic_item_id / created_at / updated_at` + `UNIQUE(pet_id,slot)` + `UNIQUE(user_cosmetic_item_id)` + `KEY idx_user_pet(user_id,pet_id)`）
- `user_cosmetic_items`：§5.9（`id / user_id / cosmetic_item_id / status / source / ...`）；`cosmetic_items`：§5.8（`id / code / name / slot / rarity / asset_url / icon_url / ...`）
- 枚举：§6.8 slot（1=hat / 2=gloves / 3=glasses / 4=neck / 5=back / 6=body / 7=tail / 99=other）/ §6.10 status（1=in_bag / 2=equipped / 3=consumed / 4=invalid）
- 穿戴事务：§8.4（行 1009-1019：校验归属与状态 → 查配置槽位 → 若该槽位已有旧装备则旧装备 status 改回 in_bag → 更新 user_pet_equips → 新装备实例 status 改 equipped，全部一个事务）
- 时序图：`docs/宠物互动App_时序图与核心业务流程设计.md` §9（行 300-340：equip 流程时序 + §9.3 卸下流程 + §9.4 实现要点；§9.3 明确"卸下 = 删 user_pet_equips 指定槽位关系 + 实例 status equipped → in_bag"）

### 易错点（review 高频命中，提前规避）

1. **equip 请求传实例 id 不是配置 id**：`userCosmeticItemId`（玩家实例 id，对应 §8.2 inventory `instances[].userCosmeticItemId`），**不是** `cosmeticItemId`（配置 id）。response 里的 `equipped.cosmeticItemId` 由 server 从实例反查（client 不传）。
2. **unequip 按 slot 不按实例 id**：unequip 请求传 `{petId, slot}`，因 §5.10 `UNIQUE(pet_id,slot)` 保证一 slot 至多 1 件，slot 已唯一定位被卸下的装备。**不要**写成传 `userCosmeticItemId`。
3. **同槽换装是 server 自动卸旧**：equip 时若 slot 已有装备，server **在同一事务内**自动删旧 user_pet_equips 行 + 旧实例 status 回 in_bag，client **无需**先调 unequip（epics.md §Story 26.3 明确"自动先卸下"）。**不要**把契约写成"client 必须先 unequip"。
4. **5008 vs 5003 边界**：5008 = 实例 `status=2 (equipped)`（可恢复，已被穿戴）；5003 = 实例 `status=3 (consumed)` / `4 (invalid)`（终态，不可用）。两者**不**能合并，client 处理策略不同。
5. **unequip 空槽报 5004 不是幂等成功**：unequip 一个无装备的 slot 返回 5004（显式报错），**不是**幂等返回 `unequipped:true`。契约层选"显式报错"让 client 感知 UI 状态不同步。
6. **无 idempotencyKey**：穿戴 / 卸下**不**接受 idempotencyKey（NFR5 仅约束开箱 / 合成两个资产消耗类接口）。重复 equip 由 5008 拦、重复 unequip 由 5004 拦、并发由 DB UNIQUE 兜底，天然防重。**不要**顺手加 idempotencyKey 字段。
7. **不反向改 DB / §8.1 / §8.2 / §5.1 / §3 / §9.x / §7.x**：本 story 只锚定 §8.3 / §8.4 + §1 追加；其他章节已锚定，只引用不修改。§5.1 `pet.equips` 是 Future Field 由 Story 26.6 真实化（**不要**在本 story 改 §5.1）；§9.x 是节点 11 Story 32.1 的（**不要**顺手锚定 compose）；§3 错误码表本体已含 5001-5008（**不要**改 §3 定义，只引用到 §8.3/§8.4 错误码段）。
8. **追加位置**：§1 节点 9 声明追加在节点 8 声明第 4 条编号 bullet（行 83）后、`---`（行 85）前，与节点 2-8 声明并列；**不**插到节点 8 子 bullet 中间，也**不**覆盖任何已有声明。
9. **JSON 示例不改**：现有 §8.3 行 1454-1487（请求体 + 返回示例 JSON）/ §8.4 行 1497-1517（请求体 + 返回示例 JSON）**保留不动**，新增字段表必须与示例**逐字段一致**（示例已正确，字段表向示例对齐——注意现有示例里 `cosmeticItemId: "12"` 是 string、`slot: 1` 是 int、`unequipped: true` 是 bool，字段表类型必须与之匹配）。
10. **equip response `equipped` 不含 assetUrl/renderConfig**：epics.md §Story 26.1 AC 钦定 equip response data 为 `{petId, equipped:{slot, userCosmeticItemId, cosmeticItemId, name}}`——**仅 4 字段**，**不**含 `assetUrl` / `rarity` / `renderConfig`（这些在 §5.1 `pet.equips[]` 由 Story 26.6 / 29.6 补充，**不**在 §8.3 equip response）。

### 范围红线（再次强调）

只改 3 个文件：`docs/宠物互动App_V1接口设计.md`（§8.3 + §8.4 + §1）/ `_bmad-output/implementation-artifacts/sprint-status.yaml`（状态流转）/ 本 story 文件（Dev Agent Record）。**不**改任何 `.go` / `.swift` / 其他 6 份 `docs/宠物互动App_*.md` / `_bmad-output/` 下其他 yaml/md。无代码 → **不**跑 `scripts/build.sh`（无可编译变更）。

### Project Structure Notes

- 编辑目标 `docs/宠物互动App_V1接口设计.md` 是 CLAUDE.md 钦定的 7 份设计文档包之一（第 3 份"V1接口设计"），是跨端契约唯一权威来源（CLAUDE.md "Repo Separation"：跨端契约通过此文档统一同步，不通过共享代码）。
- 本 story 与 server `server/` 工程目录、iOS `iphone/` 工程目录均**无**文件交互（纯文档）；下游 Story 26.3 / 26.4 实装 handler 时按 `docs/宠物互动App_Go项目结构与模块职责设计.md` §4 既有目录规则落地（`internal/domain/cosmetic/` / `internal/service/cosmetic_service.go` / `internal/repo/mysql/`），与本 story 无关。
- 无新增目录 / 模块 / 依赖。

### References

- [Source: _bmad-output/planning-artifacts/epics.md#Story 26.1（行 3496-3514）] — AC 钦定的 §8.3 / §8.4 字段集合 + 错误码 5001/5002/5003/5004/5008 + "标注契约自此 Epic 起冻结"
- [Source: _bmad-output/planning-artifacts/epics.md#Epic 26（行 3492-3637）] — 26.1 ~ 26.6 全 epic 上下文 + 26.3 equip 事务步骤 + 26.4 unequip 事务步骤 + 26.6 GET /home pet.equips 扩展
- [Source: _bmad-output/planning-artifacts/epics.md#FR19-21 / NFR1 / NFR2 / NFR11（行 58-60 / 99-100 / 113）] — 穿戴/卸下功能需求 + 事务原子 + 状态一致性 + 实例唯一装备约束
- [Source: docs/宠物互动App_V1接口设计.md#8.3（行 1448-1489）] — equip 草稿（保留请求体 + 服务端逻辑 bullet + 返回示例 JSON）
- [Source: docs/宠物互动App_V1接口设计.md#8.4（行 1491-1517）] — unequip 草稿（保留请求体 + 返回示例 JSON，无服务端逻辑段需补全）
- [Source: docs/宠物互动App_V1接口设计.md#1（行 78-85）] — 契约冻结策略区节点 8 声明（追加位置 = 行 83 后 / 行 85 `---` 前；风格参照）
- [Source: docs/宠物互动App_V1接口设计.md#3（行 137-170）] — §3 全局错误码表（5001-5008 + 1001/1002/1005/1009 定义来源）
- [Source: docs/宠物互动App_数据库设计.md#5.10（行 533-566）] — user_pet_equips schema（UNIQUE 约束 + 字段语义来源）
- [Source: docs/宠物互动App_数据库设计.md#8.4（行 1009-1019）] — 穿戴事务边界（校验 → 查槽位 → 卸旧 → 更新 user_pet_equips → 实例 status 切换，全部一个事务）
- [Source: docs/宠物互动App_时序图与核心业务流程设计.md#9（行 300-340）] — 装扮穿戴流程时序 + §9.3 卸下流程 + §9.4 实现要点
- [Source: _bmad-output/implementation-artifacts/23-1-接口契约最终化.md] — 同类契约 story（节点 8）的既有模式参照（结构 / AC 编排 / Completion Notes 自检格式）

## Dev Agent Record

### Agent Model Used

claude-opus-4-7[1m]（Opus 4.7 1M context；epic-loop 派出隔离 dev-story sub-agent）

### Debug Log References

- 无代码改动 → 无 build / test 调试日志（纯文档契约定稿 story）。
- markdown 合法性校验：`grep -c '^\`\`\`'` = 116（偶数 → fenced code block 全闭合）。
- `git status --porcelain` 确认仅 3 文件变更（sprint-status.yaml / V1接口设计.md / 本 story 文件），无 .go / .swift / 其他 docs 触碰。

### Completion Notes List

实装内容（3 处编辑，1 份文档 `docs/宠物互动App_V1接口设计.md`）：

1. **§8.3 POST /api/v1/cosmetics/equip** 就地升级：保留原 H2/H3 标题 + 原请求体 JSON + 原 6 条服务端逻辑 bullet 语义 + 原返回示例 JSON；新增"接口元信息 / 请求体字段表 / 服务端逻辑（1-11 步事务细化，含同槽自动换装 + 错误码触发点）/ 响应体字段表（5 字段）/ 可能的错误码（1001/1002/1005/5001/5002/5003/5008/1009）/ 关键约束（无 idempotencyKey + DB UNIQUE 并发兜底 + 事务一致性 NFR2 + 同槽自动换装 + 5008 vs 5003 边界 + 跨接口字段同义对齐）"。原 JSON 示例逐字未改，降为 `##### 返回示例` H5 子块。
2. **§8.4 POST /api/v1/cosmetics/unequip** 就地升级：保留原 H2/H3 标题 + 原请求体 JSON + 原返回示例 JSON；新增"接口元信息 / 请求体字段表 / 服务端逻辑（1-8 步事务流程，草稿原无此段，本 story 补全）/ 响应体字段表（3 字段）/ 可能的错误码（1001/1002/1005/5002/5004/1009）/ 关键约束（按 slot 指定 + 空槽显式报 5004 + 无 idempotencyKey + 事务一致性 NFR2 + `unequipped` 恒 true）"。原 JSON 示例逐字未改，降为 `##### 返回示例` H5 子块。
3. **§1 契约冻结策略**追加节点 9 穿戴/卸下冻结声明：在节点 8（Story 23.1）声明第 4 条编号 bullet 后、`---` 前追加（与节点 2-8 并列，未覆盖/删除任何既有声明）；含日期 2026-05-17 + commit hash 占位 + 冻结边界 + 非契约变更说明 + 4 条回归范围编号列表（iOS Epic 27 / server Epic 26 / 下游 Epic 29·32 契约 story / 标注变更原因）。

**AC5 跨文档一致性自检结论（7 条全通过）**：

1. **slot 枚举逐值一致**：§8.3/§8.4 `{1,2,3,4,5,6,7,99}` ≡ 数据库设计 §6.8（1=hat / 2=gloves / 3=glasses / 4=neck / 5=back / 6=body / 7=tail / 99=other）。✓
2. **跨接口字段同义对齐无矛盾**：§8.3 `equipped.userCosmeticItemId` ≡ §8.2 `instances[].userCosmeticItemId` / §7.2 `reward.userCosmeticItemId`（均 string BIGINT 字符串化）；`equipped.cosmeticItemId` ≡ §8.1 `items[].cosmeticItemId` / §8.2 `groups[].cosmeticItemId`；§5.1 `pet.equips[]`（Future Field，Story 26.6 真实化，§1 行 22 + Story 4.1 已锚定）同名字段同义对齐声明无矛盾。✓
3. **错误码逐码一致**：5001 道具不存在 / 5002 道具不属于当前用户 / 5003 道具状态不可用 / 5004 装备槽位不匹配 / 5008 装扮已装备 + 1001/1002/1005/1009 与 §3 全局错误码表（行 141-180）逐字对齐；未触及合成码 5005/5006/5007。✓
4. **字段表与 JSON 示例逐字段一致**：equip 示例 `slot:1`(int) / `userCosmeticItemId:"90001"`(string) / `cosmeticItemId:"12"`(string) / `name:"小黄帽"`(string)；unequip 示例 `slot:1`(int) / `unequipped:true`(bool) —— 字段表类型逐一匹配，JSON 未改。✓
5. **§1 风格一致**：节点 9 声明与节点 2-8 同结构（日期 + commit hash 占位 + 冻结边界 + 非契约变更 + 4 条回归编号列表）。✓
6. **事务边界语义一致**：§8.3/§8.4 服务端逻辑 ≡ 数据库设计 §8.4 穿戴事务（行 1009-1019）+ 时序图 §9（行 300-344，含 §9.3 卸下流程「删 user_pet_equips 指定槽位 + 实例 status equipped→in_bag」）。✓
7. **未触碰已锚定章节**：仅 §1 追加 + §8.3 + §8.4；§8.1/§8.2（Story 23.1）/ §5.1（Story 4.1）/ §9.x（Story 32.1）/ §7.x（Story 20.1）/ §3 错误码表本体 / 其他 6 份 docs 文档全部未动；`git status` 确认仅 3 文件变更。✓

**构建/测试**：纯文档 story，零 Go/Swift 代码改动，无需 `bash scripts/build.sh --test`。markdown 合法性已校验（116 个 ``` 偶数闭合 + H2/H3/H4/H5 层级清晰 + 表格语法正确）。

### File List

- `docs/宠物互动App_V1接口设计.md`（修改：§1 追加节点 9 冻结声明 + §8.3 equip schema 锚定 + §8.4 unequip schema 锚定）
- `_bmad-output/implementation-artifacts/sprint-status.yaml`（修改：26-1-接口契约最终化 ready-for-dev → in-progress → review）
- `_bmad-output/implementation-artifacts/26-1-接口契约最终化.md`（修改：Tasks 勾选 + Dev Agent Record + Status → review）

## Change Log

| 日期 | 变更 | 说明 |
|---|---|---|
| 2026-05-17 | Story 26.1 创建 | bmad-create-story 由 epic-loop 派出隔离 sub-agent 生成；纯文档契约定稿 story（对标 23.1）；锚定 §8.3 equip + §8.4 unequip schema + §1 节点 9 冻结声明；状态 backlog → ready-for-dev；epic-26 backlog → in-progress（首 story 创建副作用） |
| 2026-05-17 | Story 26.1 dev-story 实装 | bmad-dev-story 由 epic-loop 派出隔离 sub-agent 执行；就地升级 `docs/宠物互动App_V1接口设计.md` §8.3（equip 接口元信息/请求体字段表/1-11 步服务端逻辑/响应体字段表/错误码/关键约束）+ §8.4（unequip 接口元信息/请求体字段表/1-8 步服务端逻辑/响应体字段表/错误码/关键约束）+ §1 追加节点 9 冻结声明；原 JSON 示例逐字未改；AC5 七项跨文档一致性自检全通过；零代码改动；状态 ready-for-dev → in-progress → review |
