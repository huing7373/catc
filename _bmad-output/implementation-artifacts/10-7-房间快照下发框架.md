# Story 10.7: 房间快照下发框架（SnapshotBuilder 接口 + SendRoomSnapshot 函数 + placeholder 实装抽离 + 握手集成 + 测试矩阵）

Status: done

<!-- Validation 可选。建议运行 validate-create-story 在 dev-story 前做一次质检。 -->

## Story

As a 服务端开发,
I want 在 Story 10.3（gateway 已 inline 实装 placeholder snapshot：`buildPlaceholderSnapshot` 包级函数 + `g.roomMember.ListMembers` 直读）+ 10.4（HeartbeatScanner / Session lifecycle 钩子注入）+ 10.5（BroadcastToRoom primitive；本 story 不消费）+ 10.6（PresenceRepo + Register/Unregister 钩子；本 story 不消费 presence 路径，placeholder 阶段仅查 `room_members` 单表 —— V1 §12.3 钦定）这套既有基础上，把 gateway.go 内 inline 的 placeholder snapshot 实装**抽离**到新文件 `server/internal/app/ws/snapshot.go`，提供 (a) `SnapshotBuilder` 接口（签名 `BuildSnapshot(ctx context.Context, roomID uint64) (Snapshot, error)`；epics.md 行 1793 钦定）+ (b) placeholder 实装 `placeholderSnapshotBuilder`（内部走 `roomMemberRepo.ListMembers(ctx, roomID)` 单表查询，与既有 inline 实装语义严格一致：room.id 字符串化 / room.maxMembers=4 / room.memberCount=len(members) / members[i].userId 字符串化 / members[i].nickname="" / members[i].pet.petId="" / members[i].pet.currentState=1 —— V1 §12.3 字段表 placeholder 行严格按位对齐 + lesson `2026-05-06-ws-snapshot-placeholder-full-roster-and-disconnect-broadcast-r8.md` 钦定全 roster 行为）+ (c) `SendRoomSnapshot(ctx, session, roomID, builder) error` 包级函数（封装 "build → marshal serverEnvelope → conn 同步写"流程，让 gateway.Handle 调用点从原 ~30 行 inline 流程收敛为单行 `SendRoomSnapshot` 调用），同时**严格保留** Story 10.3 r10 P1 钉死的握手时序（**snapshot 同步写入 → mgr.Register → 启动 readLoop / writeLoop**；snapshot **先于** Register 执行让 transient snapshot 失败时旧 session 不被 evict 的"事务性 reconnect"语义不变）+ snapshot 失败处理路径（builder 返 error / write 返 error 都走 close 1011 reason `"snapshot build failed"` —— V1 §12.1 close code 表 1011 行 + §12.3 末尾"snapshot 构建失败处理路径"注 + lesson `2026-05-06-ws-frozen-section-authz-and-snapshot-coherence-r6.md` 三方钉死，**禁止**改走"推 type=error 消息"路径，6005 错误码保留给 Epic 11.x / 14.x 业务流程的运行时状态错误推送）；并交付完整测试矩阵：≥ 3 个单元测试 case（mocked SnapshotBuilder + mocked Session.Send 路径）+ ≥ 1 个集成测试 case（真实 WS client 拨号 → 立即收到 placeholder room.snapshot，房间预置 ≥2 成员行验证全 roster 返）；**不**修改 V1 §12.3 / 数据库设计 §5.13-5.14 / Go 项目结构 §6 三份设计文档（snapshot.go 已隐含在 §6 ws/ 模块边界内，本 story 是落地实装）+ **不**改既有 Story 10.3 ~ 10.6 任何已落地代码的对外行为（仅 gateway.Handle 内部用 SendRoomSnapshot 替换 inline 实装；包级 buildPlaceholderSnapshot / snapshotPayload struct 等可保留作 unexported helper，但握手路径必须改走 SendRoomSnapshot）+ **不**让 snapshot 路径反向消费 PresenceRepo（10.6 r3 P1 lesson 已钦定 placeholder 阶段不依赖 presence；ListOnline 由 Story 11.7 真实实装时消费），

so that Epic 10 收官 + 让 Story 11.7（房间快照真实实现）只需要 (a) 新增 `realSnapshotBuilder` 实装 `SnapshotBuilder` 接口（内部走 `room_members` JOIN `users` JOIN `pets` 聚合丰富字段）+ (b) 在 main.go bootstrap 期把 placeholder builder 替换为 real builder，**不**碰 gateway.Handle / SendRoomSnapshot / Snapshot struct；同时让 Epic 11.7 / Epic 14.4（pet.state.changed 触发 snapshot 全量重发，未来 epic）能复用同一 `SendRoomSnapshot` 路径（V1 §12.3 钦定"服务端**可选**在 Session 生命周期内重复推送"）—— 即本 story 是把 inline 实装升级为 stable 接口契约的"最后一公里"，**接口形态 + 调用点**自此冻结，下游 epic 仅在 builder 实装层面叠加。

## 故事定位（Epic 10 第七条 = 收官 / 节点 4 server WS 框架封顶；对标 Story 10.5 BroadcastToRoom primitive + 10.6 PresenceRepo 的"接口契约最后一公里"角色，本 story 完成后 Epic 10 全部 done，节点 4 demo 验收（Story 9.2）+ Epic 11 房间业务（Story 11.1 ~ 11.10）可基于稳定 SnapshotBuilder 接口启动）

- **Epic 10 进度**：10.1 (WS 协议骨架文档锚定) done → 10.2 (Redis 接入) done → 10.3 (WS 网关骨架 + lifecycle 钩子接口 + inline placeholder snapshot) done → 10.4 (心跳框架 + HeartbeatScanner) done → 10.5 (BroadcastToRoom primitive) done → 10.6 (Redis presence repo + lifecycle 钩子挂载) done → **本 story (10.7 房间快照下发框架)**
- **强前置关系**：
  - **Story 10.3 done 提供的强前置**（本 story 主要重构对象）：
    - `gateway.go:174-330` Handle 函数（已 r10 P1 调整为 "snapshot 先于 Register" 顺序；本 story **不**改顺序，仅把 snapshot 段从 inline 替换为 SendRoomSnapshot 调用）
    - `gateway.go:262` `g.roomMember.ListMembers(ctx, roomID)` 调用（已实装；本 story 把这一行从 gateway.Handle 移到 placeholderSnapshotBuilder.BuildSnapshot 内部）
    - `gateway.go:275-296` inline 实装的 `buildPlaceholderSnapshot` 调用 + `conn.WriteMessage(websocket.TextMessage, snapshotBytes)` 同步写入路径（本 story 把这部分重构进 SendRoomSnapshot；保留 `buildPlaceholderSnapshot` 函数 + `snapshotPayload` / `snapshotRoom` / `snapshotMember` / `snapshotPet` struct 作为 unexported helper 给 placeholderSnapshotBuilder 内部用 —— 不增不减字段，避免改动 V1 §12.3 字段语义）
    - `gateway.go:352-425` 既有 placeholder snapshot helper（`buildPlaceholderSnapshot` 函数 + 4 个 struct）—— 本 story **保留**为 unexported helper（移到 snapshot.go 或留在 gateway.go 都可，但建议**移到 snapshot.go** 保持模块边界清晰；与 broadcast.go 把 BroadcastToRoom + helper 全收在一起同模式）
    - `Session.Send(msg []byte) error`（已实装；本 story SendRoomSnapshot 走 conn.WriteMessage **同步路径**，**不**走 Session.Send 异步队列 —— 与 gateway.Handle 既有第 6.2 步同步写入语义一致；详见 §"实装关键决策" §3）
    - `closeWriteDeadline = 500ms` 包级 const（已实装；本 story 仍走该 deadline）
  - **Story 10.4 done 提供的强前置**：
    - HeartbeatScanner 不直接消费 SnapshotBuilder（snapshot 是握手期一次性事件，不在 scanner 周期路径）；本 story **不**改 scanner
    - V1 §12.3 钦定"服务端**可选**在 Session 生命周期内重复推送 snapshot"—— 节点 4 阶段不实装重复推送，但 SendRoomSnapshot 接口设计要支持 future 复用（详见 §"实装关键决策" §6 接口可重入语义）
  - **Story 10.5 done 提供的强前置**：
    - BroadcastToRoom primitive 已实装；本 story **不**消费 broadcast 路径（snapshot 是单点推送给握手的 Session 自己，不需要 broadcast 全房间；BroadcastToRoom 走 ListSessionsByRoomID → 多 session fanout，与 SendRoomSnapshot 走 conn.WriteMessage 同步写当前握手 session 的语义完全正交）
  - **Story 10.6 done 提供的强前置**：
    - PresenceRepo 已实装；本 story **不**消费 presence 路径 —— V1 §12.3 钦定 placeholder 阶段 `members[]` 来自 `room_members` 单表查询，**不**含 isOnline 字段；Story 11.7 真实实装时由 realSnapshotBuilder 内部消费 `presenceRepo.ListOnline` 标 isOnline，与本 story 接口边界正交
    - SessionManager Register/Unregister 钩子已挂 presence 钩子；本 story 不动钩子注入路径
  - **下游立即依赖**：
    - **Story 11.1（接口契约最终化）**：本 story SnapshotBuilder 接口 + Snapshot struct **不**包含 `member.joined` / `member.left` 字段（业务消息由 11.1 锚定）；本 story 仅交付 placeholder 阶段 snapshot schema 的 server 实装，业务消息独立路径
    - **Story 11.7（房间快照真实实现）**：直接实装 `realSnapshotBuilder` 替换 placeholder；接口 + Snapshot struct + SendRoomSnapshot 函数全部不动；main.go bootstrap 改一行 builder 注入
    - **Story 11.4 / 11.5（加入 / 退出房间事务）**：调 BroadcastToRoom(roomID, member.joined / member.left) 触发广播；**不**触发 SendRoomSnapshot 全量重发（节点 4 阶段不实装"成员变化触发重发"路径）
    - **Story 14.4（pet.state.changed WS 广播）**：调 BroadcastToRoom；**不**触发 SnapshotBuilder（pet 状态由独立 WS 消息推；snapshot 仅初始全量）
    - **Epic 9.1（节点 3 跨端 e2e）/ 9.2 节点 3 demo 验收**：本 story **不**直接参与（节点 3 是步数 / 猫动作 e2e，节点 4 才是房间）；本 story 间接给节点 4 demo 提供"WS 握手 → 收 placeholder snapshot"的可演示路径
    - **Epic 13.1（节点 4 跨端 e2e）/ 13.2 节点 4 demo 验收**：依赖本 story + 11.7 联合落地后做端到端验证（client 收 placeholder snapshot 时机 + 字段语义 + close 1011 重连等）
- **Epic 4 / 7 / 10 已完成 story 是本 story 的依赖**：
  - **Story 1-1 / 1-5**（已 done）：testify + miniredis + dockertest 测试栈已就绪；本 story 单测复用既有 ws_test 包的 fakeSession / fakeConn 模式（如 ws_test.go 内既有的"假 WS conn 缓冲" pattern，详见 §"测试设计与单测/集成测覆盖"）
  - **Story 1-9 ctx 传播**（已 done）：本 story SnapshotBuilder.BuildSnapshot / SendRoomSnapshot 首参数 `ctx context.Context`（与 ADR-0007 钦定一致）；ctx cancel 时 placeholderSnapshotBuilder 内 ListMembers 自然走 GORM `.WithContext(ctx)` 中断
  - **Story 4.2 MySQL 接入**（已 done）：本 story placeholder builder 直接消费 `mysql.RoomMemberRepo.ListMembers(ctx, roomID)` —— 不绕过抽象层
  - **Story 10.1 V1 §12.3 冻结**（已 done）：room.snapshot payload schema 锚定（room.{id, maxMembers, memberCount} + members[].{userId, nickname, pet.{petId, currentState}}）；本 story 严格按字段表 placeholder 行实装，**不**改字段名 / 类型 / placeholder 行为（avatarUrl 仍按 §12.3 字段表注 "Story 11.7 真实实现时启用，节点 4 placeholder 不返回该字段" 处理 —— 本 story 不在字段表加 avatarUrl）
  - **Story 10.3 review r6 / r7 / r8 / r9 / r10**（已 done）：lesson 链固化 r10 "snapshot 必须**先于** Register 写入" 不变量；本 story SendRoomSnapshot 调用点严格保留该顺序，**不**反向移到 Register 之后
- **iOS / 跨端契约**：
  - V1 §12.3 已锚定 room.snapshot payload schema；本 story server 实装严格按字段表 placeholder 行下发：
    - `payload.room.id`：roomID 字符串化（`strconv.FormatUint(roomID, 10)`）
    - `payload.room.maxMembers`：固定 `4`（节点 4 阶段；Story 11.x 真实实装亦同 —— 房间最大成员数是静态契约）
    - `payload.room.memberCount`：`len(members)`（必须严格等于 `members[]` 数组长度，V1 §12.3 不变量小节钦定）
    - `payload.members`：来自 `room_members` 单表全部行；每行 `{userId: 字符串化, nickname: "", pet: {petId: "", currentState: 1}}`
    - `requestId`：固定 `""`（V1 §12.3 通用信封钦定主动推送类）
    - `ts`：`time.Now().UnixMilli()`
  - 本 story **不**改 V1 任何文档；ListMembers 单表查询与"placeholder 字段值来源说明"小节（§12.3 行 1577）严格一致
  - 本 story 不依赖任何 iOS 代码改动；iOS 端 Story 12.3（解析 room.snapshot Codable）+ 12.4（房间 ViewModel client merge contract）由 Epic 12 独立交付 —— 与本 story server 内部接口抽离完全正交（client 只看 wire 上的 JSON）
- **范围红线**（明确**不**做）：
  - **不**实装 `realSnapshotBuilder`（Story 11.7 接管；本 story 仅 placeholder 实装）
  - **不**让 snapshot 路径消费 PresenceRepo（V1 §12.3 钦定 placeholder 阶段不依赖 presence；ListOnline 由 11.7 真实实装时消费）
  - **不**实装"成员变化触发 snapshot 重发"路径（节点 4 阶段不需要；V1 §12.3 钦定服务端**可选**重复推送，但节点 4 仅握手时发一次）
  - **不**改 V1 §12.3 / 数据库设计 §5.13-5.14 / Go 项目结构 §6 任何字段或语义（snapshot.go 已在 §6 ws/ 模块边界内的"未来文件" 隐含位置）
  - **不**改既有 Story 10.3 ~ 10.6 任何已落地代码的**对外行为**（仅 gateway.Handle 内部用 SendRoomSnapshot 替换 inline 实装；buildPlaceholderSnapshot 函数 + 4 个 struct 可保留为 unexported helper，但握手路径必须走 SendRoomSnapshot）
  - **不**改 SnapshotBuilder 接口签名添加 `userID` 参数（snapshot 是房间级视图，不是用户级；userID 不参与字段计算 —— Story 11.7 真实实装时仍只需 roomID）
  - **不**改 Snapshot struct 添加 `members[].avatarUrl` 字段（V1 §12.3 字段表注钦定 avatarUrl 由 Story 11.7 启用；本 story 字段集合 = V1 §12.3 placeholder 字段表完全对齐）
  - **不**让 SendRoomSnapshot 走 Session.Send 异步队列（必须 conn.WriteMessage 同步写入；详见 §"实装关键决策" §3）
  - **不**实装 Snapshot struct 的 JSON tag 与 wire 格式分离（Snapshot struct 直接是 wire JSON 表示；与既有 snapshotPayload struct 同模式 —— 不引入"内部 domain model + 外部 DTO 转换"层，YAGNI）
  - **不**实装 metrics（counter / gauge per-room snapshot send latency）—— 节点 13+ 才做
  - **不**实装 SnapshotBuilder 缓存（每次握手都从 MySQL 查；房间 < 4 成员 + 单表 query <1ms，不需要缓存层）
  - **不**写英文文档 / OpenAPI / AsyncAPI 形式化定义
  - **不**改 docs/lessons/ 已存在的 11 个 ws-snapshot-* lesson 文件（lessons 是 retro 时由 fix-review 流程产出，本 story 是落地阶段，不反向编辑既有 lessons）

**本 story 不做**（明确范围红线，避免 dev-story 阶段 scope 漂移）：

- 不实装 realSnapshotBuilder（Story 11.7 接管）
- 不让 snapshot 消费 presence（11.7 真实实装时再加）
- 不实装"成员变化触发重发"路径（节点 4 不需要）
- 不改 V1 / 数据库设计 / Go 项目结构 三份设计文档
- 不改既有 Story 10.3 ~ 10.6 落地代码对外行为
- 不在 Snapshot struct 加 avatarUrl 字段（11.7 启用）
- 不写英文文档 / 不改既有 lessons

## Acceptance Criteria

**AC1 — 新建 `server/internal/app/ws/snapshot.go` + SnapshotBuilder 接口 + Snapshot struct + placeholder 实装 + SendRoomSnapshot 函数**

`server/internal/app/ws/snapshot.go`（**新建文件**，本 story 落地的核心代码；同 package `ws`，与 gateway.go / session.go / broadcast.go 平铺）：

```go
// Package ws 内 snapshot.go：room.snapshot 构造与下发框架（Story 10.7）。
//
// 角色：
//   - SnapshotBuilder 接口：抽象 BuildSnapshot(ctx, roomID) (Snapshot, error)，
//     让 placeholder（本 story）/ real（Story 11.7）实装可替换
//   - placeholderSnapshotBuilder：本 story 的默认实装；走 RoomMemberRepo.ListMembers
//     单表查询（不 JOIN users / pets）；丰富字段（nickname / pet.petId）降级空字符串、
//     pet.currentState 固定 1
//   - Snapshot struct：room.snapshot payload 的 Go 表示；与 V1 §12.3 字段表
//     placeholder 行严格 1:1 对齐
//   - SendRoomSnapshot 函数：封装 build → marshal → conn 同步写入 + close 1011
//     失败处理路径；gateway.Handle 单点调用
//
// 设计原则：
//   - 接口形态先行：SnapshotBuilder 是 interface，placeholder 实装是 unexported
//     struct（与 SessionManager / RedisClient / PresenceRepo 同模式）
//   - ctx 传播严格（ADR-0007）：所有方法 / 函数首参数 ctx context.Context
//   - 错误语义内化：BuildSnapshot 抛 error → SendRoomSnapshot 走 close 1011 路径，
//     **不**透传到 caller / **不**走 server "推 type=error 消息" 路径（V1 §12.1 close
//     code 表 1011 行 + §12.3 末"snapshot 构建失败处理路径"注 三方钦定）
//   - 同步写入优先：snapshot 必须**同步**写入 *websocket.Conn（不走 Session.Send 异步
//     队列），因为握手成功 → 启动 readLoop / writeLoop 之前是关键窗口期，writeLoop
//     此时还没启动（V1 §12.1 握手第 4 步钦定）
package ws

import (
    "context"
    "encoding/json"
    "fmt"
    "log/slog"
    "strconv"
    "time"

    "github.com/gorilla/websocket"

    "github.com/huing/cat/server/internal/repo/mysql"
)

// Snapshot 是 room.snapshot payload 的 Go 表示。
//
// 与 V1 §12.3 字段表完全 1:1 对齐：
//   - Room.{ID, MaxMembers, MemberCount}: room.{id, maxMembers, memberCount}
//   - Members[i].{UserID, Nickname, Pet.{PetID, CurrentState}}: members[i].{userId,
//     nickname, pet.{petId, currentState}}
//
// 不变量（V1 §12.3 末"不变量"小节钦定）：MemberCount == len(Members)
//
// 节点 4 阶段 placeholder（Story 10.7）/ Story 11.7 真实实装下发的 wire 格式严格
// 一致 —— 仅丰富字段值不同（placeholder 阶段 Nickname/PetID 空字符串 + CurrentState=1；
// 真实阶段由 users.nickname / pets.id / pets.currentState 真实回填）。
//
// 注意：本 struct 直接作为 serverEnvelope.Payload 的具体类型 —— **不**额外引入
// "内部 domain model + 外部 DTO 转换"层，YAGNI（与既有 snapshotPayload 同模式）。
type Snapshot struct {
    Room    SnapshotRoom     `json:"room"`
    Members []SnapshotMember `json:"members"`
}

// SnapshotRoom 是 room.snapshot.payload.room 的 Go 表示。
//
// MaxMembers 节点 4 阶段固定 4；Story 11.x 真实实装仍是 4（房间最大成员数是
// 静态契约）—— 字段保留是为 future epic 灵活性，但节点 4 阶段值不变。
type SnapshotRoom struct {
    ID          string `json:"id"`
    MaxMembers  int    `json:"maxMembers"`
    MemberCount int    `json:"memberCount"`
}

// SnapshotMember 是 room.snapshot.payload.members[i] 的 Go 表示。
//
// 字段：
//   - UserID: BIGINT 字符串化（与 V1 §2.5 全局 BIGINT 字符串化约定一致）
//   - Nickname: placeholder 阶段空字符串 ""（V1 §12.3 字段表 placeholder 行钦定）
//   - Pet: 嵌套 SnapshotPet（不展平 —— 与 V1 §12.3 字段表 pet.* 嵌套结构一致）
type SnapshotMember struct {
    UserID   string      `json:"userId"`
    Nickname string      `json:"nickname"`
    Pet      SnapshotPet `json:"pet"`
}

// SnapshotPet 是 room.snapshot.payload.members[i].pet 的 Go 表示。
//
// 字段：
//   - PetID: placeholder 阶段空字符串 ""（V1 §12.3 字段表 placeholder 行钦定）
//   - CurrentState: 节点 4 阶段固定 1 (stationary_or_unknown)；Story 11.7 真实
//     实装亦固定 1（Epic 14 才真实驱动）—— V1 §12.3 字段表钦定
type SnapshotPet struct {
    PetID        string `json:"petId"`
    CurrentState int    `json:"currentState"`
}

// SnapshotBuilder 抽象 room.snapshot 构造路径。
//
// 节点 4 placeholder 实装：placeholderSnapshotBuilder（本文件 NewPlaceholderSnapshotBuilder
// 构造）—— 走 RoomMemberRepo.ListMembers 单表查询。
//
// Story 11.7 真实实装：realSnapshotBuilder（在 Story 11.7 落地，本 story 不实装）
// —— 走 room_members JOIN users JOIN pets 聚合丰富字段；接口签名不变。
//
// 接口边界：BuildSnapshot 仅参与 server → client 推送路径；**不**参与 REST GET
// /rooms/{roomId}（REST handler 走 service 层 + 自己的 DTO，与 ws snapshot 字段
// 集合允许 drift —— V1 §12.3 字段表与 §10 REST 字段表本来就独立锚定）。
type SnapshotBuilder interface {
    // BuildSnapshot 构造给定 roomID 的 Snapshot。
    //
    // 错误语义：
    //   - room 不存在 / DB 异常 → 返 error；caller（SendRoomSnapshot）走 close 1011
    //   - room 内 0 成员 → 返 Snapshot{Members: []}（节点 4 阶段不会出现，因为
    //     gateway.Handle 第 5 步已校验当前用户在 room_members；但接口语义上接受）
    //
    // 不抛 panic：所有底层异常用 error 透传；调用方走 close 1011 让客户端按 transient
    // network failure 重连，避免 panic 影响其他 session
    //
    // ctx：透传给底层 ListMembers 调用；ctx cancel 时返 ctx.Err 包装的 error
    BuildSnapshot(ctx context.Context, roomID uint64) (Snapshot, error)
}

// NewPlaceholderSnapshotBuilder 构造节点 4 阶段 placeholder 实装。
//
// 参数：
//   - roomMember: RoomMemberRepo（已在 main.go bootstrap 期 wire；与 gateway 共享同一
//     实例，避免重复构造）
//
// 调用方：main.go bootstrap 期把构造产物注入 gateway.NewGateway(... , builder)。
//
// Story 11.7 切换路径：把 NewPlaceholderSnapshotBuilder 替换为 NewRealSnapshotBuilder
// （Story 11.7 提供）—— gateway 层完全不感知。
func NewPlaceholderSnapshotBuilder(roomMember mysql.RoomMemberRepo) SnapshotBuilder {
    return &placeholderSnapshotBuilder{roomMember: roomMember}
}

// placeholderSnapshotBuilder 是节点 4 阶段 SnapshotBuilder 的默认实装。
//
// 唯一依赖：RoomMemberRepo.ListMembers —— 单表查询（不 JOIN users / pets）。
//
// 行为（V1 §12.3 字段表 placeholder 行 + lesson `2026-05-06-ws-snapshot-placeholder-
// full-roster-and-disconnect-broadcast-r8.md` 钦定）：
//   - 调 roomMember.ListMembers(ctx, roomID) 拿全部 user_id（**禁止**只取当前握手用户
//     —— 房间已有 ≥2 成员时漏返其他成员会让 client 把 snapshot 当 authoritative state
//     错误清空已加载的 roster）
//   - 对每行构造 SnapshotMember{UserID: 字符串化, Nickname: "", Pet: {PetID: "",
//     CurrentState: 1}}
//   - room.id 字符串化、room.maxMembers=4、room.memberCount=len(members)
type placeholderSnapshotBuilder struct {
    roomMember mysql.RoomMemberRepo
}

// BuildSnapshot 实装 SnapshotBuilder.BuildSnapshot 接口。
func (b *placeholderSnapshotBuilder) BuildSnapshot(ctx context.Context, roomID uint64) (Snapshot, error) {
    members, err := b.roomMember.ListMembers(ctx, roomID)
    if err != nil {
        return Snapshot{}, fmt.Errorf("ws snapshot: list members for room %d: %w", roomID, err)
    }
    out := Snapshot{
        Room: SnapshotRoom{
            ID:          strconv.FormatUint(roomID, 10),
            MaxMembers:  4,
            MemberCount: len(members),
        },
        Members: make([]SnapshotMember, 0, len(members)),
    }
    for _, uid := range members {
        out.Members = append(out.Members, SnapshotMember{
            UserID:   strconv.FormatUint(uid, 10),
            Nickname: "",
            Pet: SnapshotPet{
                PetID:        "",
                CurrentState: 1,
            },
        })
    }
    return out, nil
}

// SendRoomSnapshot 把房间 snapshot 同步写入到 conn（gateway.Handle 调用点）。
//
// **必须同步写入**（不走 Session.Send 异步队列）—— 因为本函数在 readLoop / writeLoop
// 启动**之前**调用（V1 §12.1 握手第 3 步钦定 snapshot 同步写入；Story 10.3 r10 P1
// 修后顺序为 snapshot → Register → 启动 readLoop/writeLoop）；writeLoop 此时还没
// 启动，async 入队消息将永不被发送（lesson `2026-05-06-ws-snapshot-startup-order-
// and-placeholder-r7.md` 钦定）。
//
// 错误处理：
//   - builder.BuildSnapshot 抛 error → 返 error；caller 走 close 1011 reason
//     "snapshot build failed"（V1 §12.1 1011 + §12.3 末"snapshot 构建失败处理路径"
//     钦定，**禁止**走 "推 type=error 消息" 路径，6005 错误码保留给 Epic 11.x / 14.x
//     业务流程的运行时状态错误推送）
//   - json.Marshal 抛 error（理论不可能，Snapshot struct 全是 marshalable 字段）
//     → 返 error；caller 同走 close 1011
//   - conn.WriteMessage 抛 error → 返 error；caller 同走 close 1011
//
// 写超时：调用方（gateway.Handle）已 SetWriteDeadline（cfg.WriteTimeoutSec）；本函数
// 不再额外设 deadline（与既有 inline 实装语义一致）。
//
// 注意签名：本函数签名是 (ctx, conn, roomID, builder, writeTimeout)；不传 *Session
// —— 因为本函数在 Session 进入 manager **之前**调用，session 此时是"裸"的没有
// sessionID（gateway.Handle 第 6.1 步：newSession("", userID, roomID, conn, ...)）；
// 直接传 conn 简化依赖。
//
// 节点 4 阶段调用点：仅 gateway.Handle 第 6.2 步（一次性握手期）；不参与 Session 生命
// 周期内的重复推送（V1 §12.3 钦定服务端**可选**重复推送，但节点 4 不实装）。
//
// future：Story 11.7 真实实装替换 builder；Epic 14 / Epic 11 future story 如需
// "成员变化触发 snapshot 重发" 可在 Session.Send 路径调本函数（届时本函数若需
// 走 Session.Send 异步队列，再加 SendRoomSnapshotAsync 平行函数；当前不做）。
func SendRoomSnapshot(
    ctx context.Context,
    conn *websocket.Conn,
    roomID uint64,
    builder SnapshotBuilder,
    writeTimeout time.Duration,
) error {
    snap, err := builder.BuildSnapshot(ctx, roomID)
    if err != nil {
        return fmt.Errorf("ws snapshot: build for room %d: %w", roomID, err)
    }
    env := serverEnvelope{
        Type:      "room.snapshot",
        RequestID: "", // V1 §12.3 主动推送类固定 ""
        Payload:   snap,
        Ts:        time.Now().UnixMilli(),
    }
    bytes, err := json.Marshal(env)
    if err != nil {
        // json.Marshal 在 marshalable struct 下不可能失败；防御性 wrap
        return fmt.Errorf("ws snapshot: marshal envelope for room %d: %w", roomID, err)
    }
    if writeTimeout > 0 {
        _ = conn.SetWriteDeadline(time.Now().Add(writeTimeout))
    }
    if err := conn.WriteMessage(websocket.TextMessage, bytes); err != nil {
        return fmt.Errorf("ws snapshot: write to conn for room %d: %w", roomID, err)
    }
    slog.Default().Debug("ws snapshot sent",
        slog.Uint64("roomID", roomID),
        slog.Int("memberCount", snap.Room.MemberCount),
    )
    return nil
}
```

**代码不变量**（dev-story 实装时必须保留，review 阶段会逐条 check）：

1. SnapshotBuilder 接口签名严格 `BuildSnapshot(ctx context.Context, roomID uint64) (Snapshot, error)`（不加 userID 参数 / 不返 *Snapshot 指针 / 不返 ([]byte, error) bytes 直接 —— Snapshot 是 domain struct，序列化由 SendRoomSnapshot 内做）
2. Snapshot / SnapshotRoom / SnapshotMember / SnapshotPet 字段集合 + JSON tag 与 V1 §12.3 字段表 placeholder 行 1:1 对齐（不增不减字段）
3. SendRoomSnapshot 走 conn.WriteMessage（**同步**），不走 Session.Send（异步队列）
4. 错误统一返 error；caller（gateway.Handle）走 close 1011（**不**走推 type=error 消息路径）
5. memberCount == len(members) 不变量在 placeholderSnapshotBuilder.BuildSnapshot 内强制（用 `len(out.Members)` 直接赋值）
6. ctx 透传给 ListMembers（ADR-0007）

---

**AC2 — 修改 `server/internal/app/ws/gateway.go`：用 SendRoomSnapshot 替换 inline placeholder 实装 + Gateway struct 加 builder 字段 + NewGateway 加 builder 参数**

`server/internal/app/ws/gateway.go` 修改点（具体行号以当前 HEAD 为准 —— 见 §"Source tree components to touch"）：

1. **Gateway struct 加字段**（行 40-48 附近）：
   ```go
   type Gateway struct {
       signer       *auth.Signer
       mgr          SessionManager
       roomMember   mysql.RoomMemberRepo
       upgrader     *websocket.Upgrader
       logger       *slog.Logger
       cfg          config.WSConfig
       writeTimeout time.Duration
       builder      SnapshotBuilder // Story 10.7 加：snapshot 构造路径
   }
   ```

2. **NewGateway 函数签名加 builder 参数**（行 68-118 附近）：
   ```go
   func NewGateway(
       signer *auth.Signer,
       mgr SessionManager,
       roomMember mysql.RoomMemberRepo,
       cfg config.WSConfig,
       envName string,
       builder SnapshotBuilder, // Story 10.7 加
   ) *Gateway {
       // ... 既有 prod contract panic 保留 ...
       return &Gateway{
           // ... 既有字段保留 ...
           builder: builder,
       }
   }
   ```
   - **builder 不可 nil**：NewGateway 内显式 panic（与 signer / mgr / roomMember 同模式 —— Gateway struct 字段无 nil-safe 保护，缺失依赖在启动期 fail-fast 比 request 期 panic 更安全）

3. **gateway.Handle 第 6.2 步替换 inline 实装**（行 260-296 附近）：
   ```go
   // 6.2 同步段构造 + 写 placeholder room.snapshot（**先于** Register；
   // transient 失败时旧 session 不被 evict）
   if err := SendRoomSnapshot(ctx, conn, roomID, g.builder, g.writeTimeout); err != nil {
       g.closeWithCode(conn, 1011, "snapshot build failed")
       g.logger.Error("ws handshake: snapshot send failed",
           slog.Uint64("roomID", roomID), slog.Any("error", err))
       _ = session.Close()
       return
   }
   ```
   - 删除原 `g.roomMember.ListMembers` 调用（已被 placeholderSnapshotBuilder.BuildSnapshot 内吸收）
   - 删除原 `buildPlaceholderSnapshot` 调用（已被 SendRoomSnapshot 内吸收）
   - 删除原 `conn.SetWriteDeadline` + `conn.WriteMessage` 同步写入（已被 SendRoomSnapshot 内吸收）
   - **保留** `_ = session.Close()` 兜底（与既有失败路径同模式，释放 session.conn 资源）

4. **保留既有 helper 但移除 inline 调用**（行 352-425 附近）：
   - `snapshotPayload` / `snapshotRoom` / `snapshotMember` / `snapshotPet` 4 个 struct + `buildPlaceholderSnapshot` 函数：可选**全部删除**（已被 snapshot.go 替代）；或**保留**为 unexported helper 标 `// Deprecated: 由 Story 10.7 SnapshotBuilder 替代`（建议**全部删除**保持代码整洁，删除前确认 ws_test.go / ws_integration_test.go 不再消费这些标识符）
   - 既有 `nilRoomMemberRepo` struct + 编译时接口断言 `var _ mysql.RoomMemberRepo = (*nilRoomMemberRepo)(nil)`（行 427-441）：**保留不动**（既有断言路径不受本 story 影响）

5. **修改 Handle 头注释顺序说明**（行 136-164 附近）：把"6.2 同步段写 placeholder room.snapshot"步骤注释更新为引用 `SendRoomSnapshot`；保留 r10 P1 顺序 rationale（snapshot 必须先于 Register）

**代码不变量**（review 会逐条 check）：

1. NewGateway 多了一个 builder 参数；调用方（bootstrap/router.go）必须同步更新
2. NewGateway 内 builder == nil → panic（fail-fast）
3. gateway.Handle 调 SendRoomSnapshot 单点 —— 不再有 inline `g.roomMember.ListMembers` + `buildPlaceholderSnapshot` + `conn.WriteMessage` 三段直连代码
4. snapshot 失败处理路径仍 close 1011 reason "snapshot build failed"（不变；仅日志改为 "snapshot send failed" 体现新的错误来源）
5. 握手时序不变（snapshot **先于** Register；Register **先于** readLoop / writeLoop 启动）

---

**AC3 — 修改 `server/internal/app/bootstrap/router.go`：构造 SnapshotBuilder 并传入 NewGateway**

`server/internal/app/bootstrap/router.go` 修改点（行 375-391 附近，本 story Patch）：

```go
if deps.SessionMgr != nil {
    if !wsTablesReady(deps.GormDB) {
        panic("ws backing tables missing: rooms / room_members must exist (run migrations 0007 / 0008)")
    }
    roomMemberRepo := repomysql.NewRoomMemberRepo(deps.GormDB)
    snapshotBuilder := wsapp.NewPlaceholderSnapshotBuilder(roomMemberRepo) // Story 10.7 加
    gateway := wsapp.NewGateway(
        deps.Signer,
        deps.SessionMgr,
        roomMemberRepo,
        deps.WSCfg,
        deps.EnvName,
        snapshotBuilder, // Story 10.7 加
    )
    r.GET("/ws/rooms/:roomId", gateway.Handle)
}
```

**代码不变量**：

1. snapshotBuilder 与 gateway 共享同一个 `roomMemberRepo` 实例（避免重复构造 + 避免不同 repo 实例对同一 query 走不同 GORM session 引起细微差异）
2. **不**通过 `bootstrap.Deps` 传 SnapshotBuilder（本地构造即可；与 roomMemberRepo 同模式 —— Deps 是跨多 handler 共享的"基础设施"，SnapshotBuilder 仅 ws gateway 用，不需要进 Deps）
3. **不**改 bootstrap.Deps struct（保持 Story 10.3 / 10.6 既有字段集合）

---

**AC4 — 测试矩阵：单元测试 ≥ 3 case + 集成测试 ≥ 1 case**

#### **单元测试**（`server/internal/app/ws/snapshot_test.go`，新建文件）

测试包：`package ws_test`（black-box，与既有 ws_test.go 同模式）；**必须**用 testify (`require`) + 既有 `ws.NewGateway` / 既有 fakeConn 模式（如有；如没有则用本测试文件局部 helper：`bytes.Buffer` + `gorilla/websocket.NewConn` 配对 OR 复用 ws_test.go 内的 fakeConn）。

**Case 1: BuildSnapshot 返 N=2 成员行 → SendRoomSnapshot 成功 → 写入 conn 的字节流可解码为 V1 §12.3 钦定的 room.snapshot 信封**

```go
func TestSendRoomSnapshot_Happy_TwoMembers(t *testing.T) {
    // arrange: stub builder 返 2 成员
    stubBuilder := &fakeBuilder{
        snap: ws.Snapshot{
            Room:    ws.SnapshotRoom{ID: "100", MaxMembers: 4, MemberCount: 2},
            Members: []ws.SnapshotMember{
                {UserID: "1001", Nickname: "", Pet: ws.SnapshotPet{PetID: "", CurrentState: 1}},
                {UserID: "1002", Nickname: "", Pet: ws.SnapshotPet{PetID: "", CurrentState: 1}},
            },
        },
    }
    // 准备一个真实 *websocket.Conn 走 net.Pipe 模式（与 ws_integration_test.go 既有
    // pattern 一致），客户端从 server-side conn 读出 frame 并 json.Unmarshal
    serverConn, clientConn := newPipeConn(t)
    defer serverConn.Close()
    defer clientConn.Close()

    // act
    err := ws.SendRoomSnapshot(context.Background(), serverConn, 100, stubBuilder, time.Second)
    require.NoError(t, err)

    // assert: client 端读出 frame，json 解析后字段完全一致
    _, frame, readErr := clientConn.ReadMessage()
    require.NoError(t, readErr)
    var env serverEnvelopeForTest // 测试本地 mirror 结构（含 Type/RequestID/Payload/Ts）
    require.NoError(t, json.Unmarshal(frame, &env))
    require.Equal(t, "room.snapshot", env.Type)
    require.Equal(t, "", env.RequestID)
    // 进一步解析 payload
    var snap ws.Snapshot
    payloadBytes, _ := json.Marshal(env.Payload)
    require.NoError(t, json.Unmarshal(payloadBytes, &snap))
    require.Equal(t, "100", snap.Room.ID)
    require.Equal(t, 4, snap.Room.MaxMembers)
    require.Equal(t, 2, snap.Room.MemberCount)
    require.Len(t, snap.Members, 2)
    require.Equal(t, "1001", snap.Members[0].UserID)
    require.Equal(t, "", snap.Members[0].Nickname)
    require.Equal(t, "", snap.Members[0].Pet.PetID)
    require.Equal(t, 1, snap.Members[0].Pet.CurrentState)
    require.Greater(t, env.Ts, int64(0)) // unix ms epoch
}
```

**Case 2: BuildSnapshot 返 error → SendRoomSnapshot 返 error（让 caller 走 close 1011）**

```go
func TestSendRoomSnapshot_BuildSnapshotError(t *testing.T) {
    stubBuilder := &fakeBuilder{err: errors.New("DB connection lost")}
    serverConn, clientConn := newPipeConn(t)
    defer serverConn.Close()
    defer clientConn.Close()

    err := ws.SendRoomSnapshot(context.Background(), serverConn, 100, stubBuilder, time.Second)
    require.Error(t, err)
    require.Contains(t, err.Error(), "DB connection lost")
    // **关键不变量**：不能写 type=error 消息到 wire；client 端不应读到任何 frame
    // （SendRoomSnapshot 在 build 失败时直接返 error，未走 conn.WriteMessage）
    clientConn.SetReadDeadline(time.Now().Add(100 * time.Millisecond))
    _, frame, readErr := clientConn.ReadMessage()
    require.Error(t, readErr) // 应该是 i/o timeout 或 EOF
    require.Empty(t, frame)
}
```

**Case 3: placeholderSnapshotBuilder.BuildSnapshot 走真实 fakeRoomMemberRepo（mock ListMembers 返 [1001, 1002]）→ Snapshot 字段对齐 V1 §12.3 placeholder 行**

```go
func TestPlaceholderSnapshotBuilder_BuildSnapshot_FullRoster(t *testing.T) {
    fakeRepo := &fakeRoomMemberRepo{members: []uint64{1001, 1002}}
    builder := ws.NewPlaceholderSnapshotBuilder(fakeRepo)

    snap, err := builder.BuildSnapshot(context.Background(), 100)
    require.NoError(t, err)

    require.Equal(t, "100", snap.Room.ID)
    require.Equal(t, 4, snap.Room.MaxMembers)
    require.Equal(t, 2, snap.Room.MemberCount)
    require.Len(t, snap.Members, 2)
    // 不变量：MemberCount == len(Members)
    require.Equal(t, snap.Room.MemberCount, len(snap.Members))
    // placeholder 字段降级
    for i, m := range snap.Members {
        require.Equal(t, "", m.Nickname, "members[%d].nickname must be empty in placeholder", i)
        require.Equal(t, "", m.Pet.PetID, "members[%d].pet.petId must be empty in placeholder", i)
        require.Equal(t, 1, m.Pet.CurrentState, "members[%d].pet.currentState must be 1 in placeholder", i)
    }
    // userId 字符串化
    require.Equal(t, "1001", snap.Members[0].UserID)
    require.Equal(t, "1002", snap.Members[1].UserID)
}
```

**附加单元测试（强烈建议但非强制）**：

- **Case 4**: BuildSnapshot 返 0 成员（房间空）→ Snapshot{MemberCount: 0, Members: []}（验证 marshal 空数组而非 null：JSON 序列化结果含 `"members":[]` 字符串 —— V1 §12.3 不变量小节钦定）
- **Case 5**: ctx 已 cancel → fakeRoomMemberRepo.ListMembers 返 ctx.Err → BuildSnapshot 返 error（验证 ctx 透传）
- **Case 6**: placeholderSnapshotBuilder ListMembers 返 [1001, 1002, 1003, 1004]（房间满员，节点 4 阶段 maxMembers=4）→ MemberCount=4

**Helper（snapshot_test.go 内或 ws_test.go 内复用）**：

```go
// fakeBuilder 实装 ws.SnapshotBuilder；走 stub 字段
type fakeBuilder struct {
    snap ws.Snapshot
    err  error
}

func (f *fakeBuilder) BuildSnapshot(ctx context.Context, roomID uint64) (ws.Snapshot, error) {
    if f.err != nil {
        return ws.Snapshot{}, f.err
    }
    return f.snap, nil
}

// fakeRoomMemberRepo 实装 mysql.RoomMemberRepo（仅 ListMembers 走 stub；其他 2 方法
// happy-path 默认值）
type fakeRoomMemberRepo struct {
    members []uint64
    err     error
}

func (f *fakeRoomMemberRepo) RoomExists(_ context.Context, _ uint64) (bool, error) { return true, nil }
func (f *fakeRoomMemberRepo) IsUserInRoom(_ context.Context, _ uint64, _ uint64) (bool, error) { return true, nil }
func (f *fakeRoomMemberRepo) ListMembers(_ context.Context, _ uint64) ([]uint64, error) {
    if f.err != nil {
        return nil, f.err
    }
    return f.members, nil
}

// newPipeConn 用 net.Pipe 创建配对的 server / client *websocket.Conn（与
// ws_integration_test.go 既有 pattern 一致）；测试结束 defer Close。
// （具体实装可复用 ws_test.go / ws_integration_test.go 既有 helper 或新建；
// review 期会 check 不重复造轮子）
func newPipeConn(t *testing.T) (server, client *websocket.Conn) { ... }
```

#### **集成测试**（`server/internal/app/ws/snapshot_integration_test.go`，新建文件 OR 加到既有 `ws_integration_test.go`）

测试 case：

**Case 1（必须）: 真实 WS 客户端拨号 → 立即收到 placeholder room.snapshot，房间预置 ≥2 成员**

- 测试 build tag: `//go:build integration`
- 测试栈：dockertest MySQL（与既有 ws_integration_test.go 同模式 —— 检查 ws_integration_test.go 是否已用 dockertest 起 MySQL；如有则**复用**既有 fixture setup）
- Setup: 创建 1 个 room (id=100) + 2 个 user (id=1001, 1002) + 2 个 room_members 行（user_id=1001, 1002，room_id=100）
- Act: WS dial `ws://localhost:port/ws/rooms/100?token=<合法 1001 token>`
- Assert:
  - HTTP 101 Switching Protocols（握手成功）
  - 第一条 frame 是 `{"type":"room.snapshot", ...}`，JSON 解析后：
    - `payload.room.id == "100"`
    - `payload.room.maxMembers == 4`
    - `payload.room.memberCount == 2`（**不**是 1，**不**是 0 —— 全 roster 不变量；lesson `2026-05-06-ws-snapshot-placeholder-full-roster-and-disconnect-broadcast-r8.md` 钦定）
    - `payload.members[]` 长度 == 2
    - `payload.members[].userId` 集合 == `{"1001", "1002"}`（顺序按 user_id ASC，与 ListMembers 排序一致）
    - `payload.members[].nickname == ""`、`payload.members[].pet.petId == ""`、`payload.members[].pet.currentState == 1`
  - close conn 后 Session 在 manager 中被 Unregister（与既有 ws_integration_test.go 集成测试 close 路径模式一致）

**约束**：

- 集成测试运行 `bash scripts/build.sh --integration`（与既有约束一致）
- 测试时间 ≤ 5s（与 Story 10.6 集成测同约束）
- **禁止**断言 `members: []` 或 memberCount=0 或 memberCount=1（V1 §12.3 不变量小节 + lesson r8 钦定）

---

**AC5 — 联合验证 + 跑全包测试 + 集成测试**

- [ ] `bash scripts/build.sh --test` 全包通过（含本 story 新增 case + 既有跨包 case 不 regression）
- [ ] `bash scripts/build.sh --integration` 通过（含本 story 集成测 case）
- [ ] `bash scripts/build.sh --test --race`（Windows CGO 受限：Story 10.3/10.4/10.5/10.6 既有约束 —— **race detector 在 Windows 跑不了**，本 story 同样不强求；macOS / Linux 环境有条件可跑；dev-story 阶段记录环境约束即可）
- [ ] `go vet ./...` 无 issue
- [ ] `gofmt -l ./internal/app/ws/snapshot.go ./internal/app/ws/snapshot_test.go ./internal/app/ws/snapshot_integration_test.go` 无 issue（**新建文件**全 LF clean；既有 CRLF 文件不动 —— 与 Story 10.6 既有约束一致）
- [ ] 启动 server 验证延后到 Epic 9（端真机联调阶段）；本 story 的 wire 顺序通过 build.sh 编译已验证

---

## Tasks / Subtasks

- [x] **Task 1** (AC: 1): 新建 `server/internal/app/ws/snapshot.go`
  - [x] Subtask 1.1: 写 Package doc + import 块
  - [x] Subtask 1.2: 定义 `Snapshot` / `SnapshotRoom` / `SnapshotMember` / `SnapshotPet` 4 个 struct（exported；与 V1 §12.3 字段表 1:1 对齐 + JSON tag）
  - [x] Subtask 1.3: 定义 `SnapshotBuilder` 接口（exported；签名 `BuildSnapshot(ctx, roomID) (Snapshot, error)`）
  - [x] Subtask 1.4: 定义 `placeholderSnapshotBuilder` struct（unexported）+ `NewPlaceholderSnapshotBuilder` 构造函数（exported）
  - [x] Subtask 1.5: 实装 `placeholderSnapshotBuilder.BuildSnapshot` 方法（调 roomMember.ListMembers + 构造 Snapshot）
  - [x] Subtask 1.6: 实装 `SendRoomSnapshot` 包级函数（builder.BuildSnapshot → json.Marshal → conn.WriteMessage 同步写入；错误透传）
  - [x] Subtask 1.7: 跑 `go vet ./internal/app/ws/...` 无 issue + `gofmt -l snapshot.go` 无 issue

- [x] **Task 2** (AC: 2): 修改 `server/internal/app/ws/gateway.go`
  - [x] Subtask 2.1: Gateway struct 加 `builder SnapshotBuilder` 字段
  - [x] Subtask 2.2: NewGateway 加 `builder SnapshotBuilder` 参数 + nil 检查 panic + struct 初始化赋值
  - [x] Subtask 2.3: gateway.Handle 第 6.2 步：删除 inline `g.roomMember.ListMembers` + `buildPlaceholderSnapshot` + `conn.WriteMessage` 三段直连，替换为单行 `SendRoomSnapshot` 调用 + 错误处理（close 1011 + log + session.Close）
  - [x] Subtask 2.4: 修改 Handle 头注释（行 136-164 附近）：把第 6.2 步从"调 ListMembers + 构造 placeholder + 同步写入"改为引用 `SendRoomSnapshot`；保留 r10 P1 顺序 rationale + r10 P1 lesson 引用
  - [x] Subtask 2.5: 删除 gateway.go 内既有 `snapshotPayload` / `snapshotRoom` / `snapshotMember` / `snapshotPet` 4 个 struct + `buildPlaceholderSnapshot` 函数（已删除；保留 `nilRoomMemberRepo` 编译时接口断言；同时清理无用的 `encoding/json` import）
  - [x] Subtask 2.6: 跑 `go vet ./internal/app/ws/...` + `gofmt -l gateway.go` 无 issue

- [x] **Task 3** (AC: 3): 修改 `server/internal/app/bootstrap/router.go`
  - [x] Subtask 3.1: 在 `wsTablesReady` 检查通过后、`wsapp.NewGateway` 调用前，构造 `snapshotBuilder := wsapp.NewPlaceholderSnapshotBuilder(roomMemberRepo)`
  - [x] Subtask 3.2: NewGateway 调用加 `snapshotBuilder` 实参（与 NewGateway 签名同步更新）
  - [x] Subtask 3.3: 跑 `bash scripts/build.sh` 确认 main + bootstrap 编译通过

- [x] **Task 4** (AC: 4): 写 `server/internal/app/ws/snapshot_test.go` 单测（≥ 3 case）
  - [x] Subtask 4.1: 写 `fakeBuilder` helper + 复用 ws_test.go 既有 stubRoomMemberRepo（避免重复造轮子）
  - [x] Subtask 4.2: 写 `newPipeWSConnPair` helper（local httptest.Server + Upgrader pattern；与 internal `newPipeWebsocketConn` 同模式但本文件 package ws_test 不能直接复用 internal helper）
  - [x] Subtask 4.3: 写 Case 1 - 3（必须）+ Case 4 (空房间 `[]` 而非 null) + Case 5 (ctx 透传)；共 5 case
  - [x] Subtask 4.4: 跑 `bash scripts/build.sh --test`（含 `-count=1`）确认通过

- [x] **Task 5** (AC: 4): 写 `server/internal/app/ws/snapshot_integration_test.go` 集成测试（≥ 1 case）
  - [x] Subtask 5.1: 写 build tag `//go:build integration` + Case 1（真实 WS 拨号 → 收 placeholder snapshot 验全 roster）
  - [x] Subtask 5.2: 复用 ws_integration_test.go 既有 dockertest MySQL fixture (`startMySQLWithRoomMemberFixture`) + WS dial helper (`startGatewayWithRealMySQL`)
  - [x] Subtask 5.3: 跑 `bash scripts/build.sh --integration` 确认通过

- [x] **Task 6** (AC: 5): 联合验证
  - [x] Subtask 6.1: 跑 `bash scripts/build.sh --test` 全包通过
  - [x] Subtask 6.2: 跑 `bash scripts/build.sh --integration` 通过
  - [x] Subtask 6.3: 跑 `go vet ./...` 无 issue；新建 3 个文件全 LF + gofmt clean
  - [x] Subtask 6.4: 在 Completion Notes 记录 race detector 在 Windows 受限的环境约束（与 Story 10.3/10.4/10.5/10.6 同模式）

---

## Dev Notes

### 实装关键决策

#### §1 SnapshotBuilder 接口位置 ＝ `internal/app/ws/` 包内，与 gateway 平级

**为什么不放 service 层 / domain/room 子目录**：

- snapshot 是 **WS 协议层产物**，不是业务 service —— 它的"输入"是 WS 握手的 roomID + ctx，"输出"是 WS frame；与 ws.Session / ws.Gateway 同生命周期，与 service 层（HTTP REST handler 调用的业务逻辑）正交
- Go 项目结构 §6 ws/ 模块下虽然没有显式列出 snapshot.go，但既有 broadcast.go / heartbeat_scanner.go 同样不在 §6 显式列表里 —— ws/ 子目录是按"协议运作角色"分文件，snapshot.go 与 broadcast.go / heartbeat_scanner.go 同模式
- 防止"snapshot 字段语义跟 REST GET /rooms/{roomId} 字段语义被强行统一"的过度耦合 —— V1 §10 REST 与 §12.3 WS 字段表本来就独立锚定（如 REST 字段表含 avatarUrl，WS placeholder 不含），protocol layer 隔离让两边可独立演化

**反例**：如果放 `internal/service/snapshot_service.go`，service 层的 snapshot 又要跨 layer 透传 *websocket.Conn / writeTimeout —— 违反 service 层"纯业务，无 protocol 细节"原则。

#### §2 SnapshotBuilder 接口签名 ＝ `BuildSnapshot(ctx, roomID) (Snapshot, error)`

**为什么不返 ([]byte, error)**：

- Snapshot 是 domain struct，序列化由调用层决定 —— SendRoomSnapshot 需要包 serverEnvelope（含 type/requestId/ts），如果 BuildSnapshot 提前 marshal 成 []byte，SendRoomSnapshot 还得 unmarshal 再包 envelope，两次 JSON 编解码无意义
- 单测层面：返 Snapshot struct 让测试断言字段值更精确（如 `require.Equal(t, "100", snap.Room.ID)` 比 `require.Contains(t, string(bytes), "\"id\":\"100\"")` 更可读）
- future Story 11.7 / 14.x 复用：realSnapshotBuilder 同样返 Snapshot struct；envelope 包装由 SendRoomSnapshot 统一 —— 单点改进路径

**为什么不返 *Snapshot 指针**：

- Snapshot struct 是值类型语义（不可变结构）；Go 惯例对 < 100 字段、读多写少的 struct 用值传递更安全（无 nil panic 风险）
- 与 Story 10.6 PresenceRepo 接口（IsOnline 返 (bool, error)）同模式 —— 接口返"开放可序列化"基础类型

#### §3 SendRoomSnapshot 走 conn.WriteMessage 同步 + 不走 Session.Send 异步

**关键约束**（V1 §12.1 握手第 3 步 + lesson `2026-05-06-ws-snapshot-startup-order-and-placeholder-r7.md` 钦定）：

- snapshot 必须**同步**写入 *websocket.Conn —— 因为 SendRoomSnapshot 调用时机是 readLoop / writeLoop **未启动**之前（gateway.Handle 第 6.2 步 → 第 6.4 步才 `go session.readLoop() / writeLoop()`）
- 如果走 Session.Send 异步队列：
  1. Send 把 msg 入 sendChan（缓冲 channel）
  2. writeLoop 还没启动 → sendChan 永远不被消费
  3. 第 6.4 步启动 writeLoop 后，writeLoop 从 sendChan 取出 snapshot frame 写到 conn
  4. **窗口期问题**：第 6.4 步与 client 发第一个 ping 之间存在 race —— client 可能在 writeLoop 启动 + sendChan 取出 snapshot 之间发了 ping，server readLoop 已启动 → 同步处理 ping → 调 Session.Send 把 pong 入队（这时 sendChan 头部是 snapshot 后面是 pong）→ writeLoop 顺序消费应该没问题，但**如果用 SendPriority** 路径（V1 §12.2 协议层 ping 应优先），pong 可能跳到 snapshot 前面 → snapshot 不再是第一条 frame，违反 V1 §12.1 "first must be snapshot" 契约
- 同步写入 conn.WriteMessage 物理保证 snapshot frame 在 wire 上**绝对**领先所有后续 frame —— writeLoop 此时还不存在，无法产生任何竞争性写入

**lesson 链**：

- `2026-05-06-ws-snapshot-startup-order-and-placeholder-r7.md`: snapshot 必须同步写入
- `2026-05-06-ws-handshake-register-after-snapshot-r10.md`: snapshot **先于** Register 的事务性 reconnect 语义
- `2026-05-06-ws-frozen-section-authz-and-snapshot-coherence-r6.md`: snapshot 失败走 close 1011 而非 type=error 消息

#### §4 snapshot 失败 → close 1011 reason "snapshot build failed"，不走推 type=error 消息

**V1 §12.3 末"snapshot 构建失败处理路径"注 + §12.1 close code 表 1011 行**钦定：

- BuildSnapshot 抛 error / json.Marshal 抛 error / conn.WriteMessage 抛 error 都走 close 1011
- **禁止**改走 "推 type=error 消息" 路径，因为 §12.1 钦定 room.snapshot 是握手成功后必发的第一条消息，若仅推 error 而保持连接，client 会永远等待一个永不到达的 snapshot，房间页无法初始化
- close 1011 触发 client 按 transient network failure 自动重连（指数退避）—— 让客户端在下次重连时尝试新的 snapshot 构造

**反例**：6005（房间状态异常）保留给 Epic 11.x / 14.x 业务流程的运行时状态错误推送（如 join 后房间被解散），**不**用于初始 snapshot 失败场景。

**测试断言**（snapshot_test.go Case 2 + snapshot_integration_test.go 失败路径附加 case）：

- BuildSnapshot 返 error → SendRoomSnapshot 返 error，**不**走 conn.WriteMessage（client 端读 frame 应超时 / EOF）
- 集成测可加 case：mock RoomMemberRepo.ListMembers 返 error → WS 客户端收到 close frame，code=1011，reason="snapshot build failed"

#### §5 SnapshotBuilder 接口边界严格 1 个方法（不加 SendSnapshot / RebuildSnapshot 之类）

**YAGNI**：epics.md 行 1793 钦定接口签名 `BuildSnapshot(ctx, roomID) (Snapshot, error)`；本 story 严格遵守。

- **不加** SendSnapshot 方法到接口：发送路径由 SendRoomSnapshot 包级函数承担，与 builder 解耦（builder 只关心"构造"，不关心"发送"）—— 让 future Story 14.x（如需"成员状态变化触发 snapshot 重发"）能复用同一 builder + 在 Session.Send 路径调用，无需 builder 内部嵌发送逻辑
- **不加** GetEmptySnapshot / GetCachedSnapshot 之类便利方法：节点 4 阶段不需要缓存；Story 11.7 真实实装如需缓存自带（在 realSnapshotBuilder 内私有，不污染接口）
- **不加** RebuildSnapshot 之类的"幂等重发"：节点 4 不实装重复推送；Story 11.7 / 14.x 真实需要时再加（届时直接调 BuildSnapshot 即可，本来就是幂等的）

#### §6 接口可重入语义（节点 4 不实装但需要预留）

V1 §12.3 钦定服务端**可选**在 Session 生命周期内重复推送 snapshot；本 story 不实装重复推送，但接口设计要让 future 复用：

- BuildSnapshot 是**幂等纯函数**（多次调用同一 roomID 返回相同 Snapshot 结构 —— 字段值可能因 DB 状态变化不同，但语义是"返回当前真实状态"）
- SendRoomSnapshot 单次调用是 atomic：要么 build + write 都成功（返 nil），要么任一步失败返 error（不做"部分写入兜底"）
- future Session 生命周期内重复推送：Story 14.x 评估是否需要在 SendRoomSnapshotAsync（async 走 Session.Send）+ SendRoomSnapshot（sync 走 conn.WriteMessage）平行函数；当前不引入

#### §7 ctx 与超时

- BuildSnapshot 首参数 ctx：透传给 ListMembers 调用（GORM `.WithContext(ctx)` 路径自然中断）
- SendRoomSnapshot 首参数 ctx：透传给 BuildSnapshot；conn.WriteMessage 不直接消费 ctx（gorilla websocket API 不接受 ctx 参数），但 SetWriteDeadline 走 writeTimeout 兜底
- gateway.Handle 调用 SendRoomSnapshot 时传的是 `c.Request.Context()` —— 与既有 ListMembers 调用一致

#### §8 与既有 inline 实装的字段对齐

**严格 1:1 对齐 V1 §12.3 字段表 placeholder 行**（不增不减）：

- room.id（string，roomID 字符串化）
- room.maxMembers（int，固定 4）
- room.memberCount（int，len(members)）
- members[].userId（string，userID 字符串化）
- members[].nickname（string，placeholder 阶段 ""）
- members[].pet.petId（string，placeholder 阶段 ""）
- members[].pet.currentState（int，placeholder 阶段 1）
- envelope.type（string，"room.snapshot"）
- envelope.requestId（string，主动推送类固定 ""）
- envelope.payload（Snapshot struct，序列化为 object）
- envelope.ts（int64，time.Now().UnixMilli()）

**反例**：

- 不加 avatarUrl 字段（V1 §12.3 字段表注 "Story 11.7 真实实现时启用，节点 4 placeholder 不返回该字段"）
- 不加 isOnline 字段（V1 §12.3 placeholder 行不含；Story 11.7 真实实装时再评估）
- 不加 currentRoomId 字段（与 GET /rooms/current REST 字段属于不同 layer）

#### §9 NewGateway 签名变更影响

新增 `builder SnapshotBuilder` 参数，影响调用点：

- `bootstrap/router.go` 行 383-389（必改 —— 见 AC3）
- `ws_test.go` / `ws_integration_test.go` / `gateway_test.go`（如有）—— review 期 grep 全 `wsapp.NewGateway(` 调用点确认全部更新

**风险点**：测试代码可能用 nil builder 走 happy path —— NewGateway 内 nil 检查 panic 让此类调用立即 fail-fast。dev-story 实装时若 ws_test.go / ws_integration_test.go 有调用 NewGateway 的 case，必须同步加 builder 参数（用 `&fakeBuilder{}` 或 `wsapp.NewPlaceholderSnapshotBuilder(fakeRoomMemberRepo)` 注入）。

### Source tree components to touch

**新建文件**：
- `server/internal/app/ws/snapshot.go`（核心代码：SnapshotBuilder 接口 + Snapshot struct + placeholderSnapshotBuilder 实装 + SendRoomSnapshot 包级函数）
- `server/internal/app/ws/snapshot_test.go`（单测 ≥ 3 case；black-box `package ws_test`）
- `server/internal/app/ws/snapshot_integration_test.go`（集成测 ≥ 1 case；build tag `//go:build integration`；可选合并到既有 ws_integration_test.go）

**修改文件**：
- `server/internal/app/ws/gateway.go`：Gateway struct 加 builder 字段 / NewGateway 加 builder 参数 / Handle 第 6.2 步用 SendRoomSnapshot 替换 inline 实装 / 删除既有 buildPlaceholderSnapshot + 4 个 struct（或保留为 Deprecated）
- `server/internal/app/bootstrap/router.go`：构造 `snapshotBuilder := wsapp.NewPlaceholderSnapshotBuilder(roomMemberRepo)` + 传入 `wsapp.NewGateway`
- `server/internal/app/ws/ws_test.go`（如有 NewGateway 调用，需同步加 builder 实参）
- `server/internal/app/ws/ws_integration_test.go`（如有 NewGateway 调用，需同步加 builder 实参）
- `server/internal/app/bootstrap/router_ws_test.go`（如有 NewGateway 调用，需同步加 builder 实参）

**不动文件**：
- `server/internal/app/ws/session.go` / `session_manager.go` / `heartbeat_scanner.go` / `broadcast.go`（10.3 / 10.4 / 10.5 / 10.6 已锁定）
- `server/internal/repo/redis/presence_repo.go`（10.6 已锁定；本 story 不消费 presence）
- `server/internal/repo/mysql/room_member_repo.go`（10.3 已锁定；本 story 复用 ListMembers）
- `server/internal/infra/redis/client.go` / `mock.go` / `redis.go`（RedisClient 接口边界已锁定）
- `server/cmd/server/main.go`（Story 10.6 已 wire SessionMgr / presenceRepo 等；本 story 通过 bootstrap/router.go 注入 builder，不需要改 main.go）
- `docs/宠物互动App_V1接口设计.md` / `docs/宠物互动App_数据库设计.md` / `docs/宠物互动App_Go项目结构与模块职责设计.md`（设计文档锚点已就位，本 story 是落地实装；snapshot.go 隐含在 §6 ws/ 模块边界内）
- `docs/lessons/`（既有 11 个 ws-snapshot-* lesson 文件不动；本 story 是落地阶段，不反向编辑既有 lessons —— lessons 是 retro 时由 fix-review 流程产出）

### Testing standards summary

按 ADR-0001（test-stack）+ ADR-0007（context propagation）+ Story 1-1 / 1-5 / 4.7 / 10.2 / 10.3 / 10.4 / 10.5 / 10.6 既有模式：

- **测试栈**：testify (require + assert) + 既有 ws_test 包 helper（fakeConn / fakeSession / net.Pipe pattern；如已有则复用，避免重复造轮子）+ slog（结构化日志，不在测试内 assert 日志内容）
- **mock 策略**：fakeBuilder（实装 SnapshotBuilder）+ fakeRoomMemberRepo（实装 mysql.RoomMemberRepo，仅 ListMembers 走 stub）；**不**复杂 generated mock（YAGNI）
- **build tag**：集成测试用 `//go:build integration`（与既有跨包模式一致）
- **race detector**：本 story 涉及 conn 同步写入 + ctx 透传，无显式 goroutine 创建路径；race detector 在 Windows CGO 环境受限不跑（与 Story 10.3/10.4/10.5/10.6 既有约束一致）；macOS / Linux 有条件可跑
- **断言风格**：require 用于"前置失败即 t.Fatal" / assert 用于"非阻断性断言"（与既有跨包模式一致，本 story 单测全用 require 简化逻辑）
- **不写**：performance benchmark（节点 13+ 才需）、E2E 真 ws connection 集成（Story 12.x iOS 端）、metrics assertion（节点 13+ 引入 metrics 后再加）

### Project Structure Notes

#### Alignment with unified project structure

- `server/internal/app/ws/snapshot.go` 路径与既有 `gateway.go` / `session.go` / `broadcast.go` / `heartbeat_scanner.go` 平铺 —— 与 docs/宠物互动App_Go项目结构与模块职责设计.md §6 ws/ 模块边界一致
- 包名 `ws` 与既有保持一致
- 接口命名 `SnapshotBuilder` / 实装 `placeholderSnapshotBuilder` —— 与既有 `SessionManager` / `sessionManager` 同命名模式（exported interface + unexported struct）
- 文件位置：`snapshot.go` + `snapshot_test.go` + `snapshot_integration_test.go` 同目录平铺（与既有 broadcast.go + broadcast_perf_internal_test.go 模式一致）

#### Detected conflicts or variances

- Go 项目结构 §6 ws/ 模块**未显式**列出 snapshot.go（既有列表只到 broadcast.go / heartbeat_scanner.go / event_dispatcher.go / message_types.go / room_hub.go 等）；本 story 的 snapshot.go **隐含**在 §6 ws/ 模块边界内（与 broadcast.go / heartbeat_scanner.go 同样不在显式列表里同模式）—— 不视为冲突，不修改 §6 文档（本 story 范围红线钦定）

### References

- [Source: _bmad-output/planning-artifacts/epics.md#Story 10.7]: 本 story AC 钦定（行 1780-1800）—— 接口签名 `BuildSnapshot(ctx, roomID) (Snapshot, error)` + SendRoomSnapshot 函数 + close 1011 路径 + 单测 ≥ 3 case + 集成测 ≥ 1 case
- [Source: docs/宠物互动App_V1接口设计.md#12.1 连接地址]: 握手成功流程第 3 步钦定 snapshot 同步写入；§12.1 close code 表 1011 行钦定 snapshot 构建失败 close 1011
- [Source: docs/宠物互动App_V1接口设计.md#12.3 服务端→客户端消息]: room.snapshot payload schema 字段表 placeholder 行 + "client merge contract" + "不变量" + 末尾 "snapshot 构建失败处理路径" 注
- [Source: docs/宠物互动App_数据库设计.md#5.13 rooms]: room 表 schema（rooms.id / status；本 story 不直接读 rooms 表，但 placeholder builder 间接通过 ListMembers 关联 room_id）
- [Source: docs/宠物互动App_数据库设计.md#5.14 room_members]: room_members 表 schema（room_id, user_id；本 story placeholder builder 走 ListMembers 单表查询）
- [Source: docs/宠物互动App_Go项目结构与模块职责设计.md#6 Realtime 模块]: ws/ 模块边界（snapshot.go 隐含在 §6 内，与 broadcast.go / heartbeat_scanner.go 同模式）
- [Source: docs/lessons/2026-05-06-ws-snapshot-startup-order-and-placeholder-r7.md]: snapshot 必须同步写入到 conn（不走 Session.Send 异步队列；writeLoop 启动前的关键窗口期）
- [Source: docs/lessons/2026-05-06-ws-snapshot-placeholder-full-roster-and-disconnect-broadcast-r8.md]: placeholder 必须返回**全 roster**（房间已有 ≥2 成员时禁止只返 1 个）；与本 story Case 1 集成测断言 `members[].length == 2` 严格对齐
- [Source: docs/lessons/2026-05-06-ws-frozen-section-authz-and-snapshot-coherence-r6.md]: snapshot 失败统一走 close 1011（不走推 type=error 消息）；与 V1 §12.3 末注三方钦定一致
- [Source: docs/lessons/2026-05-06-ws-handshake-register-after-snapshot-r10.md]: snapshot **先于** Register 的事务性 reconnect 语义（本 story 严格保留 Story 10.3 r10 P1 顺序）
- [Source: docs/lessons/2026-05-06-ws-snapshot-merge-contract-and-roomid-source-r10.md]: roomid 字符串化 + client merge contract（server 实装层不关心 client 如何 merge，只保证下发字段语义符合本契约）
- [Source: _bmad-output/implementation-artifacts/decisions/0001-test-stack.md]: testify + miniredis + dockertest 栈钦定
- [Source: _bmad-output/implementation-artifacts/decisions/0007-context-propagation.md]: ctx 传播规则（首参数 ctx + WithContext 调用）
- [Source: server/internal/app/ws/gateway.go:174-330]: 当前 Handle 函数实装（本 story 重构对象）
- [Source: server/internal/app/ws/gateway.go:352-425]: 当前 inline 实装的 buildPlaceholderSnapshot + 4 个 struct（本 story 删除或迁移到 snapshot.go）
- [Source: server/internal/app/bootstrap/router.go:375-391]: 当前 NewGateway 调用点（本 story 修改加 builder 参数）
- [Source: server/internal/repo/mysql/room_member_repo.go:51-88]: RoomMemberRepo 接口（含 ListMembers，本 story placeholder builder 复用）
- [Source: _bmad-output/implementation-artifacts/10-6-redis-presence-repo.md]: 上一个 story（10.6）的 Dev Notes / 实装关键决策 —— 本 story 的接口形态先行 / ctx 传播 / 错误语义内化等设计决策与之同模式

## Dev Agent Record

### Agent Model Used

claude-opus-4-7[1m]（dev-story workflow，2026-05-07）

### Debug Log References

- `bash scripts/build.sh --test` 全包通过（ws 包 6.8s；其他包总计 ~13s）
- `bash scripts/build.sh --integration` 全包通过（含本 story 新增 `TestWSIntegration_PlaceholderSnapshot_FullRoster` + 既有 ws integration cases）
- `go vet ./...` 无 issue；`gofmt -l ./internal/app/ws/snapshot{.go,_test.go,_integration_test.go}` 无 issue

### Completion Notes List

**实装重点**：

1. **接口契约最后一公里**：把 gateway.go 内 inline 的 placeholder snapshot 实装（`buildPlaceholderSnapshot` 函数 + 4 struct + `g.roomMember.ListMembers` + `conn.WriteMessage` 同步写入三段直连）抽离到新文件 `snapshot.go`，提供：
   - `SnapshotBuilder` interface（签名钦定 `BuildSnapshot(ctx, roomID) (Snapshot, error)`）
   - `Snapshot` / `SnapshotRoom` / `SnapshotMember` / `SnapshotPet` 4 个 exported struct（V1 §12.3 字段表 placeholder 行 1:1 对齐）
   - `placeholderSnapshotBuilder` unexported struct + `NewPlaceholderSnapshotBuilder` exported 构造函数
   - `SendRoomSnapshot(ctx, conn, roomID, builder, writeTimeout) error` 包级函数（封装 build → marshal envelope → conn.WriteMessage 同步写入；统一错误透传到 caller 走 close 1011）

2. **gateway.Handle 重构**：原 ~30 行 inline 三段（ListMembers + buildPlaceholderSnapshot + conn.WriteMessage 含 SetWriteDeadline）收敛为单行 `SendRoomSnapshot(ctx, conn, roomID, g.builder, g.writeTimeout)`。错误处理（close 1011 reason "snapshot build failed" + log + session.Close）保留在 Handle 调用点，single source of truth 不变。删除 `buildPlaceholderSnapshot` + 4 个 unexported struct（gateway.go 不再需要这些 helper）+ 清理无用的 `encoding/json` import。

3. **NewGateway 签名扩展**：加 `builder SnapshotBuilder` 第 6 参数 + nil 检查 panic（fail-fast，与 signer / mgr / roomMember 同模式）。bootstrap/router.go 同步更新；ws_test.go 既有调用点（startGatewayServer / TestSession_Send_BufferFull / 5 个 NewGateway prod-contract gate test）+ ws_integration_test.go 同步更新。

4. **新增 `TestNewGateway_NilBuilder_Panics`** 单测覆盖 fail-fast 路径。

5. **测试矩阵**：
   - **单测 5 case**（snapshot_test.go，black-box `package ws_test`）：
     - Case 1：`SendRoomSnapshot` happy path（2 成员）→ wire 上 envelope 字段 + payload 字段全对齐 V1 §12.3
     - Case 2：`SendRoomSnapshot` BuildSnapshot 错误 → 返 error + **不**写 wire（关键不变量：不走推 type=error 路径，由 caller 走 close 1011；lesson r6 钦定）
     - Case 3：`placeholderSnapshotBuilder.BuildSnapshot` 真实路径走 stub repo → 字段降级语义验证 + MemberCount==len(Members) 不变量
     - Case 4：空房间（0 成员）→ JSON 序列化为 `"members":[]` 而非 `null`（V1 §12.3 不变量）
     - Case 5：ctx cancel 透传 → BuildSnapshot 返 ctx.Err 包装 error（ADR-0007）
   - **集成测 1 case**（snapshot_integration_test.go，build tag `integration`）：真实 dockertest mysql:8.0 + 既有 fixture（roomID=3001 / 2 成员）→ WS 拨号 → 第一条 frame 是 placeholder room.snapshot，全 roster 字段对齐（lesson r8 钦定全 roster 不变量）

6. **辅助 helper**：
   - `fakeBuilder` 实装 `wsapp.SnapshotBuilder`：单测 Case 1 / 2 用
   - 复用 ws_test.go 既有 `stubRoomMemberRepo`（避免重复造轮子；本文件 black-box 包能直接访问）：单测 Case 3 / 4 / 5 用
   - `newPipeWSConnPair` 用 httptest.Server + Upgrader 构造配对的 server / client `*websocket.Conn`（与 session_close_internal_test.go `newPipeWebsocketConn` 同模式，但因为 package 边界本文件 local 实装一份）

**关键纪律保留**（10.3 ~ 10.6 已锁定的不变量本 story 全保留）：

- ✅ Send/Close 并发安全（session.go 不变）
- ✅ priority chan 防 starve（session.go 不变）
- ✅ onUnregister hook 在所有 close 路径触发（session_manager.go 不变）
- ✅ 全 UUID sessionID（session_manager.go 不变）
- ✅ **handshake snapshot-then-Register 顺序**（gateway.go 6.2 → 6.3 顺序保留；snapshot 仍**先于** Register；r10 P2 onRegister(NEW) 先于 replaced.Close 顺序也不动）
- ✅ ListAllSessions / ListSessionsByRoomID sort outside lock（session_manager.go 不变）
- ✅ heartbeat scanner shutdown ordering（heartbeat_scanner.go 不变）
- ✅ BroadcastToRoom per-room mutex + bytes.Clone + Load fast path（broadcast.go 不变）
- ✅ Presence repo 的 Lua atomic + per-user mutex 共享 + Register 顺序 r10 fix（presence_repo.go 不变）

**范围红线遵守**：

- ✅ 不实装 `realSnapshotBuilder`（Story 11.7 接管）
- ✅ 不让 snapshot 路径消费 PresenceRepo（V1 §12.3 钦定 placeholder 阶段不依赖 presence）
- ✅ 不实装"成员变化触发 snapshot 重发"路径（节点 4 阶段不需要）
- ✅ 不改 V1 §12.3 / 数据库设计 §5.13-5.14 / Go 项目结构 §6 三份设计文档
- ✅ 不改既有 Story 10.3 ~ 10.6 任何已落地代码的对外行为（仅 gateway.Handle 内部用 SendRoomSnapshot 替换 inline 实装）
- ✅ 不改 SnapshotBuilder 接口签名添加 `userID` 参数 / 不返 *Snapshot 指针 / 不返 ([]byte, error)
- ✅ 不改 Snapshot struct 添加 `members[].avatarUrl` 字段
- ✅ 不让 SendRoomSnapshot 走 Session.Send 异步队列（必须 conn.WriteMessage 同步写入）
- ✅ 不实装 metrics / 不实装 SnapshotBuilder 缓存 / 不写英文文档 / 不改既有 lessons

**环境约束**：

- Race detector 在 Windows CGO 环境受限不跑（与 Story 10.3 / 10.4 / 10.5 / 10.6 同约束）；macOS / Linux 有条件可跑。本 story 涉及 conn 同步写入 + ctx 透传，无显式 goroutine 创建路径，race 暴露面极低。
- 集成测试需要 docker daemon 可达（dockertest MySQL 容器）；docker 不可用时 `t.Skip`（与既有 ws_integration_test.go 模式一致）。

### File List

**新增**：
- `server/internal/app/ws/snapshot.go`（核心实装：SnapshotBuilder 接口 + Snapshot struct + placeholderSnapshotBuilder + SendRoomSnapshot）
- `server/internal/app/ws/snapshot_test.go`（5 case 单测，black-box `package ws_test`）
- `server/internal/app/ws/snapshot_integration_test.go`（1 case 集成测，build tag `integration`）

**修改**：
- `server/internal/app/ws/gateway.go`（Gateway struct 加 builder 字段；NewGateway 加 builder 参数 + nil panic；Handle 6.2 步用 SendRoomSnapshot 替换 inline 三段；删除 buildPlaceholderSnapshot 函数 + 4 个 struct；清理 encoding/json import；handshake completed log 去掉 memberCount 字段）
- `server/internal/app/bootstrap/router.go`（构造 placeholder builder 注入 NewGateway）
- `server/internal/app/ws/ws_test.go`（startGatewayServer + TestSession_Send_BufferFull + 5 个 NewGateway prod-contract gate test 同步加 builder 参数；新增 `TestNewGateway_NilBuilder_Panics`）
- `server/internal/app/ws/ws_integration_test.go`（startGatewayWithRealMySQL 同步加 builder 参数）
- `_bmad-output/implementation-artifacts/sprint-status.yaml`（10-7 status: ready-for-dev → in-progress → review）
- `_bmad-output/implementation-artifacts/10-7-房间快照下发框架.md`（Status + Tasks 勾选 + Dev Agent Record + Change Log）

## Change Log

| 日期 | 变更 | 作者 |
|---|---|---|
| 2026-05-06 | create-story 完成；Status: ready-for-dev | bmad-create-story |
| 2026-05-07 | dev-story 落地：snapshot.go 抽离 + gateway.go 重构 + bootstrap 注入 + 5 case 单测 + 1 case 集成测；全 build / test / vet / gofmt 通过；Status: in-progress → review | bmad-dev-story |