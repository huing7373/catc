# Story 11.1: 接口契约最终化（房间 5 个 REST 接口 + 2 个 WS 业务消息）

Status: review

<!-- Validation 可选。建议运行 validate-create-story 在 dev-story 前做一次质检。 -->

## Story

As a 跨端契约负责人,
I want 在写第一行 server 房间业务代码（Story 11.2 ~ 11.10）之前，把节点 4 涉及的房间 5 个 REST 接口（`POST /rooms` / `GET /rooms/current` / `GET /rooms/{roomId}` / `POST /rooms/{roomId}/join` / `POST /rooms/{roomId}/leave`）+ 2 个业务 WS 消息（服务端 → 客户端 `member.joined` / `member.left`）的 schema 在 `docs/宠物互动App_V1接口设计.md` §10.1 / §10.2 / §10.3 / §10.4 / §10.5 / §12.3 里逐字段锚定（含必填项、类型、长度/范围约束、错误码 6001 / 6002 / 6003 / 6004 / 6005 触发条件、节点 4 阶段占位字段语义、节点 4 阶段冻结声明），同时把 Story 10.1 §12.3 末尾"业务消息延后锚定"引用块里 `member.joined` / `member.left` 状态从"Epic 10 草稿"升级为"Epic 11 已锚定"+ 把这两条消息合法加入 server → client active message set,
so that iOS Epic 12（房间页 + WS 集成）和 server Epic 11 后续 Story（11.2 ~ 11.10）以及 Story 11.7（房间快照真实实现，依赖本 story 锚定的 §10.3 GET /rooms/{roomId} response.members[].pet 字段表作为 SnapshotBuilder 真实实装的 enrich 字段来源）可以基于同一份冻结契约**并行**开工，不再出现"REST 接口字段大小写不一致 / `pet.equips` 节点 4 阶段填什么不清楚 / `pet.currentState` 节点 4 / 5 / 9 各阶段值如何过渡 / `member.joined` payload 字段名与 client Codable struct 对不上 / 房间错误码 6003 vs 6004 触发条件混淆 / iOS 端 `LeaveRoomUseCase` 没法预知 server 端最后一人离开是否需要单独 close 钩子"的返工。

## 故事定位（Epic 11 第一条 = 节点 4 第二个 Server Epic 的契约起手；对标 Story 4.1 / 7.1 / 10.1 在 Epic 4 / 7 / 10 的角色，本 story 是 Epic 11 **唯一的纯文档 story**，开局而非收官）

- **Epic 11 进度**：本 story → 11.2 (rooms + room_members migration) → 11.3 (POST /rooms 创建房间事务) → 11.4 (POST /rooms/{roomId}/join 加入房间事务) → 11.5 (POST /rooms/{roomId}/leave 退出房间事务) → 11.6 (GET /rooms/current + GET /rooms/{roomId} 房间详情查询) → 11.7 (房间快照真实实现，替换 E10.7 placeholder) → 11.8 (member.joined / member.left WS 广播) → 11.9 (Layer 2 集成测试 - 房间生命周期全流程) → 11.10 (GET /home 扩展 room.currentRoomId 真实数据)。**11.1 是 11.3 ~ 11.8 / 11.10 的强前置**：handler 层 DTO 必须严格按 11.1 锚定后的 schema；service 层错误码映射必须严格按 11.1 锚定的 6001 ~ 6005 触发条件；广播 payload 必须严格按 11.1 锚定的 `member.joined.{userId, nickname}` / `member.left.{userId}` 字段；同时是 iOS Epic 12 的强前置（Story 12.3 房间快照解析 + 12.4 成员加入 / 离开 WS 消息处理 + 12.7 创建 / 加入 / 退出 use case 必须严格按 11.1 冻结契约）
- **epics.md AC 钦定**：`_bmad-output/planning-artifacts/epics.md` §Story 11.1（行 1806-1826）已**精确**列出 §10 房间 5 接口必须锚定的字段 + §12.3 `member.joined` / `member.left` payload + 错误码 6001 ~ 6005 触发条件 + "契约自此 Epic 起冻结"声明
- **Story 10.1 历史遗留对接**：本 story 必须把 Story 10.1 §12.3 末尾"业务消息延后锚定"引用块里 `member.joined` / `member.left` 两条目从"草稿待 Story 11.1 正式锚定"升级为"已由 Story 11.1 锚定（commit hash X）"，并把 §12.3 已存在的 `### 成员加入` / `### 成员离开` 两个 H3 小节从"草稿示例"扩充为完整字段表（保留既有 JSON 示例 + 前置 `**触发**` 段 + `**字段**` 表 + `**关键约束**`）。Story 10.1 r10 不变量"server / client active message set 的冻结范围**仅适用于 Epic 10 阶段**，Epic 11 / 14 / 17 起允许新业务消息合法加入 active message set"在本 story 真正生效 —— 本 story 是**第一次**把新业务消息（`member.joined` / `member.left`）正式加入 server-active 集合的契约 story
- **下游立即依赖**：
  - **Story 11.2（rooms + room_members migration）**：与本 story 文档侧无直接耦合（migration 是 DDL 层，不在 V1 接口文档锚定）；但本 story §10 字段表必须**严格反映** rooms / room_members 表结构（`creator_user_id` / `status` / `max_members` / `room_id` / `user_id` 等字段名 + 类型来源数据库设计 §5.13 / §5.14；`status` 枚举 `1=active / 2=closed` 来源 §6.12）
  - **Story 11.3（POST /rooms 创建房间事务）**：handler request 严格按 11.1 §10.1 锚定的"空 request"（`{}`）；response 严格按 11.1 §10.1 锚定的 `{room: {id, creatorUserId, maxMembers, memberCount, status}}` —— 注意 BIGINT 主键字符串化（§2.5 全局约定）；service 层"用户已在其他房间 → 返回 6003"映射严格按 11.1 §10.1 错误码表
  - **Story 11.4（POST /rooms/{roomId}/join 加入房间事务）**：handler request 严格按 11.1 §10.4 锚定的"空 request"（`{}`，roomId 来自 path 参数）；response 严格按 `{roomId, joined: true}`；错误码 6001（房间不存在）/ 6002（房间已满）/ 6003（用户已在房间中）/ 6005（房间状态异常 status != 1）触发条件严格按 11.1 §10.4 错误码表 + 服务端逻辑顺序
  - **Story 11.5（POST /rooms/{roomId}/leave 退出房间事务）**：handler request 严格按 11.1 §10.5 锚定的"空 request"；response 严格按 `{roomId, left: true}`；错误码 6004（用户不在房间中）触发条件严格按 11.1 §10.5；"最后一人离开 → 房间 status=2 closed"是**服务端逻辑**层面的副作用，**不**改变 response shape（client 不直接感知房间是否 closed —— 仅 `data.left = true`）
  - **Story 11.6（GET /rooms/current + GET /rooms/{roomId} 房间详情查询）**：handler 层严格按 11.1 §10.2 锚定的 `{roomId: <users.current_room_id> | null}` 返回 / §10.3 锚定的 `{room, members}` 嵌套结构；`members[].pet.currentState` 节点 4 阶段固定 `1`（rest，本 story 文档侧明确节点 4 / 5 过渡逻辑）；`members[].pet.equips` 节点 4 阶段返回 `[]`（Future Fields，节点 9 由 Epic 26 / Story 26.6 真实回填，节点 10 由 Epic 29 / Story 29.6 加 `renderConfig`）
  - **Story 11.7（房间快照真实实现）**：本 story §12.3 不重复锚定 `room.snapshot` schema（§12.3 已在 Story 10.1 锚定 + Story 10.7 placeholder 实装；本 story 仅锚定 `member.joined` / `member.left`）；但本 story §10.3 `members[].{userId, nickname, avatarUrl, pet: {petId, currentState, equips}}` 是 **Story 11.7 SnapshotBuilder 真实实装**的 enrich 字段来源（11.7 会让 `room.snapshot` 真实下发 nickname / petId 非空字符串，迁移自 placeholder 阶段的 `""`）；本 story 必须明确"节点 4 placeholder（Story 10.7）/ 节点 4 真实（Story 11.7）/ 节点 5 真实（Epic 14）/ 节点 9 真实（Epic 26）/ 节点 10 真实（Epic 29）"五阶段过渡表，避免 Story 11.7 实装时纠结"现在该填什么"
  - **Story 11.8（member.joined / member.left WS 广播）**：广播 payload 严格按 11.1 §12.3 `### 成员加入` / `### 成员离开` 两节锚定的字段表（`member.joined.payload.{userId, nickname, avatarUrl, pet.{petId, currentState}}` / `member.left.payload.{userId}`）；广播触发时机严格按 11.1 §12.3 触发条件段：`member.joined` = 11.4 加入事务**成功提交后**；`member.left` = 11.5 退出事务**成功提交后**（r3 锁定：唯一触发条件，任何 WS 断开 / 心跳超时**不**触发 `member.left`，仅清 ephemeral 层）；广播失败 fire-and-forget 仅 log（与 Story 10.5 BroadcastToRoom primitive 既有语义一致，本 story 文档侧引用既有不变量不重复锚定）
  - **Story 11.10（GET /home 扩展 room.currentRoomId 真实数据）**：本 story **不**改 §5.1 GET /home schema（§5.1 已在 Story 4.1 锚定 + Future Fields 已标注"`room.currentRoomId` 节点 4 由 Story 11.10 注入真实数据"）；本 story 文档侧仅在 §10 房间章节末尾**引用**（"`GET /home.data.room.currentRoomId` 由 Story 11.10 真实实装"），**不**反向修改 §5.1
  - **iOS Story 12.3（房间快照解析）**：iOS 端 `RoomSnapshotMessage` Codable struct（含 `members[].userId` / `nickname` / `pet.{petId, currentState}`）字段名严格 `camelCase`（沿用 Story 10.1 / Story 12.3 已锚定的解析层）；本 story 仅在 §10.3 GET /rooms/{roomId} response 字段表锚定"REST 拉取的 members 字段集" + `pet.equips` 节点 4 占位 `[]`（影响 Story 12.3 client merge contract enrich 路径 —— 当 client 通过 `GET /rooms/{roomId}` 拉到真实 nickname / petId 后，再收到 `room.snapshot` placeholder 的空字符串字段时按 §12.3 client merge contract 保留真实值，Story 10.1 r10 已锚定）
  - **iOS Story 12.4（成员加入 / 离开 WS 消息处理）**：iOS 端 `MemberJoinedMessage` / `MemberLeftMessage` Codable struct 严格按 11.1 §12.3 字段表（`payload.userId` / `payload.nickname` 均 `String`，nickname 在 `member.joined` 必填且非空 —— 加入事件由 server 在事务提交后广播，server 必有真实 nickname；不存在 placeholder 阶段空字符串场景）；client 收到 `member.joined` 时**应**按 §12.3 client merge contract 字段级 merge（与 `room.snapshot` 同规则：非空覆盖 / 空字符串保留），收到 `member.left` 时**应**从 client roster 移除对应 `userId` entry
  - **iOS Story 12.7（创建 / 加入 / 退出 use case + 主界面入口完善）**：iOS 端 `CreateRoomUseCase` / `JoinRoomUseCase` / `LeaveRoomUseCase` 严格按 11.1 §10.1 / §10.4 / §10.5 request / response DTO；错误码映射严格按 11.1 §10.X 错误码表（6001 ~ 6005）；UX 文案降级（如"房间已满"对应 6002）走 Story 12.7 实装层决定，本 story 仅锚定错误码触发条件**不**锚定 UX 文案
- **范围红线**：本 story **只**改 `docs/宠物互动App_V1接口设计.md` 一份文件 + sprint-status.yaml + 本 story 文件；**不**改任何 Go / Swift 代码、**不**改 `_bmad-output/` 下其他 yaml / md（除自己的 story 文件 + sprint-status.yaml 标记 ready-for-dev → in-progress → review → done）、**不**改其他 6 份 `docs/宠物互动App_*.md`（包括数据库设计 / Go 项目结构 / iOS 项目结构 / 时序图 / 总体架构 / MVP 节点规划 等 —— 它们对应的 schema 已锚定，本 story 只**对齐 / 引用**，不**修改**）

**本 story 不做**（明确范围红线）：

- 不实装任何 Go handler / service / repo / WS 广播代码（11.2 ~ 11.10 才做；本 story 是 contract first 的契约定稿）
- 不实装任何 Swift Codable DTO / WebSocketClient 解析层（iOS Epic 12 各 story 才做；本 story 提供给 iOS 的是文档 + Git commit hash，不是代码）
- 不修改 §5.1 GET /home schema（§5.1 已在 Story 4.1 锚定 + Future Fields 已标注"`room.currentRoomId` 节点 4 由 Story 11.10 注入真实数据"；本 story **不**反向修改 §5.1）
- 不修改 §12.1（连接地址 + close code 表 + 服务端校验顺序）/ §12.2（客户端 → 服务端通用消息信封 + ping）/ §12.3 `room.snapshot` / `pong` / `error` schema（这三处已在 Story 10.1 / 10.7 锚定 + 冻结；本 story 仅扩充 §12.3 新增的 `### 成员加入` / `### 成员离开` 业务消息字段表 + 升级末尾"业务消息延后锚定"引用块状态）
- 不修改 `docs/宠物互动App_数据库设计.md`（rooms §5.13 / room_members §5.14 / users.current_room_id 已锚定；本 story 的 API 字段语义来源是数据库设计，方向是 DB → API，**不反向**修改 DB；`rooms.status` 枚举来源 §6.12，本 story 引用不修改）
- 不修改 `docs/宠物互动App_Go项目结构与模块职责设计.md`（Go 工程目录已锚定 §4 + §6 + §9 + §12；本 story 不引入新目录 / 模块；`internal/domain/room/` / `internal/service/room_service.go` 已在 §6 锚定）
- 不修改 `docs/宠物互动App_iOS客户端工程结构与模块职责设计.md`（iOS 工程目录已 ADR-0002 + Story 2.2 + Story 37.x 落地；本 story 不影响 iOS 目录；`RoomViewModel` / `WebSocketClient` 已在 §9 锚定）
- 不修改 `docs/宠物互动App_时序图与核心业务流程设计.md`（§11.1 创建房间流程 / §11.2 加入房间流程 / §13 WebSocket 建连流程已存在；本 story 字段层契约对齐**不**触发流程级修订；时序图描述的 `member.joined` / `member.left` 业务流程在本 story 锚定字段层契约后被引用，本 story 不重写时序图）
- 不修改 `docs/宠物互动App_总体架构设计.md` / `docs/宠物互动App_MVP节点规划与里程碑.md`（这两份是更上层的产品设计，房间业务规则在节点 4 §4.4 已锚定；本 story 只是字段层契约定稿，不触发更上层文档修订）
- 不为 schema 字段写 OpenAPI / AsyncAPI / JSON Schema 形式化定义（旧架构残留；新架构 V1 文档以 markdown 表格 + JSON 示例为唯一真相，参见 V1接口设计 §1）
- 不追加 §10 / §12.3 之外的接口或消息锚定（§10 / §12.3 之外的接口在对应节点 epic 的 §X.1 锚定 —— 如 §11 表情接口走 Story 17.1 / §12.3 `pet.state.changed` 走 Story 14.1 / §12.3 `emoji.received` 走 Story 17.1）
- 不在 V1 文档里写 server 实装细节（如"用 GORM 还是 sqlx"、"事务用 WithTx 包"、"广播是同步还是 fire-and-forget"）—— 这些属 ADR / Go 项目结构文档 / Story 11.3 ~ 11.8 实装层决定，V1 文档**只**写客户端可见的 HTTP request/response + WS message 语义
- 不为 dev-only / test-only 接口（如 `/dev/force-close-room`）的 schema 锚定（节点 4 阶段 epics.md 未规划房间 dev 端点；如未来需要由对应 epic 的 dev 端点 story 内联定义）
- 不写英文版接口文档（项目 communication_language=Chinese；用户 huing7373@gmail.com 单一开发者，无国际化诉求）
- 不在本 story 锚定 close code 段（close code 已在 Story 10.1 §12.1 锚定 4001 / 4002 / 4003 / 4004 / 1000 / 1001 / 1006 / 1011 完整表；本 story 不追加 / 不修改 close code 段）

## Acceptance Criteria

**AC1 — 文档定位与编辑范围**

唯一受编辑的文档：`docs/宠物互动App_V1接口设计.md`（绝对路径 `C:/fork/cat/docs/宠物互动App_V1接口设计.md`）。

- 编辑模式：**就地修改**已有 §10.1 / §10.2 / §10.3 / §10.4 / §10.5 / §12.3 章节的内容（不新增章节、不删除章节、不重排顺序）；§1 末尾追加节点 4 房间业务契约冻结声明（与节点 2 / 节点 3 / 节点 4 协议骨架冻结声明并列，**不**覆盖 / 删除）
- 编辑形式：**保留**现有 markdown 结构（H2 / H3 / H4 标题层级 + ` ```json ` fenced code block + 段落顺序）；只**补全**字段说明表格 + 必填 / 类型 / 长度约束 + 错误码列表 + 服务端逻辑细化 + 节点 4 / 5 / 9 / 10 阶段过渡表
- 文件编码 UTF-8 + LF 行尾（与项目其他 .md 一致）
- 修改后的文档**仍可直接被复制粘贴**作为 client / server 双方实装参照（即所有 JSON 示例可直接 `curl` 出来比对；所有字段说明可直接当作"DTO / Codable struct 字段表"用）
- 不删除任何**已有**字段示例或说明（仅追加 / 补全）；§2.5 全局约定 / §3 错误码表 / §4 / §5 / §6 / §7 / §8 / §9 / §11 / §12.1 / §12.2 / §12.3 `room.snapshot` / `pong` / `error` 等已锚定章节**不动**
- §10.1 现有内容（行 1048-1081）：`POST /rooms` 标题 + 空 request `{}` JSON / 服务端逻辑 3 条 bullet / 返回示例 JSON → **保留 + 补接口元信息 / 字段表 / 错误码**
- §10.2 现有内容（行 1085-1113）：`GET /rooms/current` 标题 + 返回示例两版（已加入 / 未加入）→ **保留 + 补接口元信息 / 字段表**
- §10.3 现有内容（行 1117-1160）：`GET /rooms/{roomId}` 标题 + 返回示例 JSON（含 `members[].pet.currentState=2` 的真实示例）→ **保留 + 补接口元信息 / 字段表 / 错误码 / 节点 4 / 5 / 9 / 10 五阶段过渡表**
- §10.4 现有内容（行 1164-1193）：`POST /rooms/{roomId}/join` 标题 + 空 request `{}` JSON / 服务端逻辑 4 条 bullet / 返回示例 JSON → **保留 + 补接口元信息 / 字段表 / 错误码**
- §10.5 现有内容（行 1197-1225）：`POST /rooms/{roomId}/leave` 标题 + 空 request `{}` JSON / 服务端逻辑 3 条 bullet / 返回示例 JSON → **保留 + 补接口元信息 / 字段表 / 错误码**
- §12.3 现有内容：`### 成员加入`（行 1593-1605）/ `### 成员离开`（行 1607-1617）两个 H3 小节 + JSON 示例 → **保留示例 + 前置 `**触发**` / `**字段**` 表 / `**关键约束**`**；§12.3 末尾"业务消息延后锚定"引用块（行 1708-1714）→ **保留 + 把 `member.joined` / `member.left` 状态从"待 Story 11.1 锚定"升级为"已由 Story 11.1 锚定"**

**AC2 — §10.1 POST /rooms schema 锚定**

V1 文档 §10.1 编辑后必须包含以下完整内容：

**§10.1.1 接口元信息**（**新增** H4 块，紧接 `### POST /api/v1/rooms` 之后）：

```markdown
#### 接口元信息

| 字段 | 值 |
|---|---|
| HTTP Method | POST |
| Path | /api/v1/rooms |
| 认证 | **需要** Bearer token（auth 中间件） |
| 限频 | 默认（按 Story 4.5 rate_limit 默认值 60 次/分；房间创建是低频操作，无需特殊限频） |
| 幂等 | **非幂等**（每次调用都创建一个新房间；用户已在房间中再调将返回 6003）；**不**接受 `idempotencyKey` |
| 节点 | 节点 4（Epic 11 落地，Epic 12 客户端集成） |
```

**§10.1.2 请求体**（**就地扩充**现有 `#### 请求体` 小节：保留现有空对象 `{}` JSON 示例 + 前置说明）：

```markdown
#### 请求体

空对象（`{}`）—— 当前用户身份从 `Authorization: Bearer <token>` 解析，房间属性（`max_members=4` / `status=1`）由 server 端写死，client 不传任何业务字段。

JSON 示例：

\`\`\`json
{}
\`\`\`

> 注：未来若引入"自定义房间名 / 私有房间标识"等扩展字段，将在新 epic 的 §X.1 story 锚定（不在本节点 4 范围内）。
```

**§10.1.3 服务端逻辑**（**就地扩充**现有 `#### 服务端逻辑` 小节：保留现有 3 条 bullet + 补充事务步骤顺序与错误码触发点）：

```markdown
#### 服务端逻辑

事务内严格按顺序执行（详见 Story 11.3 实装）：

1. 查 `users.current_room_id`，**非 null** → 立即返回 **6003 用户已在房间中**（不开事务）
2. 插入 `rooms`（`creator_user_id = 当前 user`, `status = 1`, `max_members = 4`），拿到 `room_id`
3. 插入 `room_members`（`room_id`, `user_id = 当前 user`）
4. 更新 `users.current_room_id = room_id`
5. 提交事务
6. 返回 `data.room` 见 §10.1.4 响应体字段表

**事务边界规则**：步骤 2 ~ 4 必须在同一 MySQL 事务中（参见数据库设计 §8.6 房间事务边界），任一步失败 → 全部回滚 → 返回 1009 服务繁忙。

**WS 广播**：本接口**不**触发 `member.joined` 广播 —— 房间刚创建、当前用户是房间内**唯一**成员，无其他在线成员需要被通知；`member.joined` 仅在 `POST /rooms/{roomId}/join` 成功后由 Story 11.8 触发（详见 §12.3 `### 成员加入` 触发条件段）。
```

**§10.1.4 响应体**（**就地扩充**现有 `#### 返回示例` 小节：保留现有 JSON 示例 + 前置字段表）：

```markdown
#### 响应体

成功（code = 0）：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `data.room.id` | string | 必填 | 房间主键（BIGINT 字符串化下发，遵循 §2.5 全局约定） |
| `data.room.creatorUserId` | string | 必填 | 创建者 user 主键（BIGINT 字符串化）；来自 `rooms.creator_user_id` |
| `data.room.maxMembers` | number (int) | 必填 | 房间容量上限；节点 4 阶段固定 `4`（来源数据库设计 §5.13 `max_members TINYINT UNSIGNED NOT NULL DEFAULT 4`） |
| `data.room.memberCount` | number (int) | 必填 | 当前成员数；本接口创建后必为 `1`（自动加入了创建者自己） |
| `data.room.status` | number (int) | 必填 | 房间状态枚举：`1 = active` / `2 = closed`（来源数据库设计 §6.12 `rooms.status`）；本接口创建后必为 `1` |

JSON 示例：

\`\`\`json
{
  "code": 0,
  "message": "ok",
  "data": {
    "room": {
      "id": "3001",
      "creatorUserId": "1001",
      "maxMembers": 4,
      "memberCount": 1,
      "status": 1
    }
  },
  "requestId": "req_xxx"
}
\`\`\`
```

**§10.1.5 可能的错误码**（**新增** H4 块）：

```markdown
#### 可能的错误码

| code | message | 触发条件 |
|---|---|---|
| 1001 | 未登录 / token 无效 | auth 中间件拦截（无 Authorization 头 / token 非法 / token 过期） |
| 1005 | 操作过于频繁 | rate_limit 中间件拦截（同 IP 每分钟 > 60 次） |
| 6003 | 用户已在房间中 | 服务端逻辑步骤 1：`users.current_room_id` 非 null |
| 1009 | 服务繁忙 | 事务回滚 / DB 异常 / 内部 panic（详见 Story 11.3 实装） |

> **注**：本接口**不**会触发 6001 / 6002 / 6004 / 6005 —— 房间在事务内才被创建，不存在"找不到 / 已满 / 状态异常"场景；6004（用户不在房间中）仅在 `POST /rooms/{roomId}/leave` 出现。
```

**关键约束**：

- §10.1 字段表中所有 BIGINT 主键 / 外键（`id` / `creatorUserId`）严格按 §2.5 全局约定字符串化下发
- `memberCount` 与 `maxMembers` 类型为 `number (int)`，**不**字符串化（数值字段不受 §2.5 BIGINT 字符串化规则约束 —— `memberCount` ≤ `maxMembers` ≤ 4，远低于 `Number.MAX_SAFE_INTEGER`，client `Int` / Swift `Int` 解析无精度风险）
- 6003 是**预检错误**（在事务开启前就返回），不消耗事务资源；这与 6001 / 6002 / 6005 在 join 接口里的"事务内校验"语义不同（详见 §10.4）

**AC3 — §10.2 GET /rooms/current schema 锚定**

V1 文档 §10.2 编辑后必须包含以下完整内容：

**§10.2.1 接口元信息**（**新增** H4 块，紧接 `### GET /api/v1/rooms/current` 之后）：

```markdown
#### 接口元信息

| 字段 | 值 |
|---|---|
| HTTP Method | GET |
| Path | /api/v1/rooms/current |
| 认证 | **需要** Bearer token（auth 中间件） |
| 限频 | 默认（按 Story 4.5 rate_limit 默认值 60 次/分） |
| 幂等 | 幂等（GET，无副作用） |
| 节点 | 节点 4（Epic 11 落地，Epic 12 客户端集成） |
```

**§10.2.2 请求**（**新增** H4 块）：

```markdown
#### 请求

无请求体（GET 方法）；当前用户身份从 `Authorization: Bearer <token>` 解析。
```

**§10.2.3 响应体**（**就地扩充**现有 `#### 返回示例` 小节：保留现有两版 JSON 示例 + 前置字段表）：

```markdown
#### 响应体

成功（code = 0）：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `data.roomId` | string \| null | 必填（值可为 null） | 当前用户所在房间主键（BIGINT 字符串化），即 `users.current_room_id` 当前值；用户不在任何房间时为 `null`（**显式** null，**不**省略 key） |

JSON 示例（用户当前在房间）：

\`\`\`json
{
  "code": 0,
  "message": "ok",
  "data": {
    "roomId": "3001"
  },
  "requestId": "req_xxx"
}
\`\`\`

JSON 示例（用户当前不在任何房间）：

\`\`\`json
{
  "code": 0,
  "message": "ok",
  "data": {
    "roomId": null
  },
  "requestId": "req_xxx"
}
\`\`\`
```

**§10.2.4 可能的错误码**（**新增** H4 块）：

```markdown
#### 可能的错误码

| code | message | 触发条件 |
|---|---|---|
| 1001 | 未登录 / token 无效 | auth 中间件拦截 |
| 1005 | 操作过于频繁 | rate_limit 中间件拦截 |
| 1009 | 服务繁忙 | DB 异常 / 内部 panic |

> **注**：本接口**不**会触发 6001 ~ 6005 —— 用户不在房间是合法场景（返回 `roomId: null` + code = 0），不视为业务错误。
```

**关键约束**：

- 与 `GET /me.user.currentRoomId` **不**等价 —— `GET /me` 中的 `currentRoomId` 是**永久 schema 占位**始终返回 `null`（详见 §4.3 Future Fields 引用块），本接口才是获取真实房间归属的权威路径
- 与 `GET /home.data.room.currentRoomId` 字段语义**等价**（两者都查 `users.current_room_id`），但 `GET /home` 是首页聚合接口（含 user / pet / stepAccount / chest / room 五段），本接口是单字段轻量查询；client 在房间页内用本接口、在首页加载用 `GET /home`（节点 4 由 Story 11.10 真实实装 `room.currentRoomId`）
- 客户端 WS 重连时**不**应优先用本接口拿 roomId（按 §12.1 客户端 roomId 来源规则：`本地持久化最近一次 WS roomId` 优先；本接口可作为冷启场景兜底，但不是首选）

**AC4 — §10.3 GET /rooms/{roomId} schema 锚定**

V1 文档 §10.3 编辑后必须包含以下完整内容：

**§10.3.1 接口元信息**（**新增** H4 块，紧接 `### GET /api/v1/rooms/{roomId}` 之后）：

```markdown
#### 接口元信息

| 字段 | 值 |
|---|---|
| HTTP Method | GET |
| Path | /api/v1/rooms/{roomId} |
| 认证 | **需要** Bearer token（auth 中间件） |
| 限频 | 默认（按 Story 4.5 rate_limit 默认值 60 次/分） |
| 幂等 | 幂等（GET，无副作用） |
| 节点 | 节点 4（Epic 11 落地，Epic 12 客户端集成） |
```

**§10.3.2 请求**（**新增** H4 块）：

```markdown
#### 请求

| 参数位置 | 字段 | 类型 | 必填 | 长度/范围约束 | 说明 |
|---|---|---|---|---|---|
| Path | `roomId` | string | 必填 | 必须是 BIGINT 数字字符串（如 `"3001"`），1 ≤ length ≤ 20 字符 | 目标房间主键；server 内部转 `uint64` |

> **注（ACL — r7 锁定）**：本接口**强制要求** caller 是该房间的当前成员（即 `users.current_room_id == path roomId`），否则返回 **6004 用户不在房间中**（含 `current_room_id == NULL` / `current_room_id != path roomId` 两种场景）。**rationale**：响应体 `members[].nickname` / `avatarUrl` / `pet.petId` / `pet.currentState` 是其他用户的隐私字段，且 `rooms.id` 是 BIGINT auto_increment 顺序号（数据库设计.md §6.6），任何认证用户都能查任意 roomId 会形成"枚举 roomId → 抓全站房间成员关系"的隐私 / security 攻击面。访问控制模型与 §10.5 leave 接口步骤 1 一致（caller 必须是该房间成员）。
```

**§10.3.2 服务端逻辑**（**新增** H4 块）：

```markdown
#### 服务端逻辑

**事务边界**：开 MySQL 事务（隔离级别 = REPEATABLE READ，InnoDB 默认）包以下全部 4 个步骤。**snapshot 隔离 + FOR SHARE 行锁互补**：snapshot 提供事务内部一致性（两次 SELECT 同一时刻状态），FOR SHARE 提供外部一致性（阻止并发 leave 的 DELETE 在本读事务期间提交，确保 HTTP 响应发出时 caller 仍是成员）。详见数据库设计 §8.8 + V1 §10.3。

按顺序执行（全部在同一 REPEATABLE READ 事务内）：

1. 查 `users.current_room_id`，**与请求的 `roomId` 不一致**（含 null）→ 返回 **6004**；通过后追加 `SELECT 1 FROM room_members WHERE room_id = ? AND user_id = caller FOR SHARE`（共享锁锁定 caller 自己的成员行；命中 0 行视同 ACL 失败 → 6004）
2. 查 `rooms WHERE id = ?`，**找不到** → 返回 **6001**（兜底）
3. 查 `room_members WHERE room_id = ?` + JOIN `users` / `pets`（按 `room_members.joined_at ASC` 稳定排序）
4. 提交事务并返回 `data.{room, members}`
```

**§10.3.3 响应体**（**就地扩充**现有 `#### 返回示例` 小节：保留现有 JSON 示例 + 前置字段表）：

```markdown
#### 响应体

成功（code = 0）：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `data.room.id` | string | 必填 | 房间主键（BIGINT 字符串化） |
| `data.room.creatorUserId` | string | 必填 | 创建者 user 主键（BIGINT 字符串化） |
| `data.room.maxMembers` | number (int) | 必填 | 房间容量上限（节点 4 阶段固定 `4`） |
| `data.room.memberCount` | number (int) | 必填 | 当前成员总数；**必须严格等于** `data.members[]` 数组长度（不变量，详见本节末"不变量"小节） |
| `data.room.status` | number (int) | 必填 | 房间状态枚举：`1 = active` / `2 = closed` |
| `data.members` | array | 必填 | 房间全成员列表（**含离线成员**；不区分在线 / 离线）；来自 `room_members WHERE room_id = ?` 全部行 + JOIN `users` / `pets` 聚合 |
| `data.members[].userId` | string | 必填 | 成员 user 主键（BIGINT 字符串化） |
| `data.members[].nickname` | string | 必填 | 成员昵称；来自 `users.nickname`（节点 2 阶段首次创建时 server 写入 `"用户{id}"`，可被未来"修改昵称"功能覆盖） |
| `data.members[].avatarUrl` | string | 必填 | 成员头像 URL；首次创建为空字符串 `""`（不为 null）；空字符串语义 = "暂无头像"，client 渲染时降级为占位头像 |
| `data.members[].pet.petId` | string | 必填 | 成员当前宠物主键（BIGINT 字符串化）；来自 `pets.id`（每个 user 节点 2 阶段唯一 1 只默认猫，详见 Story 4.6 首次初始化事务） |
| `data.members[].pet.currentState` | number (int) | 必填 | 宠物当前状态枚举：`1 = rest` / `2 = walk` / `3 = run`（来源数据库设计 §6.4 `pets.current_state`）；**节点 4 阶段固定返回 `1`**（Epic 14 才真实驱动 motion_state） |
| `data.members[].pet.equips` | array | 必填 | 成员当前装备数组；**节点 4 阶段固定返回 `[]`**（Future Fields，详见本节末"五阶段过渡表"） |

JSON 示例（节点 4 阶段，3 成员房间）：

\`\`\`json
{
  "code": 0,
  "message": "ok",
  "data": {
    "room": {
      "id": "3001",
      "creatorUserId": "1001",
      "maxMembers": 4,
      "memberCount": 3,
      "status": 1
    },
    "members": [
      {
        "userId": "1001",
        "nickname": "用户1001",
        "avatarUrl": "",
        "pet": {
          "petId": "2001",
          "currentState": 1,
          "equips": []
        }
      },
      {
        "userId": "1002",
        "nickname": "用户1002",
        "avatarUrl": "",
        "pet": {
          "petId": "2002",
          "currentState": 1,
          "equips": []
        }
      },
      {
        "userId": "1003",
        "nickname": "用户1003",
        "avatarUrl": "",
        "pet": {
          "petId": "2003",
          "currentState": 1,
          "equips": []
        }
      }
    ]
  },
  "requestId": "req_xxx"
}
\`\`\`
```

**§10.3.4 可能的错误码**（**新增** H4 块）：

```markdown
#### 可能的错误码

| code | message | 触发条件 |
|---|---|---|
| 1001 | 未登录 / token 无效 | auth 中间件拦截 |
| 1002 | 参数错误 | `roomId` 路径参数格式错（非数字 / 长度 > 20 字符） |
| 1005 | 操作过于频繁 | rate_limit 中间件拦截 |
| 6001 | 房间不存在 | 服务端逻辑步骤 2：`rooms WHERE id = ?` 查不到（兜底） |
| 6004 | 用户不在房间中 | 服务端逻辑步骤 1：`users.current_room_id != path roomId`（含 NULL）—— caller 不是该房间成员，**禁止**查看（r7 ACL 锁定） |
| 1009 | 服务繁忙 | DB 异常 / 内部 panic |

> **注**：本接口**不**触发 6002 / 6003 —— 这两个错误码是 join 路径上的语义；6005 在节点 4 阶段**不**用于本接口（即使 `rooms.status = 2 closed` 也允许查询，但前提是 caller 仍是该房间成员）。**6004 触发条件与 §10.5 leave 步骤 1 同源**，统一"caller 不是该房间成员 → 6004"语义。
```

**§10.3.5 节点 4 / 5 / 9 / 10 五阶段过渡表**（**新增** H4 块）：

```markdown
#### `data.members[]` 字段五阶段过渡表

各字段在 MVP 不同节点 / Epic 落地后的下发值演进：

| 字段 | 节点 4 placeholder（Story 10.7） | 节点 4 真实（Story 11.6 / 11.7） | 节点 5 真实（Epic 14） | 节点 9 真实（Epic 26） | 节点 10 真实（Epic 29） |
|---|---|---|---|---|---|
| `userId` | `room_members.user_id` 字符串化 | 同 placeholder | 同 placeholder | 同 placeholder | 同 placeholder |
| `nickname` | `""`（不 JOIN `users`，详见 §12.3 placeholder 说明） | `users.nickname`（GET /rooms/{roomId} 始终走真实路径；WS room.snapshot 由 Story 11.7 SnapshotBuilder 真实实装） | 同节点 4 真实 | 同节点 4 真实 | 同节点 4 真实 |
| `avatarUrl` | **不下发**（§12.3 字段表 placeholder 不含此字段） | `users.avatar_url`（首次创建为 `""`，本接口始终走真实路径） | 同节点 4 真实 | 同节点 4 真实 | 同节点 4 真实 |
| `pet.petId`（仅 `pet ≠ null`） | `pets.id` 字符串化（与节点 4 真实同 —— LEFT JOIN `pets` 后 `pet ≠ null` 分支即 `pets.id` 真实值） | `pets.id` 字符串化 | 同节点 4 真实 | 同节点 4 真实 | 同节点 4 真实 |
| `pet.currentState` | 固定 `1` | 固定 `1` | `pets.current_state`（真实 1/2/3，由 Epic 14 状态机驱动） | 同节点 5 | 同节点 5 |
| `pet.equips` | **不下发**（§12.3 字段表 placeholder 不含此字段） | `[]`（节点 4 阶段 Story 11.6 GET /rooms/{roomId} 固定返回空数组；WS room.snapshot 同样不下发） | 同节点 4 真实 | `user_pet_equips JOIN cosmetic_items` 聚合（Story 26.6 真实回填，**不**含 `renderConfig` 子字段） | 同节点 9 + 加 `renderConfig` 子字段（Story 29.6 真实回填 `pet.equips[].renderConfig`） |

**关键解读**：

1. **GET /rooms/{roomId}（本接口）从节点 4 起就走"真实"路径**（Story 11.6 实装时 JOIN `users` / `pets`）；`pet.currentState` / `pet.equips` 仍按上表节点 4 列返回固定值，但 `nickname` / `avatarUrl` / `pet.petId` 在节点 4 真实路径下就是真实值（与 placeholder 不同）。即"REST 接口本身节点 4 真实，仅 pet 状态字段后续 epic 真实驱动"。
2. **WS `room.snapshot` placeholder（Story 10.7）/ 真实（Story 11.7）路径**与 REST 接口路径独立：placeholder 阶段允许 `nickname` 空字符串（不 JOIN `users`），真实阶段填真实值；`pet` 整体 placeholder 阶段已与真实阶段对齐（LEFT JOIN `pets`，pet-less 下发 `null`，否则 `pet.petId` 为 `pets.id` 真实值），**不**做 placeholder 简化（与本表 `pet.petId` 行 placeholder / 真实两列同值一致）；详见 §12.3 placeholder 字段值来源说明。**Story 10.7 落地实装的过渡兼容**：Story 10.7 当前实装走单表查询（不 JOIN `pets`），所有 member 一律下发 `pet ≠ null` + `petId: ""` —— 这是 r10 之前的契约形态，r14 起本节五阶段表已锁定 Story 10.7 placeholder 的 going-forward 契约为 LEFT JOIN + nullable，但 Story 10.7 已 done 不回工；Story 11.7 真实 SnapshotBuilder 落地时**必须**切换到 LEFT JOIN `pets` + pet-less 下发 `null` 的 going-forward 契约形态。
3. **client merge contract**（§12.3 末尾 client merge contract 引用块）保证：client 通过 GET /rooms/{roomId}（本接口节点 4 真实路径）拿到的真实 nickname / petId / avatarUrl，在收到 `room.snapshot` placeholder 的空字符串字段时**保留**真实值，不被空串覆盖。这条契约对本接口的节点 4 真实路径同样适用。
4. **`pet.equips` 节点 4 阶段固定 `[]`**：与 §5.1 GET /home 的 `pet.equips` 节点 4 阶段语义一致（详见 §5.1 字段表 + Future Fields）；本接口 `members[].pet.equips` 与 §5.1 单 user `pet.equips` 是两个独立字段路径，但占位语义相同。
```

**§10.3.6 不变量**（**新增** H4 块）：

```markdown
#### 不变量（response 内部一致性）

- `data.room.memberCount` **必须严格等于** `data.members[]` 数组长度
- `data.members[]` 必须包含**全部** `room_members WHERE room_id = ?` 行（**含离线成员**）；service 实装层**禁止**做"在线态过滤"（与 §12.3 `room.snapshot` 不变量一致：snapshot / GET /rooms 都是 full roster view，不是 online-only view）
- `data.members[]` 顺序由 server 决定（建议按 `room_members.joined_at ASC` 稳定排序，便于 client 渲染顺序稳定）；client 解析层**不**应假设特定顺序（除"稳定"外的语义）
- `data.room.status` 与 `data.members[]` 的关系：`status = 2 closed` 时 `members[]` 必为 `[]`（因为退出房间事务在最后一人离开时同时删除最后一行 `room_members` + 更新 `rooms.status = 2`，详见 §10.5 服务端逻辑）；`status = 1 active` 时 `members[]` 必非空（房间至少有 1 个成员，否则按业务规则 status 应已变 closed）
```

**关键约束**：

- §10.3 字段表中所有 BIGINT 主键 / 外键（`id` / `creatorUserId` / `userId` / `petId`）严格按 §2.5 全局约定字符串化下发
- `data.members[].avatarUrl` 必须存在且为 string（可空字符串 `""`，**不**为 null）；与 §4.1 / §4.3 / §5.1 中 `user.avatarUrl` 处理一致
- `data.members[].pet.equips` 节点 4 阶段必须为 `[]`（空数组，**不**省略 key、**不**为 null）；client 解析层按 `[<EquipDTO>]` 解析，节点 4 阶段数组永远为空，节点 9 由 Epic 26 真实回填非空数组
- 节点 4 阶段**不**下发 `members[].pet.equips[].renderConfig`（Future Fields，节点 10 由 Epic 29 / Story 29.6 加；本字段表**不**列入）

**AC5 — §10.4 POST /rooms/{roomId}/join schema 锚定**

V1 文档 §10.4 编辑后必须包含以下完整内容：

**§10.4.1 接口元信息**（**新增** H4 块）：

```markdown
#### 接口元信息

| 字段 | 值 |
|---|---|
| HTTP Method | POST |
| Path | /api/v1/rooms/{roomId}/join |
| 认证 | **需要** Bearer token（auth 中间件） |
| 限频 | 默认（按 Story 4.5 rate_limit 默认值 60 次/分） |
| 幂等 | **非幂等**（同一用户重复 join 自己已在的房间 → 返回 6003；不能用 `idempotencyKey` 跳过校验） |
| 节点 | 节点 4（Epic 11 落地，Epic 12 客户端集成） |
```

**§10.4.2 请求**（**就地扩充**现有 `#### 请求体` 小节）：

```markdown
#### 请求

| 参数位置 | 字段 | 类型 | 必填 | 长度/范围约束 | 说明 |
|---|---|---|---|---|---|
| Path | `roomId` | string | 必填 | 必须是 BIGINT 数字字符串（如 `"3001"`），1 ≤ length ≤ 20 字符 | 目标房间主键 |
| Body | - | object | 必填 | - | 空对象 `{}`；当前用户身份从 token 解析 |

JSON 示例（请求体）：

\`\`\`json
{}
\`\`\`
```

**§10.4.3 服务端逻辑**（**就地扩充**现有 `#### 服务端逻辑` 小节，补充事务步骤顺序与错误码触发点）：

```markdown
#### 服务端逻辑

事务内严格按顺序执行（详见 Story 11.4 实装）：

1. 查 `users.current_room_id`，**非 null** → 立即返回 **6003 用户已在房间中**（不开事务）；**特例**：若 `current_room_id == 当前请求的 roomId` 也返回 6003
2. 开事务，对 `rooms` 行加排他锁：`SELECT ... FROM rooms WHERE id = ? FOR UPDATE`（**双重职责**：① 同房间并发 join 串行化；② 与 §10.5 leave 跨事务串行化，解决 r9 P1#2 race）；**找不到** → 回滚 + 返回 **6001 房间不存在**
3. 查到的 `rooms.status != 1` → 回滚 + 返回 **6005 房间状态异常**
4. 查 `room_members WHERE room_id = ?` 数量，`>= 4` → 回滚 + 返回 **6002 房间已满**
5. 插入 `room_members`；如果遇到 DB UNIQUE(`user_id`) 兜底冲突 → 回滚 + 返回 **6003**
6. 更新 `users.current_room_id = roomId`
7. 提交事务
8. **事务成功提交后**触发 WS 广播 `member.joined`（payload 见 §12.3；fire-and-forget）
9. 返回 `data.{roomId, joined: true}`

**事务边界规则**：步骤 2 ~ 6 必须在同一 MySQL 事务中（参见数据库设计 §8.6 加入房间事务边界）；步骤 8 在事务**外**触发（fire-and-forget）。

**并发保护**：步骤 2 的 `SELECT rooms ... FOR UPDATE` 行锁是关键。详见 Story 11.4 单测 / 11.9 集成测试并发场景（含 cross-tx leave-then-join race 用例 + 5-抢-1-空位用例）。
```

**§10.4.4 响应体**（**就地扩充**现有 `#### 返回示例` 小节）：

```markdown
#### 响应体

成功（code = 0）：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `data.roomId` | string | 必填 | 加入的房间主键（BIGINT 字符串化）；与请求 path 中 `roomId` 一致，回带方便 client 校验 |
| `data.joined` | boolean | 必填 | 固定 `true`（成功路径必返）；**不**为 `false` —— 失败时返回错误码而非 `joined: false` |

JSON 示例：

\`\`\`json
{
  "code": 0,
  "message": "ok",
  "data": {
    "roomId": "3001",
    "joined": true
  },
  "requestId": "req_xxx"
}
\`\`\`
```

**§10.4.5 可能的错误码**（**新增** H4 块）：

```markdown
#### 可能的错误码

| code | message | 触发条件 |
|---|---|---|
| 1001 | 未登录 / token 无效 | auth 中间件拦截 |
| 1002 | 参数错误 | `roomId` 路径参数格式错（非数字 / 长度 > 20 字符） |
| 1005 | 操作过于频繁 | rate_limit 中间件拦截 |
| 6001 | 房间不存在 | 服务端逻辑步骤 2：`rooms WHERE id = ?` 找不到 |
| 6002 | 房间已满 | 服务端逻辑步骤 4：`room_members` 当前数 `>= 4` |
| 6003 | 用户已在房间中 | 服务端逻辑步骤 1（预检）或步骤 5（DB UNIQUE 兜底）：用户已在任意房间（含目标房间） |
| 6005 | 房间状态异常 | 服务端逻辑步骤 3：`rooms.status != 1`（如已 closed） |
| 1009 | 服务繁忙 | 事务回滚 / DB 异常 / 内部 panic |

> **注**：本接口**不**触发 6004（用户不在房间中）—— 6004 仅用于 leave 接口。
```

**关键约束**：

- 错误码触发顺序严格按服务端逻辑步骤：步骤 1 → 6003（预检）；步骤 2 → 6001；步骤 3 → 6005；步骤 4 → 6002；步骤 5 → 6003（兜底）。**不**允许实装层重排顺序（如先查容量再查 status，会让"closed 房间满员"场景错误返回 6002 而非 6005）
- 6003 在两条路径下触发（步骤 1 预检 + 步骤 5 DB UNIQUE 兜底），但 client **不**应区分这两种场景（都是"用户已在某房间"，UX 处理一致）
- `data.joined` 固定 `true`：成功路径必返；失败路径返回错误码 + 不返回 `joined` 字段（按 §2.4 通用响应结构，错误时 `data` 为空对象 / 不含业务字段）

**AC6 — §10.5 POST /rooms/{roomId}/leave schema 锚定**

V1 文档 §10.5 编辑后必须包含以下完整内容：

**§10.5.1 接口元信息**（**新增** H4 块）：

```markdown
#### 接口元信息

| 字段 | 值 |
|---|---|
| HTTP Method | POST |
| Path | /api/v1/rooms/{roomId}/leave |
| 认证 | **需要** Bearer token（auth 中间件） |
| 限频 | 默认（按 Story 4.5 rate_limit 默认值 60 次/分） |
| 幂等 | **非幂等**（同一用户重复 leave 同一房间 → 第二次返回 6004；client 应在收到 200 后清本地状态，不要重试） |
| 节点 | 节点 4（Epic 11 落地，Epic 12 客户端集成） |
```

**§10.5.2 请求**（**就地扩充**现有 `#### 请求体` 小节）：

```markdown
#### 请求

| 参数位置 | 字段 | 类型 | 必填 | 长度/范围约束 | 说明 |
|---|---|---|---|---|---|
| Path | `roomId` | string | 必填 | 必须是 BIGINT 数字字符串，1 ≤ length ≤ 20 字符 | 目标房间主键 |
| Body | - | object | 必填 | - | 空对象 `{}` |

JSON 示例（请求体）：

\`\`\`json
{}
\`\`\`
```

**§10.5.3 服务端逻辑**（**就地扩充**现有 `#### 服务端逻辑` 小节）：

```markdown
#### 服务端逻辑

事务内严格按顺序执行（详见 Story 11.5 实装；与已落地的 V1 §10.5 步骤序列 zip 对齐）：

1. 查 `users.current_room_id`，**与请求的 `roomId` 不一致**（含 null）→ 立即返回 **6004**（不开事务）
2. 开事务，对 `rooms` 行加排他锁：`SELECT ... FROM rooms WHERE id = ? FOR UPDATE`（**与并发 §10.4 join 串行化**，解决 r9 P1#2 race；找不到 rooms 行视为内部状态不一致 → 1009）
3. `DELETE FROM room_members WHERE room_id = ? AND user_id = ?` + 检查 `RowsAffected == 0` → 回滚 + 返回 **6004**（同一 user 并发两次 leave 输家兜底，r4 已锁定）
4. 更新 `users.current_room_id = NULL`
5. 查 `room_members WHERE room_id = ?` 剩余数量；若 `== 0` → 更新 `rooms.status = 2 closed`
6. 提交事务
7. 关闭 leaver 自己的 WS Session（close 4007 + SessionManager unregister；fire-and-forget；**必须在 step 8 broadcast 之前**，r13 锁定 —— 该顺序保证 BroadcastToRoom 调用 ListSessionsByRoomID 时返回列表已不含 leaver Session，无需 primitive 提供 `excludeUserID` 参数）
8. **事务成功提交后**触发 WS 广播 `member.left`（payload 见 §12.3；fire-and-forget）；fanout 列表自动跳过 leaver（步骤 7 已 unregister）；空房间 fanout no-op
9. 返回 `data.{roomId, left: true}`

**事务边界规则**：步骤 2 ~ 5 必须在同一 MySQL 事务中（参见数据库设计 §8.7 退出房间事务边界）；步骤 7/8 在事务**外**触发（fire-and-forget）。**步骤 7（close 4007 + unregister）必须先于步骤 8（broadcast）**（r13 锁定，与 V1 §10.5 落地一致）—— 顺序由"BroadcastToRoom primitive 不带 excludeUserID 参数 → fanout 给 ListSessionsByRoomID 全部 session"约束推导，先 close + unregister 让 leaver Session 物理上不在 fanout 列表，无需扩 primitive 即满足"leaver 不收自己 member.left"语义。**步骤 2 `FOR UPDATE` 与步骤 3 `RowsAffected == 0` 兜底正交不可互替**：前者解决跨事务 leave-vs-join 状态 drift，后者解决同 user 并发 leave 输家走完后续步骤（详见 V1 §10.5 关键约束）。

**WS 断线场景与本接口的关系**（r3 锁定语义；详见已落地的 V1接口设计.md §10.5）：心跳超时（→ close 4005）/ client 主动 close / app 关闭 / TCP 1006 等任何 WS 层断开**都不**走本接口的事务路径 —— 它们仅清 ephemeral 层（SessionManager / Redis presence），不删 `room_members`、不改 `users.current_room_id`、不广播 `member.left`。**只有**本接口（HTTP leave）+ 步骤 7 close 4007 是真正的"离开房间"路径；这条钦定让 §12.1 4005 retryable 语义自洽。
```

**§10.5.4 响应体**（**就地扩充**现有 `#### 返回示例` 小节）：

```markdown
#### 响应体

成功（code = 0）：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `data.roomId` | string | 必填 | 离开的房间主键（BIGINT 字符串化）；与请求 path 中 `roomId` 一致 |
| `data.left` | boolean | 必填 | 固定 `true`（成功路径必返） |

JSON 示例：

\`\`\`json
{
  "code": 0,
  "message": "ok",
  "data": {
    "roomId": "3001",
    "left": true
  },
  "requestId": "req_xxx"
}
\`\`\`

> **注**：响应中**不**包含"房间是否已 closed"字段 —— client 不直接感知房间状态变化，仅 `data.left = true` 表示当前 user 已离开；房间状态变化是 server 内部副作用，**不**对外暴露（避免 client 围绕"我是否是最后一人"做特殊 UX 分支）。
```

**§10.5.5 可能的错误码**（**新增** H4 块）：

```markdown
#### 可能的错误码

| code | message | 触发条件 |
|---|---|---|
| 1001 | 未登录 / token 无效 | auth 中间件拦截 |
| 1002 | 参数错误 | `roomId` 路径参数格式错（非数字 / 长度 > 20 字符） |
| 1005 | 操作过于频繁 | rate_limit 中间件拦截 |
| 6004 | 用户不在房间中 | 服务端逻辑步骤 1：`users.current_room_id != roomId`（含 null） |
| 1009 | 服务繁忙 | 事务回滚 / DB 异常 / 内部 panic |

> **注**：本接口**不**触发 6001 / 6002 / 6003 / 6005 —— leave 仅校验"用户与房间归属"，不校验房间是否存在（即使 `rooms` 已被某种方式删除，只要 `users.current_room_id` 与 path `roomId` 一致仍允许 leave；6001 不触发因为 leave 不查 `rooms` 表存在性，仅查 `users.current_room_id`）。
```

**关键约束**：

- 6004 触发条件包含两种场景：(a) `users.current_room_id == NULL`（用户当前不在任何房间）+ (b) `users.current_room_id != path roomId`（用户在其他房间）—— 都返回 6004，client UX 一致处理（无法离开 = 用户没在该房间）
- "最后一人离开 → 房间 closed"是 server 端**单向**状态变化：closed 房间无法被重新激活（节点 4 阶段无"重启房间"接口）；这条规则确保 `rooms.status` 严格单调（1 → 2，无回退路径），简化 server 状态机
- `data.left` 固定 `true`：与 §10.4 `data.joined` 设计对称，避免引入"left: false" 模糊语义

**AC7 — §12.3 业务消息 `### 成员加入` (member.joined) schema 锚定**

V1 文档 §12.3 `### 成员加入` 小节编辑后必须包含以下完整内容：

**§12.3 `### 成员加入` 改造**（**就地扩充**现有 H3 小节：保留现有 JSON 示例 + 前置 `**触发**` / `**字段**` / `**关键约束**`）：

```markdown
### 成员加入（member.joined）

**触发**：

- `POST /api/v1/rooms/{roomId}/join` 加入房间事务**成功提交后**，server 调用 `BroadcastToRoom(roomID, {type: "member.joined", payload: {userId, nickname}})` 广播给该房间内所有**其他**在线成员（不发给加入者自己 —— 加入者自己应收 HTTP 响应即知自己已加入，且即将收到 `room.snapshot` 含自己）
- 节点 4 阶段**仅** Story 11.8 一处触发；任何 WS 层事件（含握手、心跳超时、断开重连）**都不**触发 `member.joined` —— `member.joined` 与 `room_members` 行的"新增"语义严格 1:1 对应，新增持久层行**仅**通过 HTTP join 接口完成；后续 epic 不在本路径加新触发条件（如 Story 14.x 状态广播走独立 `pet.state.changed`）

**字段**：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `type` | string | 必填 | 固定值 `"member.joined"` |
| `requestId` | string | 必填 | 固定 `""`（主动推送类消息，遵循 §12.3 通用信封） |
| `payload.userId` | string | 必填 | 加入的成员 user 主键（BIGINT 字符串化）；来自加入事务的当前 user |
| `payload.nickname` | string | 必填 | 加入的成员昵称；来自 `users.nickname`；**必非空字符串**（节点 2 阶段首次创建时 server 写入 `"用户{id}"`，必有真实值；不存在 placeholder 阶段空字符串场景） |
| `ts` | number (int64) | 必填 | 服务端发送时间戳（ms） |

JSON 示例：

\`\`\`json
{
  "type": "member.joined",
  "requestId": "",
  "payload": {
    "userId": "1002",
    "nickname": "用户1002"
  },
  "ts": 1776920345000
}
\`\`\`

**关键约束**：

- `payload.nickname` **必非空字符串** —— 与 `room.snapshot.payload.members[].nickname` 在 placeholder 阶段允许空字符串的语义**不同**：`member.joined` 在加入事务**成功提交后**触发，server 必有真实 nickname（`users` 表已被读取过用于事务决策），无 placeholder 阶段；client 解析层应按 `nickname != ""` 假设处理（节点 4 阶段所有 `member.joined` 消息字段值都是真实数据）
- 广播范围：**仅**该房间内当前在线的其他 Session（不含加入者自己）—— 由 BroadcastToRoom primitive（Story 10.5）实装的 fanout 路径决定；加入者自己 = 当前 HTTP 请求方，自己收 HTTP 200 响应已知道结果，不需要再收一份 WS 通知
- `payload.userId` 与 `payload.nickname` 都必填（**禁止** payload 为 `{}` 或仅 `userId`）—— client UX 需要昵称展示"X 加入了房间"消息，userId 是身份标识不是展示字段
- 不下发 `payload.avatarUrl` / `payload.pet.*` 等丰富字段：`member.joined` 是**轻量加入事件**，不包房间快照；client 收到 `member.joined` 后**应**走 §12.3 client merge contract 字段级 merge：(a) snapshot 中已存在该 userId entry → 字段级覆盖 nickname（如果新值非空）；(b) snapshot 中不存在该 userId entry → 新增 entry 仅含 userId / nickname，其他字段（如 `pet.petId` / `pet.currentState`）保留为空字符串 / 默认值，等下次 `room.snapshot` 全量推送时由 server 真实驱动 enrich
- 节点 4 阶段加入者**不**主动收到自己的 `member.joined`：server 实装上应从 fanout 列表中排除加入者自己的 Session，client 解析层不需要做"自己 != 自己"的过滤（防御性编程层面 client 仍**应**对 `payload.userId == 当前 user.id` 走 noop 安全路径）
```

**AC8 — §12.3 业务消息 `### 成员离开` (member.left) schema 锚定**

V1 文档 §12.3 `### 成员离开` 小节编辑后必须包含以下完整内容：

**§12.3 `### 成员离开` 改造**（**就地扩充**现有 H3 小节：保留现有 JSON 示例 + 前置 `**触发**` / `**字段**` / `**关键约束**`）：

```markdown
### 成员离开（member.left）

**触发**：

- `POST /api/v1/rooms/{roomId}/leave` 退出房间事务**成功提交后**，server 调用 `BroadcastToRoom(roomID, {type: "member.left", payload: {userId}})` 广播给该房间内所有**其他**在线成员（不发给离开者自己 —— 离开者已收 HTTP 响应）
- **唯一触发条件**就是上一条 HTTP leave 路径（r3 锁定）；任何 WS 层断开（含心跳超时 → close 4005、client 主动 close、app 关闭 / 切后台、TCP 1006 异常断开）**都不**触发 `member.left` —— WS 断开仅清 ephemeral 层（SessionManager + Redis presence），不改 `room_members` / `users.current_room_id`，不广播 `member.left`；这条钦定与 §12.1 close code 4005 retryable 语义自洽（行不删 → reconnect 握手时校验通过 → 用户保留座位）

**字段**：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `type` | string | 必填 | 固定值 `"member.left"` |
| `requestId` | string | 必填 | 固定 `""`（主动推送类消息，遵循 §12.3 通用信封） |
| `payload.userId` | string | 必填 | 离开的成员 user 主键（BIGINT 字符串化）；来自退出事务的 user 或心跳超时钩子捕获的 Session.userID |
| `ts` | number (int64) | 必填 | 服务端发送时间戳（ms） |

JSON 示例：

\`\`\`json
{
  "type": "member.left",
  "requestId": "",
  "payload": {
    "userId": "1002"
  },
  "ts": 1776920345000
}
\`\`\`

**关键约束**：

- `payload` 字段**精简**为仅 `userId`（与 `member.joined` 含 `nickname` 不同）—— 离开事件 client UX 不需要显示昵称（"X 离开了房间"中的 X 可由 client 从已有 roster 查到 nickname；即使没查到，UX 文案降级为"有人离开"也可接受），减少 server 加载压力
- 广播范围：**仅**该房间内当前在线的其他 Session（不含离开者自己）；离开者已收 HTTP 200 响应作为 leave authoritative signal；§10.5 步骤 7（close 4007 + SessionManager unregister）在步骤 8 broadcast 之前执行（r13 锁定），fanout 时 leaver Session 物理上已不在 ListSessionsByRoomID 列表中，**无需** BroadcastToRoom primitive 提供 `excludeUserID` 参数
- client 解析层收到 `member.left` 时**应**按 §12.3 client merge contract 集合层规则：从 client roster 中**移除** `payload.userId` 对应 entry（这是 authoritative 的离开信号，与 snapshot 集合层 authoritative 一致）；client **不**应等下一次 `room.snapshot` 才更新 roster
- 节点 4 阶段离开者**不**主动收到自己的 `member.left`：server 实装上 leaver Session 在 §10.5 步骤 7（close 4007 + SessionManager unregister）之后已不在列表，**步骤 8** broadcast 时 `BroadcastToRoom` 调 `ListSessionsByRoomID` 返回列表自然不含 leaver Session，fanout 物理上跳过离开者 —— 该顺序（step 7 close 在 step 8 broadcast 之前，r13 锁定）是契约层钦定的实装路径，**无需** BroadcastToRoom primitive 提供 `excludeUserID` 参数；client 解析层防御性走 `payload.userId == 当前 user.id` 的 noop 安全路径
- **触发不重复**：server 实装层应保证 `member.left` 广播严格 1:1 对应"`room_members` 行被删除"事件 —— 由于唯一删行路径是 HTTP leave 事务，同一 user 在同一 leave 事件中**只触发一次** `member.left` 广播；任何 WS 断开（含心跳超时）**禁止**走"被动 leave"路径删 row + 广播 `member.left`
```

**AC9 — §12.3 末尾 "业务消息延后锚定" 引用块状态升级**

V1 文档 §12.3 末尾"业务消息延后锚定"引用块（行 1708-1714）改造：

```markdown
> **业务消息延后锚定**：上文 `### 成员加入` / `### 成员离开` / `### 收到表情广播` 三个小节中，**前两个**（`member.joined` / `member.left`）已由 **Story 11.1**（Epic 11 房间业务契约，节点 4 中段，commit hash `<TBD-本 story 完成时回填>`）正式锚定字段表 + 触发条件 + 关键约束 —— 自此**接入** Epic 11 / 节点 4 server / client active message set。`### 收到表情广播`（`emoji.received`）保持"草稿示例"状态，待 Story 17.1 锚定。
>
> 各消息字段层契约的正式锚定 epic 如下（**已升级状态以粗体标注**）：
>
> - `member.joined` / `member.left` → **Story 11.1（已锚定）**（Epic 11 房间业务契约，节点 4 中段）
> - `pet.state.changed` → **Story 14.1**（Epic 14 宠物状态同步契约，节点 5）—— **注意**：现有 §12.3 草稿中**不存在** `pet.state.changed` 消息示例（仅在 epics.md 业务消息列表里出现），Story 14.1 落地时**需新增**该小节
> - `emoji.received` → **Story 17.1**（Epic 17 表情广播契约，节点 6）
>
> **server / client active message set 升级**（自 Story 11.1 起生效，覆盖 Story 10.1 r10 钉死的"Epic 10 阶段冻结"边界）：
>
> - **server → client active message set**：`room.snapshot` / `pong` / `error`（Epic 10 锚定）+ **`member.joined` / `member.left`（本 story 锚定）**
> - **client → server active message set**：`ping`（Epic 10 锚定）—— 节点 4 阶段 client 不主动发新业务消息（`emoji.send` 由 Story 17.1 锚定加入，非节点 4）
>
> 客户端 Story 12.3 / 12.4 解析层在节点 4 阶段**应**对未识别的 `type` 值（如收到不该到达的 `emoji.received` / `pet.state.changed`）走"安全忽略 + log warn"路径，**不**因未识别消息 close 连接 / crash app（Story 10.1 既有规则，本 story 沿用）。
```

**AC10 — §1 节点 4 房间业务契约冻结声明**

V1 文档 §1 末尾**追加**新冻结声明（与节点 2 / 节点 3 / 节点 4 协议骨架冻结声明并列，**不**覆盖 / 删除既有声明）：

```markdown
- 自 2026-05-XX（Story 11.1 完成日，对应 git commit hash 见 commit message）起，§10.1（POST /rooms）/ §10.2（GET /rooms/current）/ §10.3（GET /rooms/{roomId}）/ §10.4（POST /rooms/{roomId}/join）/ §10.5（POST /rooms/{roomId}/leave）五个节点 4 房间 REST 接口的 schema + §12.3 `### 成员加入`（`member.joined`）/ `### 成员离开`（`member.left`）两个节点 4 业务 WS 消息的字段表进入**冻结**状态。
- 任何字段名 / 字段类型 / 错误码（6001 ~ 6005）触发条件 / 业务 WS 消息 payload 字段的修改都必须：
  1. 触发 iOS Epic 12 重新评审（影响 Story 12.3 / 12.4 / 12.7 所有房间 client / WS 集成 story）
  2. 触发 server Epic 11 已完成 story 的回归（影响 Story 11.3 ~ 11.10 已落地的 handler / service / 广播路径）
  3. 触发后续业务 Epic 14 / 17 的契约 story（14.1 / 17.1）回归（业务消息基于本骨架扩展）
  4. 在本 story 文件 + epics.md 同步标注变更原因 + 影响范围
- `rooms.max_members` 默认值 **4** 属契约一部分：**prod 部署必须使用默认值**，不允许通过 schema migration / 配置覆盖 —— 否则不同 prod 实例会在不同容量上拒绝 join，重新引入跨端契约漂移；**dev / test 环境**可通过 fixture 覆盖默认值（仅用于单测 / 调试 / fixture），**不**视为契约变更；**修改默认值本身**视为契约变更走完整冻结流程。
- `rooms.status` 枚举（`1 = active` / `2 = closed`）属契约一部分；新增枚举值（如 `3 = paused`）视为契约变更。
- Future Fields 标记的字段（`members[].pet.equips` 节点 9 落地 / `members[].pet.equips[].renderConfig` 节点 10 落地 / `members[].pet.currentState` 节点 5 真实驱动）**不**视为契约变更，由对应节点 epic 自然激活；具体过渡见 §10.3.5 五阶段过渡表。
- WS 业务消息（`pet.state.changed` / `emoji.send` / `emoji.received` 等）的字段层契约**不**在节点 4 房间业务冻结范围内，由对应 Epic 的 §X.1 story（Story 14.1 / 17.1）独立锚定 + 独立冻结。
```

**关键约束**：

- 冻结声明的格式与既有节点 2 / 3 / 4 协议骨架冻结声明保持一致（4 条主体 + 配置默认值钳制 + Future Fields 引用 + 业务消息延后锚定引用 + 模式：5 段式）
- "2026-05-XX" 是占位符，**实际填写值**为本 story dev-story 完成提交日（与 4.1 / 7.1 / 10.1 同模式）
- "git commit hash" 留作 review 阶段回填占位（dev-story 阶段提交后，commit hash 在 commit message 中可见，无需在 V1 文档内显式写死 —— 既有 4.1 / 7.1 / 10.1 同处理）

**AC11 — Story 文件 Status 流转 + sprint-status.yaml 更新**

- 本 story 文件 `_bmad-output/implementation-artifacts/11-1-接口契约最终化.md` 在 create-story 阶段 Status 为 `ready-for-dev`
- dev-story 完成后 Status 流转为 `in-progress` → `review` → `done`（sprint-status.yaml 同步更新）
- sprint-status.yaml 中 `11-1-接口契约最终化` 的状态由 create-story workflow 自动从 `backlog` 升级为 `ready-for-dev`；epic-11 状态同步从 `backlog` 升级为 `in-progress`（首个 story 创建触发 —— epic-loop 文档已说明这是预期行为）

**AC12 — 完成判定与 dev / review 关注点**

完成本 story 的判定标准（dev-story 阶段）：

- AC1 ~ AC10 全部落地到 `docs/宠物互动App_V1接口设计.md`
- AC10 §1 末尾冻结声明已追加（与节点 2 / 3 / 4 协议骨架声明并列）
- 修改后的 V1 文档**不**含任何字段类型 / 字段名 / 错误码与现有 Story 4.1 / 7.1 / 10.1 锚定章节冲突（如 §2.5 BIGINT 字符串化 / §3 错误码 6001 ~ 6005 沿用）
- 修改后的 V1 文档**不**修改其他 6 份 `docs/宠物互动App_*.md`（数据库设计 / Go 项目结构 / iOS 项目结构 / 时序图 / 总体架构 / MVP 节点规划）
- 单独 git commit（commit message 含 hash 引用占位）
- sprint-status.yaml 中 `11-1-接口契约最终化` 状态为 `done`（review pass 后由 story-done workflow 标记）

review 阶段必须命中的检查（参考 epic-10 retrospective §6 A5："协议契约类 Story 的 review 阶段必须把'文档内部一致性'作为 hunter checklist 一等公民"）：

- §10.1 ~ §10.5 五个 REST 接口字段表与现有 §4 / §5 / §6 / §7 / §8 / §9 / §11 命名风格一致（字段元信息 H4 块格式 / 字段表列顺序 / 错误码表格式）
- §10 字段表与数据库设计 §5.13 / §5.14 字段对应关系正确（`creator_user_id` ↔ `creatorUserId` / `room_id` ↔ `roomId` / `user_id` ↔ `userId` / `current_state` ↔ `currentState` 等 snake_case → camelCase 映射规则一致）
- §10.3 `members[].pet.currentState` 节点 4 阶段固定值与 §12.3 `room.snapshot.payload.members[].pet.currentState` placeholder 阶段固定值一致（都是 `1 = rest`）—— 跨章节一致性
- §10.3.5 五阶段过渡表与 §5.1 GET /home Future Fields 引用块的 `pet.equips` 节点过渡描述一致（节点 9 由 Epic 26 落地 / 节点 10 加 renderConfig 由 Epic 29 落地）
- §12.3 `member.joined` / `member.left` 字段表与 §12.3 通用信封（`type` / `requestId` / `payload` / `ts`）一致
- §12.3 末尾"业务消息延后锚定"引用块状态升级（`member.joined` / `member.left` 从"待锚定"升级为"已锚定"）准确反映 server / client active message set 变化
- AC10 §1 冻结声明的"配置 / 枚举默认值钳制"段（`rooms.max_members = 4` / `rooms.status` 枚举）与 §10 字段表中固定值描述一致

## Tasks / Subtasks

- [x] Task 1: 编辑 §10.1 POST /rooms (AC: AC2)
  - [x] 在 `### POST /api/v1/rooms` 之后追加 §10.1.1 接口元信息 H4 块
  - [x] 扩充 `#### 请求体` 小节（保留空对象 `{}` JSON + 前置说明）
  - [x] 扩充 `#### 服务端逻辑` 小节（保留现有 3 条 bullet + 加事务步骤顺序 + 错误码触发点 + WS 广播说明）
  - [x] 扩充 `#### 返回示例` 小节（保留现有 JSON + 前置字段表）
  - [x] 追加 §10.1.5 可能的错误码 H4 块
- [x] Task 2: 编辑 §10.2 GET /rooms/current (AC: AC3)
  - [x] 在 `### GET /api/v1/rooms/current` 之后追加 §10.2.1 接口元信息 H4 块
  - [x] 追加 §10.2.2 请求 H4 块（无请求体说明）
  - [x] 扩充 `#### 返回示例` 小节（保留两版 JSON + 前置字段表）
  - [x] 追加 §10.2.4 可能的错误码 H4 块
- [x] Task 3: 编辑 §10.3 GET /rooms/{roomId} (AC: AC4)
  - [x] 在 `### GET /api/v1/rooms/{roomId}` 之后追加 §10.3.1 接口元信息 H4 块
  - [x] 追加 §10.3.2 请求 H4 块（path 参数表）
  - [x] 扩充 `#### 返回示例` 小节（保留现有 JSON + 前置字段表 / 节点 4 阶段示例改为 currentState=1）
  - [x] 追加 §10.3.4 可能的错误码 H4 块
  - [x] 追加 §10.3.5 节点 4 / 5 / 9 / 10 五阶段过渡表 H4 块
  - [x] 追加 §10.3.6 不变量 H4 块
- [x] Task 4: 编辑 §10.4 POST /rooms/{roomId}/join (AC: AC5)
  - [x] 在 `### POST /api/v1/rooms/{roomId}/join` 之后追加 §10.4.1 接口元信息 H4 块
  - [x] 扩充 `#### 请求体` 小节为 §10.4.2 请求（path 参数表 + body 空对象）
  - [x] 扩充 `#### 服务端逻辑` 小节（保留 4 条 bullet + 加事务步骤顺序 + SELECT FOR UPDATE 行锁说明 + 错误码触发点 + WS 广播说明）
  - [x] 扩充 `#### 返回示例` 小节（保留 JSON + 前置字段表）
  - [x] 追加 §10.4.5 可能的错误码 H4 块
- [x] Task 5: 编辑 §10.5 POST /rooms/{roomId}/leave (AC: AC6)
  - [x] 在 `### POST /api/v1/rooms/{roomId}/leave` 之后追加 §10.5.1 接口元信息 H4 块
  - [x] 扩充 `#### 请求体` 小节为 §10.5.2 请求（path 参数表 + body 空对象）
  - [x] 扩充 `#### 服务端逻辑` 小节（保留 3 条 bullet + 加事务步骤顺序 + 心跳超时被动断线说明 + 错误码触发点 + WS 广播说明）
  - [x] 扩充 `#### 返回示例` 小节（保留 JSON + 前置字段表）
  - [x] 追加 §10.5.5 可能的错误码 H4 块
- [x] Task 6: 编辑 §12.3 `### 成员加入` (member.joined) (AC: AC7)
  - [x] 保留现有 JSON 示例
  - [x] 在示例**前置** `**触发**` 段（含主动 join 触发 + 节点 4 不存在被动 join）
  - [x] 在示例**前置** `**字段**` 表（`type` / `requestId` / `payload.userId` / `payload.nickname` / `ts`）
  - [x] 在示例**后置** `**关键约束**` 段（nickname 必非空 / 广播范围排除加入者 / 不下发丰富字段 / client merge 行为）
- [x] Task 7: 编辑 §12.3 `### 成员离开` (member.left) (AC: AC8)
  - [x] 保留现有 JSON 示例
  - [x] 在示例**前置** `**触发**` 段（仅 HTTP leave；任何 WS 断开 / 心跳超时**不**触发；r3 锁定）
  - [x] 在示例**前置** `**字段**` 表（`type` / `requestId` / `payload.userId` / `ts`，**不**含 nickname）
  - [x] 在示例**后置** `**关键约束**` 段（payload 精简理由 / 广播范围 / client roster 移除 / 触发不重复 1:1 对应删行事件）
- [x] Task 8: 编辑 §12.3 末尾"业务消息延后锚定"引用块 (AC: AC9)
  - [x] 保留既有结构
  - [x] 把 `member.joined` / `member.left` 状态升级为"已由 Story 11.1 锚定"
  - [x] 添加 server / client active message set 升级说明
- [x] Task 9: 编辑 §1 文档说明追加节点 4 房间业务冻结声明 (AC: AC10)
  - [x] 在 §1 末尾既有冻结声明列表（节点 2 / 3 / 4 协议骨架）之后追加节点 4 房间业务冻结声明
  - [x] 含 5 段：日期 + 影响 epic / story 列表 + 配置默认值钳制（`rooms.max_members = 4` / `rooms.status` 枚举）+ Future Fields 引用 + 业务消息延后锚定引用
- [x] Task 10: 内部一致性自检 + git commit (AC: AC11, AC12)
  - [x] 全文 grep 确认 §10 字段表与数据库设计 §5.13 / §5.14 字段对应关系正确
  - [x] 全文 grep 确认 §10.3 `members[].pet.currentState` 与 §12.3 `room.snapshot.payload.members[].pet.currentState` 节点 4 阶段固定值一致（都是 1）
  - [x] 全文 grep 确认 §10.3.5 五阶段过渡表与 §5.1 GET /home Future Fields 引用块的 `pet.equips` 节点过渡描述一致
  - [x] 全文 grep 确认 AC10 §1 冻结声明的配置默认值钳制段与 §10 字段表中固定值描述一致
  - [ ] 单独 git commit（commit message 含 "Story 11.1 接口契约最终化（房间 5 个 REST + member.joined / member.left WS 消息）"） — dev-story workflow 不 commit；该步由后续 fix-review / story-done 流程承接（与 4.1 / 7.1 / 10.1 同模式）
  - [x] 更新本 story 文件 Status 为 `review`（sprint-status.yaml 同步）

## Dev Notes

### 角色定位与 contract first

本 story 是 Epic 11 唯一的纯文档 story，定位为"contract first 的契约定稿" —— 所有 server 实装（11.2 ~ 11.10）和 iOS 实装（Epic 12）都基于本 story 锚定的 schema 平行展开。这与 Story 4.1 / 7.1 / 10.1 在各自 Epic 的角色完全对称。

**Epic 10 retrospective §6 A5 钦定**：协议契约类 Story 的 review 严格度 **≥** 实装 Story —— Story 10.1 跑满 10 轮 review 子循环验证了"纯文档 story 也可能是 review 大户"。本 story dev / review 阶段必须把"文档内部一致性"作为 hunter checklist 一等公民，避免重蹈 10.1 r1~r10 的覆辙。

### 与 Epic 10 既有契约的交接边界

Epic 10 已锚定：

- §12.1 连接地址 + close code 表 + 服务端校验顺序（4001 / 4002 / 4003 / 4004 / 1000 / 1001 / 1006 / 1011）
- §12.2 客户端 → 服务端通用消息信封 + `ping`
- §12.3 服务端 → 客户端通用消息信封 + `room.snapshot` + `pong` + `error`

本 story 在 Epic 10 既有契约**之上**扩展：

- §10 房间 REST 5 接口（**Epic 10 完全没有锚定 REST**，§10 现有内容是节点 4 之前的草稿）
- §12.3 `member.joined` / `member.left` 业务 WS 消息（Epic 10 §12.3 末尾"业务消息延后锚定"引用块明确说"待 Story 11.1 锚定"）

本 story 的 §12.3 业务消息字段表必须严格沿用 Story 10.1 锚定的通用消息信封（`type` / `requestId` / `payload` / `ts`）—— 这是 Epic 10 r1~r10 反复打磨的 invariant，不允许在本 story 重新定义。

### Epic 10 retrospective §5.1 - 13 条不变量在本 story 的适用性

Epic 10 沉淀的 13 条 WS 不变量中，本 story（纯文档）**不**直接适用并发 / 生命周期类（如 #1 / #2 / #4 / #5 / #6 / #7 / #8 / #9 / #10 / #11）—— 这些是 server 实装 invariant，由 Story 11.7 / 11.8 实装时遵守；但**直接适用**的两条：

- **#3 handshake snapshot 先于 Register**：本 story §10.4 服务端逻辑步骤 8 触发 `member.joined` 广播必须在事务**成功提交后**——这是与 #3 同源的"状态变化必须在 authoritative 成功后才广播"原则；本 story 文档侧严格写"事务成功提交后" 5 个字，避免 Story 11.8 实装时把广播提前到事务内（违反 #3）
- **#13 snapshot 必须返全 roster**：本 story §10.3 `data.members[]` 必须包含全部 `room_members` 行（含离线成员）—— 与 Story 10.1 r8 锁定的 snapshot 全 roster 不变量同源；本 story §10.3.6 不变量小节明确写出，避免 Story 11.6 实装时做"在线态过滤"

### epics.md 钦定 vs 本 story 的语义补全

epics.md §Story 11.1（行 1806-1826）已列出基础 schema 字段，但 epics.md 是**规划层**文档不是**契约层**文档；本 story 必须在 epics.md 基础上补全：

| epics.md 仅列出 | 本 story 必须补全 |
|---|---|
| `POST /rooms` response `{room: {id, creatorUserId, maxMembers, memberCount, status}}` | 各字段类型 / BIGINT 字符串化规则 / 节点 4 阶段固定值（`maxMembers=4` / `status=1`）+ 错误码 6003 触发条件 + WS 广播不触发说明 |
| `GET /rooms/current` response `{roomId: string \| null}` | null 字段必须显式不省略 + 与 `GET /me.user.currentRoomId` 永久 null schema 占位的关系说明 |
| `GET /rooms/{roomId}` response `{room: ..., members: [...]}` | 所有 sub 字段类型 + `pet.currentState` 节点 4 / 5 / 9 / 10 过渡表 + `pet.equips` Future Fields + 不变量 |
| `POST /rooms/{roomId}/join` response `{roomId, joined: true}` | 错误码 6001 / 6002 / 6003 / 6005 触发顺序 + SELECT FOR UPDATE 并发保护 + WS 广播触发时机 |
| `POST /rooms/{roomId}/leave` response `{roomId, left: true}` | 错误码 6004 触发条件（含两种场景）+ "最后一人 leave → closed"内部副作用不对外暴露 + 步骤 7（close 4007 + unregister leaver Session）必须**先于** 步骤 8（broadcast `member.left`）执行（r13 锁定 —— 让 BroadcastToRoom fanout 列表物理上排除 leaver，无需扩展 primitive）+ 任何 WS 断开 / 心跳超时**不**走 leave 事务（r3 锁定） |
| `member.joined` payload `{userId, nickname, avatarUrl, pet.{petId, currentState}}` | 必含丰富字段（snapshot 仅握手时下发 → member.joined 是 enrich 唯一路径）/ 广播范围 / client merge 行为 |
| `member.left` payload `{userId}` | 唯一触发 = HTTP leave 事务提交后（r3 锁定，任何 WS 断开 / 心跳超时**不**触发）/ payload 精简理由 |

### 字段命名一致性 checklist（review 必查）

| DB 字段（snake_case） | API 字段（camelCase） | 类型映射 | 出现章节 |
|---|---|---|---|
| `rooms.id` | `room.id` / `roomId` | BIGINT → string | §10.1 / §10.2 / §10.3 / §10.4 / §10.5 / §12.3 |
| `rooms.creator_user_id` | `room.creatorUserId` | BIGINT → string | §10.1 / §10.3 |
| `rooms.max_members` | `room.maxMembers` | TINYINT → int | §10.1 / §10.3 |
| `rooms.status` | `room.status` | TINYINT → int (1/2) | §10.1 / §10.3 |
| `room_members.user_id` | `members[].userId` / `payload.userId` | BIGINT → string | §10.3 / §12.3 |
| `users.nickname` | `members[].nickname` / `payload.nickname` | VARCHAR → string | §10.3 / §12.3 |
| `users.avatar_url` | `members[].avatarUrl` | VARCHAR → string (空 = "") | §10.3 |
| `pets.id` | `pet.petId` | BIGINT → string | §10.3 |
| `pets.current_state` | `pet.currentState` | TINYINT → int (1/2/3) | §10.3 |
| `users.current_room_id` | `data.roomId` | BIGINT → string \| null | §10.2 |
| - | `data.members[].pet.equips` | `[]` 节点 4 | §10.3 |
| - | `data.joined` / `data.left` | bool 固定 true | §10.4 / §10.5 |

### 错误码一致性 checklist（review 必查）

| 错误码 | 触发接口 | 触发条件 | 数据库设计 §8.X 事务边界对照 |
|---|---|---|---|
| 6001 房间不存在 | §10.3 / §10.4 | `rooms WHERE id = ?` 找不到 | §8.7 加入房间事务 |
| 6002 房间已满 | §10.4 | `room_members` 数 >= 4 | §8.7 |
| 6003 用户已在房间中 | §10.1 / §10.4 | `users.current_room_id` 非 null（含目标房间） | §8.6 创建房间 / §8.7 加入房间 |
| 6004 用户不在房间中 | §10.5 | `users.current_room_id != roomId`（含 null） | §8.6 退出房间 |
| 6005 房间状态异常 | §10.4 | `rooms.status != 1`（如 closed） | §8.7 |

**review 必查**：§10 字段表错误码触发条件描述与 §3 错误码定义表完全一致；任一接口的"可能的错误码"小节包含的码集必须 ≥ 服务端逻辑步骤中触发的码集（不能漏列）。

### Project Structure Notes

- 编辑文件路径：`C:/fork/cat/docs/宠物互动App_V1接口设计.md`（绝对路径，避免相对路径混淆）
- 不引入新文件、不修改其他 6 份 `docs/宠物互动App_*.md`、不修改任何 Go / Swift 代码
- sprint-status.yaml 中 `11-1-接口契约最终化` 状态由 create-story workflow 自动从 `backlog` 升级为 `ready-for-dev`；本 story 完成后 dev 流程会进一步流转为 `in-progress` → `review` → `done`
- epic-11 状态由 create-story workflow 自动从 `backlog` 升级为 `in-progress`（首个 story 创建触发，符合 epic-loop 文档说明的预期行为）

### References

- [Source: docs/宠物互动App_V1接口设计.md#1（契约冻结策略）] — 节点 2 / 3 / 4 协议骨架冻结声明格式参考
- [Source: docs/宠物互动App_V1接口设计.md#2.5（字段类型与编码约定）] — BIGINT 字符串化、可空字段 null、字符串长度按字符数计
- [Source: docs/宠物互动App_V1接口设计.md#3（错误码定义）] — 6001 ~ 6005 复用现有码段
- [Source: docs/宠物互动App_V1接口设计.md#10（房间接口现状）] — §10.1 ~ §10.5 五接口现有内容（草稿待扩充）
- [Source: docs/宠物互动App_V1接口设计.md#12.1（连接地址 + close code 表 + 服务端校验顺序）] — Story 10.1 已锚定，本 story 引用不修改
- [Source: docs/宠物互动App_V1接口设计.md#12.3（服务端 → 客户端通用消息信封 + room.snapshot + pong + error）] — Story 10.1 已锚定通用信封 + Story 10.7 实装 placeholder snapshot；本 story 仅扩充 `### 成员加入` / `### 成员离开` 两个 H3 小节 + 末尾"业务消息延后锚定"引用块状态升级
- [Source: docs/宠物互动App_数据库设计.md#5.13（rooms）] — 表结构 + 字段类型 + 索引
- [Source: docs/宠物互动App_数据库设计.md#5.14（room_members）] — 表结构 + UNIQUE(user_id) + UNIQUE(room_id, user_id) 兜底约束
- [Source: docs/宠物互动App_数据库设计.md#6.4（pets.current_state）] — 1=rest / 2=walk / 3=run 枚举来源
- [Source: docs/宠物互动App_数据库设计.md#6.12（rooms.status）] — 1=active / 2=closed 枚举来源
- [Source: docs/宠物互动App_数据库设计.md#8.6 / §8.7（房间事务边界）] — 创建 / 加入 / 退出事务步骤与边界
- [Source: docs/宠物互动App_时序图与核心业务流程设计.md#11.1 / §11.2（房间创建 / 加入流程）] — 既有时序图，本 story 字段层契约对齐**不**触发流程级修订
- [Source: _bmad-output/planning-artifacts/epics.md#Story 11.1（行 1806-1826）] — Epic 11 §Story 11.1 AC 钦定 schema 字段 + 错误码 6001 ~ 6005 + "契约自此 Epic 起冻结"声明
- [Source: _bmad-output/implementation-artifacts/4-1-接口契约最终化.md] — 节点 2 第一个契约 story，可参考 §1 冻结声明 + §2.5 全局约定 + 字段表格式
- [Source: _bmad-output/implementation-artifacts/7-1-接口契约最终化.md] — 节点 3 第一个契约 story，可参考"配置 / 阈值钳制声明"段格式（本 story 用于 `rooms.max_members = 4` / `rooms.status` 枚举钳制）
- [Source: _bmad-output/implementation-artifacts/10-1-接口契约最终化.md] — 节点 4 第一个契约 story，可参考 §12 WS 信封 / close code / 服务端校验顺序 / 业务消息延后锚定引用块格式
- [Source: _bmad-output/implementation-artifacts/10-7-房间快照下发框架.md] — Epic 10 收官 story，可参考 §10.3 五阶段过渡表中 placeholder / 真实路径分离的语义来源
- [Source: _bmad-output/implementation-artifacts/epic-10-retro-2026-05-08.md] — Epic 10 retrospective，§5.1 13 条不变量 + §6 A5 协议契约 review 严格度要求

## Dev Agent Record

### Agent Model Used

Claude Opus 4.7 (1M context)

### Debug Log References

无（纯文档 story，无运行时日志）。

### Completion Notes List

- 完成 AC1 ~ AC10 全部落地到 `docs/宠物互动App_V1接口设计.md`：
  - **AC2 §10.1 POST /rooms**：新增接口元信息 H4 + 扩充请求体 / 服务端逻辑（含 6 步事务 + WS 广播说明）/ 响应体（含字段表）/ 新增可能错误码 H4 + 关键约束段
  - **AC3 §10.2 GET /rooms/current**：新增接口元信息 H4 + 请求 H4 + 扩充响应体（字段表 + 两版示例）+ 新增可能错误码 H4 + 关键约束段
  - **AC4 §10.3 GET /rooms/{roomId}**：新增接口元信息 H4 + 请求 H4 + 扩充响应体（11 行字段表，覆盖 room + members[].pet 完整结构）+ 新增可能错误码 H4 + 节点 4/5/9/10 五阶段过渡表 H4 + 不变量 H4 + 关键约束段；JSON 示例统一 currentState=1（与 placeholder 阶段一致，避免与 §12.3 placeholder 冲突）
  - **AC5 §10.4 POST /rooms/{roomId}/join**：新增接口元信息 H4 + 扩充请求（path 参数 + body 空对象）/ 服务端逻辑（9 步事务 + SELECT FOR UPDATE 行锁 + WS 广播在事务成功提交后）/ 响应体（字段表）/ 新增可能错误码 H4（含 6001/6002/6003/6005 触发顺序）+ 关键约束段
  - **AC6 §10.5 POST /rooms/{roomId}/leave**：新增接口元信息 H4 + 扩充请求 / 服务端逻辑（7 步事务 + 心跳超时被动断线说明 + 最后一人离开 closed 内部副作用不对外暴露）/ 响应体（字段表）/ 新增可能错误码 H4（仅 6004）+ 关键约束段；§10 章节末尾追加引用块说明 `GET /home.data.room.currentRoomId` 由 Story 11.10 真实实装
  - **AC7 §12.3 `### 成员加入`**：标题升级为 `### 成员加入（member.joined）`+ 触发段（主动 join + 节点 4 不存在被动 join）+ 字段表（`type` / `requestId` / `payload.userId` / `payload.nickname` / `ts` 完整 5 字段）+ JSON 示例（保留并改 nickname → "用户1002" 与节点 2 阶段命名一致）+ 关键约束（nickname 必非空 / 广播范围 / payload 精简理由 / client merge 行为 / 加入者自己排除）
  - **AC8 §12.3 `### 成员离开`**：标题升级为 `### 成员离开（member.left）`+ 触发段（主动 + 心跳超时被动 + 客户端断开三类）+ 字段表（4 字段，**不**含 nickname）+ JSON 示例（保留）+ 关键约束（payload 精简 / 广播范围 / client roster authoritative 移除 / 主动 vs 被动事件去重 / 离开者自己排除）
  - **AC9 §12.3 末尾"业务消息延后锚定"引用块**：`member.joined` / `member.left` 状态从"待 Story 11.1 锚定"升级为"**Story 11.1（已锚定）**"+ 新增 server / client active message set 升级段落（覆盖 Story 10.1 r10 钉死的"Epic 10 阶段冻结"边界）
  - **AC10 §1 节点 4 房间业务契约冻结声明**：在既有节点 2 / 3 / 4 协议骨架冻结声明之后追加完整 5 段（日期 + 影响面 + 配置默认值钳制 `rooms.max_members = 4` / `rooms.status` 枚举 + Future Fields 引用 + 业务消息延后锚定引用）
- AC11 sprint-status.yaml 流转：`ready-for-dev` → `in-progress` → `review`（本步落地）；最终 `done` 由后续 story-done workflow 标记
- AC12 内部一致性自检通过：
  - §10 字段命名（`creatorUserId` / `roomId` / `userId` / `petId` / `currentState` / `maxMembers` / `memberCount` / `status` / `nickname` / `avatarUrl` / `equips`）严格按数据库设计 §5.13 / §5.14 snake_case → camelCase 映射；BIGINT 主键 / 外键全部按 §2.5 字符串化下发
  - §10.3 `members[].pet.currentState` 节点 4 阶段固定 `1` 与 §12.3 `room.snapshot.payload.members[].pet.currentState` placeholder 阶段固定 `1` 一致（两处都是 rest 状态，节点 4 placeholder 与节点 4 真实路径都不真实驱动 motion_state，等 Epic 14 才真实下发 1/2/3）
  - §10.3.5 五阶段过渡表与 §5.1 GET /home Future Fields 引用块的 `pet.equips` 节点过渡描述一致：节点 9 由 Epic 26 / Story 26.6 落地，节点 10 由 Epic 29 / Story 29.6 加 `renderConfig`
  - §1 末尾冻结声明的"配置默认值钳制"段（`rooms.max_members = 4` + `rooms.status` 枚举 1/2）与 §10.1 / §10.3 / §10.4 字段表中固定值 / 枚举语义描述一致
  - §10 五接口"可能的错误码"H4 块全部沿用 §3 错误码定义表（6001 ~ 6005 + 1001 / 1002 / 1005 / 1009），无错误码新增 / 重定义
  - §12.3 `member.joined` / `member.left` 通用信封（`type` / `requestId` / `payload` / `ts`）严格沿用 Story 10.1 锚定的信封结构，未引入新顶层字段
- **回归测试现状（含 HALT 注释）**：跑 `bash scripts/build.sh --test` 时 `internal/app/ws` 包出现一处 FAIL，但同一命令在 `git stash` 排除本 story 改动后仍 FAIL —— 已验证为**预先存在**的测试失败，**不**由本 story 引入（本 story 仅改 `docs/*.md` + sprint-status.yaml + story 文件，零行 Go / Swift 代码改动）。该失败属 Epic 10 范围 / 节点 4 既有 ws 测试遗留问题，与本 story 契约定稿无关；不阻塞本 story 进入 review。建议 review / fix-review 阶段单独立 issue 跟踪 ws 测试包的 flaky / 既有失败问题，不在本 story 范围内修复（避免范围扩散）。
- **范围红线遵守**：仅改 `docs/宠物互动App_V1接口设计.md` + `_bmad-output/implementation-artifacts/sprint-status.yaml` + 本 story 文件三处；**未**改任何 Go / Swift 代码 / 其他 6 份 `docs/宠物互动App_*.md` / `_bmad-output/` 下其他 yaml / md。

### File List

修改：

- `docs/宠物互动App_V1接口设计.md`（§1 末尾追加节点 4 房间业务契约冻结声明 5 段；§10.1 / §10.2 / §10.3 / §10.4 / §10.5 五个 REST 接口完整 schema 锚定 H4 块批量补全；§12.3 `### 成员加入` / `### 成员离开` 两个 H3 小节扩充触发 + 字段表 + 关键约束；§12.3 末尾"业务消息延后锚定"引用块状态升级 + active message set 升级声明；§10 章节末尾追加 `GET /home.data.room.currentRoomId` 由 Story 11.10 实装的引用）
- `_bmad-output/implementation-artifacts/sprint-status.yaml`（development_status[`11-1-接口契约最终化`] 流转：`ready-for-dev` → `in-progress` → `review`；last_updated 同步）
- `_bmad-output/implementation-artifacts/11-1-接口契约最终化.md`（Status: `ready-for-dev` → `in-progress` → `review`；Tasks/Subtasks 全部勾选 [x]；Dev Agent Record 填充 Completion Notes / File List / Change Log）

无新增 / 删除文件。

### Change Log

| 日期 | 描述 | 改动文件 |
|---|---|---|
| 2026-05-08 | dev-story workflow 落地：§10.1 ~ §10.5 五 REST 接口字段层契约锚定 + §12.3 `member.joined` / `member.left` 业务 WS 消息字段层契约锚定 + §1 节点 4 房间业务契约冻结声明追加；story Status 流转 `ready-for-dev` → `review` | `docs/宠物互动App_V1接口设计.md` / `_bmad-output/implementation-artifacts/sprint-status.yaml` / 本文件 |
| 2026-05-08 | fix-review r1：(1) 扩展 `member.joined.payload` 字段表新增 `avatarUrl` / `pet.petId` / `pet.currentState` 三字段（必填）+ Future Fields 标记 `pet.equips` / `equips[].renderConfig`；移除"等下次 `room.snapshot` 全量推送时 enrich"的虚假承诺（snapshot 仅握手时下发一次，已连接成员不会收到追发的 snapshot），明确 client 收到 `member.joined` 即可直接 append 完整 roster entry；(2) 调整 §10.3 `data.members` 字段说明 + 不变量小节 + §12.3 `room.snapshot.members` / 不变量小节 / placeholder 示例描述：移除"含离线成员"语义，改为"`room_members` 全行 = server 端仍在房间的成员"，明确节点 4 阶段不引入 online / offline 字段层区分；新增 §10.3 末尾"roster 语义与被动断线交互"小节，钦定心跳超时清理钩子 = 真删 `room_members` 行 + 触发 `member.left` 广播 + 后续 snapshot/GET 不再含该 user 的统一语义，与 Story 10.4 r6 lesson 自洽 | `docs/宠物互动App_V1接口设计.md` / `docs/lessons/2026-05-08-room-roster-contract-self-consistency-11-1-r1.md` / 本文件 |
