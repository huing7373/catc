# Story 17.1: 接口契约最终化（GET /emojis + WS emoji.send / emoji.received）

Status: done

<!-- Validation 可选。建议运行 validate-create-story 在 dev-story 前做一次质检。 -->

## Story

As a 跨端契约负责人,
I want 在写第一行 server Epic 17 业务代码（Story 17.2 ~ 17.5）之前，把节点 6 涉及的 `GET /api/v1/emojis` REST 接口与 `emoji.send`（client → server）/ `emoji.received`（server → client）两条 WS 业务消息的字段层 schema 在 `docs/宠物互动App_V1接口设计.md` §11.1 / §12.2 `### 发送表情`/ §12.3 `### 收到表情广播` 里逐字段锚定（含必填项、类型、长度 / 范围约束、`emojiCode` 取值约束（必须在 `emoji_configs` 中存在且 `is_enabled=1`）、错误码 1001 / 1002 / 1005 / 1009 / **7001** / **6004** 触发条件、广播触发点与 fire-and-forget 语义、`emoji.received` payload 字段表 + 关键约束、节点 6 阶段冻结声明），同时把 V1 文档 §12.3 末尾"业务消息延后锚定"引用块（行 2349-2362）里 `emoji.received` 状态从"现有 §12.3 草稿中**存在** `emoji.received` JSON 示例但**缺**字段表 + 触发 + 关键约束，Story 17.1 落地时**需补齐**该小节"升级为"已由 Story 17.1 锚定（commit hash 见 commit message）"+ 把 §12.2 `### 发送表情` 草稿示例同步升级为"已由 Story 17.1 锚定"+ 把 `emoji.received` 合法加入 server → client active message set + 把 `emoji.send` 合法加入 client → server active message set（Story 10.1 r10 锚定的 active message set 升级规则 + Story 11.1 / 14.1 已扩展 server → client 集合后的第三次扩展、+ Epic 10 后第一次扩展 client → server 集合）,
so that iOS Epic 18（表情面板交互 + 广播接收动效）和 server Epic 17 后续 Story（17.2 ~ 17.5）可以基于同一份冻结契约**并行**开工，不再出现"REST 接口字段名 / 大小写不一致 / `emojiCode` 是 string 还是 int 不清楚 / `emoji.received` payload 字段名与 client Codable struct 对不上 / 7001 触发条件不精确（emoji 不存在 vs disabled 各自走什么路径模糊）/ 6004 在 WS 路径上是否触发不清楚 / iOS Epic 18 本地立即动效 vs server 广播去重策略与契约层 self-broadcast 语义冲突 / 表情列表是否分页 / 是否 cache 不清楚 / 广播 fire-and-forget 是否影响 client 期待 ack 不清楚 / 用户不在房间发 emoji.send 是否触发 error 还是 close 连接不清楚"的返工。

## 故事定位（Epic 17 第一条 = 节点 6 第一个 Server Epic 的契约起手；对标 Story 4.1 / 7.1 / 10.1 / 11.1 / 14.1 在各自 Epic 的角色，本 story 是 Epic 17 **唯一的纯文档 story**，开局而非收官）

- **Epic 17 进度**：本 story → 17.2 (emoji_configs migration) → 17.3 (emoji_configs seed ≥4 个表情) → 17.4 (GET /emojis 接口) → 17.5 (WS emoji.send 处理 + emoji.received 广播)。**17.1 是 17.2 / 17.3 / 17.4 / 17.5 的强前置**：handler 层 request / response DTO 必须严格按 17.1 锚定后的 schema；service 层错误码映射必须严格按 17.1 锚定的 7001 / 6004 触发条件；广播 payload 必须严格按 17.1 锚定的 `emoji.received.payload.{userId, emojiCode}` 字段；同时是 iOS Epic 18 的强前置（Story 18.1 表情面板 SwiftUI + 18.3 SendEmojiUseCase WS 发送 + 18.4 接收 emoji.received 在对应成员猫上方播放飞出动效必须严格按 17.1 冻结契约）
- **epics.md AC 钦定**：`_bmad-output/planning-artifacts/epics.md` §Story 17.1（行 2520-2537）已**精确**列出 §11.1 必须锚定的字段（response data: `{items: [{code, name, assetUrl, sortOrder}]}`）+ §12.2 客户端→服务端 `emoji.send` payload `{emojiCode: string}` + §12.3 服务端→客户端 `emoji.received` payload `{userId, emojiCode}` + `error` 当 emojiCode 无效时返回 `{code: 7001, message: "emoji not found"}` + "契约自此 Epic 起冻结"声明
- **Story 10.1 / 11.1 / 14.1 历史遗留对接**：
  - 本 story 必须把 V1 文档 §12.2 `### 发送表情`（行 1894-1908）从"节点 4 之前的协议草稿示例 + Story 17.1 锚定时正式落地"升级为"已由 Story 17.1 锚定"，并**补齐**该节缺失的"触发 / 字段表 / 关键约束 / 错误响应说明"内容（草稿示例已存在，但未走完成"H3 标题 + `**触发**` 段 + `**字段**` 表 + JSON 示例 + `**关键约束**` 段"模板）
  - 本 story 必须把 V1 文档 §12.3 `### 收到表情广播`（行 2261-2273）从"现有 §12.3 草稿中**存在** `emoji.received` JSON 示例但**缺**字段表 + 触发 + 关键约束"升级为"已由 Story 17.1 锚定"，并**补齐**该节缺失的"触发 / 字段表 / 关键约束"内容（草稿 JSON 示例已存在）
  - 本 story 必须把 V1 文档 §12.3 末尾"业务消息延后锚定"引用块（行 2349-2362）里 `emoji.received` 状态从"待 Story 17.1 锚定"升级为"已由 Story 17.1 锚定（commit hash X）"
  - 本 story 必须把 §12.3 末尾"server / client active message set 升级"块（行 2357-2360）扩展：**server → client active message set 追加 `emoji.received`**（继 Story 11.1 锚定 `member.joined` / `member.left` + Story 14.1 锚定 `pet.state.changed` 之后的第三次扩展）；**client → server active message set 追加 `emoji.send`**（继 Epic 10 锚定 `ping` 之后的第一次扩展 —— 注意 Story 11.1 / 14.1 都没有扩展 client → server 集合，因为 Epic 11 / 14 业务消息都是 server → client 单向广播，client 通过 HTTP 触发；Epic 17 是首个引入 client → server 业务消息的 epic）。Story 10.1 r10 不变量"server / client active message set 的冻结范围**仅适用于 Epic 10 阶段**，Epic 11 / 14 / 17 起允许新业务消息合法加入 active message set"在 Story 11.1 / 14.1 已生效两次（server → client 集合扩展），本 story 是**第三次**把新业务消息（`emoji.received`）正式加入 server → client active 集合，同时是**首次**扩展 client → server active 集合（追加 `emoji.send`）
- **下游立即依赖**：
  - **Story 17.2（emoji_configs migration）**：migration DDL 严格按 数据库设计 §5.15（`id` / `code` / `name` / `asset_url` / `sort_order` / `is_enabled` / `created_at` / `updated_at` + `UNIQUE KEY uk_code` + `KEY idx_enabled_sort`）；本 story **不**改 数据库设计 §5.15 schema（DB 已锚定，DB → API 方向）
  - **Story 17.3（emoji_configs seed ≥4 个表情）**：seed 至少 4 个表情（`wave` / `love` / `laugh` / `cry`），每个表情 `asset_url` 可访问；本 story **不**钦定具体 4 个 emojiCode 字面量（由 17.3 实装层决定，但 17.1 锚定 `emojiCode` 字段为 string 类型 + 1 ≤ length ≤ 64 + 只允许 `[a-z0-9_-]`，与数据库设计 `code VARCHAR(64)` 一致）
  - **Story 17.4（GET /emojis 接口）**：handler 响应严格按 17.1 §11.1 锚定的 `{items: [{code, name, assetUrl, sortOrder}]}` schema；service 层"仅返回 `is_enabled=1` 的表情"+ "按 `sort_order` 排序"严格按 17.1 §11.1 服务端逻辑段；错误码 1001 / 1009 严格按 17.1 §11.1 错误码表（**不**触发 7001 —— GET /emojis 是列表查询，无单 emoji code 校验）；**不**分页（表情列表 4-20 个 short list）；**不**接受任何 query 参数（无筛选 / 排序 / 分页参数）
  - **Story 17.5（WS emoji.send 处理 + emoji.received 广播）**：dispatcher 路由严格按 17.1 §12.2 `### 发送表情` 锚定的 `type = "emoji.send"`；handler request payload 严格按 17.1 §12.2 锚定的 `{emojiCode: string}`（**仅** 1 个字段，最小契约）；handler 校验顺序严格按 17.1 §12.2 服务端逻辑段：(1) 校验当前用户在房间且**与本 WS Session 所属房间一致**（`users.current_room_id != NULL` **且** `users.current_room_id == Session.roomID` —— **不可仅判 `!= NULL`**，否则封不住 stale-Session 跨房间注入；NULL / 不匹配两类**都**回 `error.payload.code = 6004` "用户不在房间中"）→ (2) 校验 emojiCode 在 emoji_configs 存在且 enabled（**不**存在或 disabled → 回 `error.payload.code = 7001` "emoji not found"）→ (3) 调用 `BroadcastToRoom(Session.roomID, {type: "emoji.received", payload: {userId, emojiCode}})`（广播目标 = `Session.roomID`，不是 `users.current_room_id` —— 二者在步骤 1 已校验相等，此处显式以 `Session.roomID` 为参，与 §10.4 / §10.5 HTTP 接口 ACL 以 path roomId 为权威源一致）；广播失败 fire-and-forget（仅 log warning，**不**回 error 给发起者 —— 与 Story 10.5 / Story 11.8 `member.joined` / `member.left` / Story 14.4 `pet.state.changed` 广播失败语义一致）；广播范围**包含**发起者自己（与 `pet.state.changed` 同语义；**不**与 `member.joined` / `member.left` 同语义 —— 表情是事件本身，server 不区分发起者 / 其他成员，让 iOS 端 client 决定是否过滤 —— 简化 server 实装 + 让 iOS 端 ViewModel 用统一路径处理 emoji.received）；表情**不入库**（按数据库设计 §14.3，MVP 不强制落库表情事件日志）；限频建议（同 user 每秒 ≤ 5 个表情）**MVP 不实装**（仅 tech debt 登记，由对应 future epic 决定）
  - **iOS Story 18.1（表情面板 SwiftUI + GET /emojis）**：iOS 端 `EmojiConfigDTO` Codable struct 严格按 17.1 §11.1 字段表（`code` / `name` / `assetUrl` 均 `String`；`sortOrder` 是 `Int`）；client **应**在 App 生命周期内**缓存**表情列表（首次进表情面板 → GET /emojis → 后续不再重复拉取，与 §15.6 "client 不应重复拉取静态配置" 一致；缓存失效条件由 Epic 18 实装决定，节点 6 阶段简化为"App 生命周期内不失效"）
  - **iOS Story 18.3（SendEmojiUseCase WS 发送 emoji.send + 本地立即动效）**：iOS 端 `EmojiSendMessage` Codable struct 严格按 17.1 §12.2 字段表（`type` / `requestId` / `payload.emojiCode`）；发送时机由 iOS Story 18.3 实装层决定（用户选中表情瞬间发送 + 同时本地立即动效，**不**等 server `emoji.received` 回信 —— 本地立即动效在 SendEmojiUseCase 层用同步路径触发，与 server 广播解耦）；client 发 `emoji.send` 不依赖 server `emoji.received` 回信驱动自己的动效（自己的动效在本地触发；server 的 `emoji.received` 由 Story 18.4 处理，且 18.4 **应**对 `payload.userId == 当前 user.id` 走"跳过 / 安全去重"路径 —— 否则自己会看到两次动效）
  - **iOS Story 18.4（接收 emoji.received → 对应成员猫上方播放动效）**：iOS 端 `EmojiReceivedMessage` Codable struct 严格按 17.1 §12.3 字段表（`payload.userId` 是 `String` BIGINT 字符串化；`payload.emojiCode` 是 `String`）；client 收到 `emoji.received` 时**应**按以下规则处理：(a) `payload.userId == 当前 user.id` → **跳过**（本地动效已在 Story 18.3 播过，不重复触发；与 §5.2 `pet.state.changed` self-broadcast 规则**不同语义** —— 表情是 transient UI 事件，server 不持久化，client 端跳过自己的 echo 不会导致状态 stale；`pet.state.changed` 是持久化状态变更，self-broadcast 兜底是状态对齐的双轨之一）；(b) `payload.userId ∈ 当前房间 roster` → 在该成员猫位上方触发飞出动效；(c) `payload.userId` 不在 roster（**合法 race window** —— sender 发完 emoji 立即 leave，receiver 先收到 `member.left` 把 sender 从本地 roster 移除，再收到 `emoji.received`；server 端两类广播走**不同**路径，物理到达顺序不保证一致）→ **不**得简单 drop；执行降级渲染：以 `payload` 自带字段（`userId` / `emojiCode`）触发飞出动效，但因无 PetSpriteView 锚点，**降级到房间中心位置**（或屏幕中央安全区）展示该表情；log info（**不** log warn / error，这是预期合法行为）；(d) `payload.emojiCode` 不在 client 已缓存的表情列表（理论不该 —— server 已在 17.5 service 层校验 emojiCode 合法）→ 显示 fallback 占位（默认问号），不报错
- **范围红线**：本 story **只**改 `docs/宠物互动App_V1接口设计.md` 一份文件 + sprint-status.yaml + 本 story 文件；**不**改任何 Go / Swift 代码、**不**改 `_bmad-output/` 下其他 yaml / md（除自己的 story 文件 + sprint-status.yaml 标记 ready-for-dev → in-progress → review → done）、**不**改其他 6 份 `docs/宠物互动App_*.md`（包括数据库设计 / Go 项目结构 / iOS 项目结构 / 时序图 / 总体架构 / MVP 节点规划 等 —— 它们对应的 schema 已锚定，本 story 只**对齐 / 引用**，不**修改**）

**本 story 不做**（明确范围红线）：

- 不实装任何 Go handler / service / repo / WS dispatcher / 广播代码（17.2 ~ 17.5 才做；本 story 是 contract first 的契约定稿）
- 不实装任何 Swift Codable DTO / WebSocketClient 解析层 / EmojiPanelView / SendEmojiUseCase（iOS Epic 18 各 story 才做；本 story 提供给 iOS 的是文档 + Git commit hash，不是代码）
- 不修改 `docs/宠物互动App_数据库设计.md` §5.15 emoji_configs schema（已锚定；本 story 的 API 字段语义来源是数据库设计，方向是 DB → API，**不反向**修改 DB；`code VARCHAR(64)` / `name VARCHAR(64)` / `asset_url VARCHAR(255)` / `sort_order INT` 字段长度来源 §5.15，本 story 引用不修改）
- 不修改 §10.3 GET /rooms/{roomId} schema / §10.4 / §10.5 房间事务接口（已 Story 11.1 锚定）/ §5.2 POST /pets/current/state-sync schema（已 Story 14.1 锚定）/ §12.1（连接地址 + close code 表）/ §12.2 通用消息信封 + ping / §12.3 通用信封 + `room.snapshot` / `### 成员加入` / `### 成员离开` / `### 宠物状态变更（pet.state.changed）` / `pong` / `error` 各小节字段表（已在 Story 10.1 / 11.1 / 14.1 锚定 + 冻结；本 story 仅扩充 §11.1 / §12.2 `### 发送表情` / §12.3 `### 收到表情广播` + 升级末尾"业务消息延后锚定"引用块状态 + 升级 server / client active message set）
- 不修改 §5.1 GET /home schema / §4.1 ~ §4.3 / §6 步数 / §7 宝箱 / §8 装扮 / §9 合成（已在 Story 4.1 / 7.1 等锚定；本 story 不影响）
- 不修改 §3 错误码表（7001 / 6004 / 1001 / 1002 / 1005 / 1009 已锚定；本 story 仅**引用**这些错误码到 §11.1 / §12.2 `### 发送表情` 错误码段，**不**新增 / **不**修改错误码定义）
- 不修改 `docs/宠物互动App_Go项目结构与模块职责设计.md`（Go 工程目录已锚定 §4 + §6 + §9 + §12；本 story 不引入新目录 / 模块；`internal/domain/emoji/` / `internal/service/emoji_service.go`（或 step / room 同级）由 Story 17.4 / 17.5 实装时按既有目录规则落地）
- 不修改 `docs/宠物互动App_iOS客户端工程结构与模块职责设计.md`（iOS 工程目录已 ADR-0002 + Story 2.2 + Story 37.x 落地；本 story 不影响 iOS 目录；`RoomViewModel` / `WebSocketClient` 已在 §9 锚定）
- 不修改 `docs/宠物互动App_时序图与核心业务流程设计.md`（§13 WebSocket 建连流程已存在；本 story 字段层契约对齐**不**触发流程级修订）
- 不修改 `docs/宠物互动App_总体架构设计.md` / `docs/宠物互动App_MVP节点规划与里程碑.md`（这两份是更上层的产品设计，节点 6 业务规则在 MVP 节点规划已锚定；本 story 只是字段层契约定稿，不触发更上层文档修订）
- 不为 schema 字段写 OpenAPI / AsyncAPI / JSON Schema 形式化定义（旧架构残留；新架构 V1 文档以 markdown 表格 + JSON 示例为唯一真相，参见 V1接口设计 §1）
- 不追加 §11.1 / §12.2 `### 发送表情` / §12.3 `### 收到表情广播` 之外的接口或消息锚定
- 不在 V1 文档里写 server 实装细节（如"用 GORM 还是 sqlx"、"广播 BroadcastToRoom 调用方式"具体代码）—— 这些属 ADR / Go 项目结构文档 / Story 17.4 / 17.5 实装层决定，V1 文档**只**写客户端可见的 HTTP request / response + WS message 语义；但"广播失败不影响 client（fire-and-forget）"的语义层契约**必须**在 17.1 文档侧写明
- 不为 dev-only / test-only 接口（如 `/dev/force-emit-emoji`）的 schema 锚定（节点 6 阶段 epics.md 未规划 emoji dev 端点）
- 不写英文版接口文档（项目 communication_language=Chinese；用户 huing7373@gmail.com 单一开发者，无国际化诉求）
- 不在本 story 锚定 close code 段（close code 已在 Story 10.1 §12.1 锚定完整表；本 story 不追加 / 不修改 close code 段）
- 不实装表情限频中间件（每用户每秒 5 个表情）—— epics.md §Story 17.5 已钦定"MVP 可不做，但 tech debt 登记"；本 story 文档侧仅在 §12.2 `### 发送表情` 服务端逻辑段标注"server 当前**不**对 `emoji.send` 做特殊限频（仅走 Story 4.5 通用 rate_limit 中间件按 user_id 每分钟 60 次默认）"，**不**在契约层引入"每秒 N 个表情"的新限频规则

## Acceptance Criteria

**AC1 — 文档定位与编辑范围**

唯一受编辑的文档：`docs/宠物互动App_V1接口设计.md`（绝对路径 `C:/fork/cat/docs/宠物互动App_V1接口设计.md`）。

- 编辑模式：**就地修改**已有 §11.1 章节的内容（补全接口元信息 / 服务端逻辑 / 响应体字段表 / 错误码 / 关键约束；保留现有 JSON 示例 + 标题）+ **就地修改**已有 §12.2 `### 发送表情` 章节（补全触发 / 字段表 / 关键约束；保留现有 JSON 示例 + 标题）+ **就地修改**已有 §12.3 `### 收到表情广播` 章节（补全触发 / 字段表 / 关键约束；保留现有 JSON 示例 + 标题）+ **就地修改** §12.2 `### 发送表情` 末尾"业务消息延后锚定"引用块（行 1906-1908，把 `emoji.send` 状态升级为"已锚定"+ 升级 client → server active message set）+ **就地修改** §12.3 末尾"业务消息延后锚定"引用块（行 2349-2362，把 `emoji.received` 状态升级为"已锚定"+ 升级 server → client active message set）+ §1 末尾**追加**节点 6 表情业务契约冻结声明（与节点 2 / 节点 3 / 节点 4 协议骨架冻结 / 节点 4 房间业务冻结 / 节点 5 宠物状态业务冻结声明并列，**不**覆盖 / 删除）
- 编辑形式：**保留**现有 markdown 结构（H2 / H3 / H4 标题层级 + ` ```json ` fenced code block + 段落顺序）；只**补全**字段说明表格 + 必填 / 类型 / 长度约束 + 错误码列表 + 服务端逻辑细化 + JSON 示例（§11.1 已有 response JSON 示例 / §12.2 `### 发送表情` 已有 request JSON 示例 / §12.3 `### 收到表情广播` 已有 broadcast JSON 示例，全部保留）
- 文件编码 UTF-8 + LF 行尾（与项目其他 .md 一致）
- 修改后的文档**仍可直接被复制粘贴**作为 client / server 双方实装参照（即所有 JSON 示例可直接 `curl` / WS frame send 出来比对；所有字段说明可直接当作"DTO / Codable struct 字段表"用）
- 不删除任何**已有**字段示例或说明（仅追加 / 补全）；§2.5 全局约定 / §3 错误码表 / §4 / §5 / §6 / §7 / §8 / §9 / §10 / §12.1 / §12.2 通用信封 + `ping` / §12.3 通用信封 + `room.snapshot` / `### 成员加入` / `### 成员离开` / `### 宠物状态变更（pet.state.changed）` / `pong` / `error` 等已锚定章节**不动**
- §11.1 现有内容（行 1719-1749）：`GET /api/v1/emojis` 标题 + `#### 返回示例` JSON `{items: [...]}` → **保留 + 补接口元信息 / 服务端逻辑 / 响应体字段表 / 错误码 / 关键约束**
- §12.2 `### 发送表情` 现有内容（行 1894-1908）：`### 发送表情` H3 标题 + JSON 示例 `{type, requestId, payload: {emojiCode}}` + "业务消息延后锚定"引用块 → **保留 + 补触发 / 字段表 / 关键约束 + 升级末尾"业务消息延后锚定"引用块状态（`emoji.send` 已锚定 + client → server active message set 追加 `emoji.send`）**
- §12.3 `### 收到表情广播` 现有内容（行 2261-2273）：`### 收到表情广播` H3 标题 + JSON 示例 `{type, requestId, payload: {userId, emojiCode}, ts}` → **保留 + 补触发 / 字段表 / 关键约束**
- §12.3 末尾"业务消息延后锚定"引用块（行 2349-2362）→ **保留 + 把 `emoji.received` 状态升级为"已由 Story 17.1 锚定"+ 升级 server → client active message set 追加 `emoji.received`**

**AC2 — §11.1 GET /api/v1/emojis schema 锚定**

V1 文档 §11.1 编辑后必须包含以下完整内容：

**§11.1.1 接口元信息**（**新增** H4 块，紧接 `### GET /api/v1/emojis` 之后）：

```markdown
#### 接口元信息

| 字段 | 值 |
|---|---|
| HTTP Method | GET |
| Path | /api/v1/emojis |
| 认证 | **需要** Bearer token（auth 中间件） |
| 限频 | 默认（按 Story 4.5 rate_limit 默认值 60 次/分；表情列表是静态配置，client 应在 App 生命周期内缓存 —— 每用户每分钟实际调用 << 60 次） |
| 幂等 | 天然幂等（GET 查询，无副作用） |
| 节点 | 节点 6（Epic 17 落地 / Epic 18 客户端集成） |
| 分页 | **不**分页（表情列表 4 ~ 20 个 short list，全量返回） |
| Query 参数 | **无**（不接受任何 query string，不支持筛选 / 排序参数） |
```

**§11.1.2 请求**（**新增** H4 块）：

```markdown
#### 请求

无请求体（GET 接口）；仅需 `Authorization: Bearer <token>` Header。
```

**§11.1.3 服务端逻辑**（**新增** H4 块，紧接 `#### 请求` 之后）：

```markdown
#### 服务端逻辑

1. **认证 & 限频**：auth 中间件校验 Bearer token；rate_limit 中间件按默认配置限频（60 次/分按 user_id 计；已认证业务路由统一按 user_id 而非 IP，与 §6.1 / §6.2 / §10.x / §5.2 同语义）
2. **查询**：`SELECT id, code, name, asset_url, sort_order FROM emoji_configs WHERE is_enabled = 1 ORDER BY sort_order ASC, id ASC`
   - **次要排序键 `id ASC`**：保证 `sort_order` 相同时返回顺序确定（避免 client 端"同 sort_order 表情顺序在不同请求间不一致"问题）
   - **`is_enabled = 1`** 过滤：disabled 表情**不**返回给 client（被 server 管理员临时下架 / WIP 阶段不放出的表情）
3. **DTO 转换**：把 DB row 映射为 response item（`id` 字段**不**下发，client 不需要数据库主键；`code` 作为业务标识符；`is_enabled` 不下发；`created_at` / `updated_at` 不下发）
4. **响应**：返回 `data.items` 数组，**不**分页（即使数组为空也返回 `items: []`，**不**返回 `null`）

**事务边界规则**：本接口**不**需要 MySQL 事务（仅 1 个 SELECT 查询）。
```

**§11.1.4 响应体**（**就地扩充**现有 `#### 返回示例` 小节：保留现有 JSON 示例 + 前置字段表，将标题改为 `#### 响应体`）：

```markdown
#### 响应体

成功（code = 0）：

| 字段 | 类型 | 必填 | 范围/约束 | 说明 |
|---|---|---|---|---|
| `data.items` | array | 必填 | length ≥ 0（**不**分页 / 无上限） | 已 enabled 的表情列表（全量返回，与 §11.1 服务端逻辑步骤 4 "不分页" 对齐 —— **不**设条目数上限）；按 `sort_order` 升序 + 次要键 `id` 升序排列；列表可能为空（如 seed 未执行 / 全部 disabled）—— 空时返回 `items: []`，**不**返回 `null`；MVP 阶段 enabled 表情数量较少（Story 17.3 seed 钦定 8-10 个），未来如需限制由对应 epic 引入分页 query 参数（如 `?limit=N&offset=M`）+ 服务端 `LIMIT/OFFSET` SQL 改造，本接口契约**不**预留 |
| `data.items[].code` | string | 必填 | 1 ≤ length ≤ 64；只允许 `[a-z0-9_-]`（与数据库 `emoji_configs.code VARCHAR(64)` + `UNIQUE KEY uk_code` 一致） | 表情业务标识符（如 `"wave"` / `"love"`）；client 用作 `emoji.send.payload.emojiCode` 入参；全局唯一（DB 唯一约束保证） |
| `data.items[].name` | string | 必填 | 1 ≤ length ≤ 64 | 表情中文名（如 `"挥手"` / `"爱心"`），client 用作 UI 展示文字；DB 来源 `emoji_configs.name` |
| `data.items[].assetUrl` | string | 必填 | 0 ≤ length ≤ 255 | 表情资源 URL（PNG / GIF / WebP / SVG 等图片资源）；MVP 阶段允许 placeholder URL（如 `https://placehold.co/64x64?text=Wave`）；可空字符串 `""`（首次 seed 未上传时；client 解析层应按 `String` 处理并降级为问号 fallback —— 与数据库 `emoji_configs.asset_url DEFAULT ''` 一致） |
| `data.items[].sortOrder` | number (int) | 必填 | 0 ≤ value ≤ 2^31 - 1 | 表情显示顺序（升序）；DB 来源 `emoji_configs.sort_order`；client 接收时**不**需要二次排序（server 已按 `sort_order ASC, id ASC` 排序） |

JSON 示例（保留现有，无需修改）：

\`\`\`json
{
  "code": 0,
  "message": "ok",
  "data": {
    "items": [
      {
        "code": "wave",
        "name": "挥手",
        "assetUrl": "https://...",
        "sortOrder": 1
      },
      {
        "code": "love",
        "name": "爱心",
        "assetUrl": "https://...",
        "sortOrder": 2
      }
    ]
  },
  "requestId": "req_xxx"
}
\`\`\`

JSON 示例（空列表 edge case）：

\`\`\`json
{
  "code": 0,
  "message": "ok",
  "data": {
    "items": []
  },
  "requestId": "req_xxx"
}
\`\`\`

> **注**：本接口**不**下发 `id` / `is_enabled` / `created_at` / `updated_at` 字段 —— `id` 是数据库主键 client 不需要（client 用 `code` 作为业务标识符）；`is_enabled` 已在 server 端过滤（仅下发 enabled 表情）；时间戳字段 client 无 UI 用途。
>
> **client 缓存契约**：表情列表是**静态配置**，client **应**在 App 生命周期内首次拉取后缓存，后续不再重复拉取（iOS Story 18.1 实装：表情面板首次显示 → GET /emojis → 缓存到 ViewModel / Singleton；后续表情面板显示直接读缓存）。如 server 端表情配置变更（admin 后台改 `is_enabled` / `sort_order` / 新增 emoji），client 需要重启 App 或主动刷新才能看到新值 —— 节点 6 MVP 阶段无 push 通知机制，可接受；后续若产品要求"实时刷新表情列表"，由对应 epic 决定是否引入 ETag / If-None-Match / WS 通知 emoji_configs.changed 等机制。
```

**§11.1.5 可能的错误码**（**新增** H4 块）：

```markdown
#### 可能的错误码

| code | message | 触发条件 |
|---|---|---|
| 1001 | 未登录 / token 无效 | auth 中间件拦截（无 Authorization 头 / token 非法 / token 过期 / token 解析失败） |
| 1005 | 操作过于频繁 | rate_limit 中间件拦截（**已认证路由**按 `user_id` 限频，每用户每分钟 > 60 次；按 Story 4.5 默认值，配置可调） |
| 1009 | 服务繁忙 | DB 异常（SELECT 执行返回 `err != nil`，含 driver / 网络 / 慢查询超时等）/ 内部 panic（见 Story 17.4 service 实装） |

> **注**：本接口**不**触发 1002 参数错误（无 query / body 字段可校验）/ **不**触发 7001 表情不存在（这是列表查询，无单 emoji code 校验，7001 仅在 `emoji.send` WS 路径校验失败时触发，详见 §12.2 `### 发送表情` 错误响应段）/ **不**触发 6xxx 房间相关错误（本接口与房间无关）/ **不**触发 3xxx / 4xxx / 5xxx（步数 / 宝箱 / 装扮）。
```

**关键约束**：

- **不分页**：表情列表全量返回，最大长度 50（实际 MVP 阶段 4-20 个）；client 不需要处理"加载更多" / "下一页" UI；如未来表情数量增长到需要分页（业务上低概率），由对应 epic 重新评审契约
- **不接受 query 参数**：本接口不支持任何 query string 筛选（如 `?category=greeting`）/ 排序（如 `?orderBy=name`）/ 分页（如 `?page=1`）；client 端如需 UI 层分类，自己实装本地分组 —— server 不引入分类字段（MVP 节点 6 不规划表情分类，KISS）
- **响应空列表 `items: []` 与 `items: null` 语义不同**：server **永远**返回 `items: []`（空数组）而**不**返回 `null`；client 解析层按"数组"假设处理（与 §10.3 `data.members[]` / §5.1 `data` 数组字段处理一致）—— `null` 会触发 Swift Codable 解析失败（除非 client 把字段标 Optional，但本字段为必填）
- **assetUrl 可空字符串 `""`**：与数据库 `emoji_configs.asset_url VARCHAR(255) NOT NULL DEFAULT ''` 一致；首次 seed 未上传 asset 时为空；client 解析层**应**按 `String` 处理（不是 Optional），并在 UI 层降级（如显示问号占位 / 不渲染图片）；**禁止** server 下发 `null` 触发 client 解析失败
- **BIGINT 主键字段**（`emoji_configs.id`）**不**下发 → 无 §2.5 字符串化诉求；其余字段都是 string / int / int32，遵循 §2.5 全局约定（int 不字符串化）
- **`sortOrder` 不字符串化**：是普通 int 顺序字段，**不**是主键，不受 §2.5 BIGINT 字符串化规则约束（与 §11.1 `data.items[].sortOrder` 同语义）

**AC3 — §12.2 `### 发送表情`（emoji.send）字段层锚定**

V1 文档 §12.2 `### 发送表情` 编辑后必须包含完整内容（紧接现有 H3 标题 + JSON 示例之后，**就地扩充** —— 保留草稿 JSON 示例 + 末尾"业务消息延后锚定"引用块）：

```markdown
### 发送表情

**触发**：

- iOS 客户端在房间页面用户选中表情面板（Story 18.1）中某个表情时，由 SendEmojiUseCase（Story 18.3）通过已建立的 WebSocket 连接（Story 12.2 WebSocketClient）发送 `emoji.send` text frame；client 在发送的**同时**触发本地立即动效（不等 server `emoji.received` 回信），与 server 广播解耦
- 节点 6 阶段**仅** Story 18.3 一处触发；其他客户端流程（如自动重连 / 心跳）**都不**触发 `emoji.send`

**字段**（基于 §12.2 通用消息信封 + 业务字段）：

| 字段 | 类型 | 必填 | 范围/约束 | 说明 |
|---|---|---|---|---|
| `type` | string | 必填 | 固定值 `"emoji.send"` | 消息类型，遵循 §12.2 通用信封约束（全小写点分 + `[a-z0-9.]`） |
| `requestId` | string | 选填 | 0 ≤ length ≤ 64 字符 | client 生成的请求 ID，遵循 §12.2 通用信封约束；server 处理失败时（如 7001 / 6004）回 `error` 消息**回带**该 `requestId`；server 处理成功后广播的 `emoji.received` **不**回带 `requestId`（广播类消息固定 `""`，详见 §12.3 通用信封）；client 端推荐格式 `"emoji_<seq>"` / `"emoji_<ts_ms>"`，方便与 `error` 响应配对 |
| `payload.emojiCode` | string | 必填 | 1 ≤ length ≤ 64 字符；只允许 `[a-z0-9_-]`（与 §11.1 `data.items[].code` 同语义 + 数据库 `emoji_configs.code` 同语义） | 客户端选中的表情业务标识符；server 校验该 `emojiCode` 必须在 `emoji_configs` 中存在 + `is_enabled=1`，否则回 `error.payload.code = 7001`（见下方"错误响应"段） |

JSON 示例（保留现有，无需修改 —— 与本节字段表对齐）：

\`\`\`json
{
  "type": "emoji.send",
  "requestId": "msg_001",
  "payload": {
    "emojiCode": "wave"
  }
}
\`\`\`

> **注**：本消息走 §12.2 client → server 通用信封（**不**带 `ts` 字段 —— 客户端发起方向不需要 client 时间戳，详见 §12.2 通用消息信封字段表 + 注），且 `payload` 仅 `emojiCode` 一个字段（保持最小契约）。

**服务端逻辑**（Story 17.5 实装锚定）：

1. **接收 & 解析**：server WS dispatcher（Story 10.3）按 `type = "emoji.send"` 路由到 EmojiHandler；解析 `payload.emojiCode` 字段
2. **参数校验**：`emojiCode` 必填 + 类型为 string + 1 ≤ length ≤ 64 + 字符集 `[a-z0-9_-]`；任何不通过 → 回 `error.payload.code = 1002` "参数错误"（**响应类** error，`requestId` 回带 `emoji.send.requestId`），**不**广播
3. **房间归属校验**（**权威源 = 收到 `emoji.send` frame 的 WS Session 上携带的 `roomID`** —— 该 `Session.roomID` 由 §12.1 握手 path `/ws/rooms/{roomId}` 写入、并已在 §12.1 校验顺序步骤 5 通过 `room_members WHERE user_id=? AND room_id=?` 校验，**是协议层钦定的"本次 WS 连接所属房间"权威源**；详见 §9.1 `Session` 字段表 + §12.1 校验顺序步骤 4 / 5）：`SELECT current_room_id FROM users WHERE id = ?`（当前 user.id 来自 WS 握手 token；查得后**必须**与 `Session.roomID` 比对 —— **不可仅判 `!= NULL`**，否则 stale Session 跨房间注入风险无法封堵，详见本步骤末"r1 review 锁定的反 stale-Session 校验" 注）
   - **`current_room_id == NULL`**（用户当前不在任何房间 —— 与 §10.4 / §10.5 房间归属同义）→ 回 `error.payload.code = 6004` "用户不在房间中"（**响应类** error，`requestId` 回带 `emoji.send.requestId`），**不**广播
   - **`current_room_id != NULL` 但 `current_room_id != Session.roomID`**（用户在房间 A 持有 stale Session、`users.current_room_id` 已切到房间 B —— 来源场景：(a) §10.5 leave 事务步骤 7 的 close 4007 是 best-effort cleanup，4007 frame 写入失败 / 客户端尚未读到时 leaver Session 在内存中残留；(b) 多设备 / 跨标签场景：用户 A 在设备 1 持房间 X Session、在设备 2 通过 HTTP join 房间 Y 后 `users.current_room_id` = Y、设备 1 Session 未收到任何 close 仍在；(c) 客户端实装 bug 持 stale socket 继续发送）→ 回 `error.payload.code = 6004` "用户不在房间中"（**响应类** error，`requestId` 回带 `emoji.send.requestId`），**不**广播；同时 server **应** log warn 级别记录该跨房间发送企图（含 `userId` / `Session.roomID` / `users.current_room_id`），便于排查多设备 stale Session 残留
   - **`current_room_id != NULL` 且 `current_room_id == Session.roomID`** → 记录 `currentRoomId = Session.roomID` 进入步骤 4

   > **r1 review 锁定的反 stale-Session 校验**：仅判 `current_room_id != NULL` **不足以**保证 `emoji.send` 广播范围正确。`GET /ws/rooms/{roomId}` 的 path 已经决定了"本次 WS 连接所属房间"，但 `users.current_room_id` 可在 WS 连接生命周期内被 HTTP `POST /rooms/{roomId}/leave` + `POST /rooms/{otherRoomId}/join` 改变；若仅以 `users.current_room_id` 为广播目标，stale Session（房间 A）发来的 `emoji.send` 会被广播到当前 `users.current_room_id` = 房间 B，造成**跨房间消息注入**。该校验改用 `Session.roomID` 作为广播目标 + 同时比对 `users.current_room_id == Session.roomID`，与 §10.4 / §10.5 HTTP 房间接口 ACL 行为对齐（HTTP 路径用 path roomId 校验，WS 路径同样用握手 path roomId 校验）。
4. **表情合法性校验**：`SELECT 1 FROM emoji_configs WHERE code = ? AND is_enabled = 1`（按入参 `emojiCode` 查）
   - **0 行**（`emojiCode` 不存在 / 或存在但 `is_enabled=0` —— 两种情况合并为同一错误，避免 server 暴露 enabled / disabled 状态信息）→ 回 `error.payload.code = 7001` "emoji not found"（**响应类** error，`requestId` 回带 `emoji.send.requestId`），**不**广播
   - **1 行** → 进入步骤 5
5. **广播（fire-and-forget）**：调用 `BroadcastToRoom(Session.roomID, {type: "emoji.received", payload: {userId, emojiCode}})`（**广播目标 = `Session.roomID`，不是 `users.current_room_id`** —— 二者在步骤 3 已校验相等，此处显式以 `Session.roomID` 为参表达"本次 WS 连接所属房间是广播权威源"，详见 §12.3 `### 收到表情广播`）；广播失败仅 log warning，**不**回 error 给发起者（与 Story 11.8 `member.joined` / Story 14.4 `pet.state.changed` 广播失败语义一致；**注**：与 `pet.state.changed` 的区别 —— `pet.state.changed` HTTP 路径仍返回 200 OK；`emoji.send` 是 WS 路径，无 HTTP 响应，server 端"成功"= 仅完成上述 5 步 + 广播尝试，**不**回 ack 消息给发起者；如发起者需要"server 确认收到"的信号，依赖广播范围**包含发起者自己**的语义 —— 发起者会收到自己的 `emoji.received`，那就是 server 确认信号）
6. **不入库**：表情事件**不**持久化到任何表（按数据库设计 §14.3，MVP 不强制落库表情事件日志；与 `pets.current_state` UPDATE 持久化的 `pet.state.changed` 语义**不同**）

**WS 广播 vs 客户端本地动效的关系（含发起者自己的 self-broadcast 去重规则）**：`emoji.send` 是 client → server 单向消息，**无**像 HTTP 那样的 server response 信号；server 处理后通过 `emoji.received` 广播事件通知（包含发起者自己）。client 端发出 `emoji.send` 后**应**立即在本地触发自己的动效（不等 server 广播 —— iOS Story 18.3 钦定），后续 server 广播到达时按以下规则去重：

- **对"别人的表情"的权威信号**：以 WS 广播 `emoji.received` 为**唯一**信号（与 CLAUDE.md §"工作纪律 / 状态以 server 为准"一致）；client **不**通过任何其他渠道驱动别人的动效
- **对"发起者自己的表情"的去重规则（与 §5.2 `pet.state.changed` self-broadcast 兜底规则**语义不同**）**：考虑到表情是 **transient UI 事件**（server 不持久化、UI 上动效结束即消失、状态对齐不存在"stale 风险"），client **应**对 `emoji.received.payload.userId == 当前 user.id` 走"跳过 / 不重复触发动效"路径（iOS Story 18.4 钦定）。理由：(a) 本地动效已在 SendEmojiUseCase 触发瞬间播完；(b) 重复触发会让发起者看到"两次同一动效"，UX 差；(c) 与 `pet.state.changed` self-broadcast **必须接收并 merge** 的语义**不同** —— `pet.state.changed` 是持久化状态变更，self-broadcast 是双轨之一防 stale；`emoji.received` 是 transient 事件，本地动效已是完整 UI 体验，self-broadcast 仅作为 server 端 "广播路径完整执行了" 的探测信号（client 端 log debug 即可，不驱动 UI）
- **广播失败容忍**：如 `emoji.received` 广播因网络抖动 / Session 已 close 失败（含发起者自己 Session 失败 → 自己收不到 self-broadcast），fire-and-forget 仅 log warning；client 端不应假设每条 `emoji.send` 都能 server-acknowledged via self-broadcast，本地动效是发起者的**主要** UX 反馈（与 18.3 钦定一致）；网络极差时 server 也可能根本没收到 `emoji.send`（WS 连接断开）→ 同样仅本地动效 + 18.3 钦定弹温和 toast 提示"网络不佳，对方可能看不到"（toast 不阻塞 UI）

**错误响应**：

| code | message | 触发条件 | 失败后 client 推荐处理 |
|---|---|---|---|
| 1002 | 参数错误 | `payload.emojiCode` 字段缺失 / 类型非 string / length 不在 1 ~ 64 / 字符不在 `[a-z0-9_-]`（与 §3 错误码表 1002 一致） | client 端代码 bug（按 §11.1 缓存的表情列表选取，不应触发）；不重试，log error 上报 |
| 6004 | 用户不在房间中 | service 层步骤 3 查到 (a) `users.current_room_id == NULL`，**或** (b) `users.current_room_id != NULL` 但 `users.current_room_id != Session.roomID`（即"用户已切到别的房间但仍在本 WS Session 上发送" stale-Session 跨房间企图，详见步骤 3 r1 review 锁定的反 stale-Session 校验）；两类合并报同一 `6004`，避免 server 暴露具体房间归属细节（与 §3 错误码表 6004 一致；该错误码原由 §10.4 / §10.5 房间 HTTP 接口触发，本接口是 WS 路径首次复用） | client 端房间状态与 server 不一致（本地以为在该房间，server 已踢出 / 已切到别的房间）；UX 提示用户"未在房间中"，回退到主界面 + 刷新 GET /home 状态；不重试本次发送 |
| 7001 | emoji not found | service 层步骤 4 查到 `emojiCode` 不在 `emoji_configs` / 或存在但 `is_enabled=0`（与 §3 错误码表 7001 一致） | client 端缓存的表情列表与 server 不一致（如 admin 临时下架某表情但 client 未刷新缓存）；建议清除本地表情缓存 + 下次表情面板打开时重新 GET /emojis；本次发送已失败（不重试） |

错误响应通过 §12.3 `error` 消息回送，遵循 `error` 消息字段表（`type: "error"` / `requestId` 回带 `emoji.send.requestId` / `payload.code` / `payload.message` / `ts`）。

JSON 示例（7001 错误响应）：

\`\`\`json
{
  "type": "error",
  "requestId": "msg_001",
  "payload": {
    "code": 7001,
    "message": "emoji not found"
  },
  "ts": 1776920345000
}
\`\`\`

JSON 示例（6004 错误响应）：

\`\`\`json
{
  "type": "error",
  "requestId": "msg_001",
  "payload": {
    "code": 6004,
    "message": "用户不在房间中"
  },
  "ts": 1776920345000
}
\`\`\`

**关键约束**：

- **client 端发送约束**：iOS client 在调用 `WebSocketClient.send` 前**应**校验 `emojiCode` 来自 §11.1 缓存的合法表情列表（取 `data.items[].code` 字段值），**禁止**直接 hardcode `emojiCode` 字面量（避免 client / server 不同步导致 7001 频发）；如 §11.1 GET /emojis 尚未拉取过 → client 应先拉取再启用表情面板（与 iOS Story 18.1 表情列表缓存契约一致）
- **房间归属校验顺序 + 权威源**：server 端先做参数校验（1002）再做房间归属校验（6004）再做表情校验（7001）—— 这是契约层钦定的校验顺序，避免出现"参数不合法但报房间问题"误导 client；server 实装层（Story 17.5）严格按此顺序。**房间归属权威源 = `Session.roomID`（握手 path 写入，§12.1 步骤 5 已校验），不是 `users.current_room_id`**：步骤 3 必须比对 `users.current_room_id == Session.roomID`，仅判 `!= NULL` 不足以封堵 stale-Session 跨房间注入风险（详见 r1 review 锁定的反 stale-Session 校验注），与 §10.4 / §10.5 HTTP 房间接口"用 path roomId 校验" ACL 语义对齐
- **不限频**：节点 6 阶段 server **不**对 `emoji.send` 做特殊限频（仅走 §12.1 / Story 10.4 心跳层 + Story 4.5 通用 rate_limit 中间件按 user_id 每分钟 60 次默认 —— **注**：rate_limit 中间件挂在 HTTP 路由，**不**挂 WS 路由，故 `emoji.send` 实际**不**走 1005 限频拦截；如需限频，由 future tech debt 处理）；epics.md §Story 17.5 已钦定"限频建议：同一用户每秒最多 5 个表情（防止刷屏，可在 Story 4.5 rate_limit 基础上扩展）—— MVP 可不做，但 tech debt 登记"，本契约层**不**预留限频错误码字段
- **不入库**：表情事件**不**持久化（按数据库设计 §14.3）—— 与 `pet.state.changed` 持久化到 `pets.current_state` 不同；reconnect 后**不**重放历史表情事件（无历史可重放），与 `room.snapshot` 不下发"近期表情列表"语义一致
- **client → server active message set 升级**：自本 story 起，`emoji.send` 正式加入 **client → server active message set**（继 Epic 10 `ping` 之后的首次扩展，详见 §12.3 末尾"server / client active message set 升级"块）

> **业务消息延后锚定**：上文 `### 发送表情`（`emoji.send`）已由 **Story 17.1** 锚定字段表 + 触发条件 + 关键约束 —— 自此**接入** Epic 17 / 节点 6 client → server active message set（具体升级见 §12.3 末尾"server / client active message set 升级"块）。
>
> **节点 6 阶段 client → server active message set**：`ping`（Epic 10 锚定）+ **`emoji.send`（本 story 锚定）**；其他 `type` 值 client **不**应发送，server 收到非 active set 内消息当前阶段**忽略**（log warn + 不 close 连接，避免兼容性问题；具体行为 Story 17.5 实装时锁定，与 Story 10.3 既有规则一致）。
```

**AC4 — §12.3 `### 收到表情广播`（emoji.received）字段层锚定**

V1 文档 §12.3 `### 收到表情广播` 编辑后必须包含完整内容（紧接现有 H3 标题 + JSON 示例之后，**就地扩充** —— 保留草稿 JSON 示例）：

```markdown
### 收到表情广播（emoji.received）

**触发**：

- WS 客户端发送 `emoji.send`（详见 §12.2 `### 发送表情`）→ server 端 EmojiHandler 完成 5 步校验（鉴权 / 参数校验 / 房间归属校验 **= `Session.roomID == users.current_room_id`** / 表情合法性校验 / 广播）全部通过后，service 层调用 `BroadcastToRoom(Session.roomID, {type: "emoji.received", payload: {userId, emojiCode}})` 广播给该房间内所有当前在线 Session（**包含**发起者自己 —— 与 `### 成员加入` `### 成员离开` 排除发起者**不同**语义，与 `### 宠物状态变更（pet.state.changed）` 包含发起者同语义）；广播失败 fire-and-forget（仅 log warning，**不**回 error 给发起者）
- 节点 6 阶段**仅** Story 17.5 一处触发；任何 WS 层事件（含握手、心跳超时、断开重连、用户进出房间）**都不**触发 `emoji.received`；后续 epic 不在本路径加新触发条件（如假设性的"server 主动广播促销 emoji"等不在 MVP 范围）

**字段**：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `type` | string | 必填 | 固定值 `"emoji.received"` |
| `requestId` | string | 必填 | 固定 `""`（主动推送类消息，遵循 §12.3 通用信封；**不**回带 `emoji.send.requestId` —— 广播 fanout 给房间内所有 Session，server 端无法对所有接收者都"配对" `emoji.send.requestId`；发起者自己的 self-broadcast 也走广播路径，故 `requestId` 同样固定 `""`） |
| `payload.userId` | string | 必填 | 发送表情的 user 主键（BIGINT 字符串化，遵循 §2.5）；来自 `emoji.send` 当前 user.id（即 WS 握手 token 解码后的 user.id） |
| `payload.emojiCode` | string | 必填 | 客户端发送的表情业务标识符；server 已在 §12.2 `### 发送表情` 服务端逻辑步骤 4 校验过该 `emojiCode` 必然存在于 `emoji_configs` 且 `is_enabled=1`（client 收到 `emoji.received` 时无需再次校验 —— server 端为 single source of truth）；与 §11.1 `data.items[].code` 同语义；client 用作查找 §11.1 缓存表情列表的 key，定位 `assetUrl` / `name` 等渲染所需字段 |
| `ts` | number (int64) | 必填 | 服务端发送时间戳（ms）；遵循 §12.3 通用信封约束 |

JSON 示例（保留现有，无需修改 —— 与本节字段表对齐）：

\`\`\`json
{
  "type": "emoji.received",
  "requestId": "",
  "payload": {
    "userId": "1002",
    "emojiCode": "wave"
  },
  "ts": 1776920345000
}
\`\`\`

**关键约束**：

- 广播范围：**该房间内所有当前在线 Session**（**包含**发起者自己）—— 与 `### 成员加入` 排除加入者自己、`### 成员离开` 排除离开者自己的语义**不同**；与 `### 宠物状态变更（pet.state.changed）` 包含发起者**同**语义。原因：(a) `member.joined` / `member.left` 是"成员关系变化通知"，发起者已通过 HTTP response 知道结果，再收一份 WS 通知是冗余；(b) `emoji.received` 是"表情事件本身"，server **不**区分 broadcast 接收者是发起者 / 其他成员（统一发给房间内全员），让 client 端用单一 WS 入口处理表情事件，**简化 server 实装**。**发起者自己 self-broadcast 的 UI 处理职责（与 `pet.state.changed` 语义不同）**：client 端**应**对 `payload.userId == 当前 user.id` 走"跳过 / 不触发动效"路径（iOS Story 18.4 钦定）—— 表情是 transient UI 事件，本地动效已在 SendEmojiUseCase 触发瞬间（Story 18.3）播完，self-broadcast 仅作为 server 端 "广播路径完整执行了" 的探测信号（log debug，不驱动 UI）；这与 `pet.state.changed` "self-broadcast 与 HTTP 200 ack 任一先到都驱动 UI" 的双轨兜底**不同**（`pet.state.changed` 是持久化状态变更，self-broadcast 是状态对齐的双轨之一；`emoji.received` 是 transient 事件，无 stale 风险，跳过自己 echo 完全无副作用）
- `payload.userId` / `payload.emojiCode` 都必填（**禁止** payload 为 `{}` 或缺任一字段）；缺字段视为契约违反，client 解析层**应**走"安全忽略 + log warn"路径（与 Story 10.1 `安全忽略未识别 type` 相同的容错策略，避免单条 malformed 消息把房间页搞崩）
- client 收到 `emoji.received` 后**应**按以下规则处理（与 §12.3 `room.snapshot` client merge contract 字段级 merge **不同语义** —— 表情是 transient 事件，**不**进入 client roster / 持久化状态）：
  - (a) `payload.userId == 当前 user.id`（self-broadcast）→ **跳过**（本地动效已播过）；log debug "self-emoji-broadcast received"
  - (b) `payload.userId ∈ 当前房间 roster` 且 `payload.userId != 当前 user.id` → 在该成员 PetSpriteView 上方触发飞出动效（iOS Story 18.4 实装）
  - (c) `payload.userId` 不在 roster（**合法 race window**，**不**作契约违反处理）—— 触发场景：sender A 发送 `emoji.send` 后**立即** leave 房间，server 端 `emoji.received` 广播与 `member.left` 广播走**不同**服务端路径（Story 17.5 EmojiService BroadcastToRoom vs Story 11.x RoomService leave 路径），到达 receiver B 的物理顺序**不保证**严格一致；可能出现 receiver B 先收 `member.left`（已把 A 从本地 roster 移除）→ 后收 A 的 `emoji.received`，此时本地 roster 已查不到 A。client **不**得简单 drop，应执行**降级渲染**：① 优先使用 `payload` 自带字段（`userId` / `emojiCode`）渲染飞出动效；② 由于 `emoji.received` payload **不**自带 sender 头像 / 昵称（见本节"Future Fields"，仅含 `userId` + `emojiCode`，渲染所需的 `assetUrl` / `name` 由 §11.1 表情缓存查），client 端无法在该用户原 PetSpriteView 位置触发动效（其 sprite 已随 `member.left` 移除）→ 降级到**房间中心位置**（或屏幕中央安全区）展示该表情飞出动效；log info "emoji.received after member.left race, fell back to center anchor"（**不** log warn / error —— 这是预期合法行为）；iOS Story 18.4 实装层需具体落实"无 anchor 时的中心位置降级"渲染策略
  - (d) `payload.emojiCode` 不在 client 已缓存的表情列表（理论不该 —— server 已在 §12.2 服务端逻辑步骤 4 校验 emojiCode 合法；除非 race condition：用户 A 持 stale cache，用户 B 发送新 seed 的 emoji，A 收到 emoji.received 时本地 cache 找不到）→ 显示 fallback 占位（如问号 / 默认 emoji icon），**不**报错（与 §12.3 `pet.state.changed` `payload.currentState` 默认 fallback 渲染同语义）
- **广播 fire-and-forget**：如该房间内某些 Session 因网络抖动 / 已 close / SessionManager 状态不一致导致广播失败，server **不**重试（仅 log warning）—— client 实装层（iOS Story 18.x）**不**应假设每条 `emoji.received` 都到达；表情是 transient 事件，**不**存在"状态对齐 fallback"路径（与 `pet.state.changed` 不同 —— `pet.state.changed` 有 reconnect 后 `room.snapshot` 全量重新对齐兜底；`emoji.received` 没有兜底，丢失即丢失，UX 表现为"对方看不到我的表情"，是可接受的弱网降级行为）。这条 fire-and-forget 语义与 Story 10.5 BroadcastToRoom primitive、Story 11.8 `member.joined` / `member.left` / Story 14.4 `pet.state.changed` 广播失败语义一致
- `ts` 字段（int64 ms）来源是 server 端 `time.Now().UnixMilli()`（具体调用方式由 Story 17.5 实装层决定），与 `### 成员加入` / `### 成员离开` / `### 宠物状态变更` 的 `ts` 字段语义一致；用途**仅限**客户端日志关联 + UI 辅助展示，**禁止**用作业务排序 / 表情新旧判定（与 §12.2 line 1961 全局 WS envelope `ts` 字段约束一致）；表情新旧判定由 **同一 WS 连接内消息的物理到达顺序（FIFO 保证）** 决定，**不**依赖 `ts` 字段
- **不持久化**：server 端**不**把 `emoji.received` 事件写入任何表（与 `pet.state.changed` 触发的 `pets.current_state` UPDATE **不同**；表情按数据库设计 §14.3 不入库）；client 端 reconnect 后**收不到**历史表情事件（无历史可重放，与 `room.snapshot` 不下发"近期表情列表"语义一致）；如未来产品要求"历史表情回放"或"未读提醒"，由对应 epic 决定是否引入 `emoji_events` 持久化表 + 接口

> **Future Fields（节点 6 阶段为占位 / 后续 epic 落地）**：
>
> - 节点 6 阶段 `emoji.received` payload 仅含 `userId` / `emojiCode`，**不**含 `assetUrl` / `name` 等渲染字段（client 端通过 §11.1 缓存的表情列表查 `emojiCode` 对应的 `assetUrl` / `name` —— 避免每次广播都重复传递静态配置数据，减少 WS 流量）。后续若产品要求"广播携带动效配置"（如 server 控制每个表情的动画时长 / 路径），由对应 epic 单开 WS 消息或扩展 `emoji.received` payload，本 story 不预留字段。
> - 节点 6 阶段**不**支持"表情自定义文案"（如类似 Slack reaction 的自定义文字），server 不广播自定义文本字段；如未来产品引入文本表情或 reaction，由对应 epic 决定是否新增字段或独立 WS 消息。
```

**AC5 — §12.3 末尾"业务消息延后锚定"引用块升级**

V1 文档 §12.3 末尾"业务消息延后锚定"引用块（行 2349-2362）必须升级：

1. **状态行升级**：把 `emoji.received` 从"待 Story 17.1 锚定"升级为"**已由 Story 17.1 锚定**"，整段文案改写为：

```markdown
> **业务消息延后锚定**：上文 `### 成员加入` / `### 成员离开` / `### 宠物状态变更` / `### 收到表情广播` 四个小节已全部锚定 —— 已由 **Story 11.1**（Epic 11 房间业务契约，节点 4 中段；锚定 revision 见 `git log --grep='story-11-1'` 检出的 Story 11.1 收官 commit `chore(story-11-1): ...`，以及 `_bmad-output/implementation-artifacts/sprint-status.yaml` 中 `11-1-接口契约最终化` 状态行）/ **Story 14.1**（Epic 14 宠物状态同步契约，节点 5；锚定 revision 见 `git log --grep='story-14-1'` 检出的 Story 14.1 收官 commit `chore(story-14-1): ...`，以及 `_bmad-output/implementation-artifacts/sprint-status.yaml` 中 `14-1-接口契约最终化` 状态行）/ **Story 17.1**（Epic 17 表情广播契约，节点 6；锚定 revision 见 `git log --grep='story-17-1'` 检出的 Story 17.1 收官 commit `chore(story-17-1): ...`，以及 `_bmad-output/implementation-artifacts/sprint-status.yaml` 中 `17-1-接口契约最终化` 状态行）正式锚定字段表 + 触发条件 + 关键约束 —— 自此**接入** Epic 11 / Epic 14 / Epic 17 / 节点 4 / 节点 5 / 节点 6 server / client active message set。所有 §12.3 业务消息（`member.joined` / `member.left` / `pet.state.changed` / `emoji.received`）的字段层契约已 100% 锚定，节点 6 之后无新增业务消息字段层契约任务。
>
> 各消息字段层契约的正式锚定 epic 如下（**已升级状态以粗体标注**）：
>
> - `member.joined` / `member.left` → **Story 11.1（已锚定）**（Epic 11 房间业务契约，节点 4 中段）
> - `pet.state.changed` → **Story 14.1（已锚定）**（Epic 14 宠物状态同步契约，节点 5）
> - `emoji.received` → **Story 17.1（已锚定）**（Epic 17 表情广播契约，节点 6）
```

2. **server / client active message set 升级块**（紧接上方"已升级状态"段之后）：

```markdown
> **server / client active message set 升级**（自 Story 11.1 起生效，覆盖 Story 10.1 r10 钉死的"Epic 10 阶段冻结"边界；自 Story 14.1 起 server → client 集合再次扩展；自 Story 17.1 起 server → client + client → server 双向集合再次扩展）：
>
> - **server → client active message set**：`room.snapshot` / `pong` / `error`（Epic 10 锚定）+ `member.joined` / `member.left`（Story 11.1 锚定）+ `pet.state.changed`（Story 14.1 锚定）+ **`emoji.received`（本 story 锚定）**
> - **client → server active message set**：`ping`（Epic 10 锚定）+ **`emoji.send`（本 story 锚定，节点 6 起 client 首次获得新业务消息发送能力）**
>
> 客户端 Story 12.3 / 12.4 / 15.2 / 18.4 解析层在节点 4 / 节点 5 / 节点 6 阶段**应**对未识别的 `type` 值走"安全忽略 + log warn"路径，**不**因未识别消息 close 连接 / crash app（Story 10.1 既有规则，本 story 沿用）。
```

**AC6 — §1 末尾追加节点 6 表情业务契约冻结声明**

V1 文档 §1 末尾必须追加一段冻结声明（与节点 2 / 节点 3 / 节点 4 协议骨架冻结声明 / 节点 4 房间业务冻结声明 / 节点 5 宠物状态业务冻结声明并列，**不**覆盖 / 删除现有冻结声明）：

```markdown
- 自 2026-05-13（Story 17.1 完成日，对应 git commit hash 见 commit message）起，§11.1（GET /api/v1/emojis）节点 6 REST 接口的 schema + §12.2 `### 发送表情`（`emoji.send`）+ §12.3 `### 收到表情广播`（`emoji.received`）两个节点 6 业务 WS 消息的字段表进入**冻结**状态。
- 任何字段名 / 字段类型 / `emojiCode` 字符集约束（`[a-z0-9_-]` + length 1-64） / 错误码（1001 / 1002 / 1005 / 1009 / 6004 / 7001）触发条件 / `emoji.received` payload 字段（`userId` / `emojiCode`）+ 顶层 envelope 字段（`type` / `requestId` / `ts`，遵循 §12.3 通用信封）/ 广播范围（**包含**发起者自己，与 `pet.state.changed` 同语义）/ fire-and-forget 语义 / **表情不持久化语义**（与 `pet.state.changed` 持久化 `pets.current_state` 不同）/ **client 端对 self-broadcast 的去重规则**（跳过 `payload.userId == 当前 user.id` 的 echo，与 `pet.state.changed` self-broadcast 双轨兜底**不同**语义）/ GET /emojis 不分页 + 不接受 query 参数 + 不返回 `is_enabled=0` 表情等核心契约的修改都必须（**冻结边界说明**：1005 的"触发条件"冻结在**抽象层** —— "走通用 rate_limit 中间件按 `user_id` 维度限频拦截"——这一不变量；**具体阈值**（如 60/min）由 Story 4.5 默认值 + 配置层管理，调整阈值**不**视为本接口契约变更；同理 7001 / 6004 的"触发条件"冻结在抽象层 —— "走 emoji_configs `is_enabled=1` 查询 / 走 `users.current_room_id == Session.roomID` 校验（含 NULL → 6004 + 不匹配 → 6004 两类，r1 review 锁定后由 §12.2 步骤 3 钦定）"这一不变量；**具体 DB 查询路径调优**（如加索引 / 改 cache）**不**视为契约变更）：
  1. 触发 iOS Epic 18 重新评审（影响 Story 18.1 / 18.2 / 18.3 / 18.4 所有表情面板 + 发送 + 接收动效 story）
  2. 触发 server Epic 17 已完成 story 的回归（影响 Story 17.2 / 17.3 / 17.4 / 17.5 已落地的 migration / seed / handler / dispatcher / 广播路径）
  3. 在本 story 文件 + epics.md 同步标注变更原因 + 影响范围
- `emoji_configs.code` 字符集约束（`[a-z0-9_-]` + length 1-64，与 §5.15 数据库 `VARCHAR(64)` 字段一致）属契约一部分；新增字符集字符（如允许 `.` / `:` 等点分形式）视为契约变更；`emoji_configs.is_enabled` 仅 0 / 1 两值（与 §6 状态枚举一致）属契约一部分。
- `emoji.received` 广播范围（**包含**发起者自己）属契约一部分；与 `member.joined` / `member.left` 排除发起者 / 离开者**不同**语义、与 `pet.state.changed` **同**语义，是显式设计选择（表情是事件本身，让 client 用统一 WS 入口处理；server 不区分接收者）；如未来需要切换为排除发起者，视为契约变更。
- `emoji.received` fire-and-forget 语义（广播失败仅 log，不回 error 给发起者）属契约一部分；如未来需要切换为强一致（如广播失败回错给发起者），视为契约变更。
- `emoji.received` self-broadcast 去重契约（client 端跳过 `payload.userId == 当前 user.id` 的 echo，与 `pet.state.changed` self-broadcast 双轨兜底**不同**）属契约一部分；如未来 client 改为"接收 self-echo 并触发某 UI 反馈"（如 ack indicator），视为契约变更。
- **表情不持久化**契约（按数据库设计 §14.3，server 不写入任何表）属契约一部分；如未来引入 `emoji_events` 表 + 历史回放接口，视为契约变更。
- **GET /emojis 不分页 + 不接受 query 参数 + 不返回 disabled 表情**契约属契约一部分；如未来引入分页 / 筛选 / disabled 表情对 admin 可见，视为契约变更。
```

**AC7 — 验证步骤**

- 文档编辑完成后，通过以下方式验证契约一致性：
  - **AC7.1 字段表与 JSON 示例对齐**：手动检查 §11.1 `data.items[].code` / `name` / `assetUrl` / `sortOrder` 四字段在字段表和 JSON 示例中**完全一致**（字段名拼写 + 类型 + 必填性）；§12.2 `### 发送表情` 的 `type` / `requestId` / `payload.emojiCode` 三字段同样对齐；§12.3 `### 收到表情广播` 的 `type` / `requestId` / `payload.userId` / `payload.emojiCode` / `ts` 五字段同样对齐
  - **AC7.2 错误码一致性**：§11.1 错误码表列出的 1001 / 1005 / 1009 与 §3 全局错误码表（行 100-152）对应行的 message 完全一致；§12.2 `### 发送表情` 错误响应表列出的 1002 / 6004 / 7001 与 §3 全局错误码表对应行的 message 完全一致
  - **AC7.3 跨章节引用完整**：§11.1 关键约束段引用的 §2.5 / §3 / §10.3 / §15.6 等章节实际存在且引用正确；§12.2 `### 发送表情` 引用的 §11.1 / §12.1 / §12.3 通用信封 / `error` / §3 错误码表 / Story 18.x 钦定等实际存在
  - **AC7.4 冻结声明并列结构**：§1 末尾追加的节点 6 表情业务冻结声明与节点 2 / 节点 3 / 节点 4 协议骨架 / 节点 4 房间业务 / 节点 5 宠物状态业务的冻结声明在 markdown 结构上**形式一致**（同样的"自 YYYY-MM-DD（Story X.1 完成日）起 + §X.Y 进入冻结状态 + 任何修改必须触发 X 步流程"模板）
  - **AC7.5 git diff 检查**：编辑完成后 `git diff docs/宠物互动App_V1接口设计.md` 输出**仅**包含上述五处修改区域（§1 末尾追加 + §11.1 / §12.2 `### 发送表情` / §12.3 `### 收到表情广播` / §12.3 末尾业务消息延后锚定引用块）的增删；**不**出现其他章节的意外修改

## Tasks / Subtasks

- [x] Task 1: 准备阶段（AC: #1）
  - [x] Subtask 1.1: 阅读本 story 全文 + `docs/宠物互动App_V1接口设计.md` 当前 §11.1 / §12.2 / §12.3 / §1 现状，确认编辑起止行号
  - [x] Subtask 1.2: 读 `docs/宠物互动App_数据库设计.md` §5.15 emoji_configs 表，确认 `code` / `name` / `asset_url` / `sort_order` / `is_enabled` 字段类型 + 长度约束
  - [x] Subtask 1.3: 读 `_bmad-output/implementation-artifacts/14-1-接口契约最终化.md` 已 done 的姊妹 story，参考其文档编辑模式 + 冻结声明文案
  - [x] Subtask 1.4: 读 `_bmad-output/implementation-artifacts/11-1-接口契约最终化.md`（如存在）了解 §12.3 `### 成员加入` / `### 成员离开` 锚定模式（H3 标题 + `**触发**` 段 + `**字段**` 表 + JSON 示例 + `**关键约束**` 段）
- [x] Task 2: 就地修改 §11.1 GET /api/v1/emojis（AC: #2）
  - [x] Subtask 2.1: 紧接 `### GET /api/v1/emojis` 标题之后**新增** `#### 接口元信息` H4 块（按 AC2 §11.1.1 钦定的字段表）
  - [x] Subtask 2.2: 紧接接口元信息之后**新增** `#### 请求` H4 块（按 AC2 §11.1.2 钦定）
  - [x] Subtask 2.3: 紧接请求之后**新增** `#### 服务端逻辑` H4 块（按 AC2 §11.1.3 钦定的 4 步逻辑）
  - [x] Subtask 2.4: 把现有 `#### 返回示例` H4 标题改为 `#### 响应体`，在 JSON 示例**前**插入字段表 + **新增**空列表 edge case JSON 示例 + 末尾"client 缓存契约"注释段（按 AC2 §11.1.4 钦定）
  - [x] Subtask 2.5: 紧接响应体之后**新增** `#### 可能的错误码` H4 块（按 AC2 §11.1.5 钦定的 1001 / 1005 / 1009 三行错误码表）
  - [x] Subtask 2.6: 紧接错误码之后**新增** `**关键约束**` 段（按 AC2 关键约束钦定的 6 条 bullets）
- [x] Task 3: 就地修改 §12.2 `### 发送表情`（AC: #3）
  - [x] Subtask 3.1: 紧接 `### 发送表情` H3 标题 + 现有 JSON 示例之间插入 `**触发**` 段（按 AC3 钦定）
  - [x] Subtask 3.2: JSON 示例之后**新增** `**字段**` 表（按 AC3 钦定的 3 字段：type / requestId / payload.emojiCode）
  - [x] Subtask 3.3: 字段表之后保留现有 JSON 示例 + 紧接其后**新增**"注"段（说明 client → server 信封不带 `ts` 字段）
  - [x] Subtask 3.4: **新增** `**服务端逻辑**` 段（按 AC3 钦定的 6 步逻辑：接收 → 参数校验 → 房间归属校验 → 表情合法性校验 → 广播 → 不入库）
  - [x] Subtask 3.5: **新增** `**WS 广播 vs 客户端本地动效的关系**` 段（按 AC3 钦定，含 self-broadcast 去重规则 + 与 `pet.state.changed` 语义对比）
  - [x] Subtask 3.6: **新增** `**错误响应**` 段（按 AC3 钦定的 1002 / 6004 / 7001 三行错误码表 + 两段 JSON 示例：7001 + 6004）
  - [x] Subtask 3.7: **新增** `**关键约束**` 段（按 AC3 关键约束钦定的 5 条 bullets，含 client 端发送约束 / 校验顺序 / 不限频 / 不入库 / active message set 升级）
  - [x] Subtask 3.8: **就地修改**现有"业务消息延后锚定"引用块（行 1906-1908），把状态升级为"已锚定"+ 升级 client → server active message set
- [x] Task 4: 就地修改 §12.3 `### 收到表情广播`（AC: #4）
  - [x] Subtask 4.1: 把 `### 收到表情广播` H3 标题改为 `### 收到表情广播（emoji.received）`（与 `### 成员加入（member.joined）` / `### 成员离开（member.left）` / `### 宠物状态变更（pet.state.changed）` 命名模式一致）
  - [x] Subtask 4.2: 紧接 H3 标题之后**新增** `**触发**` 段（按 AC4 钦定）
  - [x] Subtask 4.3: 紧接触发段之后**新增** `**字段**` 表（按 AC4 钦定的 5 字段：type / requestId / payload.userId / payload.emojiCode / ts）
  - [x] Subtask 4.4: 保留现有 JSON 示例（紧接字段表之后）
  - [x] Subtask 4.5: JSON 示例之后**新增** `**关键约束**` 段（按 AC4 钦定的 6 条 bullets，含广播范围 / payload 必填 / client 处理规则 / fire-and-forget / ts 用途 / 不持久化）
  - [x] Subtask 4.6: 关键约束之后**新增** `**Future Fields**` 引用块（按 AC4 钦定，含节点 6 不携带 assetUrl / 不支持自定义文案）
- [x] Task 5: 就地修改 §12.3 末尾"业务消息延后锚定"引用块（AC: #5）
  - [x] Subtask 5.1: 升级业务消息延后锚定段落文案（按 AC5 #1 钦定的整段改写）
  - [x] Subtask 5.2: 升级"server / client active message set 升级"块（按 AC5 #2 钦定，server → client 集合追加 `emoji.received` + client → server 集合追加 `emoji.send`）
- [x] Task 6: §1 末尾追加节点 6 表情业务冻结声明（AC: #6）
  - [x] Subtask 6.1: 在节点 5 宠物状态业务冻结声明（"自 2026-05-12 起 §5.2 + §12.3 `### 宠物状态变更` 进入冻结"）**之后**追加节点 6 表情业务冻结声明（按 AC6 钦定的多条 bullets）
- [x] Task 7: 验证 + 提交（AC: #7）
  - [x] Subtask 7.1: 按 AC7.1 ~ AC7.5 五项验证步骤逐条检查 + 修正任何对齐 / 引用 / 结构问题
  - [x] Subtask 7.2: `git diff docs/宠物互动App_V1接口设计.md` 输出仅包含 5 处修改区域
  - [x] Subtask 7.3: 在 sprint-status.yaml 把本 story 状态从 in-progress 改为 review（dev-story 流程自动）
  - [ ] Subtask 7.4: 由 code-review 检出后状态切 done + 在本 story 文件 + sprint-status.yaml 状态行追加 commit hash

## Dev Notes

### 文档编辑模式（与已 done 的 14.1 / 11.1 一致）

- **本 story 是纯文档 story**：**不**写 Go / Swift 代码；**唯一**修改 `docs/宠物互动App_V1接口设计.md` 一份文件 + 自己的 story 文件 + sprint-status.yaml
- **编辑工具**：用 `Edit` 工具或直接修改 markdown（避免大段重写整个 §11.1 / §12.2 / §12.3 —— 这些章节的现有内容必须保留，仅**追加**内容）
- **避免大段重写**：现有 JSON 示例 + H3 / H4 标题 + 已有段落**全部保留**，仅在已有结构间隙**插入**新段落（参考已 done 的 Story 14.1 编辑模式：Edit 工具按"old_string + 上下文 + 新插入段落 + new_string"形式做精准 anchor 插入）
- **markdown 结构**：保留 V1 文档现有格式 —— H2 用 `## X.Y 标题` / H3 用 `### 子标题` / H4 用 `#### 进一步细分` / fenced code block 用 ` ```json ` / ` ```sql ` 等；引用块用 `> **段标题**：...`

### 跨文档语义同步检查（DB → API 单向）

- 本 story 锚定的 §11.1 / §12.2 / §12.3 字段**只**反映 `docs/宠物互动App_数据库设计.md` §5.15 emoji_configs 的语义，**禁止反向**修改 DB schema
- 如发现 §5.15 与本 story 锚定 API 字段有不一致（如 `name VARCHAR(64)` 但 API 锚定 `length ≤ 128`），**应**以 DB schema 为准并向下兼容（API length ≤ 64），**禁止**改 DB
- `emojiCode` 字符集 `[a-z0-9_-]` 是本 story 新增的契约层约束 —— DB `emoji_configs.code VARCHAR(64)` 仅约束长度，不约束字符集；本 story 在契约层补充字符集约束（避免 client 发奇怪字符 trigger SQL injection / URL 编码异常等问题），由 server 端 service 层校验（Story 17.5 实装时按 `[a-z0-9_-]` 正则校验 → 失败回 1002）

### 错误码引用

- 本 story 引用的 1001 / 1002 / 1005 / 1009 / 6004 / 7001 都已在 §3 全局错误码表（行 100-152）锚定 —— 本 story **不**新增 / **不**修改任何错误码定义
- 7001 "表情不存在" 在 §3 已定义；本 story 在 §12.2 `### 发送表情` 服务端逻辑步骤 4 钦定其触发条件（"emojiCode 不存在 / 或存在但 is_enabled=0"两种情况合并）
- 6004 "用户不在房间中" 在 §3 已定义；本 story 在 §12.2 `### 发送表情` 服务端逻辑步骤 3 钦定其在 **WS 路径**触发（原 §10.4 / §10.5 仅在 HTTP 房间事务触发；本 story 是 WS 路径首次复用 6004）

### 跨 epic 依赖追溯

- **上游冻结**：
  - §3 错误码表 / §2.5 全局约定 / §12.1 / §12.2 通用信封 + `ping` / §12.3 通用信封 + `room.snapshot` + `pong` + `error` ← Story 10.1 锚定
  - §10.x 房间 REST 接口 / §12.3 `### 成员加入` / `### 成员离开` ← Story 11.1 锚定
  - §5.2 POST /pets/current/state-sync / §12.3 `### 宠物状态变更（pet.state.changed）` ← Story 14.1 锚定
- **下游强依赖**（本 story 不动后才能开工）：
  - Story 17.2 ~ 17.5（Epic 17 后续实装 story）
  - iOS Epic 18.1 ~ 18.4（节点 6 客户端实装 story）

### 测试 / 验证

- **本 story 不写代码 → 无单测 / 集成测**：本 story 的"测试"= AC7 列出的 5 项文档一致性验证（字段表对齐 / 错误码一致 / 跨章节引用 / 冻结声明结构 / git diff 范围）
- **如需 schema 形式化 lint**：节点 6 阶段**不**引入（V1 文档以 markdown 表 + JSON 示例为唯一真相，无 OpenAPI / AsyncAPI / JSON Schema 形式化定义工具链）
- **下游验证**：本 story done 后由 Story 17.4 实装时的 dockertest 集成测试（curl GET /emojis → 验证 response 与 §11.1 字段表对齐）+ Story 17.5 实装时的 WS 集成测试（A + B 在房间 → A 发 `emoji.send` → A / B 都收到 `emoji.received` + payload 字段与 §12.3 字段表对齐）做真实验证

### 范围红线 + 风险

- **红线**：本 story **不**修改任何 Go / Swift 代码、**不**修改其他 6 份 `docs/宠物互动App_*.md`、**不**修改 `_bmad-output/` 下其他文档（除自己 story 文件 + sprint-status.yaml）
- **红线**：本 story **不**实装表情限频中间件（epics.md §Story 17.5 已钦定 MVP 不做）
- **风险**：编辑 V1 文档时可能误改 §10 / §12.3 已锚定章节（如不小心改了 `room.snapshot` 字段表）→ AC7.5 git diff 范围检查兜底
- **风险**：本 story 锚定的 self-broadcast 去重规则（client 跳过 `payload.userId == 当前 user.id`）与 §5.2 `pet.state.changed` self-broadcast 双轨兜底**不同语义** → 文档侧必须显式对比 + 说明差异原因（已在 §12.3 关键约束第 1 条钦定），避免 iOS Story 18.4 实装时复制 Story 15.2 self-broadcast merge 逻辑

### Project Structure Notes

- 本 story 唯一编辑文件：`docs/宠物互动App_V1接口设计.md`（绝对路径 `C:/fork/cat/docs/宠物互动App_V1接口设计.md`）
- 不涉及 Go / Swift 源码目录变更 —— 与 `docs/宠物互动App_Go项目结构与模块职责设计.md` / `docs/宠物互动App_iOS客户端工程结构与模块职责设计.md` 当前结构完全兼容

### References

- [Source: docs/宠物互动App_V1接口设计.md#1] 文档说明 + 现有契约冻结声明（节点 2 / 节点 3 / 节点 4 协议骨架 / 节点 4 房间业务 / 节点 5 宠物状态业务），本 story 在此末尾追加节点 6 表情业务冻结声明
- [Source: docs/宠物互动App_V1接口设计.md#2.5] 字段类型与编码约定（BIGINT 字符串化、int 不字符串化），本 story 引用
- [Source: docs/宠物互动App_V1接口设计.md#3] 错误码全局表（1001 / 1002 / 1005 / 1009 / 6004 / 7001），本 story 引用
- [Source: docs/宠物互动App_V1接口设计.md#11.1] GET /api/v1/emojis 现有内容（草稿 JSON 示例），本 story 就地扩充
- [Source: docs/宠物互动App_V1接口设计.md#12.1] WS 连接地址 + close code 表 + 服务端校验顺序，本 story 引用（**不**修改）
- [Source: docs/宠物互动App_V1接口设计.md#12.2 ### 发送表情] `emoji.send` 现有 JSON 示例 + "业务消息延后锚定"引用块，本 story 就地扩充
- [Source: docs/宠物互动App_V1接口设计.md#12.3 ### 收到表情广播] `emoji.received` 现有 JSON 示例，本 story 就地扩充
- [Source: docs/宠物互动App_V1接口设计.md#12.3 业务消息延后锚定] 末尾引用块（行 2349-2362），本 story 就地升级状态 + active message set
- [Source: docs/宠物互动App_数据库设计.md#5.15] emoji_configs 表 schema（`id` / `code` / `name` / `asset_url` / `sort_order` / `is_enabled` / `created_at` / `updated_at` + `UNIQUE KEY uk_code` + `KEY idx_enabled_sort`），本 story 引用（**不**修改）
- [Source: docs/宠物互动App_数据库设计.md#14.3] 表情不持久化语义，本 story 引用
- [Source: _bmad-output/planning-artifacts/epics.md#Story 17.1] AC 钦定 §11.1 / §12.2 / §12.3 必须锚定的字段 + 错误码 + 冻结声明
- [Source: _bmad-output/implementation-artifacts/14-1-接口契约最终化.md] Story 14.1 已 done 姊妹 story，参考编辑模式 + 冻结声明文案模板
- [Source: _bmad-output/implementation-artifacts/sprint-status.yaml] Story 17.1 状态行 + Epic 17 状态行（由本 story create + dev-story + code-review 流程逐步推进）

## Dev Agent Record

### Agent Model Used

claude-opus-4-7 (1M context)

### Debug Log References

（无 —— 纯文档 story，无运行时 debug）

### Completion Notes List

- 2026-05-13：按 AC1-AC6 在 `docs/宠物互动App_V1接口设计.md` 五处区域做就地编辑：(1) §1 末尾追加节点 6 表情业务冻结声明（与节点 2 / 节点 3 / 节点 4 / 节点 5 冻结声明并列）；(2) §11.1 GET /api/v1/emojis 补齐接口元信息 / 请求 / 服务端逻辑 / 响应体字段表 + 空列表 edge case + client 缓存契约 / 可能的错误码 / 关键约束；(3) §12.2 `### 发送表情` 补齐触发 / 字段表 / 服务端逻辑 6 步 / 广播 vs 本地动效 self-broadcast 去重规则 / 错误响应 1002/6004/7001 + JSON 示例 / 关键约束 / 升级末尾"业务消息延后锚定"引用块（client→server active message set 追加 `emoji.send`）；(4) §12.3 `### 收到表情广播` 标题升级为 `### 收到表情广播（emoji.received）` + 触发 / 字段表 / 关键约束（含 self-broadcast 跳过规则）/ Future Fields 引用块；(5) §12.3 末尾"业务消息延后锚定"引用块升级（`emoji.received` 状态升级为"已由 Story 17.1 锚定" + server→client active message set 追加 `emoji.received` + 标注节点 6 之后无新增业务消息字段层契约任务）
- AC7 验证：(7.1) §11.1 `code/name/assetUrl/sortOrder` 四字段在字段表与 JSON 示例完全一致；§12.2 `type/requestId/payload.emojiCode` 三字段对齐；§12.3 `type/requestId/payload.userId/payload.emojiCode/ts` 五字段对齐；(7.2) §3 错误码表 line 125-161 与本 story 引用的 1001/1002/1005/1009/6004/7001 message 字面量一致；(7.3) 跨章节引用（§2.5/§3/§10.x/§11.1/§12.1/§12.2 通用信封/§12.3 通用信封/§15.6/§5.2/§5.15 数据库设计/§14.3 数据库设计/Story 18.x 钦定）均指向真实存在的章节；(7.4) §1 末尾追加的节点 6 冻结声明文案与节点 5 (Story 14.1) 模板形式一致（"自 YYYY-MM-DD（Story X.1 完成日）起 + §X 进入冻结状态 + 任何修改必须触发 X 步流程"）；(7.5) `git diff --stat` 输出 `1 file changed, 215 insertions(+), 11 deletions(-)`，diff hunks 仅落在 §1 (line 57)、§11.1 (line 1725-1802)、§12.2 emoji.send (line 1895-2077)、§12.3 emoji.received (line 2261-2461)、§12.3 末尾引用块 (line 2349-2362) 五处预期修改区域，无其他章节意外修改
- 纯文档 story，**未**触发 Go build / Go test / Swift build；测试覆盖将由下游 Story 17.4（GET /emojis dockertest 集成测试）+ Story 17.5（WS emoji.send / emoji.received 集成测试）做真实验证

### File List

- `docs/宠物互动App_V1接口设计.md`（修改：§1 末尾追加节点 6 表情业务冻结声明 + §11.1 / §12.2 `### 发送表情` / §12.3 `### 收到表情广播` / §12.3 末尾"业务消息延后锚定"引用块共 5 处就地扩充）
- `_bmad-output/implementation-artifacts/sprint-status.yaml`（状态行：`17-1-接口契约最终化` ready-for-dev → in-progress → review；`last_updated` → 2026-05-13）
- `_bmad-output/implementation-artifacts/17-1-接口契约最终化.md`（本 story 文件：Status ready-for-dev → in-progress → review；Tasks/Subtasks 全勾选；Dev Agent Record 完成笔记 + File List + Change Log）

### Change Log

- 2026-05-13：完成节点 6 表情业务契约最终化（§11.1 + §12.2 `### 发送表情` + §12.3 `### 收到表情广播` 三处字段层 + §12.3 末尾引用块状态升级 + §1 末尾冻结声明追加）；Tasks 1-7 全部完成；Status: ready-for-dev → review
